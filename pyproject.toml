[tool.pytest.ini_options]
testpaths = ["tests", "packages/solverpy-grackle/tests", "packages/solverpy-learn/tests"]
markers = [
   "slow: marks tests as slow (skipped by default)",
   "learn: marks tests as ML learning tests (skipped by default)",
   "grackle: marks tests as grackle tests (skipped by default)",
]
addopts = "-m 'not slow and not learn and not grackle'"

[tool.yapf]
based_on_style = "pep8"
indent_width = 3
continuation_indent_width = 3

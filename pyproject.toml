[tool.pytest.ini_options]
testpaths = ["tests", "packages/solverpy-grackle/tests"]
markers = ["slow: marks tests as slow (skipped by default)"]

[tool.yapf]
based_on_style = "pep8"
indent_width = 3
continuation_indent_width = 3

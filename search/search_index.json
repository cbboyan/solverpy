{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"\ud83c\udfe0 SolverPy","text":"<p><code>SolverPy</code> is a generic Python interface for evaluation and machine learning of automated theorem provers (ATPs) and Satisfiability Modulo Theories solvers (SMTs). Most everyday work happens through the <code>solverpy</code> shell command and YAML experiment files \u2014 no Python code required \u2014 though a full Python API is available too.</p> <p>v2.1.2 \u00b7 GitHub \u00b7 Changelog</p> <pre><code>$ pip install solverpy\n</code></pre> <p>See Install for other ways to install SolverPy (from source, solver binaries, <code>solverpy-learn</code>).</p>"},{"location":"#what-solverpy-gives-you","title":"\ud83c\udf81 What SolverPy gives you","text":"<ul> <li>A single <code>solverpy</code> command that runs an experiment described by a YAML   file, such as evaluating a set of E Prover strategies on a set of problems.</li> <li>A uniform interface across ATP and SMT solvers \u2014 <code>E</code>,   <code>Vampire</code>, <code>Prover9</code>,   <code>Lash</code>, <code>cvc5</code>,   <code>Z3</code>, <code>Opensmt</code>, and   more \u2014 so the same experiment, database, and reporting code works for all   of them.</li> <li>A results database (<code>solverpy_db/</code>) that caches every run: re-running   an experiment reuses cached results instead of re-solving.</li> <li>Machine-learning guidance via the companion <code>solverpy-learn</code> package:   <code>enigma</code> trains E Prover's ENIGMA clause-selection models, and <code>cvc5ml</code>   trains ML-enhanced cvc5 strategies, both as iterative evaluate \u2192 train \u2192   re-evaluate loops.</li> </ul>"},{"location":"#whats-new-in-v212-2026-09-10","title":"\ud83d\ude80 What's new in v2.1.2 (2026-09-10)","text":"<ul> <li>Revamped this documentation site: Home/Install/Usage/Tutorials/Commands,   step-by-step tutorials (including a full ENIGMA training walkthrough),   and a proper favicon.</li> <li>Added the <code>SPASS-SATT</code> SMT solver plugin.</li> <li>Wired the new <code>RunHash</code> status decorator into <code>E</code> and <code>Primo</code>, for   reproducibility checks across runs.</li> <li>Fixed E's <code>--cpu-limit</code>/<code>--soft-cpu-limit</code> ordering under <code>--auto-schedule</code>.</li> </ul> <p>See the full changelog for every release.</p>"},{"location":"#where-to-go-next","title":"\ud83d\uddfa\ufe0f Where to go next","text":"<ul> <li>Install \u2014 installing SolverPy and the solver binaries it drives.</li> <li>Usage \u2014 the <code>solverpy</code> command, your first YAML experiment, and the <code>sid</code>/<code>bid</code> concepts.</li> <li>Commands \u2014 full reference for every <code>solverpy</code> subcommand.</li> <li>Tutorials \u2014 step-by-step walkthroughs, including the Python API.</li> </ul>"},{"location":"commands/","title":"\u2328\ufe0f Commands","text":"<p>Reference for the <code>solverpy</code> command-line tool. Run <code>solverpy --help</code> or <code>solverpy &lt;command&gt; --help</code> for the same information from the terminal.</p>"},{"location":"commands/#solverpy-init-solver","title":"\u25c6 <code>solverpy init [solver]</code>","text":"<p>Create <code>solverpy_db/strats/</code> in the current directory and populate it with bundled example strategies. With <code>solver</code> (e.g. <code>eprover</code>, <code>cvc5</code>), only that solver's strategies are copied (with the <code>&lt;solver&gt;-</code> filename prefix stripped) and a starter <code>eval-&lt;solver&gt;.yaml</code> is written next to <code>solverpy_db/</code>. Without an argument, strategies for every bundled solver are copied, keeping their full <code>&lt;solver&gt;-&lt;name&gt;</code> filenames.</p>"},{"location":"commands/#solverpy-run-file","title":"\u25c6 <code>solverpy run FILE</code>","text":"<p>Load a YAML experiment file and run it. The file's top-level keys map directly onto <code>Setup</code>/<code>Evalset</code>:</p> Key Meaning <code>evaluate: &lt;solver&gt;</code> Run a plain evaluation with this solver (<code>eprover</code>, <code>cvc5</code>, <code>vampire</code>, <code>prover9</code>, <code>bitwuzla</code>, <code>z3</code>, <code>llm2smt</code>, <code>opensmt</code>, <code>primo</code>, <code>spasssatt</code>, <code>yices</code>). <code>loop: &lt;type&gt;</code> Run an ML training loop instead of a plain evaluation (<code>enigma</code> or <code>cvc5ml</code>, from <code>solverpy-learn</code>); requires a <code>strategy:</code> key naming the base sid. <code>common:</code> Settings shared by <code>evals</code> and <code>devels</code> (e.g. <code>limit</code>, <code>cores</code>, <code>binary</code>). <code>evals:</code> The evaluation (or training) benchmark set: <code>benchmarks:</code> (list of <code>bid</code>s) and <code>strategies:</code> (list of <code>sid</code>s), plus optional <code>dataname:</code>. <code>devels:</code> A second, development benchmark set \u2014 used by ML loops to evaluate the model being trained. <code>options:</code> Boolean experiment options \u2014 see below. <p>Exactly one of <code>evaluate:</code> or <code>loop:</code> must be present. See Tutorials for <code>evaluate:</code> walkthroughs and Training an ENIGMA model for a <code>loop:</code> walkthrough.</p>"},{"location":"commands/#options","title":"\u25b9 Options","text":"<p>Each entry under <code>options:</code> is a boolean flag identified by name; prefix with <code>no-</code> to turn it off (e.g. <code>no-flatten</code>).</p> option description default <code>outputs</code> Keep raw solver output files from all runs. no <code>compress</code> Compress output files (outputs, trains, results). yes <code>flatten</code> Put all output files in a single directory (replace <code>/</code> with <code>_._</code>). yes <code>compress-trains</code> Compress trains. yes <code>debug-trains</code> Dump training data for each file separately. no <code>proofs</code> Store proof objects in the database. no <code>premises</code> Store premise selections in the database. no <code>headless</code> Suppress progress bars and interactive UI (for non-terminal use). no"},{"location":"commands/#the-database","title":"\u25b9 The database","text":"<p>Every run writes into <code>solverpy_db/</code> (path overridable via <code>SOLVERPY_DB</code>):</p> directory content <code>strats/</code> Strategy files (input, not written by <code>run</code>). <code>results/</code> Gzip JSON <code>{problem: result}</code> dict, per <code>sid</code>/<code>bid</code>. <code>solved/</code> List of solved problem names, one per line. <code>status/</code> <code>problem&lt;TAB&gt;status&lt;TAB&gt;runtime</code> lines, one per line. <code>logs/</code> Console log and Markdown report per <code>solverpy run</code> invocation. <code>outputs/</code> Raw solver stdout per run (only with the <code>outputs</code> option). <code>proofs/</code>, <code>premises/</code> Proof objects / premise selections (only with the <code>proofs</code>/<code>premises</code> options). <code>trains/</code>, <code>models/</code> Training data and trained models (ML loops only)."},{"location":"commands/#solverpy-clean","title":"\u25c6 <code>solverpy clean</code>","text":"<p>Delete every subdirectory of <code>solverpy_db/</code> except <code>strats/</code> (i.e. clear cached results, logs, and outputs, but keep your strategy files). Prompts for confirmation unless <code>-y</code>/<code>--yes</code>/<code>-f</code>/<code>--force</code> is given.</p>"},{"location":"commands/#solverpy-report-filemd-o-filehtml","title":"\u25c6 <code>solverpy report FILE.md [-o FILE.html]</code>","text":"<p>Convert a <code>solverpy_db/logs/*.md</code> experiment report into a self-contained, offline HTML page (default output: same name with <code>.html</code>).</p>"},{"location":"commands/#solverpy-esid2strat-input","title":"\u25c6 <code>solverpy esid2strat INPUT</code>","text":"<p>Convert <code>eprover-ho --print-strategy=&lt;name&gt;</code> output into the equivalent <code>eprover</code> command-line arguments \u2014 useful for turning an ENIGMA-selected strategy back into a plain sid file. <code>INPUT</code> may be a strategy name (looked up by invoking <code>eprover-ho</code> directly), a file path, or <code>-</code> for stdin. Flags: <code>-1</code>/<code>--one-line</code> to print all arguments on one line; <code>-s</code>/<code>--no-sine</code> to omit the <code>--sine</code> argument.</p>"},{"location":"commands/#solverpy-eval-solverpy-loop","title":"\u25c6 <code>solverpy eval</code>, <code>solverpy loop</code>","text":"<p>Stubs reserved for a future non-YAML CLI form of <code>evaluate:</code>/<code>loop:</code>; not yet implemented (they print an error and exit).</p>"},{"location":"commands/#commands-added-by-solverpy-learn","title":"\u25c6 Commands added by <code>solverpy-learn</code>","text":"<p>These subcommands only appear once <code>solverpy-learn</code> is installed.</p>"},{"location":"commands/#solverpy-tune-train-test","title":"\u25b9 <code>solverpy tune TRAIN [TEST]</code>","text":"<p>Run the Optuna hyperparameter autotuner directly on an SVM-format training (and optional testing) file, outside of a <code>loop:</code> experiment. Key flags: <code>--phases</code> (which LightGBM parameter groups to tune, default <code>l:b:m:r</code>), <code>--iters</code>, <code>--timeout</code>, <code>--min-leaves</code>/<code>--max-leaves</code>, <code>--posneg-weight</code>, <code>--tmp</code>, <code>--model-out</code>, <code>--log-file</code>, and a set of <code>--init-*</code> flags for the starting LightGBM parameters. Run <code>solverpy tune --help</code> for the full list.</p>"},{"location":"commands/#solverpy-compress-input-solverpy-decompress-input","title":"\u25b9 <code>solverpy compress INPUT</code> / <code>solverpy decompress INPUT</code>","text":"<p>Convert an SVM-Light training file to/from the chunked NPZ format used internally for faster loading.</p>"},{"location":"commands/#solverpy-deconflict-input-output","title":"\u25b9 <code>solverpy deconflict INPUT [OUTPUT]</code>","text":"<p>Remove training samples that appear as both positive and negative examples. Default output: <code>deconflicted.in</code>.</p>"},{"location":"commands/#solverpy-filter-input-output-ratio-r","title":"\u25b9 <code>solverpy filter INPUT [OUTPUT] --ratio R</code>","text":"<p>Subsample a training file to cap the positive/negative ratio: <code>--ratio r</code> (with <code>r &gt; 0</code>) caps negatives at <code>r</code> times the positive count; a negative <code>r</code> caps positives at <code>|r|</code> times the negative count instead. Default output: overwrite <code>INPUT</code>.</p>"},{"location":"install/","title":"\ud83d\udce5 Install","text":""},{"location":"install/#from-pypi","title":"\u25c6 From PyPI","text":"<pre><code>$ pip install solverpy\n</code></pre> <p>This installs the core <code>solverpy</code> package \u2014 the solver interface, benchmark evaluation, and the results database \u2014 together with the <code>solverpy</code> command-line tool.</p> <p>Add <code>solverpy-learn</code> for machine-learning guidance (ENIGMA, cvc5ml):</p> <pre><code>$ pip install solverpy-learn\n</code></pre>"},{"location":"install/#from-source-github","title":"\u25c6 From source (GitHub)","text":"<p>Clone the repository:</p> <pre><code>$ git clone https://github.com/cbboyan/solverpy.git\n$ cd solverpy\n</code></pre> <p>The repository is a monorepo of packages under <code>packages/</code>: <code>solverpy</code>, <code>solverpy-learn</code>, and <code>solverpy-grackle</code>. Install the ones you need in editable mode, so source edits take effect immediately:</p> <pre><code>$ pip install -e packages/solverpy\n$ pip install -e packages/solverpy-learn   # optional, for ML guidance\n</code></pre> <p>Or build a distributable package for one of them:</p> <pre><code>$ cd packages/solverpy\n$ python3 -m build\n</code></pre>"},{"location":"install/#solver-binaries","title":"\u25c6 Solver binaries","text":"<p>SolverPy does not bundle any solver binaries or libraries \u2014 install them separately (e.g. <code>eprover</code>, <code>cvc5</code>, <code>z3</code>, <code>opensmt</code>, ...). By default, SolverPy looks each one up on <code>PATH</code>; to use a binary that is not on <code>PATH</code>, pass its full path via the <code>binary</code> parameter of <code>Setup</code> in the Python API, or a <code>binary:</code> key in a YAML experiment file.</p>"},{"location":"install/#verify","title":"\u25c6 Verify","text":"<pre><code>$ solverpy --help\n</code></pre>"},{"location":"markdown/","title":"Markdown","text":"<p>\ud83d\udee0\ufe0f Unicode Symbols for Markdown Documentation</p> <p>\u2139\ufe0f Info Symbol  Unicode Meaning Usage Example</p> <ul> <li>\u2139\ufe0f    U+2139  Information \u2139\ufe0f Use with caution in debug mode.</li> <li>\ud83d\udec8 U+1F6C8 Alternative info symbol \ud83d\udec8 API is subject to change.</li> <li>\ud83d\udcd8 U+1F4D8 Reference/Blue book \ud83d\udcd8 See section 4.2 of the guide.</li> <li>\ud83e\uddfe U+1F9FE Documentation/Receipt   \ud83e\uddfe Generated logs are saved to /var/log.</li> <li>\ud83e\udde0 U+1F9E0 Insight/Brain   \ud83e\udde0 This technique boosts performance.</li> </ul> <p>\ud83d\udcdd Note Symbol   Unicode Meaning Usage Example</p> <ul> <li>\ud83d\udcdd U+1F4DD Memo/Note   \ud83d\udcdd This method is deprecated.</li> <li>\ud83d\uddd2\ufe0f    U+1F5D2 Notepad \ud83d\uddd2\ufe0f Make sure to configure both fields.</li> <li>\ud83d\udccc U+1F4CC Pinned Note \ud83d\udccc This setting is environment-specific.</li> <li>\ud83d\udcce U+1F4CE Paperclip/Attached  \ud83d\udcce Attached are the config templates.</li> <li>\ud83e\uddd0 U+1F9D0 Observational Note  \ud83e\uddd0 Rare edge case: input may be null.</li> </ul> <p>\ud83d\udca1 Hint / Tip Symbol Unicode Meaning Usage Example</p> <ul> <li>\ud83d\udca1 U+1F4A1 Lightbulb   \ud83d\udca1 Try caching the results for speed.</li> <li>\ud83d\udd0d U+1F50D Insight/Search  \ud83d\udd0d You can filter logs by severity.</li> <li>\ud83e\udde0 U+1F9E0 Brain/Insight   \ud83e\udde0 Consider using memoization here.</li> <li> <p>\ud83d\udd27 U+1F527 Tool/Fix tip    \ud83d\udd27 Use the --fix flag to auto-correct.</p> </li> <li> <p>\u26a0\ufe0f Warning Symbol Unicode Meaning Usage Example</p> </li> <li>\u26a0\ufe0f    U+26A0  Warning  \u26a0\ufe0f Do not delete system files.</li> <li>\u2757 U+2757  Important/Alert \u2757 Backup your data before proceeding.</li> <li>\u2755 U+2755  Light Warning   \u2755 This may cause a minor delay.</li> <li>\ud83d\udea7 U+1F6A7 Construction/Partial    \ud83d\udea7 Feature under development.</li> </ul> <p>\u274c Error / Danger Symbol Unicode Meaning Usage Example</p> <ul> <li>\u274c U+274C  Error   \u274c Invalid configuration file.</li> <li>\ud83d\udd25 U+1F525 Hot Issue   \ud83d\udd25 Major performance hit detected.</li> <li>\ud83d\uded1 U+1F6D1 Stop/Critical   \ud83d\uded1 Service will shut down immediately.</li> </ul> <p>\u2705 Success / Done Symbol Unicode Meaning Usage Example</p> <ul> <li>\u2705 U+2705  Success/Passed  \u2705 All unit tests passed.</li> <li>\u2611\ufe0f    U+2611  Checkbox    \u2611\ufe0f Enabled via config.</li> <li>\ud83c\udf89 U+1F389 Celebration \ud83c\udf89 Initial setup complete!</li> </ul>"},{"location":"options/","title":"SolverPy Options","text":"<ul> <li>An option is identified by its string name, and represents a boolean yes/no value.</li> <li>Put the option name into the string list under the key<code>\"options\"</code> in the setup.</li> <li>Use <code>no-</code>option, like <code>no-compress</code>, to set the option to <code>no</code></li> </ul> option description default <code>outputs</code> Keep raw solver output files from all runs. no <code>compress</code> Compress output files (outputs, trains, results). yes <code>flatten</code> Put all output files in a single directory (replace <code>/</code> with <code>_._</code>). yes <code>compress-trains</code> Compress trains. yes <code>debug-trains</code> Dump training data for each file separately. no <code>proofs</code> Store proof objects in the database. no <code>premises</code> Store premise selections in the database. no <code>headless</code> Suppress progress bars and interactive UI (for non-terminal use). no"},{"location":"test/","title":"PlantUML Test","text":"<p><code>![file](diagrams/out/overview.svg)</code></p>"},{"location":"tutorial/","title":"Python Interface for Automated Solvers","text":"<p><code>SolverPy</code> provides a uniform Python interface for several automated ATP and SMT solvers.</p> <p>Currently supported solvers are  <code>E</code>, <code>Vampire</code>, <code>Prover9</code>, <code>Lash</code>, <code>cvc5</code>, <code>Z3</code>, and <code>Bitwuzla</code>.</p> <p>The interface can be used for:</p> <p>\ud83d\udca1 Solving a single problem instance with compatible results. \ud83d\udd27 Benchmark parallel evaluation with database storage. \ud83e\udde0 Machine learning of models and strategies for solver guidance.  </p>"},{"location":"tutorial/#installation","title":"Installation","text":"<p>Install the Python package using <code>pip</code>:</p> <pre><code>$ pip install solverpy\n</code></pre> <p>Or clone our GitHub repository:</p> <pre><code>$ git clone https://github.com/cbboyan/solverpy.git\n</code></pre> <p>\ud83d\uddd2\ufe0f Note: The solver binaries/libraries are not part of this Python package and must be installed separately.  The binaries must be (by default) in <code>PATH</code> or specified using <code>binary</code> parameter in <code>Setup</code>, if you wish to use them from <code>solverpy</code>.</p>"},{"location":"tutorial/#overview","title":"Overview","text":""},{"location":"tutorial/#single-problem-solving","title":"\ud83d\udca1 Single problem solving","text":"<p>Single problem solving involves creating a solver object and calling its <code>solve</code> method.</p> <pre><code>from solverpy.solver.smt.cvc5 import Cvc5\n\ncvc5 = Cvc5(\"T5\")  # time limit of 5 seconds\nresult = cvc5.solve(\"myproblem.smt2\", \"--enum-inst\") # problem and strategy\n</code></pre> <p>Strategies are solver-specific, typically command line options as a string.</p> <p>\u2615 The result is a dictionary guaranteed to contain at least two keys: <code>status</code> as a string and <code>runtime</code> in seconds, apart from solver-specific keys.</p>"},{"location":"tutorial/#benchmark-evaluation","title":"\ud83d\udd27 Benchmark evaluation","text":"<p>SolverPy provides dataclass <code>Setup</code> that describes the evaluation configuration. It auromatically connects to database <code>DB</code> to store results, by default, using the <code>Jsons</code> provider.</p> <p>To evaluate a set of strategies on a set of benchmark problems, you just need to provide your experiment description as a Python <code>dict</code>.</p> <p>\ud83d\udca1 Use typed version <code>Setup</code> to avoid typos and type errors.</p> <p>Functions from the <code>setups</code> module are used to fill in the required keys and values. To run the evaluation you setup a solver for an evaluation, then launch it.</p> <pre><code>from solverpy import setups\n\nmysetup = setups.Setup(\n    cores=4,\n    benchmarks=['problems'],\n    strategies=[\"default\"],\n    limit='T10',\n)\n\nsetups.cvc5(mysetup)\nsetups.evaluation(mysetup)\n\nmysetup.launch()\n</code></pre> <p>\ud83e\udd1e Before launching the evaluation, you need to setup the SolverPy database by creating directories <code>solverpy_db/strats</code> in the current directory. This directory stores the strategy files. For the above example, there should be an empty file <code>solverpy_db/strats/default</code> (default cvc5 strategy). The problem files should be in <code>problems/</code> directory.</p> <p>\u2615 After the evaluation, you can inspect the results in the database directory <code>solverpy_db/results</code>.</p>"},{"location":"tutorial/#machine-learning","title":"\ud83e\udde0 Machine learning","text":""},{"location":"tutorial/#single-problem-solving_1","title":"Single problem solving","text":"<p>To call the solver on one problem instance, start by creating the solver object.</p> <pre><code>from solverpy.solver.smt.cvc5 import Cvc5\n\ncvc5 = Cvc5(\"T5\")  # time limit of 5 seconds\n</code></pre> <p>The constructor argument is a resource limit string, in this case, a time limit <code>T</code> in seconds.  All solvers support <code>T</code> and additional resource limits might be available depending on the solver.  Multiple resource limits can be used (separated by <code>-</code>, like <code>T10-R50000</code>).  The limit string must, however, always start with <code>T</code>.</p> <p>Then call the <code>solve</code> method:</p> <pre><code>result = cvc5.solve(\"myproblem.smt2\", \"--enum-inst\")\n</code></pre> <p>The first argument is the problem filename, the second is the solver-dependent strategy description (typically command line options as a string).</p> <p>The result is a Python <code>dict</code> with results and statistics.  The keys and values are solver-specific.  Nevertheless, the result always contains keys <code>status</code> (with the value of type <code>str</code>) and <code>runtime</code> (type <code>float</code>).</p> <p>\ud83d\udca1 Call <code>cvc5.run(p,s)</code> instead of <code>cvc5.solve(p,s)</code> to get the raw solver output without any processing.  </p> <p>\ud83d\udca1 Call <code>cvc5.command(p,s)</code> to output the shell command that is going to be executed to launch the solver.</p>"},{"location":"tutorial/#parallel-benchmark-evaluation","title":"Parallel benchmark evaluation","text":"<p>To evaluate a set of strategies on a set of benchmark problems, you just need to provide your experiment description as a Python <code>dict</code> and launch the experiments.</p> <pre><code>from solverpy.benchmark import setups\n\nmysetup = ...\n\nsetups.launch(mysetup)\n</code></pre> <p>The experiment setup (<code>mysetup</code>) must have specific keys.  The module <code>solverpy.benchmark.setups</code> contains methods to fill in the required keys and values.</p> <p>You must specify at least the following:</p> key type description <code>cores</code> <code>int</code> number of CPU cores to use for parallel evaluation <code>strategies</code> <code>[str]</code> list of strategies to evaluate <code>benchmarks</code> <code>[str]</code> list of problems to evaluate on <code>limit</code> <code>str</code> the resource limit for a single solver run"},{"location":"tutorial/#strategies-and-strategy-ids","title":"Strategies and strategy id's","text":"<p>Strategies are stored in files in the directory <code>solverpy_db/strats</code> which must exist in the current working directory (the directory is adjustable by the <code>SOLVERPY_DB</code> environment variable). The filename of each strategy is used to reference the strategy in <code>strategies</code> and it is called the strategy id (<code>sid</code>).</p> <p>Hence, for every <code>sid</code> in <code>strategies</code> in <code>mysetup</code>, there must be the file <code>solverpy_db/strats/sid</code> in the current working directory. This file contains the strategy definition (typically command line options) to pass to the <code>solver.solve</code> method.</p>"},{"location":"tutorial/#problems-and-benchmark-ids","title":"Problems and benchmark id's","text":"<p>Benchmark problem sets are represented by benchark id's (<code>bid</code>).  The benchmark id is a file path relative to the current working directory (adjustable by the <code>SOLVERPY_BENCHMARKS</code> environment variable) pointing either to a file or to a directory.</p> <p>If the path leads to a:</p> <ul> <li><code>directory</code>: Then every regular file directly in this directory is considered a benchmark problem.    Directories and hidden files are ignored and no recursive search is performed.    This variant is useful when you have a set of problem files, all in one directory.</li> <li><code>file</code>: Then the file consists of lines containing paths to corresponding problem files.    The paths are relative to the directory of the <code>bid</code> file.  For example, if  the <code>bid</code> is <code>myproblems/subset1</code> and this file contains (among others) the line <code>category1/problem23.smt2</code> then the problem must be placed in <code>myproblems/category1/problem23.smt2</code> (because the directory of the <code>bid</code> file is <code>myproblems</code>).    This variant is useful when your benchmarks are structured in subdirectories and you don't want to merge them into one directory.</li> </ul>"},{"location":"tutorial/#experiments-example","title":"Experiments example","text":"<p>Suppose you have some SMT problems in the directory <code>myproblems</code> and that you want to evaluate your cvc5 strategies <code>buzzard</code>, <code>sparrow</code>, and <code>chickadee</code>, which you have placed in the directory <code>solverpy_db/strats</code>.  You can download the archive with files for this example here.</p> <p>You proceed as follows.  First, you create the description of your experiments in <code>mysetup</code>.</p> <pre><code>from solverpy.benchmark import setups\n\nmysetup = {\n    \"cores\": 4,\n    \"limit\": \"T10\",\n    \"benchmarks\": [\"myproblems\"],\n    \"strategies\": [\"buzzard\", \"sparrow\", \"chickadee\"],\n}\n</code></pre> <p>Hint: Add <code>mysetup[\"options\"] = [\"outputs\"]</code> if you want to keep raw solver output files from all solver runs.  </p> <p>Hint: Options are slightly more described here.</p> <p>Then you specify that you want to use cvc5 and that you wish to launch an evaluation.  These methods update <code>mysetup</code> and fill in some keys required by <code>setups.launch()</code>.</p> <pre><code>setups.cvc5(mysetup)\nsetups.evaluation(mysetup)\n</code></pre> <p>Finally, you launch the experiments.</p> <pre><code>setups.launch(mysetup)\n</code></pre> <p>You will see the progress of the experiments on the screen.  Once finished, you will find the following subdirectories inside <code>solverpy_db</code>:</p> directory content <code>results</code> Results by each strategy (<code>sid</code>) for each <code>bid</code>.  The result for each <code>sid</code> and <code>bid</code> is a JSON file (gzip-ed) with a Python dictionary <code>{problem: result}</code>. <code>solved</code> List of solved problem names by each strategy for each <code>bid</code>.  One per line, easy to <code>grep</code> and <code>cat</code>. <code>status</code> Statuses of all problems by each strategy for each <code>bid</code>.  Problem name and status at one line, TAB separated.  Easy to <code>cut</code>. <code>log</code> Console log for each <code>solverpy</code> experiment run. <code>outputs</code> Raw solver output files for each solver run (only if selected). <p>Now run the script again and notice that it finished much faster.  It is because the cached results were reused and no solvers were actually launched.  So be careful and always clean the database if you want to force recompute.  Simply delete all the directories in <code>solverpy_db</code> except <code>strats</code> (see the script <code>clean_db.sh</code> in the example archive).</p>"},{"location":"usage/","title":"\ud83e\udded Usage","text":""},{"location":"usage/#bootstrap-a-project","title":"\u25c6 Bootstrap a project","text":"<pre><code>$ solverpy init eprover\n</code></pre> <p><code>solverpy init &lt;solver&gt;</code> creates <code>solverpy_db/strats/</code> in the current directory, populates it with a few bundled example strategies for <code>&lt;solver&gt;</code>, and drops a starter <code>eval-&lt;solver&gt;.yaml</code> experiment file next to it. Run <code>solverpy init</code> with no argument to bootstrap all bundled solvers at once.</p>"},{"location":"usage/#sid-and-bid","title":"\u25c6 <code>sid</code> and <code>bid</code>","text":"<p>Every experiment evaluates a set of strategies on a set of benchmark problems:</p> <ul> <li>A strategy id (<code>sid</code>) is just a filename in <code>solverpy_db/strats/</code>. Its   content is the solver's command-line options \u2014 it can even be empty, for   the solver's own defaults. So <code>strategies: [default]</code> in an experiment   means \"use the options in <code>solverpy_db/strats/default</code>\".</li> <li>A benchmark id (<code>bid</code>) is a path, relative to the current directory,   to a directory of problem files, e.g. <code>myproblems/</code>. (A <code>bid</code> can also   point to a file listing problem paths instead of a directory \u2014 see   Tutorials for that case.)</li> </ul>"},{"location":"usage/#your-first-experiment","title":"\u25c6 Your first experiment","text":"<p><code>solverpy init eprover</code> creates the strategy files above and a starter <code>eval-eprover.yaml</code>:</p> <pre><code>evaluate: eprover\ncommon:\n  limit: T1      # resource limit (T=time, M=memory in GB, e.g. T10-M4)\n  cores: 4\nevals:\n  dataname: eval # label for output directories\n  benchmarks:\n    - problems/bushy010\n  strategies:\n    - default\noptions:\n  - outputs      # keep raw solver output files\n</code></pre> <p>Put your problem files where <code>benchmarks:</code> points (here <code>problems/bushy010</code>), then launch the evaluation:</p> <pre><code>$ solverpy run eval-eprover.yaml\n</code></pre> <p>Results land under <code>solverpy_db/results</code>, <code>solverpy_db/solved</code>, and <code>solverpy_db/status</code> \u2014 see Commands for the full database layout. Re-running the same command reuses the cached results instead of re-solving everything.</p>"},{"location":"usage/#try-it-on-real-problems","title":"\u25c6 Try it on real problems","text":"<p>Rather than supplying your own, download a small set of real TPTP problems to fill in <code>problems/bushy010</code> from the experiment above:</p> <pre><code>$ wget https://cbboyan.github.io/solverpy/eprover-example.tar.gz\n# or: curl -LO https://cbboyan.github.io/solverpy/eprover-example.tar.gz\n$ tar xzf eprover-example.tar.gz\n</code></pre> <p>This extracts <code>problems/bushy010/</code> directly, matching the <code>benchmarks:</code> entry already in <code>eval-eprover.yaml</code> \u2014 so you can launch it right away:</p> <pre><code>$ solverpy run eval-eprover.yaml\n</code></pre> <p>There's also a similar downloadable set of SMT-LIB2 problems for cvc5 \u2014 see Evaluating cvc5.</p>"},{"location":"usage/#next-steps","title":"\u25c6 Next steps","text":"<p>This covers the basics. For step-by-step walkthroughs \u2014 evaluating specific E and cvc5 strategies, training an ENIGMA model, and using the Python API directly \u2014 see Tutorials.</p>"},{"location":"api/solverpy/","title":"Solverpy Overview","text":""},{"location":"api/solverpy/#solverpy","title":"<code>solverpy</code>","text":"<p>The basic concepts include:</p> <ul> <li>solver</li> <li>plugin</li> <li>strategy</li> <li>benchmarks</li> <li>result</li> <li>database</li> <li>provider</li> <li>SolverTask</li> <li>builder</li> </ul> <p> </p>"},{"location":"api/solverpy/benchmark/","title":"Benchmark Overview","text":""},{"location":"api/solverpy/benchmark/#solverpy.benchmark","title":"<code>solverpy.benchmark</code>","text":""},{"location":"api/solverpy/benchmark/#solverpy.benchmark--module-overview","title":"Module overview","text":""},{"location":"api/solverpy/benchmark/#solverpy.benchmark--legend","title":"Legend","text":""},{"location":"api/solverpy/benchmark/#solverpy.benchmark--multiprocessing","title":"Multiprocessing","text":"<p>The following diagram highlights places where new Python processes or threads are launched. <code>launcher</code> launches Pool for parallel execution of solvers. Method <code>prettytuner</code> launches <code>tuner</code> in a separate process to avoid issues with unallocated memory. Note that this might involve pool execution for ATP evaluation.</p> <p> </p>"},{"location":"api/solverpy/benchmark/#solverpy.benchmark--big-view","title":"Big View","text":"<p>%solverpy.task.SolverTask --|&gt; solverpy.task.Task</p>"},{"location":"api/solverpy/benchmark/#solverpy.benchmark--parallel-benchmark-evaluation","title":"Parallel benchmark evaluation","text":"<p>To evaluate a set of strategies on a set of benchmark problems, you just need to provide your experiment description as a Python <code>dict</code> and launch the experiments.</p> <pre><code>from solverpy import setups\n\nmysetup = ...\n\nsetups.launch(mysetup)\n</code></pre> <p>The experiment setup (<code>mysetup</code>) must have specific keys.  The module <code>solverpy.benchmark.setups</code> contains methods to fill in the required keys and values.</p> <p>You must specify at least the following:</p> key type description <code>cores</code> <code>int</code> number of CPU cores to use for parallel evaluation <code>strategies</code> <code>[str]</code> list of strategies to evaluate <code>benchmarks</code> <code>[str]</code> list of problems to evaluate on <code>limit</code> <code>str</code> the resource limit for a single solver run"},{"location":"api/solverpy/benchmark/#solverpy.benchmark--strategies-and-strategy-ids","title":"Strategies and strategy id's","text":"<p>Strategies are stored in files in the directory <code>solverpy_db/strats</code> which must exist in the current working directory (the directory is adjustable by the <code>SOLVERPY_DB</code> environment variable). The filename of each strategy is used to reference the strategy in <code>strategies</code> and it is called the strategy id (<code>sid</code>).</p> <p>Hence, for every <code>sid</code> in <code>strategies</code> in <code>mysetup</code>, there must be the file <code>solverpy_db/strats/sid</code> in the current working directory. This file contains the strategy definition (typically command line options) to pass to the <code>solver.solve</code> method.</p>"},{"location":"api/solverpy/benchmark/#solverpy.benchmark--problems-and-benchmark-ids","title":"Problems and benchmark id's","text":"<p>Benchmark problem sets are represented by benchark id's (<code>bid</code>).  The benchmark id is a file path relative to the current working directory (adjustable by the <code>SOLVERPY_BENCHMARKS</code> environment variable) pointing either to a file or to a directory.</p> <p>If the path leads to a:</p> <ul> <li><code>directory</code>: Then every regular file directly in this directory is considered a benchmark problem.    Directories and hidden files are ignored and no recursive search is performed.    This variant is useful when you have a set of problem files, all in one directory.</li> <li><code>file</code>: Then the file consists of lines containing paths to corresponding problem files.    The paths are relative to the directory of the <code>bid</code> file.  For example, if  the <code>bid</code> is <code>my/problems/subset1</code> and this file contains (among others) the line <code>category1/problem23.smt2</code> then the problem must be placed in <code>my/problems/category1/problem23.smt2</code> (because the directory of the <code>bid</code> file is <code>my/problems</code>).    This variant is useful when your benchmarks are structured in subdirectories and you don't want to merge them into one directory.</li> </ul>"},{"location":"api/solverpy/benchmark/#solverpy.benchmark--experiments-example","title":"Experiments example","text":"<p>Suppose you have some SMT problems in the directory <code>myproblems</code> and that you want to evaluate your cvc5 strategies <code>buzzard</code>, <code>sparrow</code>, and <code>chickadee</code>, which you have placed in the directory <code>solverpy_db/strats</code>.  You can download the archive with files for this example here.</p> <p>You proceed as follows.  First, you create the description of your experiments in <code>mysetup</code>.</p> <pre><code>from solverpy import setups\n\nmysetup = {\n    \"cores\": 4,\n    \"limit\": \"T10\",\n    \"benchmarks\": [\"myproblems\"],\n    \"strategies\": [\"buzzard\", \"sparrow\", \"chickadee\"],\n}\n</code></pre> <p>Hint: Add <code>mysetup[\"options\"] = [\"outputs\"]</code> if you want to keep raw solver output files from all solver runs.  </p> <p>Hint: Options are slightly more described in <code>Setup</code></p> <p>Then you specify that you want to use cvc5 and that you wish to launch an evaluation.  These methods update <code>mysetup</code> and fill in some keys required by <code>setups.launch()</code>.</p> <pre><code>setups.cvc5(mysetup)\nsetups.evaluation(mysetup)\n</code></pre> <p>Finally, you launch the experiments.</p> <pre><code>setups.launch(mysetup)\n</code></pre> <p>You will see the progress of the experiments on the screen.  Once finished, you will find the following subdirectories inside <code>solverpy_db</code>:</p> directory content <code>results</code> Results by each strategy (<code>sid</code>) for each <code>bid</code>.  The result for each <code>sid</code> and <code>bid</code> is a JSON file (gzip-ed) with a Python dictionary <code>{problem: result}</code>. <code>solved</code> List of solved problem names by each strategy for each <code>bid</code>.  One per line, easy to <code>grep</code> and <code>cat</code>. <code>status</code> Statuses of all problems by each strategy for each <code>bid</code>.  Problem name and status at one line, TAB separated.  Easy to <code>cut</code>. <code>log</code> Console log for each <code>solverpy</code> experiment run. <code>outputs</code> Raw solver output files for each solver run (only if selected). <p>Now run the script again and notice that it finished much faster.  It is because the cached results were reused and no solvers were actually launched.  So be careful and always clean the database if you want to force recompute.  Simply delete all the directories in <code>solverpy_db</code> except <code>strats</code> (see the script <code>clean_db.sh</code> in the example archive).</p>"},{"location":"api/solverpy/benchmark/evaluation/","title":"module evaluation","text":""},{"location":"api/solverpy/benchmark/evaluation/#solverpy.benchmark.evaluation","title":"<code>solverpy.benchmark.evaluation</code>","text":""},{"location":"api/solverpy/benchmark/summary/","title":"module summary","text":""},{"location":"api/solverpy/benchmark/summary/#solverpy.benchmark.summary","title":"<code>solverpy.benchmark.summary</code>","text":""},{"location":"api/solverpy/benchmark/db/","title":"Db Overview","text":""},{"location":"api/solverpy/benchmark/db/#solverpy.benchmark.db","title":"<code>solverpy.benchmark.db</code>","text":"<p>This module defines the database for benchmarking.</p>"},{"location":"api/solverpy/benchmark/db/#solverpy.benchmark.db.default","title":"<code>default(delfix: int | str | None = None) -&gt; DB</code>","text":"<p>Create a database with default providers: <code>Jsons</code>, <code>Solved</code>, and <code>Status</code>.</p> <p>Parameters:</p> Name Type Description Default <code>delfix</code> <p>Prefix to remove from problem names for <code>Solved</code> and <code>Status</code>.  See the <code>delfix</code> in <code>Setup</code> for the value format.</p> <code>None</code> <p>Returns:</p> Type Description <code>DB</code> <p>The database.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/db/__init__.py</code> <pre><code>def default(delfix: (int | str | None) = None) -&gt; DB:\n   \"\"\"\n   Create a database with default providers:\n   [`Jsons`][solverpy.benchmark.db.providers.jsons],\n   [`Solved`][solverpy.benchmark.db.providers.solved], and\n   [`Status`][solverpy.benchmark.db.providers.status].\n\n   Args:\n      delfix (): \n         Prefix to remove from problem names for\n         [`Solved`][solverpy.benchmark.db.providers.solved] and\n         [`Status`][solverpy.benchmark.db.providers.status]. \n         See the `delfix` in [`Setup`][solverpy.setups.setup.Setup]\n         for the value format.\n\n   Returns: \n      The database.\n   \"\"\"\n   return DB([\n      Jsons.Maker(),\n      Solved.Maker(delfix=delfix),\n      Status.Maker(delfix=delfix)\n   ])\n</code></pre>"},{"location":"api/solverpy/benchmark/db/cachedprovider/","title":"module cachedprovider","text":""},{"location":"api/solverpy/benchmark/db/cachedprovider/#solverpy.benchmark.db.cachedprovider","title":"<code>solverpy.benchmark.db.cachedprovider</code>","text":""},{"location":"api/solverpy/benchmark/db/db/","title":"module db","text":""},{"location":"api/solverpy/benchmark/db/db/#solverpy.benchmark.db.db","title":"<code>solverpy.benchmark.db.db</code>","text":""},{"location":"api/solverpy/benchmark/db/provider/","title":"module provider","text":""},{"location":"api/solverpy/benchmark/db/provider/#solverpy.benchmark.db.provider","title":"<code>solverpy.benchmark.db.provider</code>","text":""},{"location":"api/solverpy/benchmark/db/provider/#solverpy.benchmark.db.provider.Provider","title":"<code>Provider</code>","text":"<p>               Bases: <code>SolverPyObj</code></p> <p>A data provider that stores and/or queries results of tasks.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/db/provider.py</code> <pre><code>class Provider(SolverPyObj):\n   \"\"\"A data provider that stores and/or queries results of tasks.\"\"\"\n\n   def __init__(\n      self,\n      bid: str,\n      sid: str,\n      limit: (str | None) = None,\n      caching: bool = False,\n   ):\n      self.bid = bid\n      self.sid = sid\n      self.limit = limit\n      self.caching = caching\n      self._uptodate = False\n      \"call store for cached results.\"\n\n   @classmethod\n   def Maker(cls, **kwargs) -&gt; \"ProviderMaker\":\n      return _ProviderMaker(cls, **kwargs)\n\n   def query(\n      self,\n      task: \"SolverTask\",\n   ) -&gt; (dict[str, Any] | None):\n      \"\"\"Return the cached result for `task` or None if not available.\"\"\"\n      del task  # unused argument\n      return None\n\n   def store(\n      self,\n      task: \"SolverTask\",\n      result: dict[str, Any],\n   ):\n      \"\"\"New result for `task` was found.  Update the cache.\"\"\"\n      del task, result  # unused arguments\n      pass\n\n   def cached(\n      self,\n      task: \"SolverTask\",\n      result: dict[str, Any],\n   ):\n      \"\"\"Announcement that the cached `result` for `task` was found.\"\"\"\n      if self.caching:\n         self.store(task, result)\n\n   def commit(self) -&gt; None:\n      \"\"\"Save/flush the data.\"\"\"\n      pass\n\n   def check(self, task: \"SolverTask\") -&gt; None:\n      if (self.bid != task.bid) or (self.sid != task.sid):\n         raise Exception(\"Error: Operation on invalid bid/sid in a provider.\")\n</code></pre>"},{"location":"api/solverpy/benchmark/db/provider/#solverpy.benchmark.db.provider.Provider.cached","title":"<code>cached(task: SolverTask, result: dict[str, Any])</code>","text":"<p>Announcement that the cached <code>result</code> for <code>task</code> was found.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/db/provider.py</code> <pre><code>def cached(\n   self,\n   task: \"SolverTask\",\n   result: dict[str, Any],\n):\n   \"\"\"Announcement that the cached `result` for `task` was found.\"\"\"\n   if self.caching:\n      self.store(task, result)\n</code></pre>"},{"location":"api/solverpy/benchmark/db/provider/#solverpy.benchmark.db.provider.Provider.commit","title":"<code>commit() -&gt; None</code>","text":"<p>Save/flush the data.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/db/provider.py</code> <pre><code>def commit(self) -&gt; None:\n   \"\"\"Save/flush the data.\"\"\"\n   pass\n</code></pre>"},{"location":"api/solverpy/benchmark/db/provider/#solverpy.benchmark.db.provider.Provider.query","title":"<code>query(task: SolverTask) -&gt; dict[str, Any] | None</code>","text":"<p>Return the cached result for <code>task</code> or None if not available.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/db/provider.py</code> <pre><code>def query(\n   self,\n   task: \"SolverTask\",\n) -&gt; (dict[str, Any] | None):\n   \"\"\"Return the cached result for `task` or None if not available.\"\"\"\n   del task  # unused argument\n   return None\n</code></pre>"},{"location":"api/solverpy/benchmark/db/provider/#solverpy.benchmark.db.provider.Provider.store","title":"<code>store(task: SolverTask, result: dict[str, Any])</code>","text":"<p>New result for <code>task</code> was found.  Update the cache.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/db/provider.py</code> <pre><code>def store(\n   self,\n   task: \"SolverTask\",\n   result: dict[str, Any],\n):\n   \"\"\"New result for `task` was found.  Update the cache.\"\"\"\n   del task, result  # unused arguments\n   pass\n</code></pre>"},{"location":"api/solverpy/benchmark/db/providers/jsons/","title":"module jsons","text":""},{"location":"api/solverpy/benchmark/db/providers/jsons/#solverpy.benchmark.db.providers.jsons","title":"<code>solverpy.benchmark.db.providers.jsons</code>","text":""},{"location":"api/solverpy/benchmark/db/providers/loader/","title":"module loader","text":""},{"location":"api/solverpy/benchmark/db/providers/loader/#solverpy.benchmark.db.providers.loader","title":"<code>solverpy.benchmark.db.providers.loader</code>","text":""},{"location":"api/solverpy/benchmark/db/providers/solved/","title":"module solved","text":""},{"location":"api/solverpy/benchmark/db/providers/solved/#solverpy.benchmark.db.providers.solved","title":"<code>solverpy.benchmark.db.providers.solved</code>","text":""},{"location":"api/solverpy/benchmark/db/providers/solved/#solverpy.benchmark.db.providers.solved.delfix","title":"<code>delfix(problem: str, fix: str | int | None) -&gt; str</code>","text":"<p>Delete a prefix of a problem name.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/db/providers/solved.py</code> <pre><code>def delfix(\n      problem: str,\n      fix: (str | int | None),\n) -&gt; str:\n   \"\"\"Delete a prefix of a problem name.\"\"\"\n   if not fix:  # covers None, 0, \"\" (also False)\n      return problem\n   if (type(fix) is str) and problem.startswith(fix):\n      return problem[len(fix):]\n   if (type(fix) is int) and problem.count(\"/\") &gt;= fix:\n      parts = problem.split(\"/\")\n      return \"/\".join(parts[fix:])\n   logger.warning(f\"Uknown delfix value type {type(fix)} of '{fix}'\")\n   return problem\n</code></pre>"},{"location":"api/solverpy/benchmark/db/providers/status/","title":"module status","text":""},{"location":"api/solverpy/benchmark/db/providers/status/#solverpy.benchmark.db.providers.status","title":"<code>solverpy.benchmark.db.providers.status</code>","text":""},{"location":"api/solverpy/benchmark/path/","title":"Path Overview","text":""},{"location":"api/solverpy/benchmark/path/#solverpy.benchmark.path","title":"<code>solverpy.benchmark.path</code>","text":""},{"location":"api/solverpy/benchmark/path/#solverpy.benchmark.path--benchmark-and-strategy-paths","title":"Benchmark and strategy paths","text":"<p>Two modules handle path resolution for the two kinds of identifiers used throughout the benchmark pipeline:</p> Module Identifier Resolves to <code>bids</code> benchmark id (<code>bid</code>) directory or file listing problem paths, rooted at <code>SOLVERPY_BENCHMARKS</code> <code>sids</code> strategy id (<code>sid</code>) strategy definition file in <code>solverpy_db/strats/</code>, rooted at <code>SOLVERPY_DB</code> <p>Both identifiers are plain strings \u2014 no slashes in <code>sid</code>, forward-slash path segments in <code>bid</code>.  The resolution is always relative to environment variables so that the same setup dict can be reused across machines.</p>"},{"location":"api/solverpy/benchmark/path/bids/","title":"module bids","text":""},{"location":"api/solverpy/benchmark/path/bids/#solverpy.benchmark.path.bids","title":"<code>solverpy.benchmark.path.bids</code>","text":""},{"location":"api/solverpy/benchmark/path/bids/#solverpy.benchmark.path.bids--benchmark-identifiers-bid","title":"Benchmark identifiers (bid)","text":"<p>Benchmark problem sets are represented by benchark ids (<code>bid</code>).  The benchmark id is a file path relative to the directory in the <code>SOLVERPY_BENCHMARKS</code> environment variable, defaulting to the current working directory. The file path must point either to a file or to a directory. If the path leads to a:</p> <ul> <li>directory: then every regular file directly in this directory is considered a benchmark problem.    Directories and hidden files are ignored and no recursive search is performed.    This variant is useful when you have a set of problem files, all in one directory.</li> <li>file: then the file consists of lines containing paths to corresponding problem files.    The paths are relative to the directory of the <code>bid</code> file.  This variant is    useful when your benchmarks are structured in subdirectories and you don't    want to merge them into one directory.</li> </ul> Example <p>If  the <code>bid</code> is <code>my/problems/subset1</code> and this points to a file that contains (among others) the line <code>category1/problem23.smt2</code> then the corresponding problem must be placed in <code>my/problems/category1/problem23.smt2</code> (because the directory of the <code>bid</code> file is <code>my/problems</code>). </p>"},{"location":"api/solverpy/benchmark/path/bids/#solverpy.benchmark.path.bids--module-content","title":"Module content","text":""},{"location":"api/solverpy/benchmark/path/bids/#solverpy.benchmark.path.bids.bidpath","title":"<code>bidpath(bid: str) -&gt; str</code>","text":"<p>Return the path to the benchmark id file or directory.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/bids.py</code> <pre><code>def bidpath(bid: str) -&gt; str:\n   \"\"\"Return the path to the benchmark id file or directory.\"\"\"\n   return os.path.join(os.getenv(\"SOLVERPY_BENCHMARKS\", DEFAULT_NAME), bid)\n</code></pre>"},{"location":"api/solverpy/benchmark/path/bids/#solverpy.benchmark.path.bids.dbpath","title":"<code>dbpath(subdir: str | None = None) -&gt; str</code>","text":"<p>Return the full path to the database directory.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/bids.py</code> <pre><code>def dbpath(subdir: str | None = None) -&gt; str:\n   \"\"\"Return the full path to the database directory.\"\"\"\n   # TODO: move this elsewhere\n   base = os.getenv(\"SOLVERPY_DB\", DB_NAME)\n   return os.path.join(base, subdir) if subdir else base\n</code></pre>"},{"location":"api/solverpy/benchmark/path/bids/#solverpy.benchmark.path.bids.name","title":"<code>name(bid: str, limit: str | None = None) -&gt; str</code>","text":"<p>Translate the benchmark id to a shell-safe name.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/bids.py</code> <pre><code>def name(bid: str, limit: str | None = None) -&gt; str:\n   \"\"\"Translate the benchmark id to a shell-safe name.\"\"\"\n   bid = bid.replace(\"/\", \"--\")\n   if limit:\n      bid = f\"{bid}--{limit}\"\n   return bid\n</code></pre>"},{"location":"api/solverpy/benchmark/path/bids/#solverpy.benchmark.path.bids.path","title":"<code>path(bid: str, problem: str, flatten: bool | str = False) -&gt; str</code>","text":"<p>Return the path to the benchmark problem file.</p> <p>Parameters:</p> Name Type Description Default <code>bid</code> <code>str</code> <p>The benchmark id.</p> required <code>problem</code> <code>str</code> <p>The problem name.</p> required <code>flatten</code> <code>bool | str</code> <p>Flatten the problem name so that all problems can be placed in the same directory. If <code>False</code>, then no flattening is performed. If <code>True</code>, then <code>/</code> is replaced with <code>_._</code>. If a string, then <code>/</code> is replaced with the given string.</p> <code>False</code> <p>Returns:</p> Type Description <code>str</code> <p>Full path to the problem file.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/bids.py</code> <pre><code>def path(\n   bid: str,\n   problem: str,\n   flatten: bool | str = False,\n) -&gt; str:\n   \"\"\"\n   Return the path to the benchmark problem file.\n\n   Args:\n       bid: The benchmark id.\n       problem: The problem name.\n       flatten: Flatten the problem name so that all problems can be placed in\n           the same directory. If `False`, then no flattening is performed. If\n           `True`, then `/` is replaced with `_._`. If a string, then `/` is\n           replaced with the given string.\n\n   Returns:\n       Full path to the problem file.\n\n   \"\"\"\n   \"\"\"Return the path to the problem file.\"\"\"\n   p_bid = bidpath(bid)\n   if os.path.isfile(p_bid):\n      p_bid = os.path.dirname(p_bid)\n   if flatten and problem:\n      problem = problem.replace(\"/\", \"_._\" if flatten is True else flatten)\n   return os.path.join(p_bid, problem).rstrip(\"/\")\n</code></pre>"},{"location":"api/solverpy/benchmark/path/bids/#solverpy.benchmark.path.bids.problems","title":"<code>problems(bid: str, cache: dict[str, list[str]] = {}) -&gt; list[str]</code>","text":"<p>Return the list of all benchmark problems.</p> <p>Parameters:</p> Name Type Description Default <code>bid</code> <code>str</code> <p>The benchmark id.</p> required <code>cache</code> <code>dict[str, list[str]]</code> <p>The problem cache for every loaded <code>bid</code>.  Each <code>bid</code> is loaded just once.</p> <code>{}</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>The list of all benchmark problems.  The pair (<code>bid</code>, <code>problem</code>) is a unique problem identifier.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/bids.py</code> <pre><code>def problems(\n   bid: str,\n   cache: dict[str, list[str]] = {},\n) -&gt; list[str]:\n   \"\"\"\n   Return the list of all benchmark problems.\n\n   Args:\n       bid: The benchmark id.\n       cache: The problem cache for every loaded `bid`.  Each `bid` is loaded\n           just once.\n\n   Returns:\n       The list of all benchmark problems.  The pair (`bid`, `problem`) is\n           a unique problem identifier.\n   \"\"\"\n   if bid in cache:\n      return cache[bid]\n   p_bid = bidpath(bid)\n   if os.path.isfile(p_bid):\n      probs = open(p_bid).read().strip().split(\"\\n\")\n   else:  # now os.path.isdir(p_bid) holds\n      probs = [x for x in os.listdir(p_bid) \\\n         if os.path.isfile(os.path.join(p_bid,x))]\n   cache[bid] = probs\n   return probs\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/","title":"module sids","text":""},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids","title":"<code>solverpy.benchmark.path.sids</code>","text":""},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids--strategy-identifiers-sid","title":"Strategy identifiers (sid)","text":"<p>Strategy is a collection of solver options, parameters, settings, etc., that influence the space search behavior of the solver. In SolverPy, this collection must be stored in a file and placed inside the <code>strats</code> database directory, by default <code>solverpy_db/strats</code> (see the <code>db</code> module). The file name inside the <code>strats</code> directory is called the strategy id (<code>sid</code>). The content of the file is a solver-specific strategy definition that is passed to the <code>Solver.solve</code> method.</p> Example <p>For <code>E</code> Prover, you can have the file <code>solverpy_db/strats/auto</code> with the following content:</p> <pre><code>--auto-schedule\n</code></pre> <p>Then <code>--auto-schedule</code> is the strategy definition while <code>auto</code> is its strategy id. You can now use <code>auto</code> in <code>strategies</code> to reference the strategy in experiment setup <code>Setup</code> with <code>E</code> Prover.</p> Note <p>As opposed to a benchmark identifier <code>bid</code>, a strategy identifier <code>sid</code> can not contain <code>/</code> characters.</p>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids--parametric-strategies","title":"Parametric Strategies","text":"<p>Some solvers support parametric strategies with variable slots in the strategy definition.  The syntax for variable slots is as follows:</p> <pre><code>@@@ var : value @@@\n</code></pre> <p>where <code>var</code> is the variable name and <code>value</code> is its default value.</p> Note <p>Names <code>var</code> and <code>value</code> can not contain characters <code>@</code>, <code>:</code>, or space.</p> <p>The value of a variable can be overridden in the strategy identifier using the following format:</p> <pre><code>sid@var1=value1:var2=value2\n</code></pre> <p>Here <code>sid</code> still refers to the base strategy id, while <code>var1=value1</code> and <code>var2=value2</code> are different variable assignments. Any number of <code>:</code>-separated variable assignments can be provided in the strategy id.</p> Example <p>For <code>E</code> Prover, you can have a parametric strategy <code>gen</code> with the following definition:</p> <pre><code>--enigmatic-gen-threshold=@@@ thrgen : 0.1 @@@\n</code></pre> <p>Then the strategy id (sid)</p> <pre><code>gen@thrgen=0.5\n</code></pre> <p>results in the following strategy definition:</p> <pre><code>--enigmatic-gen-threshold=0.5\n</code></pre> See Also <p>See the <code>Sid</code> solver plugin that automatically translates strategy ids to strategy definitions. For more details about solver-specific strategy formats, see concrete solvers implemented in the <code>solver</code> module.</p>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids--module-content","title":"Module content","text":"Note <p>In the code base, arguments/variables <code>sid</code> refer to the strategy identifiers, while <code>strategy</code> refers to the strategy definition.</p>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.defaults","title":"<code>defaults(strategy: str) -&gt; dict[str, str]</code>","text":"<p>Extract variables and default values from the strategy definition.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def defaults(strategy: str) -&gt; dict[str, str]:\n   \"\"\"Extract variables and default values from the strategy definition.\"\"\"\n   ret = ARGUMENT.findall(strategy)\n   ret = {x.strip():y.strip() for (x,y) in ret}\n   return ret\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.fmt","title":"<code>fmt(base: str, args: dict[str, str]) -&gt; str</code>","text":"<p>Format strategy file with arguments.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def fmt(base: str, args: dict[str, str]) -&gt; str:\n   \"\"\"Format strategy file with arguments.\"\"\"\n   args0 = \":\".join(f\"{x}={args[x]}\" for x in sorted(args))\n   return f\"{base}@{args0}\"\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.instatiate","title":"<code>instatiate(strategy: str, args: dict[str, str]) -&gt; str</code>","text":"<p>Instantiate the strategy with arguments.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def instatiate(strategy: str, args: dict[str, str]) -&gt; str:\n   \"\"\"Instantiate the strategy with arguments.\"\"\"\n   args0 = defaults(strategy)\n   args0.update(args)\n   ret = ARGUMENT.sub(lambda mo: args0[mo.group(1)], strategy)\n   return ret\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.load","title":"<code>load(sid: str) -&gt; str</code>","text":"<p>Load the strategy definition.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def load(sid: str) -&gt; str:\n   \"\"\"Load the strategy definition.\"\"\"\n   return open(path(sid)).read().strip()\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.normalize","title":"<code>normalize(sid: str) -&gt; str</code>","text":"<p>Remove arguments from sid which set default values and sort arguments.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def normalize(sid: str) -&gt; str:\n   \"\"\"Remove arguments from sid which set default values and sort arguments.\"\"\"\n   strategy = load(sid)\n   defs = defaults(strategy)\n   (sid, args) = split(sid)\n   args = {x:y for (x,y) in args.items() if y != defs[x]}\n   return fmt(sid, args)\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.path","title":"<code>path(sid: str) -&gt; str</code>","text":"<p>Return the path to the strategy file.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def path(sid: str) -&gt; str:\n   \"\"\"Return the path to the strategy file.\"\"\"\n   f_sid = sid.split(\"@\")[0] if (\"@\" in sid) else sid\n   return os.path.join(bids.dbpath(NAME), f_sid)\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.resolve","title":"<code>resolve(sid: str, strategy: str) -&gt; str</code>","text":"<p>Instantiate the strategy definition if parametric.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def resolve(sid: str, strategy: str) -&gt; str:\n   \"\"\"Instantiate the strategy definition if parametric.\"\"\"\n   if \"@@@\" in strategy:\n      (sid, args) = split(sid)\n      strategy = instatiate(strategy, args)\n   return strategy\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.save","title":"<code>save(sid: str, strategy: str) -&gt; None</code>","text":"<p>Save the strategy definition.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def save(sid: str, strategy: str) -&gt; None:\n   \"\"\"Save the strategy definition.\"\"\"\n   f_sid = path(sid)\n   os.makedirs(os.path.dirname(f_sid), exist_ok=True)\n   open(f_sid, \"w\").write(strategy.strip())\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.split","title":"<code>split(sid: str) -&gt; tuple[str, dict[str, str]]</code>","text":"<p>Split sid into the strategy file and arguments.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def split(sid: str) -&gt; tuple[str, dict[str, str]]:\n   \"\"\"Split sid into the strategy file and arguments.\"\"\"\n   args = {}\n   if \"@\" in sid:\n      (sid, args) = sid.split(\"@\")\n      args = args.split(\":\")\n      args = [x.split(\"=\") for x in args]      \n      args = {x.strip():y.strip() for (x,y) in args}\n   return (sid, args)\n</code></pre>"},{"location":"api/solverpy/benchmark/path/sids/#solverpy.benchmark.path.sids.unspace","title":"<code>unspace(strategy: str) -&gt; str</code>","text":"<p>Merge multiple white spaces into a single space.</p> Source code in <code>packages/solverpy/src/solverpy/benchmark/path/sids.py</code> <pre><code>def unspace(strategy: str) -&gt; str:\n   \"\"\"Merge multiple white spaces into a single space.\"\"\"\n   return \" \".join(x for x in strategy.split() if x)\n</code></pre>"},{"location":"api/solverpy/benchmark/reports/data/","title":"module data","text":""},{"location":"api/solverpy/benchmark/reports/data/#solverpy.benchmark.reports.data","title":"<code>solverpy.benchmark.reports.data</code>","text":""},{"location":"api/solverpy/benchmark/reports/markdown/","title":"module markdown","text":""},{"location":"api/solverpy/benchmark/reports/markdown/#solverpy.benchmark.reports.markdown","title":"<code>solverpy.benchmark.reports.markdown</code>","text":""},{"location":"api/solverpy/benchmark/reports/progress/","title":"module progress","text":""},{"location":"api/solverpy/benchmark/reports/progress/#solverpy.benchmark.reports.progress","title":"<code>solverpy.benchmark.reports.progress</code>","text":""},{"location":"api/solverpy/report/","title":"Report Overview","text":""},{"location":"api/solverpy/report/#solverpy.report","title":"<code>solverpy.report</code>","text":""},{"location":"api/solverpy/report/#solverpy.report--report-logging-and-progress-reporting","title":"Report \u2014 logging and progress reporting","text":"<p>Contains logging configuration (<code>log</code>) and the talker class hierarchy (<code>talker</code>) for reporting evaluation and tuning progress.</p>"},{"location":"api/solverpy/report/log/","title":"module log","text":""},{"location":"api/solverpy/report/log/#solverpy.report.log","title":"<code>solverpy.report.log</code>","text":""},{"location":"api/solverpy/report/talker/","title":"Talker Overview","text":""},{"location":"api/solverpy/report/talker/#solverpy.report.talker","title":"<code>solverpy.report.talker</code>","text":""},{"location":"api/solverpy/report/talker/#solverpy.report.talker--talker-progress-reporters-for-benchmark-evaluation","title":"Talker \u2014 progress reporters for benchmark evaluation","text":"<p>Contains the talker class hierarchy for reporting evaluation and tuning progress.</p> <p> </p> <p><code>LogTalker</code> logs progress to the console/file. <code>EvalTalker</code> additionally displays a live progress bar using <code>tqdm</code>. <code>RemoteTalker</code> is a cross-process proxy that forwards method calls from a child process to the parent.</p>"},{"location":"api/solverpy/report/talker/bar/","title":"module bar","text":""},{"location":"api/solverpy/report/talker/bar/#solverpy.report.talker.bar","title":"<code>solverpy.report.talker.bar</code>","text":""},{"location":"api/solverpy/report/talker/bar/#solverpy.report.talker.bar--progress-bars","title":"Progress bars","text":"<p><code>tqdm</code>-based progress bars used by <code>EvalTalker</code>: <code>RunningBar</code> for a plain job count with an error tally, and <code>SolvingBar</code> which further splits progress into solved/unsolved/errors.</p>"},{"location":"api/solverpy/report/talker/bar/#solverpy.report.talker.bar.BuilderBar","title":"<code>BuilderBar</code>","text":"<p>               Bases: <code>tqdm</code></p> <p><code>tqdm</code> bar for model-building progress, showing per-iteration loss values.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/bar.py</code> <pre><code>class BuilderBar(tqdm):\n   \"\"\"`tqdm` bar for model-building progress, showing per-iteration loss values.\"\"\"\n\n   def __init__(\n      self,\n      total: int,\n      desc: str,\n      ascii: str = \"\u2591\u2592\u2593\u2588\",\n      colour: str = \"green\",\n      bar_format: str = BAR_BUILDER,\n      *args: Any,\n      **kwargs: Any,\n   ):\n      self._loss = []\n      self._bar_colour = COLOUR_MAP.get(colour, \"\")\n      tqdm.__init__(\n         self,\n         total=total,\n         desc=desc,\n         bar_format=bar_format,\n         ascii=ascii,\n         colour=colour,\n         *args,\n         **kwargs,\n      )\n\n   @property\n   def format_dict(self) -&gt; Any:\n      d = super().format_dict\n      d.update(\n         loss=self._loss,\n         bar_fixed=_build_bar(d[\"n\"], d[\"total\"] or 0, self._bar_colour),\n      )\n      return d\n\n   def status(\n      self,\n      metrics: dict[str, dict[str, float]],\n      n: int = 1,\n   ) -&gt; None:\n      self._loss = \"/\".join(f\"{value:.4f}\" for values in metrics.values()\n                            for value in values.values())\n      self.update(n)\n</code></pre>"},{"location":"api/solverpy/report/talker/bar/#solverpy.report.talker.bar.DefaultBar","title":"<code>DefaultBar</code>","text":"<p>               Bases: <code>tqdm</code></p> <p><code>tqdm</code> bar rendering a fixed-width filled bar and an ETA speed estimate.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/bar.py</code> <pre><code>class DefaultBar(tqdm):\n   \"\"\"`tqdm` bar rendering a fixed-width filled bar and an ETA speed estimate.\"\"\"\n\n   def __init__(\n      self,\n      total: int,\n      desc: str,\n      ascii: str = \"\u2591\u2592\u2593\u2588\",\n      colour: str = \"green\",\n      bar_format: str = BAR_DEFAULT,\n      *args: Any,\n      **kwargs: Any,\n   ):\n      self._bar_colour = COLOUR_MAP.get(colour, \"\")\n      tqdm.__init__(\n         self,\n         total=total,\n         desc=desc,\n         bar_format=bar_format,\n         ascii=ascii,\n         colour=colour,\n         *args,\n         **kwargs,\n      )\n\n   @property\n   def format_dict(self) -&gt; Any:\n      d = super().format_dict\n      speta = d[\"elapsed\"] * (d[\"total\"] or 0) / max(d[\"n\"], 1)\n      speta -= d[\"elapsed\"]\n      speta = str(datetime.timedelta(seconds=int(speta)))\n      if speta.startswith(\"0:\"):\n         speta = speta[2:]\n      d.update(\n         speta=speta,\n         bar_fixed=_build_bar(d[\"n\"], d[\"total\"] or 0, self._bar_colour),\n      )\n      return d\n\n   def status(self, status: Any, n: int = 1) -&gt; None:\n      del status  # unused argument\n      self.update(n)\n</code></pre>"},{"location":"api/solverpy/report/talker/bar/#solverpy.report.talker.bar.RunningBar","title":"<code>RunningBar</code>","text":"<p>               Bases: <code>DefaultBar</code></p> <p><code>DefaultBar</code> with an error tally shown alongside progress.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/bar.py</code> <pre><code>class RunningBar(DefaultBar):\n   \"\"\"[`DefaultBar`][solverpy.report.talker.bar.DefaultBar] with an error tally shown alongside progress.\"\"\"\n\n   def __init__(\n      self,\n      total: int,\n      desc: str,\n      bar_format: str = BAR_RUNNING,\n      postfix_width: int = 0,\n      *args: Any,\n      **kwargs: Any,\n   ):\n      self._errors = 0\n      self._postfix_width = postfix_width\n      DefaultBar.__init__(\n         self,\n         total,\n         desc,\n         bar_format=bar_format,\n         *args,\n         **kwargs,\n      )\n\n   @property\n   def format_dict(self) -&gt; Any:\n      d = super().format_dict\n      asc = f\"!{self._errors}\"\n      if self._errors:\n         asc = f\"{RED}{asc}{END}\"\n      vis = 1 + len(str(self._errors))  # visible chars of \"!N\"\n      pad = \" \" * max(0, self._postfix_width - vis)\n      d.update(errors=asc, pad=pad)\n      return d\n\n   def status(self, status: bool | None, n: int = 1) -&gt; None:\n      if status is None:\n         self._errors += n\n      self.update(n)\n</code></pre>"},{"location":"api/solverpy/report/talker/bar/#solverpy.report.talker.bar.SolvingBar","title":"<code>SolvingBar</code>","text":"<p>               Bases: <code>RunningBar</code></p> <p><code>RunningBar</code> splitting progress into solved/unsolved/errors.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/bar.py</code> <pre><code>class SolvingBar(RunningBar):\n   \"\"\"[`RunningBar`][solverpy.report.talker.bar.RunningBar] splitting progress into solved/unsolved/errors.\"\"\"\n\n   def __init__(\n      self,\n      total: int,\n      desc: str,\n      bar_format: str = BAR_SOLVING,\n      ascii: str = \"\u2591\u2592\u2593\u2588\",\n      colour: str = \"blue\",\n      *args: Any,\n      **kwargs: Any,\n   ):\n      self._solved = 0\n      self._unsolved = 0\n      RunningBar.__init__(\n         self,\n         total,\n         desc,\n         bar_format=bar_format,\n         ascii=ascii,\n         colour=colour,\n         *args,\n         **kwargs,\n      )\n\n   @property\n   def format_dict(self) -&gt; Any:\n      d = super().format_dict\n      total = d[\"total\"] or 0\n      solved_eta = int(self._solved * (total / max(d[\"n\"], 1)))\n      pw = _postfix_width(total)\n      vis = (1 + len(str(self._solved)) + 1 + len(str(self._unsolved)) + 2 +\n             len(str(self._errors)) + 2 + len(str(solved_eta)))\n      pad = \" \" * max(0, pw - vis)\n      d.update(\n         solved=f\"{PURPLE}+{self._solved}{END}\",\n         unsolved=f\"{BLUE}{self._unsolved}{END}\",\n         solved_eta=f\"{PURPLE}?{solved_eta}{END}\",\n         pad=pad,\n      )\n      return d\n\n   def status(\n      self,\n      status: bool | None,\n      n: int = 1,\n   ) -&gt; None:\n      if status is True:\n         self._solved += n\n      elif status is False:\n         self._unsolved += n\n      else:  # status is None:\n         self._errors += n\n      self.update(n)\n</code></pre>"},{"location":"api/solverpy/report/talker/evaltalker/","title":"module evaltalker","text":""},{"location":"api/solverpy/report/talker/evaltalker/#solverpy.report.talker.evaltalker","title":"<code>solverpy.report.talker.evaltalker</code>","text":""},{"location":"api/solverpy/report/talker/evaltalker/#solverpy.report.talker.evaltalker--evaltalker-interactive-progress-bar-reporter","title":"EvalTalker \u2014 interactive progress bar reporter","text":"<p>Extends <code>LogTalker</code> with tqdm progress bars: a per-job <code>SolvingBar</code> and a cumulative <code>RunningBar</code> across all jobs.  Used in interactive (terminal) mode; falls back to <code>LogTalker</code> text output in headless mode.</p>"},{"location":"api/solverpy/report/talker/evaltalker/#solverpy.report.talker.evaltalker.EvalTalker","title":"<code>EvalTalker</code>","text":"<p>               Bases: <code>LogTalker</code></p> <p>Interactive progress reporter with tqdm bars.</p> <p> </p> <p>Creates two bars on <code>begin</code>: a <code>RunningBar</code> spanning the entire evaluation and a per-job <code>SolvingBar</code> created in <code>launching</code>.  Both are closed and cleared on <code>terminate</code>.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/evaltalker.py</code> <pre><code>class EvalTalker(LogTalker):\n   \"\"\"\n   Interactive progress reporter with tqdm bars.\n\n   ```plantuml name=\"task-evaltalker\"\n   abstract class solverpy.report.talker.talker.Talker\n   class solverpy.report.talker.logtalker.LogTalker extends solverpy.report.talker.talker.Talker\n   class solverpy.report.talker.evaltalker.EvalTalker extends solverpy.report.talker.logtalker.LogTalker {\n      - _job_bar: SolvingBar | None\n      - _total_bar: RunningBar | None\n      --\n      + begin(jobs, refjob, sidnames, **kwargs)\n      + end(results, refjob)\n      + terminate()\n      + next(job)\n      + launching(tasks)\n      + done()\n      + status(new, n)\n   }\n   ```\n\n   Creates two bars on `begin`: a `RunningBar` spanning the entire evaluation\n   and a per-job `SolvingBar` created in `launching`.  Both are closed and\n   cleared on `terminate`.\n   \"\"\"\n\n   def __init__(self, headless: bool = False) -&gt; None:\n      super().__init__(headless=headless)\n      self._job_bar: SolvingBar | None = None\n      self._total_bar: RunningBar | None = None\n\n   def eval_begin(\n      self,\n      jobs: list[\"SolverJob\"],\n      *,\n      refjob: \"SolverJob | None\" = None,\n      sidnames: bool = True,\n      miniters: int = 1,\n      **kwargs,\n   ) -&gt; None:\n      \"\"\"Create the total ``RunningBar`` spanning all jobs.\"\"\"\n      super().eval_begin(jobs, refjob=refjob, sidnames=sidnames, **kwargs)\n      if self._headless:\n         return\n      dw = self._nick_dw\n      prefix_len = 2 * dw + 3  # \"[n/m] \" without trailing space\n      total_desc = f\"{' ' * prefix_len} {self._total_desc}\"\n      max_job = max(len(_bids.problems(bid)) for (_, bid, _) in jobs)\n      self._total_bar = RunningBar(\n         total=self._total_count,\n         desc=total_desc,\n         miniters=miniters,\n         postfix_width=_postfix_width(max_job),\n      )\n\n   def eval_end(\n      self,\n      results: dict[\"SolverJob\", \"Result\"],\n      refjob: \"SolverJob | None\" = None,\n      **kwargs,\n   ) -&gt; None:\n      \"\"\"Harvest error count from the total bar if present, then delegate to ``LogTalker.eval_end``.\"\"\"\n      if self._total_bar:\n         self._total_errors = self._total_bar._errors\n      super().eval_end(results, refjob=refjob, **kwargs)\n\n   def terminate(self) -&gt; None:\n      \"\"\"Close and discard both bars, then stop the log queue.\"\"\"\n      super().terminate()\n      if self._total_bar:\n         self._total_bar.close()\n         self._total_bar = None\n      if self._job_bar:\n         self._job_bar.close()\n         self._job_bar = None\n\n   def eval_next(self, job: \"SolverJob\") -&gt; None:\n      super().eval_next(job)\n\n   def eval_launch(self, tasks: Sequence[\"Task\"]) -&gt; None:\n      \"\"\"Create the per-job ``SolvingBar`` and inject the log queue into tasks.\"\"\"\n      super().eval_launch(tasks)\n      if not self._headless:\n         self._job_bar = SolvingBar(len(tasks), self._job_desc, miniters=1)\n\n   def eval_done(self) -&gt; None:\n      \"\"\"Close the per-job bar and log the completion summary.\"\"\"\n      if not self._job_bar:\n         return\n      self._job_bar.close()\n      self._job_bar = None\n      super().eval_done()\n\n   def eval_status(self, new: bool | None, n: int = 1) -&gt; None:\n      \"\"\"Forward status update to both the total bar and the per-job bar.\"\"\"\n      super().eval_status(new, n)\n      if self._total_bar:\n         self._total_bar.status(new)\n      if self._job_bar:\n         self._job_bar.status(new)\n</code></pre>"},{"location":"api/solverpy/report/talker/evaltalker/#solverpy.report.talker.evaltalker.EvalTalker.eval_begin","title":"<code>eval_begin(jobs: list[SolverJob], *, refjob: SolverJob | None = None, sidnames: bool = True, miniters: int = 1, **kwargs) -&gt; None</code>","text":"<p>Create the total <code>RunningBar</code> spanning all jobs.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/evaltalker.py</code> <pre><code>def eval_begin(\n   self,\n   jobs: list[\"SolverJob\"],\n   *,\n   refjob: \"SolverJob | None\" = None,\n   sidnames: bool = True,\n   miniters: int = 1,\n   **kwargs,\n) -&gt; None:\n   \"\"\"Create the total ``RunningBar`` spanning all jobs.\"\"\"\n   super().eval_begin(jobs, refjob=refjob, sidnames=sidnames, **kwargs)\n   if self._headless:\n      return\n   dw = self._nick_dw\n   prefix_len = 2 * dw + 3  # \"[n/m] \" without trailing space\n   total_desc = f\"{' ' * prefix_len} {self._total_desc}\"\n   max_job = max(len(_bids.problems(bid)) for (_, bid, _) in jobs)\n   self._total_bar = RunningBar(\n      total=self._total_count,\n      desc=total_desc,\n      miniters=miniters,\n      postfix_width=_postfix_width(max_job),\n   )\n</code></pre>"},{"location":"api/solverpy/report/talker/evaltalker/#solverpy.report.talker.evaltalker.EvalTalker.eval_done","title":"<code>eval_done() -&gt; None</code>","text":"<p>Close the per-job bar and log the completion summary.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/evaltalker.py</code> <pre><code>def eval_done(self) -&gt; None:\n   \"\"\"Close the per-job bar and log the completion summary.\"\"\"\n   if not self._job_bar:\n      return\n   self._job_bar.close()\n   self._job_bar = None\n   super().eval_done()\n</code></pre>"},{"location":"api/solverpy/report/talker/evaltalker/#solverpy.report.talker.evaltalker.EvalTalker.eval_end","title":"<code>eval_end(results: dict[SolverJob, Result], refjob: SolverJob | None = None, **kwargs) -&gt; None</code>","text":"<p>Harvest error count from the total bar if present, then delegate to <code>LogTalker.eval_end</code>.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/evaltalker.py</code> <pre><code>def eval_end(\n   self,\n   results: dict[\"SolverJob\", \"Result\"],\n   refjob: \"SolverJob | None\" = None,\n   **kwargs,\n) -&gt; None:\n   \"\"\"Harvest error count from the total bar if present, then delegate to ``LogTalker.eval_end``.\"\"\"\n   if self._total_bar:\n      self._total_errors = self._total_bar._errors\n   super().eval_end(results, refjob=refjob, **kwargs)\n</code></pre>"},{"location":"api/solverpy/report/talker/evaltalker/#solverpy.report.talker.evaltalker.EvalTalker.eval_launch","title":"<code>eval_launch(tasks: Sequence[Task]) -&gt; None</code>","text":"<p>Create the per-job <code>SolvingBar</code> and inject the log queue into tasks.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/evaltalker.py</code> <pre><code>def eval_launch(self, tasks: Sequence[\"Task\"]) -&gt; None:\n   \"\"\"Create the per-job ``SolvingBar`` and inject the log queue into tasks.\"\"\"\n   super().eval_launch(tasks)\n   if not self._headless:\n      self._job_bar = SolvingBar(len(tasks), self._job_desc, miniters=1)\n</code></pre>"},{"location":"api/solverpy/report/talker/evaltalker/#solverpy.report.talker.evaltalker.EvalTalker.eval_status","title":"<code>eval_status(new: bool | None, n: int = 1) -&gt; None</code>","text":"<p>Forward status update to both the total bar and the per-job bar.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/evaltalker.py</code> <pre><code>def eval_status(self, new: bool | None, n: int = 1) -&gt; None:\n   \"\"\"Forward status update to both the total bar and the per-job bar.\"\"\"\n   super().eval_status(new, n)\n   if self._total_bar:\n      self._total_bar.status(new)\n   if self._job_bar:\n      self._job_bar.status(new)\n</code></pre>"},{"location":"api/solverpy/report/talker/evaltalker/#solverpy.report.talker.evaltalker.EvalTalker.terminate","title":"<code>terminate() -&gt; None</code>","text":"<p>Close and discard both bars, then stop the log queue.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/evaltalker.py</code> <pre><code>def terminate(self) -&gt; None:\n   \"\"\"Close and discard both bars, then stop the log queue.\"\"\"\n   super().terminate()\n   if self._total_bar:\n      self._total_bar.close()\n      self._total_bar = None\n   if self._job_bar:\n      self._job_bar.close()\n      self._job_bar = None\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/","title":"module logtalker","text":""},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker","title":"<code>solverpy.report.talker.logtalker</code>","text":""},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker--logtalker-text-only-progress-reporter","title":"LogTalker \u2014 text-only progress reporter","text":"<p>Reports evaluation progress through Python <code>logging</code> calls and writes Legend/Summary/Statuses to the markdown report via <code>summary</code>.  Used in headless or non-interactive contexts where tqdm bars are not appropriate.</p> <p>Also provides default log-based implementations of tuning event methods (<code>trials</code>, <code>trying</code>, <code>tried</code>, <code>trialed</code>, <code>building</code>, <code>iteration</code>, <code>built</code>, etc.) so that subclasses only need to override what they want to change.</p>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker","title":"<code>LogTalker</code>","text":"<p>               Bases: <code>Talker</code></p> <p>Progress reporter that writes to the Python logger and markdown report.</p> <p> </p> <p>Calls <code>summary.legend</code> in <code>begin</code> and <code>summary.summarize</code> in <code>end</code> to write structured tables to the <code>.md</code> report file.  Periodic progress updates are emitted via <code>logger.info</code> at exponentially growing intervals so long runs stay observable without flooding the log.</p> <p>Set <code>headless=False</code> (as <code>EvalTalker</code> does) to demote the per-job info lines to <code>DEBUG</code> \u2014 the tqdm bars carry that role instead.</p> <p>Tuning event methods (<code>trials</code>, <code>building</code>, etc.) provide log-based default implementations.  <code>LoopTalker</code> inherits these and overrides the ones that should display tqdm bars instead.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>class LogTalker(Talker):\n   \"\"\"\n   Progress reporter that writes to the Python logger and markdown report.\n\n   ```plantuml name=\"task-logtalker\"\n   abstract class solverpy.report.talker.talker.Talker\n   class solverpy.report.talker.logtalker.LogTalker extends solverpy.report.talker.talker.Talker {\n      - _headless: bool\n      --\n      + begin(jobs, refjob, sidnames, **kwargs)\n      + end(results, refjob)\n      + next(job)\n      + launching(tasks)\n      + finished(task, result)\n      + done()\n      + status(new, n)\n      + trials(nick, iters, timeout)\n      + trying(nick, it, values)\n      + tried(stats)\n      + trialed(nick)\n      + building(f_mod, total)\n      + iteration(n, total, loss)\n      + built(score)\n      + result(val)\n      + tuning(t_start)\n      + tuned(t_end)\n      + info(msg)\n      + debug(msg)\n   }\n   class solverpy.report.talker.evaltalker.EvalTalker extends solverpy.report.talker.logtalker.LogTalker\n   ```\n\n   Calls `summary.legend` in `begin` and `summary.summarize` in `end` to\n   write structured tables to the `.md` report file.  Periodic progress\n   updates are emitted via `logger.info` at exponentially growing intervals\n   so long runs stay observable without flooding the log.\n\n   Set ``headless=False`` (as `EvalTalker` does) to demote the\n   per-job info lines to ``DEBUG`` \u2014 the tqdm bars carry that role instead.\n\n   Tuning event methods (`trials`, `building`, etc.) provide log-based\n   default implementations.  `LoopTalker` inherits these and overrides the\n   ones that should display tqdm bars instead.\n   \"\"\"\n\n   def __init__(self, headless: bool = True):\n      \"\"\"\n      Args:\n          headless: if ``True``, emit ``INFO`` log lines for each job\n              and periodic progress updates.  Set to ``False`` when a\n              visual progress bar (`EvalTalker`) handles that role.\n      \"\"\"\n      super().__init__()\n      self._headless = headless\n      self._last_time = None\n      self._suspend_info: bool = False\n      # Tuning state shared with LoopTalker\n      self._tune_iters: str = \"\"\n      self._tune_header: list[str] | None = None\n      self._tune_table: list | None = None\n      self._tune_it: int = 0\n      self._tune_values: str = \"\"\n      self._tune_desc: str = \"trial\"\n      self._result: Any = None\n      # Builder iteration timing\n      self._builder_start: float = 0.0\n      self._builder_last: float = 0.0\n      self._builder_wait: float = 5.0\n\n   def eval_begin(\n      self,\n      jobs: list[\"SolverJob\"],\n      *,\n      refjob: \"SolverJob | None\" = None,\n      sidnames: bool = True,\n      report: bool = True,\n      **kwargs,\n   ) -&gt; None:\n      \"\"\"Set up counters, build nick map, write Legend to report, log job count.\"\"\"\n      del kwargs\n      self._total_count = sum(len(bids.problems(bid)) for (_, bid, _) in jobs)\n      self._total_jobs = len(jobs)\n      self._nick_dw = len(str(self._total_jobs))\n      self._job_index = 0\n      (self._total_nicks_full,\n       self._total_desc) = summary.legend(jobs,\n                                          refjob,\n                                          sidnames=sidnames,\n                                          report=report)\n      self._total_nicks = {\n         k[1:3]: v\n         for (k, v) in self._total_nicks_full.items()\n      }\n      self._total_errors = 0\n\n      self.info(f\"Evaluating {len(jobs)} jobs with {self._total_count} tasks.\")\n\n   def eval_end(\n      self,\n      results: dict[\"SolverJob\", \"Result\"],\n      refjob: \"SolverJob | None\" = None,\n      report: bool = True,\n   ) -&gt; None:\n      \"\"\"Write Summary/Statuses to report, log error count and completion.\"\"\"\n      super().eval_end(results, refjob=refjob)\n      if self._total_errors:\n         logger.error(\n            f\"There were errors: {self._total_errors} tasks failed to evaluate.\"\n         )\n      if report:\n         summary.summarize(results, self._total_nicks_full, refjob)\n      self.info(\"Evaluation done.\")\n\n   def eval_next(self, job: \"SolverJob\") -&gt; None:\n      \"\"\"Reset per-job counters and log the start of the next job.\"\"\"\n      jname = jobname(*job)\n      self._solved = self._unsolved = self._errors = 0\n      self._job_index += 1\n      nick = self._total_nicks[job[1:3]]\n      dw = self._nick_dw\n      self._job_desc = f\"[{self._job_index:&gt;{dw}}/{self._total_jobs}] {nick}\"\n      if self._headless:\n         self.info(f\"Evaluating {jname}\")\n      logger.debug(f\"evaluating {self._job_desc}: {jname}\")\n      super().eval_next(job)\n\n   def eval_launch(self, tasks: Sequence[\"Task\"]) -&gt; None:\n      \"\"\"Record start time and inject log queue into tasks.\"\"\"\n      self._last_time = time.perf_counter()\n      self._start_time = time.perf_counter()\n      self._wait_time = 1.0\n      self._wait_total = 1\n      super().eval_launch(tasks)\n\n   def eval_taskdone(\n      self,\n      task: \"SolverTask\",\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Update solved/unsolved/error counters based on the task result.\"\"\"\n      super().eval_taskdone(task, result)\n      self.eval_status(task.status(result))\n\n   def eval_done(self) -&gt; None:\n      \"\"\"Log solved/unsolved/error summary for the completed job.\"\"\"\n      bar = f\"+{self._solved} -{self._unsolved} !{self._errors}\"\n      if self._headless:\n         self.info(f\"Evaluation done: {bar}\")\n      else:\n         logger.debug(f\"evaluation done: {bar}\")\n\n   def eval_status(self, new: bool | None, n: int = 1) -&gt; None:\n      \"\"\"\n      Update counters and emit periodic progress log lines.\n\n      Args:\n          new: ``True`` = solved, ``False`` = unsolved, ``None`` = error.\n          n: number of tasks to count (default 1).\n      \"\"\"\n      if new is True:\n         self._solved += n\n      elif new is False:\n         self._unsolved += n\n      else:  # new is None:\n         self._errors += n\n      if not self._last_time:\n         return\n      logme = logger.info if self._headless else logger.debug\n      elapsed = time.perf_counter() - self._last_time\n      after = time.perf_counter() - self._start_time\n      bar = f\"+{self._solved} -{self._unsolved} !{self._errors}\"\n      if elapsed &gt; self._wait_time:\n         self._last_time = time.perf_counter()\n         self._wait_time = self._wait_time * 1.1\n         prefix = f\"progress @ {after:0.1f}s\"\n         waiting = f\"# waiting {self._wait_time:0.1f}s\"\n         logme(f\"   {prefix}\\t{bar}\\t{waiting}\")\n      total = self._solved + self._unsolved + self._errors\n      if total &gt;= self._wait_total:\n         self._wait_total *= 2\n         prefix = f\"done {total} @ {after:0.3f}s\"\n         waiting = f\"# waiting {self._wait_total} tasks\"\n         logme(f\"   {prefix}\\t{bar}\\t{waiting}\")\n\n   # --- Tuning event defaults (log-based) ---\n\n   def tune_phase_begin(self, nick: str, iters: int, timeout: int) -&gt; None:\n      \"\"\"Start a tuning phase: write heading to report and reset trial table.\"\"\"\n      del timeout\n      report = markdown.newline() + markdown.heading(f\"Tuning `{nick}`\",\n                                                     level=3)\n      reporter.add(report)\n      self.info(f\"Running tuning phase: {nick}\")\n      self._tune_iters = f\"/{iters}\" if iters else \"\"\n      self._tune_header = [\n         \"it\", nick, \"score\", \"test.acc\", \"train.acc\", \"time\"\n      ]\n      self._tune_table = []\n\n   def tune_trial_begin(self, nick: str, it: int, values: list) -&gt; None:\n      \"\"\"Record the current trial description; log if ``_headless``.\"\"\"\n      del nick\n      self._tune_it = it + 1\n      vals = \", \".join(\"%.4f\" % v if type(v) is float else str(v)\n                       for v in values)\n      self._tune_desc = f\"[{it+1}{self._tune_iters}] {vals:8s}\"\n      self._tune_values = vals\n      if self._headless:\n         self.info(f\"Trying {self._tune_desc}\")\n\n   def tune_trial_done(self, stats: dict[str, Any]) -&gt; None:\n      \"\"\"Append the completed trial's stats to the phase table.\"\"\"\n      assert self._tune_table is not None\n      self._tune_table.append((\n         self._tune_it,\n         self._tune_values,\n         f\"{stats['score']:.4f}\",\n         human.humanacc(stats[\"valid_acc\"]),\n         human.humanacc(stats[\"train_acc\"]),\n         human.humantime(stats[\"duration\"]),\n      ))\n\n   def tune_phase_done(self, nick: str) -&gt; None:\n      \"\"\"Write the completed phase's trial table to the report.\"\"\"\n      del nick\n      assert self._tune_table and self._tune_header\n      lines = []\n      lines.extend(markdown.newline())\n      lines.extend(markdown.heading(\"Trials\", level=4))\n      lines.extend(\n         markdown.table(\n            self._tune_header,\n            self._tune_table,\n            key=lambda x: float(x[2]),\n         ))\n      lines.append(\"\")\n      reporter.add(lines)\n      self.info(\n         f\"Tuning phase '{self._tune_header[1]}' done: {len(self._tune_table)} trials.\"\n      )\n\n   def build_begin(self, f_mod: str, total: int) -&gt; None:\n      \"\"\"Log the start of a model build.\"\"\"\n      del total\n      self._builder_start = time.perf_counter()\n      self._builder_last = time.perf_counter()\n      self._builder_wait = 5.0\n      logger.debug(f\"building model: {f_mod}\")\n      if self._headless:\n         self.info(f\"Building model: {f_mod}\")\n\n   def build_step(\n      self,\n      n: int,\n      total: int,\n      metrics: dict[str, dict[str, float]],\n   ) -&gt; None:\n      \"\"\"Emit periodic progress log lines during model training.\"\"\"\n      if n &gt; 3 and (n % 10 != 0):\n         return\n      elapsed = time.perf_counter() - self._builder_last\n      if n &gt; 3 and elapsed &lt; self._builder_wait:\n         return\n      msg = \" \".join(f\"{dataset}.{metric}={value:.4f}\"\n                     for (dataset, values) in metrics.items()\n                     for (metric, value) in values.items())\n      runtime = time.perf_counter() - self._builder_start\n      if self._headless:\n         self.info(f\"   metrics @ {runtime:0.3f}s\\t{n:02d}/{total}\\t{msg}\")\n      else:\n         logger.debug(f\"   metrics @ {runtime:0.3f}s\\t{n:02d}/{total}\\t{msg}\")\n      self._builder_last = time.perf_counter()\n\n   def build_selected(\n      self,\n      iteration: int,\n      metrics: dict[str, dict[str, float]],\n   ) -&gt; None:\n      \"\"\"Log the metrics of the selected model iteration.\"\"\"\n      msg = \" \".join(f\"{dataset}.{metric}={value:.4f}\"\n                     for (dataset, values) in metrics.items()\n                     for (metric, value) in values.items())\n      self.info(f\"Selected iteration {iteration}: {msg}\")\n\n   def build_done(self, score: float) -&gt; None:\n      \"\"\"Log completion of model training.\"\"\"\n      logger.debug(f\"model built: score={score:.4f}\")\n\n   def train_data(self, stats: list[dict[str, Any]]) -&gt; None:\n      \"\"\"Write training-file statistics to the report.\"\"\"\n      if not stats:\n         return\n\n      def count(value: Any) -&gt; str:\n         return human.humanint(value) if value is not None else \"?\"\n\n      def label_ratio(stat: dict[str, Any]) -&gt; str:\n         pos = stat.get(\"positive\")\n         neg = stat.get(\"negative\")\n         if pos is None or neg is None:\n            return \"?\"\n         if pos == 0:\n            return \"inf\" if neg else \"-\"\n         return f\"{neg / pos:.2f}:1\"\n\n      def compression_ratio(stat: dict[str, Any]) -&gt; str:\n         raw = stat.get(\"raw_bytes\")\n         stored = stat.get(\"stored_bytes\")\n         if raw is None or not stored:\n            return \"?\"\n         return f\"{raw / stored:.2f}x\"\n\n      report = markdown.newline() + markdown.heading(\"Training data\", level=3)\n      for stat in stats:\n         title = f\"{stat['dataset']} `{stat['path']}`\"\n         report.extend(markdown.heading(title, level=4))\n         rows = [\n            [\"dataset\", stat[\"dataset\"]],\n            [\"file\", f\"`{stat['path']}`\"],\n            [\"format\", stat[\"format\"]],\n            [\"vectors\", count(stat.get(\"vectors\"))],\n            [\"positive\", count(stat.get(\"positive\"))],\n            [\"negative\", count(stat.get(\"negative\"))],\n            [\"neg/pos\", label_ratio(stat)],\n            [\n               \"raw\",\n               human.humanbytes(stat[\"raw_bytes\"])\n               if stat.get(\"raw_bytes\") is not None else \"?\"\n            ],\n            [\"stored\", human.humanbytes(stat[\"stored_bytes\"])],\n            [\"ratio\", compression_ratio(stat)],\n            [\"chunks\", stat[\"chunks\"]],\n            [\"files\", stat[\"files\"]],\n         ]\n         report.extend(markdown.table([\"key\", \"val\"], rows))\n         report.append(\"\")\n      reporter.add(report)\n      self.info(f\"Training data ready: {len(stats)} file(s).\")\n\n   def tune_begin(self, t_start: float, total: int = 0) -&gt; None:\n      del t_start, total\n\n   def tune_end(self, t_end: float) -&gt; None:\n      del t_end\n\n   def info(self, msg: str) -&gt; None:\n      (logger.debug if self._suspend_info else logger.info)(msg)\n\n   def debug(self, msg: str) -&gt; None:\n      logger.debug(msg)\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.__init__","title":"<code>__init__(headless: bool = True)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>headless</code> <code>bool</code> <p>if <code>True</code>, emit <code>INFO</code> log lines for each job and periodic progress updates.  Set to <code>False</code> when a visual progress bar (<code>EvalTalker</code>) handles that role.</p> <code>True</code> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def __init__(self, headless: bool = True):\n   \"\"\"\n   Args:\n       headless: if ``True``, emit ``INFO`` log lines for each job\n           and periodic progress updates.  Set to ``False`` when a\n           visual progress bar (`EvalTalker`) handles that role.\n   \"\"\"\n   super().__init__()\n   self._headless = headless\n   self._last_time = None\n   self._suspend_info: bool = False\n   # Tuning state shared with LoopTalker\n   self._tune_iters: str = \"\"\n   self._tune_header: list[str] | None = None\n   self._tune_table: list | None = None\n   self._tune_it: int = 0\n   self._tune_values: str = \"\"\n   self._tune_desc: str = \"trial\"\n   self._result: Any = None\n   # Builder iteration timing\n   self._builder_start: float = 0.0\n   self._builder_last: float = 0.0\n   self._builder_wait: float = 5.0\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.build_begin","title":"<code>build_begin(f_mod: str, total: int) -&gt; None</code>","text":"<p>Log the start of a model build.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def build_begin(self, f_mod: str, total: int) -&gt; None:\n   \"\"\"Log the start of a model build.\"\"\"\n   del total\n   self._builder_start = time.perf_counter()\n   self._builder_last = time.perf_counter()\n   self._builder_wait = 5.0\n   logger.debug(f\"building model: {f_mod}\")\n   if self._headless:\n      self.info(f\"Building model: {f_mod}\")\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.build_done","title":"<code>build_done(score: float) -&gt; None</code>","text":"<p>Log completion of model training.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def build_done(self, score: float) -&gt; None:\n   \"\"\"Log completion of model training.\"\"\"\n   logger.debug(f\"model built: score={score:.4f}\")\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.build_selected","title":"<code>build_selected(iteration: int, metrics: dict[str, dict[str, float]]) -&gt; None</code>","text":"<p>Log the metrics of the selected model iteration.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def build_selected(\n   self,\n   iteration: int,\n   metrics: dict[str, dict[str, float]],\n) -&gt; None:\n   \"\"\"Log the metrics of the selected model iteration.\"\"\"\n   msg = \" \".join(f\"{dataset}.{metric}={value:.4f}\"\n                  for (dataset, values) in metrics.items()\n                  for (metric, value) in values.items())\n   self.info(f\"Selected iteration {iteration}: {msg}\")\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.build_step","title":"<code>build_step(n: int, total: int, metrics: dict[str, dict[str, float]]) -&gt; None</code>","text":"<p>Emit periodic progress log lines during model training.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def build_step(\n   self,\n   n: int,\n   total: int,\n   metrics: dict[str, dict[str, float]],\n) -&gt; None:\n   \"\"\"Emit periodic progress log lines during model training.\"\"\"\n   if n &gt; 3 and (n % 10 != 0):\n      return\n   elapsed = time.perf_counter() - self._builder_last\n   if n &gt; 3 and elapsed &lt; self._builder_wait:\n      return\n   msg = \" \".join(f\"{dataset}.{metric}={value:.4f}\"\n                  for (dataset, values) in metrics.items()\n                  for (metric, value) in values.items())\n   runtime = time.perf_counter() - self._builder_start\n   if self._headless:\n      self.info(f\"   metrics @ {runtime:0.3f}s\\t{n:02d}/{total}\\t{msg}\")\n   else:\n      logger.debug(f\"   metrics @ {runtime:0.3f}s\\t{n:02d}/{total}\\t{msg}\")\n   self._builder_last = time.perf_counter()\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.eval_begin","title":"<code>eval_begin(jobs: list[SolverJob], *, refjob: SolverJob | None = None, sidnames: bool = True, report: bool = True, **kwargs) -&gt; None</code>","text":"<p>Set up counters, build nick map, write Legend to report, log job count.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def eval_begin(\n   self,\n   jobs: list[\"SolverJob\"],\n   *,\n   refjob: \"SolverJob | None\" = None,\n   sidnames: bool = True,\n   report: bool = True,\n   **kwargs,\n) -&gt; None:\n   \"\"\"Set up counters, build nick map, write Legend to report, log job count.\"\"\"\n   del kwargs\n   self._total_count = sum(len(bids.problems(bid)) for (_, bid, _) in jobs)\n   self._total_jobs = len(jobs)\n   self._nick_dw = len(str(self._total_jobs))\n   self._job_index = 0\n   (self._total_nicks_full,\n    self._total_desc) = summary.legend(jobs,\n                                       refjob,\n                                       sidnames=sidnames,\n                                       report=report)\n   self._total_nicks = {\n      k[1:3]: v\n      for (k, v) in self._total_nicks_full.items()\n   }\n   self._total_errors = 0\n\n   self.info(f\"Evaluating {len(jobs)} jobs with {self._total_count} tasks.\")\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.eval_done","title":"<code>eval_done() -&gt; None</code>","text":"<p>Log solved/unsolved/error summary for the completed job.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def eval_done(self) -&gt; None:\n   \"\"\"Log solved/unsolved/error summary for the completed job.\"\"\"\n   bar = f\"+{self._solved} -{self._unsolved} !{self._errors}\"\n   if self._headless:\n      self.info(f\"Evaluation done: {bar}\")\n   else:\n      logger.debug(f\"evaluation done: {bar}\")\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.eval_end","title":"<code>eval_end(results: dict[SolverJob, Result], refjob: SolverJob | None = None, report: bool = True) -&gt; None</code>","text":"<p>Write Summary/Statuses to report, log error count and completion.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def eval_end(\n   self,\n   results: dict[\"SolverJob\", \"Result\"],\n   refjob: \"SolverJob | None\" = None,\n   report: bool = True,\n) -&gt; None:\n   \"\"\"Write Summary/Statuses to report, log error count and completion.\"\"\"\n   super().eval_end(results, refjob=refjob)\n   if self._total_errors:\n      logger.error(\n         f\"There were errors: {self._total_errors} tasks failed to evaluate.\"\n      )\n   if report:\n      summary.summarize(results, self._total_nicks_full, refjob)\n   self.info(\"Evaluation done.\")\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.eval_launch","title":"<code>eval_launch(tasks: Sequence[Task]) -&gt; None</code>","text":"<p>Record start time and inject log queue into tasks.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def eval_launch(self, tasks: Sequence[\"Task\"]) -&gt; None:\n   \"\"\"Record start time and inject log queue into tasks.\"\"\"\n   self._last_time = time.perf_counter()\n   self._start_time = time.perf_counter()\n   self._wait_time = 1.0\n   self._wait_total = 1\n   super().eval_launch(tasks)\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.eval_next","title":"<code>eval_next(job: SolverJob) -&gt; None</code>","text":"<p>Reset per-job counters and log the start of the next job.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def eval_next(self, job: \"SolverJob\") -&gt; None:\n   \"\"\"Reset per-job counters and log the start of the next job.\"\"\"\n   jname = jobname(*job)\n   self._solved = self._unsolved = self._errors = 0\n   self._job_index += 1\n   nick = self._total_nicks[job[1:3]]\n   dw = self._nick_dw\n   self._job_desc = f\"[{self._job_index:&gt;{dw}}/{self._total_jobs}] {nick}\"\n   if self._headless:\n      self.info(f\"Evaluating {jname}\")\n   logger.debug(f\"evaluating {self._job_desc}: {jname}\")\n   super().eval_next(job)\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.eval_status","title":"<code>eval_status(new: bool | None, n: int = 1) -&gt; None</code>","text":"<p>Update counters and emit periodic progress log lines.</p> <p>Parameters:</p> Name Type Description Default <code>new</code> <code>bool | None</code> <p><code>True</code> = solved, <code>False</code> = unsolved, <code>None</code> = error.</p> required <code>n</code> <code>int</code> <p>number of tasks to count (default 1).</p> <code>1</code> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def eval_status(self, new: bool | None, n: int = 1) -&gt; None:\n   \"\"\"\n   Update counters and emit periodic progress log lines.\n\n   Args:\n       new: ``True`` = solved, ``False`` = unsolved, ``None`` = error.\n       n: number of tasks to count (default 1).\n   \"\"\"\n   if new is True:\n      self._solved += n\n   elif new is False:\n      self._unsolved += n\n   else:  # new is None:\n      self._errors += n\n   if not self._last_time:\n      return\n   logme = logger.info if self._headless else logger.debug\n   elapsed = time.perf_counter() - self._last_time\n   after = time.perf_counter() - self._start_time\n   bar = f\"+{self._solved} -{self._unsolved} !{self._errors}\"\n   if elapsed &gt; self._wait_time:\n      self._last_time = time.perf_counter()\n      self._wait_time = self._wait_time * 1.1\n      prefix = f\"progress @ {after:0.1f}s\"\n      waiting = f\"# waiting {self._wait_time:0.1f}s\"\n      logme(f\"   {prefix}\\t{bar}\\t{waiting}\")\n   total = self._solved + self._unsolved + self._errors\n   if total &gt;= self._wait_total:\n      self._wait_total *= 2\n      prefix = f\"done {total} @ {after:0.3f}s\"\n      waiting = f\"# waiting {self._wait_total} tasks\"\n      logme(f\"   {prefix}\\t{bar}\\t{waiting}\")\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.eval_taskdone","title":"<code>eval_taskdone(task: SolverTask, result: Result) -&gt; None</code>","text":"<p>Update solved/unsolved/error counters based on the task result.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def eval_taskdone(\n   self,\n   task: \"SolverTask\",\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Update solved/unsolved/error counters based on the task result.\"\"\"\n   super().eval_taskdone(task, result)\n   self.eval_status(task.status(result))\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.train_data","title":"<code>train_data(stats: list[dict[str, Any]]) -&gt; None</code>","text":"<p>Write training-file statistics to the report.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def train_data(self, stats: list[dict[str, Any]]) -&gt; None:\n   \"\"\"Write training-file statistics to the report.\"\"\"\n   if not stats:\n      return\n\n   def count(value: Any) -&gt; str:\n      return human.humanint(value) if value is not None else \"?\"\n\n   def label_ratio(stat: dict[str, Any]) -&gt; str:\n      pos = stat.get(\"positive\")\n      neg = stat.get(\"negative\")\n      if pos is None or neg is None:\n         return \"?\"\n      if pos == 0:\n         return \"inf\" if neg else \"-\"\n      return f\"{neg / pos:.2f}:1\"\n\n   def compression_ratio(stat: dict[str, Any]) -&gt; str:\n      raw = stat.get(\"raw_bytes\")\n      stored = stat.get(\"stored_bytes\")\n      if raw is None or not stored:\n         return \"?\"\n      return f\"{raw / stored:.2f}x\"\n\n   report = markdown.newline() + markdown.heading(\"Training data\", level=3)\n   for stat in stats:\n      title = f\"{stat['dataset']} `{stat['path']}`\"\n      report.extend(markdown.heading(title, level=4))\n      rows = [\n         [\"dataset\", stat[\"dataset\"]],\n         [\"file\", f\"`{stat['path']}`\"],\n         [\"format\", stat[\"format\"]],\n         [\"vectors\", count(stat.get(\"vectors\"))],\n         [\"positive\", count(stat.get(\"positive\"))],\n         [\"negative\", count(stat.get(\"negative\"))],\n         [\"neg/pos\", label_ratio(stat)],\n         [\n            \"raw\",\n            human.humanbytes(stat[\"raw_bytes\"])\n            if stat.get(\"raw_bytes\") is not None else \"?\"\n         ],\n         [\"stored\", human.humanbytes(stat[\"stored_bytes\"])],\n         [\"ratio\", compression_ratio(stat)],\n         [\"chunks\", stat[\"chunks\"]],\n         [\"files\", stat[\"files\"]],\n      ]\n      report.extend(markdown.table([\"key\", \"val\"], rows))\n      report.append(\"\")\n   reporter.add(report)\n   self.info(f\"Training data ready: {len(stats)} file(s).\")\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.tune_phase_begin","title":"<code>tune_phase_begin(nick: str, iters: int, timeout: int) -&gt; None</code>","text":"<p>Start a tuning phase: write heading to report and reset trial table.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def tune_phase_begin(self, nick: str, iters: int, timeout: int) -&gt; None:\n   \"\"\"Start a tuning phase: write heading to report and reset trial table.\"\"\"\n   del timeout\n   report = markdown.newline() + markdown.heading(f\"Tuning `{nick}`\",\n                                                  level=3)\n   reporter.add(report)\n   self.info(f\"Running tuning phase: {nick}\")\n   self._tune_iters = f\"/{iters}\" if iters else \"\"\n   self._tune_header = [\n      \"it\", nick, \"score\", \"test.acc\", \"train.acc\", \"time\"\n   ]\n   self._tune_table = []\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.tune_phase_done","title":"<code>tune_phase_done(nick: str) -&gt; None</code>","text":"<p>Write the completed phase's trial table to the report.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def tune_phase_done(self, nick: str) -&gt; None:\n   \"\"\"Write the completed phase's trial table to the report.\"\"\"\n   del nick\n   assert self._tune_table and self._tune_header\n   lines = []\n   lines.extend(markdown.newline())\n   lines.extend(markdown.heading(\"Trials\", level=4))\n   lines.extend(\n      markdown.table(\n         self._tune_header,\n         self._tune_table,\n         key=lambda x: float(x[2]),\n      ))\n   lines.append(\"\")\n   reporter.add(lines)\n   self.info(\n      f\"Tuning phase '{self._tune_header[1]}' done: {len(self._tune_table)} trials.\"\n   )\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.tune_trial_begin","title":"<code>tune_trial_begin(nick: str, it: int, values: list) -&gt; None</code>","text":"<p>Record the current trial description; log if <code>_headless</code>.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def tune_trial_begin(self, nick: str, it: int, values: list) -&gt; None:\n   \"\"\"Record the current trial description; log if ``_headless``.\"\"\"\n   del nick\n   self._tune_it = it + 1\n   vals = \", \".join(\"%.4f\" % v if type(v) is float else str(v)\n                    for v in values)\n   self._tune_desc = f\"[{it+1}{self._tune_iters}] {vals:8s}\"\n   self._tune_values = vals\n   if self._headless:\n      self.info(f\"Trying {self._tune_desc}\")\n</code></pre>"},{"location":"api/solverpy/report/talker/logtalker/#solverpy.report.talker.logtalker.LogTalker.tune_trial_done","title":"<code>tune_trial_done(stats: dict[str, Any]) -&gt; None</code>","text":"<p>Append the completed trial's stats to the phase table.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/logtalker.py</code> <pre><code>def tune_trial_done(self, stats: dict[str, Any]) -&gt; None:\n   \"\"\"Append the completed trial's stats to the phase table.\"\"\"\n   assert self._tune_table is not None\n   self._tune_table.append((\n      self._tune_it,\n      self._tune_values,\n      f\"{stats['score']:.4f}\",\n      human.humanacc(stats[\"valid_acc\"]),\n      human.humanacc(stats[\"train_acc\"]),\n      human.humantime(stats[\"duration\"]),\n   ))\n</code></pre>"},{"location":"api/solverpy/report/talker/remotetalker/","title":"module remotetalker","text":""},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker","title":"<code>solverpy.report.talker.remotetalker</code>","text":""},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker--remotetalker-cross-process-talker-proxy","title":"RemoteTalker \u2014 cross-process talker proxy","text":"<p>Wraps a local <code>Talker</code> and makes its lifecycle methods callable from a child process.  Every public method that is not listed in <code>LOCALS</code> is intercepted by <code>__getattribute__</code> and put on a queue instead of executing directly; a background listening thread in the parent dequeues each message and calls the real method on the wrapped local talker.</p> <p><code>LOCALS</code> names the small set of methods that must execute in the calling process (queue/thread lifecycle); everything else is forwarded automatically \u2014 no need to enumerate every talker method.</p> <p>When the <code>RemoteTalker</code> is pickled into a spawn worker only the queue is needed: <code>_local</code> is excluded from the pickled state so that UI objects (tqdm bars, etc.) are not serialised unnecessarily.</p>"},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker.RemoteTalker","title":"<code>RemoteTalker</code>","text":"<p>               Bases: <code>Talker</code></p> <p>Proxy talker that forwards method calls from a child process to the parent.</p> <p> </p> <p>Any public attribute not listed in <code>LOCALS</code> is intercepted by <code>__getattribute__</code> and replaced with a wrapper that puts <code>(name, args, kwargs)</code> on <code>_remote_queue</code>.  The listening thread in the parent picks up each message and calls the real method on <code>_local</code>.</p> <p>Methods listed in <code>LOCALS</code> execute directly in the calling process (queue and thread lifecycle helpers that must not be forwarded).</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/remotetalker.py</code> <pre><code>class RemoteTalker(Talker):\n   \"\"\"\n   Proxy talker that forwards method calls from a child process to the parent.\n\n   ```plantuml name=\"task-remotetalker\"\n   abstract class solverpy.report.talker.talker.Talker\n   class solverpy.report.talker.remotetalker.RemoteTalker extends solverpy.report.talker.talker.Talker {\n      + LOCALS: set[str]\n      - _remote_queue: Queue[Any]\n      - _listening_thread: Thread | None\n      - _local: Talker\n      --\n      + listening_start()\n      + listening_stop()\n      + listening_handle(method, args, kwargs)\n      + terminate()\n   }\n   class solverpy.report.talker.logtalker.LogTalker extends solverpy.report.talker.talker.Talker\n   class solverpy.report.talker.evaltalker.EvalTalker extends solverpy.report.talker.logtalker.LogTalker\n   solverpy.report.talker.remotetalker.RemoteTalker o-- solverpy.report.talker.talker.Talker : wraps\n   ```\n\n   Any public attribute **not** listed in `LOCALS` is intercepted by\n   `__getattribute__` and replaced with a wrapper that puts\n   ``(name, args, kwargs)`` on ``_remote_queue``.  The listening thread in the\n   parent picks up each message and calls the real method on ``_local``.\n\n   Methods listed in `LOCALS` execute directly in the calling process (queue\n   and thread lifecycle helpers that must not be forwarded).\n   \"\"\"\n\n   LOCALS = {\n      \"listening_start\",\n      \"listening_stop\",\n      \"listening_handle\",\n      \"eval_launch\",\n   }\n   \"\"\"Methods that execute locally in the calling process (not forwarded via queue).\"\"\"\n\n   def __init__(self, local: Talker, queue: \"Queue[Any]\"):\n      \"\"\"\n      Args:\n          local: the real talker whose methods are called in the parent process.\n          queue: plain ``multiprocessing.Queue`` for cross-process communication.\n      \"\"\"\n      Talker.__init__(self)\n      self._remote_queue: Queue[Any] = queue\n      self._listening_thread: threading.Thread | None = None\n      self._stop_listening: threading.Event = threading.Event()\n      self._local: Talker = local\n\n   def __getattribute__(self, name):\n      if not name.startswith('_') and name not in RemoteTalker.LOCALS:\n         def wrapper(*args, **kwargs):\n            queue = object.__getattribute__(self, '_remote_queue')\n            queue.put((name, args, kwargs))\n         return wrapper\n      return super().__getattribute__(name)\n\n   def eval_launch(self, tasks):\n      \"\"\"Inject log queue into child's tasks locally, then forward to parent for stats.\"\"\"\n      if self._log_queue is not None:\n         for task in tasks:\n            task.logqueue = self._log_queue\n      queue = object.__getattribute__(self, '_remote_queue')\n      queue.put((\"eval_launch\", (tasks,), {}))\n\n   def listening_start(self):\n      \"\"\"Start the ``_remote_queue`` listening thread.\"\"\"\n      if self._listening_thread and self._listening_thread.is_alive():\n         logger.warning(\"Listening thread already running\")\n         return\n\n      self._stop_listening.clear()\n      self._listening_thread = threading.Thread(\n         target=self._listen_loop,\n         daemon=True,\n      )\n      self._listening_thread.start()\n\n   def listening_stop(self):\n      \"\"\"Signal the listening thread to stop and drain remaining queue items.\"\"\"\n      if not (self._listening_thread and self._listening_thread.is_alive()):\n         return\n\n      self._stop_listening.set()\n      self._listening_thread.join(timeout=2.0)\n      self._listening_thread = None\n\n      try:\n         while True:\n            method, args, kwargs = self._remote_queue.get_nowait()\n            self.listening_handle(method, args, kwargs)\n      except Exception:\n         pass\n\n   def _listen_loop(self):\n      \"\"\"Drain ``_remote_queue`` and dispatch each message to ``listening_handle``.\"\"\"\n      while not self._stop_listening.is_set():\n         try:\n            method, args, kwargs = self._remote_queue.get(timeout=0.2)\n            self.listening_handle(method, args, kwargs)\n         except:\n            pass\n\n   def listening_handle(self, method, args, kwargs):\n      \"\"\"Call ``method`` on the local talker with the given args and kwargs.\"\"\"\n      local = object.__getattribute__(self, '_local')\n      if local is None:\n         return\n      handler = None\n      try:\n         handler = getattr(local, method)\n      except AttributeError:\n         logger.error(\n            f\"Method '{method}' not found on local talker\\n{local}\\n{dir(local)}\"\n         )\n      if not handler:\n         return\n      try:\n         handler(*args, **kwargs)\n      except Exception as e:\n         logger.error(f\"Error calling {method}: {e}\", exc_info=True)\n\n   def terminate(self):\n      \"\"\"Terminate the local talker (dispatched from child via queue).\"\"\"\n      local = object.__getattribute__(self, '_local')\n      if local:\n         local.terminate()\n\n   def __getstate__(self):\n      state = self.__dict__.copy()\n      state['_listening_thread'] = None\n      state['_stop_listening'] = None\n      state['_local'] = None  # only the queue is needed in spawn workers\n      return state\n\n   def __setstate__(self, state):\n      self.__dict__.update(state)\n      self._stop_listening = threading.Event()\n</code></pre>"},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker.RemoteTalker.LOCALS","title":"<code>LOCALS = {'listening_start', 'listening_stop', 'listening_handle', 'eval_launch'}</code>  <code>class-attribute</code> <code>instance-attribute</code>","text":"<p>Methods that execute locally in the calling process (not forwarded via queue).</p>"},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker.RemoteTalker.__init__","title":"<code>__init__(local: Talker, queue: Queue[Any])</code>","text":"<p>Parameters:</p> Name Type Description Default <code>local</code> <code>Talker</code> <p>the real talker whose methods are called in the parent process.</p> required <code>queue</code> <code>Queue[Any]</code> <p>plain <code>multiprocessing.Queue</code> for cross-process communication.</p> required Source code in <code>packages/solverpy/src/solverpy/report/talker/remotetalker.py</code> <pre><code>def __init__(self, local: Talker, queue: \"Queue[Any]\"):\n   \"\"\"\n   Args:\n       local: the real talker whose methods are called in the parent process.\n       queue: plain ``multiprocessing.Queue`` for cross-process communication.\n   \"\"\"\n   Talker.__init__(self)\n   self._remote_queue: Queue[Any] = queue\n   self._listening_thread: threading.Thread | None = None\n   self._stop_listening: threading.Event = threading.Event()\n   self._local: Talker = local\n</code></pre>"},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker.RemoteTalker.eval_launch","title":"<code>eval_launch(tasks)</code>","text":"<p>Inject log queue into child's tasks locally, then forward to parent for stats.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/remotetalker.py</code> <pre><code>def eval_launch(self, tasks):\n   \"\"\"Inject log queue into child's tasks locally, then forward to parent for stats.\"\"\"\n   if self._log_queue is not None:\n      for task in tasks:\n         task.logqueue = self._log_queue\n   queue = object.__getattribute__(self, '_remote_queue')\n   queue.put((\"eval_launch\", (tasks,), {}))\n</code></pre>"},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker.RemoteTalker.listening_handle","title":"<code>listening_handle(method, args, kwargs)</code>","text":"<p>Call <code>method</code> on the local talker with the given args and kwargs.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/remotetalker.py</code> <pre><code>def listening_handle(self, method, args, kwargs):\n   \"\"\"Call ``method`` on the local talker with the given args and kwargs.\"\"\"\n   local = object.__getattribute__(self, '_local')\n   if local is None:\n      return\n   handler = None\n   try:\n      handler = getattr(local, method)\n   except AttributeError:\n      logger.error(\n         f\"Method '{method}' not found on local talker\\n{local}\\n{dir(local)}\"\n      )\n   if not handler:\n      return\n   try:\n      handler(*args, **kwargs)\n   except Exception as e:\n      logger.error(f\"Error calling {method}: {e}\", exc_info=True)\n</code></pre>"},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker.RemoteTalker.listening_start","title":"<code>listening_start()</code>","text":"<p>Start the <code>_remote_queue</code> listening thread.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/remotetalker.py</code> <pre><code>def listening_start(self):\n   \"\"\"Start the ``_remote_queue`` listening thread.\"\"\"\n   if self._listening_thread and self._listening_thread.is_alive():\n      logger.warning(\"Listening thread already running\")\n      return\n\n   self._stop_listening.clear()\n   self._listening_thread = threading.Thread(\n      target=self._listen_loop,\n      daemon=True,\n   )\n   self._listening_thread.start()\n</code></pre>"},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker.RemoteTalker.listening_stop","title":"<code>listening_stop()</code>","text":"<p>Signal the listening thread to stop and drain remaining queue items.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/remotetalker.py</code> <pre><code>def listening_stop(self):\n   \"\"\"Signal the listening thread to stop and drain remaining queue items.\"\"\"\n   if not (self._listening_thread and self._listening_thread.is_alive()):\n      return\n\n   self._stop_listening.set()\n   self._listening_thread.join(timeout=2.0)\n   self._listening_thread = None\n\n   try:\n      while True:\n         method, args, kwargs = self._remote_queue.get_nowait()\n         self.listening_handle(method, args, kwargs)\n   except Exception:\n      pass\n</code></pre>"},{"location":"api/solverpy/report/talker/remotetalker/#solverpy.report.talker.remotetalker.RemoteTalker.terminate","title":"<code>terminate()</code>","text":"<p>Terminate the local talker (dispatched from child via queue).</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/remotetalker.py</code> <pre><code>def terminate(self):\n   \"\"\"Terminate the local talker (dispatched from child via queue).\"\"\"\n   local = object.__getattribute__(self, '_local')\n   if local:\n      local.terminate()\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/","title":"module talker","text":""},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker","title":"<code>solverpy.report.talker.talker</code>","text":""},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker--talker-evaluation-progress-interface","title":"Talker \u2014 evaluation progress interface","text":"<p>Base class for progress reporting during benchmark evaluation. Subclasses receive lifecycle events (<code>begin</code>, <code>next</code>, <code>launching</code>, <code>finished</code>, <code>done</code>, <code>end</code>) and decide how to surface them \u2014 as log messages (<code>LogTalker</code>), progress bars (<code>EvalTalker</code>), or forwarded across processes (<code>RemoteTalker</code>).</p> <p>Also owns the log queue machinery that lets worker processes route Python <code>logging</code> records back to the parent's handlers.</p>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker","title":"<code>Talker</code>","text":"<p>               Bases: <code>SolverPyObj</code></p> <p>Abstract base for evaluation progress reporters.</p> <p> </p> <p>Subclasses receive lifecycle events fired by the evaluation pipeline and decide how to surface them.  The base class is a silent no-op talker; every hook has a default no-op implementation so <code>Talker()</code> can be used as a drop-in \"null\" reporter wherever a talker is required.</p> <p>Also owns the cross-process log queue: <code>log_prepare</code> creates the queue, while <code>log_start</code> starts a <code>QueueListener</code> in the parent. Worker processes call <code>log_config</code> to redirect their root logger through the queue so records arrive at the parent's handlers.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>class Talker(SolverPyObj):\n   \"\"\"\n   Abstract base for evaluation progress reporters.\n\n   ```plantuml name=\"task-talker\"\n   class solverpy.report.talker.talker.Talker {\n      - _log_queue: Queue[Any] | None\n      - _listener: QueueListener | None\n\n      --\n      + {static} log_config(queue)\n      + log_prepare()\n      + log_start()\n      + log_stop()\n      + listening_start()\n      + listening_stop()\n      + begin(jobs, refjob, sidnames, **kwargs)\n      + end(results, refjob)\n      + next(job)\n      + terminate()\n      + launching(tasks)\n      + finished(task, result)\n      + done()\n   }\n   class solverpy.report.talker.logtalker.LogTalker extends solverpy.report.talker.talker.Talker\n   class solverpy.report.talker.remotetalker.RemoteTalker extends solverpy.report.talker.talker.Talker\n   ```\n\n   Subclasses receive lifecycle events fired by the evaluation pipeline and\n   decide how to surface them.  The base class is a silent no-op talker;\n   every hook has a default no-op implementation so `Talker()` can be used\n   as a drop-in \"null\" reporter wherever a talker is required.\n\n   Also owns the cross-process log queue: `log_prepare` creates the queue,\n   while `log_start` starts a `QueueListener` in the parent. Worker processes\n   call `log_config` to redirect their root logger through the queue so records\n   arrive at the parent's handlers.\n   \"\"\"\n\n   def __init__(self):\n      SolverPyObj.__init__(self)\n      self._log_queue: Queue[Any] | None = None\n      self._listener: QueueListener | None = None\n      self._result: Any = None\n\n   def represent(self) -&gt; str:\n      return type(self).__name__\n\n   @staticmethod\n   def log_config(queue: \"Queue[Any] | None\") -&gt; None:\n      \"\"\"Configure child process logger to use the queue.\"\"\"\n      if not queue:\n         return\n      root = logging.getLogger()\n      root.handlers.clear()\n      for name in logging.root.manager.loggerDict:\n         logging.getLogger(name).handlers.clear()\n      root.addHandler(QueueHandler(queue))\n      root.setLevel(logging.DEBUG)\n\n   def log_start(self) -&gt; None:\n      \"\"\"Start parent log-queue listener. No-op if queue not set or already started.\"\"\"\n      if self._log_queue is None or self._listener is not None:\n         return\n      root = logging.getLogger()\n      self._listener = QueueListener(self._log_queue,\n                                     *root.handlers,\n                                     respect_handler_level=True)\n      self._listener.start()\n\n   def log_stop(self) -&gt; None:\n      \"\"\"Stop parent log-queue listener.\"\"\"\n      if self._listener:\n         self._listener.stop()\n         self._listener = None\n\n   def listening_start(self) -&gt; None:\n      \"\"\"Start the log queue listener. Subclasses extend this to start additional threads.\"\"\"\n      self.log_start()\n\n   def listening_stop(self) -&gt; None:\n      \"\"\"Stop the log queue listener. Subclasses extend this to stop additional threads.\"\"\"\n      self.log_stop()\n\n   def eval_begin(\n      self,\n      jobs: list[\"SolverJob\"],\n      *,\n      refjob: \"SolverJob | None\" = None,\n      sidnames: bool = True,\n      **kwargs,\n   ) -&gt; None:\n      \"\"\"Called once before evaluation starts with the full list of jobs.\"\"\"\n      del jobs, refjob, sidnames, kwargs\n\n   def eval_end(\n      self,\n      results: dict[\"SolverJob\", \"Result\"],\n      refjob: \"SolverJob | None\" = None,\n   ) -&gt; None:\n      \"\"\"\n      Called once after all jobs complete. Calls `terminate` by default.\n\n      Args:\n          results: mapping from each job to its result dict.\n          refjob: the reference job used for relative reporting.\n      \"\"\"\n      del results, refjob\n      self.terminate()\n\n   def eval_next(self, job: \"SolverJob\") -&gt; None:\n      \"\"\"Called when evaluation moves to the next job.\"\"\"\n      del job\n\n   def terminate(self) -&gt; None:\n      \"\"\"Called after each evaluation job set completes.\"\"\"\n      pass\n\n   def eval_launch(self, tasks: Sequence[\"Task\"]) -&gt; None:\n      \"\"\"\n      Called just before a batch of tasks is dispatched to worker processes.\n\n      Injects the log queue into each task so workers can route their\n      ``logging`` records back to the parent.\n      \"\"\"\n      if self._log_queue is None:\n         return\n      for task in tasks:\n         task.logqueue = self._log_queue\n\n   def eval_taskdone(self, task: \"SolverTask\", result: \"Result\") -&gt; None:\n      \"\"\"Called each time a single task completes.\"\"\"\n      del task, result\n\n   def eval_done(self) -&gt; None:\n      \"\"\"Called after all tasks in the current job have finished.\"\"\"\n      pass\n\n   def eval_status(self, new: \"bool | None\", n: int = 1) -&gt; None:\n      \"\"\"Called when a task result is classified (True=solved, False=unsolved, None=error).\"\"\"\n      del new, n\n\n   def tune_begin(self, t_start: float, total: int = 0) -&gt; None:\n      \"\"\"Hyperparameter tuning session starting; total trial count known.\"\"\"\n      del t_start, total\n\n   def tune_end(self, t_end: float) -&gt; None:\n      \"\"\"Tuning session finished.\"\"\"\n      del t_end\n\n   def tune_result(self, val: Any) -&gt; None:\n      \"\"\"Tuning produced a final result value.\"\"\"\n      self._result = val\n\n   def tune_phase_begin(self, nick: str, iters: int,\n                        timeout: \"int | None\") -&gt; None:\n      \"\"\"A new tuning phase starting (e.g. ``\"leaves\"``).\"\"\"\n      del nick, iters, timeout\n\n   def tune_phase_done(self, nick: str) -&gt; None:\n      \"\"\"A tuning phase finished.\"\"\"\n      del nick\n\n   def tune_trial_begin(self, nick: str, it: int, values: \"list[Any]\") -&gt; None:\n      \"\"\"An Optuna trial starting; parameter values being evaluated are known.\"\"\"\n      del nick, it, values\n\n   def tune_trial_done(self, stats: \"dict[str, Any]\") -&gt; None:\n      \"\"\"An Optuna trial finished; scores and accuracies are known.\"\"\"\n      del stats\n\n   def tune_eval_begin(self) -&gt; None:\n      \"\"\"ATP evaluation phase inside a tuning trial is starting.\"\"\"\n\n   def tune_eval_end(self, results: \"dict\") -&gt; None:\n      \"\"\"ATP evaluation phase inside a tuning trial finished.\"\"\"\n      del results\n\n   def build_begin(self, f_mod: str, total: int) -&gt; None:\n      \"\"\"LightGBM model training starting; total iteration count known.\"\"\"\n      del f_mod, total\n\n   def build_step(\n      self,\n      n: int,\n      total: int,\n      metrics: \"dict[str, dict[str, float]]\",\n   ) -&gt; None:\n      \"\"\"One LightGBM training iteration completed; labeled metrics are known.\"\"\"\n      del n, total, metrics\n\n   def build_selected(\n      self,\n      iteration: int,\n      metrics: \"dict[str, dict[str, float]]\",\n   ) -&gt; None:\n      \"\"\"The selected LightGBM iteration and its final metrics are known.\"\"\"\n      del iteration, metrics\n\n   def build_done(self, score: float) -&gt; None:\n      \"\"\"Model training finished; final ML score known.\"\"\"\n      del score\n\n   def train_data(self, stats: \"list[dict[str, Any]]\") -&gt; None:\n      \"\"\"Training-data files are ready; storage and sample statistics are known.\"\"\"\n      del stats\n\n   def info(self, msg: str) -&gt; None:\n      \"\"\"Log an info-level message.\"\"\"\n      del msg\n\n   def debug(self, msg: str) -&gt; None:\n      \"\"\"Log a debug-level message.\"\"\"\n      del msg\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.build_begin","title":"<code>build_begin(f_mod: str, total: int) -&gt; None</code>","text":"<p>LightGBM model training starting; total iteration count known.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def build_begin(self, f_mod: str, total: int) -&gt; None:\n   \"\"\"LightGBM model training starting; total iteration count known.\"\"\"\n   del f_mod, total\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.build_done","title":"<code>build_done(score: float) -&gt; None</code>","text":"<p>Model training finished; final ML score known.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def build_done(self, score: float) -&gt; None:\n   \"\"\"Model training finished; final ML score known.\"\"\"\n   del score\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.build_selected","title":"<code>build_selected(iteration: int, metrics: dict[str, dict[str, float]]) -&gt; None</code>","text":"<p>The selected LightGBM iteration and its final metrics are known.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def build_selected(\n   self,\n   iteration: int,\n   metrics: \"dict[str, dict[str, float]]\",\n) -&gt; None:\n   \"\"\"The selected LightGBM iteration and its final metrics are known.\"\"\"\n   del iteration, metrics\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.build_step","title":"<code>build_step(n: int, total: int, metrics: dict[str, dict[str, float]]) -&gt; None</code>","text":"<p>One LightGBM training iteration completed; labeled metrics are known.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def build_step(\n   self,\n   n: int,\n   total: int,\n   metrics: \"dict[str, dict[str, float]]\",\n) -&gt; None:\n   \"\"\"One LightGBM training iteration completed; labeled metrics are known.\"\"\"\n   del n, total, metrics\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.debug","title":"<code>debug(msg: str) -&gt; None</code>","text":"<p>Log a debug-level message.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def debug(self, msg: str) -&gt; None:\n   \"\"\"Log a debug-level message.\"\"\"\n   del msg\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.eval_begin","title":"<code>eval_begin(jobs: list[SolverJob], *, refjob: SolverJob | None = None, sidnames: bool = True, **kwargs) -&gt; None</code>","text":"<p>Called once before evaluation starts with the full list of jobs.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def eval_begin(\n   self,\n   jobs: list[\"SolverJob\"],\n   *,\n   refjob: \"SolverJob | None\" = None,\n   sidnames: bool = True,\n   **kwargs,\n) -&gt; None:\n   \"\"\"Called once before evaluation starts with the full list of jobs.\"\"\"\n   del jobs, refjob, sidnames, kwargs\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.eval_done","title":"<code>eval_done() -&gt; None</code>","text":"<p>Called after all tasks in the current job have finished.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def eval_done(self) -&gt; None:\n   \"\"\"Called after all tasks in the current job have finished.\"\"\"\n   pass\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.eval_end","title":"<code>eval_end(results: dict[SolverJob, Result], refjob: SolverJob | None = None) -&gt; None</code>","text":"<p>Called once after all jobs complete. Calls <code>terminate</code> by default.</p> <p>Parameters:</p> Name Type Description Default <code>results</code> <code>dict[SolverJob, Result]</code> <p>mapping from each job to its result dict.</p> required <code>refjob</code> <code>SolverJob | None</code> <p>the reference job used for relative reporting.</p> <code>None</code> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def eval_end(\n   self,\n   results: dict[\"SolverJob\", \"Result\"],\n   refjob: \"SolverJob | None\" = None,\n) -&gt; None:\n   \"\"\"\n   Called once after all jobs complete. Calls `terminate` by default.\n\n   Args:\n       results: mapping from each job to its result dict.\n       refjob: the reference job used for relative reporting.\n   \"\"\"\n   del results, refjob\n   self.terminate()\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.eval_launch","title":"<code>eval_launch(tasks: Sequence[Task]) -&gt; None</code>","text":"<p>Called just before a batch of tasks is dispatched to worker processes.</p> <p>Injects the log queue into each task so workers can route their <code>logging</code> records back to the parent.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def eval_launch(self, tasks: Sequence[\"Task\"]) -&gt; None:\n   \"\"\"\n   Called just before a batch of tasks is dispatched to worker processes.\n\n   Injects the log queue into each task so workers can route their\n   ``logging`` records back to the parent.\n   \"\"\"\n   if self._log_queue is None:\n      return\n   for task in tasks:\n      task.logqueue = self._log_queue\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.eval_next","title":"<code>eval_next(job: SolverJob) -&gt; None</code>","text":"<p>Called when evaluation moves to the next job.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def eval_next(self, job: \"SolverJob\") -&gt; None:\n   \"\"\"Called when evaluation moves to the next job.\"\"\"\n   del job\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.eval_status","title":"<code>eval_status(new: bool | None, n: int = 1) -&gt; None</code>","text":"<p>Called when a task result is classified (True=solved, False=unsolved, None=error).</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def eval_status(self, new: \"bool | None\", n: int = 1) -&gt; None:\n   \"\"\"Called when a task result is classified (True=solved, False=unsolved, None=error).\"\"\"\n   del new, n\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.eval_taskdone","title":"<code>eval_taskdone(task: SolverTask, result: Result) -&gt; None</code>","text":"<p>Called each time a single task completes.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def eval_taskdone(self, task: \"SolverTask\", result: \"Result\") -&gt; None:\n   \"\"\"Called each time a single task completes.\"\"\"\n   del task, result\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.info","title":"<code>info(msg: str) -&gt; None</code>","text":"<p>Log an info-level message.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def info(self, msg: str) -&gt; None:\n   \"\"\"Log an info-level message.\"\"\"\n   del msg\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.listening_start","title":"<code>listening_start() -&gt; None</code>","text":"<p>Start the log queue listener. Subclasses extend this to start additional threads.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def listening_start(self) -&gt; None:\n   \"\"\"Start the log queue listener. Subclasses extend this to start additional threads.\"\"\"\n   self.log_start()\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.listening_stop","title":"<code>listening_stop() -&gt; None</code>","text":"<p>Stop the log queue listener. Subclasses extend this to stop additional threads.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def listening_stop(self) -&gt; None:\n   \"\"\"Stop the log queue listener. Subclasses extend this to stop additional threads.\"\"\"\n   self.log_stop()\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.log_config","title":"<code>log_config(queue: Queue[Any] | None) -&gt; None</code>  <code>staticmethod</code>","text":"<p>Configure child process logger to use the queue.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>@staticmethod\ndef log_config(queue: \"Queue[Any] | None\") -&gt; None:\n   \"\"\"Configure child process logger to use the queue.\"\"\"\n   if not queue:\n      return\n   root = logging.getLogger()\n   root.handlers.clear()\n   for name in logging.root.manager.loggerDict:\n      logging.getLogger(name).handlers.clear()\n   root.addHandler(QueueHandler(queue))\n   root.setLevel(logging.DEBUG)\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.log_start","title":"<code>log_start() -&gt; None</code>","text":"<p>Start parent log-queue listener. No-op if queue not set or already started.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def log_start(self) -&gt; None:\n   \"\"\"Start parent log-queue listener. No-op if queue not set or already started.\"\"\"\n   if self._log_queue is None or self._listener is not None:\n      return\n   root = logging.getLogger()\n   self._listener = QueueListener(self._log_queue,\n                                  *root.handlers,\n                                  respect_handler_level=True)\n   self._listener.start()\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.log_stop","title":"<code>log_stop() -&gt; None</code>","text":"<p>Stop parent log-queue listener.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def log_stop(self) -&gt; None:\n   \"\"\"Stop parent log-queue listener.\"\"\"\n   if self._listener:\n      self._listener.stop()\n      self._listener = None\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.terminate","title":"<code>terminate() -&gt; None</code>","text":"<p>Called after each evaluation job set completes.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def terminate(self) -&gt; None:\n   \"\"\"Called after each evaluation job set completes.\"\"\"\n   pass\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.train_data","title":"<code>train_data(stats: list[dict[str, Any]]) -&gt; None</code>","text":"<p>Training-data files are ready; storage and sample statistics are known.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def train_data(self, stats: \"list[dict[str, Any]]\") -&gt; None:\n   \"\"\"Training-data files are ready; storage and sample statistics are known.\"\"\"\n   del stats\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.tune_begin","title":"<code>tune_begin(t_start: float, total: int = 0) -&gt; None</code>","text":"<p>Hyperparameter tuning session starting; total trial count known.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def tune_begin(self, t_start: float, total: int = 0) -&gt; None:\n   \"\"\"Hyperparameter tuning session starting; total trial count known.\"\"\"\n   del t_start, total\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.tune_end","title":"<code>tune_end(t_end: float) -&gt; None</code>","text":"<p>Tuning session finished.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def tune_end(self, t_end: float) -&gt; None:\n   \"\"\"Tuning session finished.\"\"\"\n   del t_end\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.tune_eval_begin","title":"<code>tune_eval_begin() -&gt; None</code>","text":"<p>ATP evaluation phase inside a tuning trial is starting.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def tune_eval_begin(self) -&gt; None:\n   \"\"\"ATP evaluation phase inside a tuning trial is starting.\"\"\"\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.tune_eval_end","title":"<code>tune_eval_end(results: dict) -&gt; None</code>","text":"<p>ATP evaluation phase inside a tuning trial finished.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def tune_eval_end(self, results: \"dict\") -&gt; None:\n   \"\"\"ATP evaluation phase inside a tuning trial finished.\"\"\"\n   del results\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.tune_phase_begin","title":"<code>tune_phase_begin(nick: str, iters: int, timeout: int | None) -&gt; None</code>","text":"<p>A new tuning phase starting (e.g. <code>\"leaves\"</code>).</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def tune_phase_begin(self, nick: str, iters: int,\n                     timeout: \"int | None\") -&gt; None:\n   \"\"\"A new tuning phase starting (e.g. ``\"leaves\"``).\"\"\"\n   del nick, iters, timeout\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.tune_phase_done","title":"<code>tune_phase_done(nick: str) -&gt; None</code>","text":"<p>A tuning phase finished.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def tune_phase_done(self, nick: str) -&gt; None:\n   \"\"\"A tuning phase finished.\"\"\"\n   del nick\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.tune_result","title":"<code>tune_result(val: Any) -&gt; None</code>","text":"<p>Tuning produced a final result value.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def tune_result(self, val: Any) -&gt; None:\n   \"\"\"Tuning produced a final result value.\"\"\"\n   self._result = val\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.tune_trial_begin","title":"<code>tune_trial_begin(nick: str, it: int, values: list[Any]) -&gt; None</code>","text":"<p>An Optuna trial starting; parameter values being evaluated are known.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def tune_trial_begin(self, nick: str, it: int, values: \"list[Any]\") -&gt; None:\n   \"\"\"An Optuna trial starting; parameter values being evaluated are known.\"\"\"\n   del nick, it, values\n</code></pre>"},{"location":"api/solverpy/report/talker/talker/#solverpy.report.talker.talker.Talker.tune_trial_done","title":"<code>tune_trial_done(stats: dict[str, Any]) -&gt; None</code>","text":"<p>An Optuna trial finished; scores and accuracies are known.</p> Source code in <code>packages/solverpy/src/solverpy/report/talker/talker.py</code> <pre><code>def tune_trial_done(self, stats: \"dict[str, Any]\") -&gt; None:\n   \"\"\"An Optuna trial finished; scores and accuracies are known.\"\"\"\n   del stats\n</code></pre>"},{"location":"api/solverpy/scripts/clean/","title":"module clean","text":""},{"location":"api/solverpy/scripts/clean/#solverpy.scripts.clean","title":"<code>solverpy.scripts.clean</code>","text":""},{"location":"api/solverpy/scripts/cli/","title":"module cli","text":""},{"location":"api/solverpy/scripts/cli/#solverpy.scripts.cli","title":"<code>solverpy.scripts.cli</code>","text":""},{"location":"api/solverpy/scripts/esid2strat/","title":"module esid2strat","text":""},{"location":"api/solverpy/scripts/esid2strat/#solverpy.scripts.esid2strat","title":"<code>solverpy.scripts.esid2strat</code>","text":""},{"location":"api/solverpy/scripts/init/","title":"module init","text":""},{"location":"api/solverpy/scripts/init/#solverpy.scripts.init","title":"<code>solverpy.scripts.init</code>","text":""},{"location":"api/solverpy/scripts/report/","title":"module report","text":""},{"location":"api/solverpy/scripts/report/#solverpy.scripts.report","title":"<code>solverpy.scripts.report</code>","text":""},{"location":"api/solverpy/scripts/run/","title":"module run","text":""},{"location":"api/solverpy/scripts/run/#solverpy.scripts.run","title":"<code>solverpy.scripts.run</code>","text":""},{"location":"api/solverpy/setups/","title":"Setups Overview","text":""},{"location":"api/solverpy/setups/#solverpy.setups","title":"<code>solverpy.setups</code>","text":""},{"location":"api/solverpy/setups/#solverpy.setups--experiment-setups","title":"Experiment setups","text":"<p>The <code>setups</code> module is the primary high-level API for configuring and launching solver experiments.  An experiment is described by a <code>Setup</code> dict that is populated by a chain of setup functions and then executed by <code>launch</code>.</p> <pre><code>from solverpy import setups\n\nsetup = setups.Setup(limit=\"T10\", benchmarks=[\"myproblems\"], strategies=[\"default\"])\nsetups.eprover(setup)      # choose solver and fill in defaults\nsetups.evaluation(setup)   # configure benchmark evaluation\nsetups.launch(setup)       # run everything\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups--setup-functions","title":"Setup functions","text":"<p>Each solver has a corresponding setup function that sets the solver instance and its static options in <code>setup</code>:</p> Function Solver <code>eprover(setup)</code> <code>E</code> (E Prover) <code>vampire(setup)</code> <code>Vampire</code> <code>prover9(setup)</code> <code>Prover9</code> <code>cvc5(setup)</code> <code>Cvc5</code> <code>z3(setup)</code> <code>Z3</code> <code>bitwuzla(setup)</code> <code>Bitwuzla</code> <code>llm2smt(setup)</code> <code>Llm2smt</code> <code>primo(setup)</code> <code>Primo</code> <code>opensmt(setup)</code> <code>Opensmt</code> <code>spasssatt(setup)</code> <code>SpassSatt</code> <code>yices(setup)</code> <code>Yices</code> <p>After choosing a solver, call <code>evaluation</code> to configure the benchmark evaluation pipeline (DB providers, parallel cores, strategy and benchmark id lists), then call <code>launch</code> to execute.</p>"},{"location":"api/solverpy/setups/#solverpy.setups.Evalset","title":"<code>Evalset</code>","text":"<p>               Bases: <code>TypedDict</code></p> <p>Per-dataset configuration: problems, strategies, and ML training state.</p> Source code in <code>packages/solverpy/src/solverpy/setups/evalset.py</code> <pre><code>class Evalset(TypedDict, total=False):\n   \"\"\"Per-dataset configuration: problems, strategies, and ML training state.\"\"\"\n   label: str\n   benchmarks: list[str]\n   strategies: list[str]\n   bidfile: str\n   sidfile: str\n   refs: list[str]\n   ref: bool | int | str | None\n   limit: str\n   cores: int\n   binary: str\n   static: list[str]\n   complete: bool\n   force: bool\n   shuffle: bool\n   solvedby: str\n   max_proofs: int\n   pool_context: str\n   plugin: Any  # SvmTrains\n   plugins: list[\"Plugin\"]\n   solver: \"SolverPy\"\n   dataname: str\n   basedataname: str\n   previous_trains: str | tuple[str]\n   proofs: dict[str, int] | None\n   start_dataname: str\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.Setup","title":"<code>Setup</code>","text":"<p>               Bases: <code>TypedDict</code></p> <p>A (typed) dictionary summarizing the configuration of the experiment. An ordinary dictionary can be used in its place but type annotations provide additional safety.</p> <p>Some parameters should be set by the user, others are automatically filled in by functions in the <code>setups</code> module.</p>"},{"location":"api/solverpy/setups/#solverpy.setups.Setup--evaluation-parameters","title":"Evaluation parameters","text":"<p>The following are the most important parameters to be set by the user for an evaluation run:</p> <p>Attributes:</p> Name Type Description <code>limit</code> <code>str</code> <p>Resource limit for solvers.</p> <code>cores</code> <code>int</code> <p>Number of CPU cores for parallel evaluation.</p> <code>benchmarks</code> <code>int</code> <p>List of benchmark ids. </p> <code>strategies</code> <code>int</code> <p>List of strategy ids.</p> <code>complete</code> <code>bool</code> <p>Complete use of the solver (sat/Satisfiable are successes).</p> <p>The following parameters additional parameters for an evaluation run:</p> <p>Attributes:</p> Name Type Description <code>ref</code> <p>Reference strategy id.</p> <code>binary</code> <code>str</code> <p>Custom solver binary.  Must be in <code>PATH</code>.</p> <code>static</code> <code>list[str]</code> <p>Fixed solver options.</p> <code>ntfy</code> <code>str</code> <p>Address for user push notifications via ntfy.</p> <code>options</code> <code>list[str]</code> <p>Experiment boolean options. An option is identified by its string name, and represents a boolean yes/no value. Use <code>no-</code>option, like <code>no-compress</code>, to set the option to <code>no</code>. Currently supported options are:</p> option description default <code>outputs</code> Keep raw solver output files from all runs. no <code>compress</code> Compress output files (outputs, trains, results). yes <code>flatten</code> Put all output files in a single directory (replace <code>/</code> with <code>_._</code>). yes <code>compress-trains</code> Compress trains. yes <code>debug-trains</code> Dump training data for each file separately. no <code>proofs</code> Store proof objects in the database. no <code>premises</code> Store premise selections in the database. no <code>headless</code> Suppress progress bars and interactive UI (for non-terminal use). no <code>plugins</code> <code>list[Plugin]</code> <p>List of additional solver plugins.</p> <code>delfix</code> <code>int | str | None</code> <p>Prefix to remove from problem names.</p> <ul> <li>If the value is of type <code>str</code>, then this prefix is removed from the beginning of every problem name.</li> <li>If the value is of type <code>int</code>, then the problem name is expected to be a <code>/</code>-separated path, and the first <code>delfix</code> components are removed.</li> <li>If the value <code>None</code>, nothing is removed.</li> </ul> <p>The typical use is to remove the prefix <code>problems/</code> from problem names when the problems are stored inside <code>problems/</code> subdirectory. In that case, set <code>delfix=1</code>.</p> <p>This applies to DB providers that support <code>delfix</code> (e.g. <code>Solved</code> and <code>Status</code>). The default value is <code>None</code>.</p>"},{"location":"api/solverpy/setups/#solverpy.setups.Setup--ml-parameters","title":"ML parameters","text":"<p>The following are user options for ML experiments.</p> <p>Attributes:</p> Name Type Description <code>loops</code> <code>int</code> <p>Number of iterations of the eval/ML loop.</p> <code>force</code> <code>bool</code> <p>Recompute everything.</p> <code>shuffle</code> <code>bool</code> <p>Shuffle problem order.</p> <code>e_training_examples</code> <code>str</code> <p>Output format of training samples for <code>eprover</code>.</p> <code>gen_features</code> <code>str</code> <p>ENIGMA features for generation filtering.</p> <code>sel_features</code> <code>str</code> <p>ENIGMA features for clause selection.</p> <code>posneg_ratio</code> <code>float</code> <p>Maximum ratio of negative to positive examples.</p> <code>templates</code> <code>list[str]</code> <p>Templates for strategy generation.</p> <code>evals</code> <code>Evalset</code> <p>Primary evaluation dataset configuration (Evalset with benchmarks, strategies, plugin, etc.).</p> <code>devels</code> <code>Evalset</code> <p>Development dataset configuration (Evalset).</p>"},{"location":"api/solverpy/setups/#solverpy.setups.Setup--internal-parameters","title":"Internal parameters","text":"<p>The following parameters are used internally and should not be set directly by the user.</p> <p>Attributes:</p> Name Type Description <code>it</code> <code>int</code> <p>Current iteration number.</p> <code>news</code> <code>list[str]</code> <p>New ML strategies.</p> <code>db</code> <code>DB</code> <p>Database object.</p> <code>builder</code> <code>Any</code> <p>Builder object.</p> Source code in <code>packages/solverpy/src/solverpy/setups/setup.py</code> <pre><code>class Setup(TypedDict, total=False):\n   \"\"\"\n   A (typed) dictionary summarizing the configuration of the experiment.\n   An ordinary dictionary can be used in its place but type annotations\n   provide additional safety.\n\n   Some parameters should be set by the user, others are automatically\n   filled in by functions in the [`setups`][solverpy.setups] module.\n\n   # Evaluation parameters\n\n   The following are the most important parameters to be set by the user for an evaluation run:\n\n   Attributes: \n       limit: Resource limit for solvers.\n       cores: Number of CPU cores for parallel evaluation.\n       benchmarks: List of benchmark ids. \n       strategies: List of strategy ids.\n       complete: Complete use of the solver (sat/Satisfiable are successes).\n\n   The following parameters additional parameters for an evaluation run:\n\n   Attributes:\n       ref: Reference strategy id.\n       binary: Custom solver binary.  Must be in `PATH`.\n       static: Fixed solver options.\n       ntfy: Address for user push notifications via [ntfy](https://ntfy.sh).\n       options: Experiment boolean options.\n           An *option* is identified by its string name, and represents a boolean yes/no value.\n           Use `no-`option, like `no-compress`, to set the option to `no`.\n           Currently supported options are:\n\n           |option|description|default|\n           |------|-----------|-------|\n           |`outputs`|Keep raw solver output files from all runs.|no|\n           |`compress`|Compress output files (outputs, trains, results).|yes|\n           |`flatten`|Put all output files in a single directory (replace `/` with `_._`).|yes|\n           |`compress-trains`|Compress trains.|yes|\n           |`debug-trains`|Dump training data for each file separately.|no|\n           |`proofs`|Store proof objects in the database.|no|\n           |`premises`|Store premise selections in the database.|no|\n           |`headless`|Suppress progress bars and interactive UI (for non-terminal use).|no|\n\n       plugins: List of additional solver plugins.\n       delfix: \n           Prefix to remove from problem names.\n\n           + If the value is of type `str`, then this prefix is removed from the\n           beginning of every problem name.\n           + If the value is of type `int`, then the problem name is expected to\n           be a `/`-separated path, and the first `delfix` components are\n           removed.\n           + If the value `None`, nothing is removed.\n\n           The typical use is to remove the prefix `problems/` from problem\n           names when the problems are stored inside `problems/` subdirectory.\n           In that case, set `delfix=1`.\n\n           This applies to DB providers that support `delfix` (e.g.\n           [`Solved`][solverpy.benchmark.db.providers.solved] and\n           [`Status`][solverpy.benchmark.db.providers.status]).\n           The default value is `None`.\n\n\n\n\n\n   # ML parameters\n\n   The following are user options for ML experiments.\n\n   Attributes:\n       loops: Number of iterations of the eval/ML loop.\n       force: Recompute everything.\n       shuffle: Shuffle problem order.\n       e_training_examples: Output format of training samples for `eprover`.\n       gen_features: ENIGMA features for generation filtering.\n       sel_features: ENIGMA features for clause selection.\n       posneg_ratio: Maximum ratio of negative to positive examples.\n       templates: Templates for strategy generation.\n       evals: Primary evaluation dataset configuration (Evalset with benchmarks, strategies, plugin, etc.).\n       devels: Development dataset configuration (Evalset).\n\n   # Internal parameters\n\n   The following parameters are used internally and should not be set\n   directly by the user.\n\n   Attributes:\n       it: Current iteration number.\n       news: New ML strategies.\n       db: Database object.\n       builder: Builder object.\n   \"\"\"\n   limit: str\n   cores: int\n   binary: str\n   static: list[str]\n   ntfy: str\n   it: int\n   loops: int\n   news: list[str]\n   options: list[str]\n   common: dict[str, Any]\n   delfix: (int | str | None)\n   complete: bool\n\n   force: bool\n   shuffle: bool\n\n   db: \"DB\"\n   builder: Any  # set by solverpy_learn\n   talker: \"Talker\"\n   reloader: bool\n   evals: \"Evalset\"  # primary evaluation dataset (set by solverpy_learn or evaluation())\n   devels: \"Evalset\"  # development dataset (set by solverpy_learn)\n   plugins: list[\"Plugin\"]\n   e_training_examples: str\n   gen_features: str\n   sel_features: str\n   posneg_ratio: float\n   posneg_weight: float\n   templates: list[str]\n   chunk_size: int  # rows per NPZ chunk when compressing SVM-Light trains (default 1_000_000)\n   pool_context: str  # multiprocessing start method for the evaluation pool (default \"forkserver\")\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.bitwuzla","title":"<code>bitwuzla(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Bitwuzla</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def bitwuzla(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Bitwuzla`][solverpy.solver.smt.bitwuzla.Bitwuzla] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Bitwuzla, BWZ_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.cvc5","title":"<code>cvc5(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Cvc5</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def cvc5(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Cvc5`][solverpy.solver.smt.cvc5.Cvc5] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Cvc5, CVC5_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.eprover","title":"<code>eprover(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>E</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def eprover(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`E`][solverpy.solver.atp.eprover.E] solver.\"\"\"\n   assert \"evals\" in setup\n   init(setup)\n   assert \"options\" in setup\n   assert \"plugins\" in setup\n   plugins = [\n      EProverSid() if isinstance(p, Sid) else p\n      for p in setup[\"plugins\"]\n   ]\n   setup[\"plugins\"] = plugins\n   for key in (\"evals\", \"devels\"):\n      if key not in setup:\n         continue\n      evalset = setup[key]\n      default(evalset, \"limit\", \"T1\")\n      default(evalset, \"static\", E_STATIC.split())\n      evalset[\"plugins\"] = copy.deepcopy(plugins)\n      evalset[\"solver\"] = make_solver(\n         evalset,\n         E,\n         evalset[\"plugins\"],\n         reloader=setup.get(\"reloader\", False),\n         options=setup[\"options\"],\n      )\n   setup.pop(\"solver\", None)\n   return setup\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.evaluation","title":"<code>evaluation(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> for benchmark evaluation: DB, cores, strategies, and benchmarks.</p> Source code in <code>packages/solverpy/src/solverpy/setups/loop.py</code> <pre><code>def evaluation(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` for benchmark evaluation: DB, cores, strategies, and benchmarks.\"\"\"\n   def configure_evalset(evalset: Evalset) -&gt; None:\n      default(evalset, \"ref\", True)\n      if \"strategies\" not in evalset:\n         default(evalset, \"sidfile\", \"sids\")\n         with open(evalset[\"sidfile\"]) as f:\n            evalset[\"strategies\"] = f.read().strip().split(\"\\n\")\n      if \"benchmarks\" not in evalset:\n         default(evalset, \"bidfile\", \"bids\")\n         with open(evalset[\"bidfile\"]) as f:\n            evalset[\"benchmarks\"] = f.read().strip().split(\"\\n\")\n\n   def check_list(evalset: Evalset, key: str, fun: Callable) -&gt; None:\n      if key not in evalset:\n         return\n      for id in evalset[key]:\n         try:\n            fun(id)\n         except Exception as e:\n            logger.error(f\"Wrong id in {key}: {id}\")\n            logger.error(f\"Exception: {e}\")\n            sys.exit(0)\n\n   default(setup, \"cores\", 4)\n   default(setup, \"delfix\", None)\n   assert \"delfix\" in setup\n   default(setup, \"db\", db.default(delfix=setup[\"delfix\"]))\n   default(setup, \"ntfy\", None)\n\n   for key in (\"evals\", \"devels\"):\n      if key not in setup:\n         continue\n      evalset = setup[key]\n      configure_evalset(evalset)\n      check_list(evalset, \"strategies\", sids.load)\n      check_list(evalset, \"refs\", sids.load)\n      check_list(evalset, \"benchmarks\", bids.problems)\n   return setup\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.experiment","title":"<code>experiment(setup: Setup) -&gt; Setup</code>","text":"<p>Normalize a user-facing setup before evaluation or looping starts.</p> Source code in <code>packages/solverpy/src/solverpy/setups/loop.py</code> <pre><code>def experiment(setup: Setup) -&gt; Setup:\n   \"\"\"Normalize a user-facing setup before evaluation or looping starts.\"\"\"\n   common = setup.pop(\"common\", {})\n   if \"evals\" in setup and \"trains\" in setup:\n      if setup[\"evals\"] != setup[\"trains\"]:\n         raise ValueError(\"Use either 'evals' or 'trains', not both.\")\n      setup.pop(\"trains\")\n   elif \"trains\" in setup:\n      setup[\"evals\"] = setup.pop(\"trains\")\n\n   for key in (\"evals\", \"devels\"):\n      if key not in setup:\n         continue\n      evalset = setup[key]\n      setup[key] = Evalset(common, **evalset)\n   return setup\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.launch","title":"<code>launch(setup: Setup) -&gt; Setup | None</code>","text":"<p>Run the configured evaluation to completion, then shut down the runtime.</p> Source code in <code>packages/solverpy/src/solverpy/setups/loop.py</code> <pre><code>def launch(setup: Setup) -&gt; Setup | None:\n   \"\"\"Run the configured evaluation to completion, then shut down the runtime.\"\"\"\n   runtime = None\n   try:\n      log.ntfy(setup, \"solverpy: init\")\n      evaluator.init(setup)\n      runtime = boot(setup)\n      assert \"evals\" in setup\n      evaluator.launch(setup[\"evals\"], **setup)\n      log.ntfy(setup, \"solverpy: done\")\n      return setup\n   except KeyboardInterrupt:\n      logger.warning(\"Terminated (keyboard interrupt)\")\n      print(\"Terminated (keyboard interrupt)\")\n      sys.exit(0)\n   finally:\n      if runtime:\n         setup[\"talker\"].log_stop()\n         runtime.shutdown()\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.llm2smt","title":"<code>llm2smt(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Llm2smt</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def llm2smt(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Llm2smt`][solverpy.solver.smt.llm2smt.Llm2smt] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Llm2smt, LLM2SMT_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.opensmt","title":"<code>opensmt(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Opensmt</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def opensmt(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Opensmt`][solverpy.solver.smt.opensmt.Opensmt] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Opensmt, OPENSMT_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.primo","title":"<code>primo(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Primo</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def primo(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Primo`][solverpy.solver.smt.primo.Primo] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Primo, PRIMO_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.prover9","title":"<code>prover9(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Prover9</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def prover9(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Prover9`][solverpy.solver.atp.prover9.Prover9] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Prover9)\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.spasssatt","title":"<code>spasssatt(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>SpassSatt</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def spasssatt(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`SpassSatt`][solverpy.solver.smt.spasssatt.SpassSatt] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, SpassSatt, SPASSSATT_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.vampire","title":"<code>vampire(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Vampire</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def vampire(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Vampire`][solverpy.solver.atp.vampire.Vampire] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Vampire, V_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.yices","title":"<code>yices(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Yices</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def yices(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Yices`][solverpy.solver.smt.yices.Yices] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Yices, YICES_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/#solverpy.setups.z3","title":"<code>z3(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Z3</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def z3(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Z3`][solverpy.solver.smt.z3.Z3] solver.\"\"\"\n   init(setup)\n   # No static: `Z3` is a `StdinSolver`, where `static` is the prefix of the\n   # stdin payload and not the command line.  `Z3_STATIC` is already baked\n   # into the command by `Z3.__init__`, and passing it here as well sent\n   # `-smt2 -st` down the SMT-LIB2 stream, so every z3 run answered with a\n   # leading `(error \"line 1 column 1: invalid command, '(' expected\")`.\n   return _evalset_solvers(setup, Z3)\n</code></pre>"},{"location":"api/solverpy/setups/common/","title":"module common","text":""},{"location":"api/solverpy/setups/common/#solverpy.setups.common","title":"<code>solverpy.setups.common</code>","text":""},{"location":"api/solverpy/setups/common/#solverpy.setups.common--setup-helpers","title":"Setup helpers","text":"<p>Shared helpers used by the solver setup functions in <code>solver</code> to fill in <code>Setup</code> defaults and build solver instances.</p>"},{"location":"api/solverpy/setups/common/#solverpy.setups.common.default","title":"<code>default(setup: Any, key: str, val: Any) -&gt; None</code>","text":"<p>Set <code>setup[key] = val</code> unless <code>key</code> is already present.</p> Source code in <code>packages/solverpy/src/solverpy/setups/common.py</code> <pre><code>def default(setup: Any, key: str, val: Any) -&gt; None:\n   \"\"\"Set `setup[key] = val` unless `key` is already present.\"\"\"\n   if key not in setup:\n      setup[key] = val\n</code></pre>"},{"location":"api/solverpy/setups/common/#solverpy.setups.common.ensure","title":"<code>ensure(options: list[str], option: str) -&gt; None</code>","text":"<p>Append <code>option</code> to <code>options</code> unless it or its <code>no-</code> negation is present.</p> Source code in <code>packages/solverpy/src/solverpy/setups/common.py</code> <pre><code>def ensure(options: list[str], option: str) -&gt; None:\n   \"\"\"Append `option` to `options` unless it or its `no-` negation is present.\"\"\"\n   baseopt = option if not option.startswith(\"no-\") else option[3:]\n   if (baseopt not in options) and (f\"no-{baseopt}\" not in options):\n      options.append(option)\n</code></pre>"},{"location":"api/solverpy/setups/common/#solverpy.setups.common.init","title":"<code>init(setup: Setup) -&gt; Setup</code>","text":"<p>Fill in <code>options</code>, <code>limit</code>, <code>dataname</code>, and <code>plugins</code> defaults on <code>setup</code>.</p> Source code in <code>packages/solverpy/src/solverpy/setups/common.py</code> <pre><code>def init(setup: Setup) -&gt; Setup:\n   \"\"\"Fill in `options`, `limit`, `dataname`, and `plugins` defaults on `setup`.\"\"\"\n   default(setup, \"options\", [\"flatten\", \"compress\"])\n   assert \"options\" in setup\n   options = setup[\"options\"]\n   ensure(options, \"flatten\")\n   ensure(options, \"compress\")\n   default(setup, \"limit\", \"T1\")\n   default(setup, \"dataname\", \"noname\")\n\n   if \"plugins\" not in setup:\n      if \"outputs\" not in options:\n         plugs = plugins.db()\n      else:\n         plugs = plugins.outputs(\n            flatten=\"flatten\" in options,\n            compress=\"compress\" in options,\n         )\n      if \"proofs\" in options or \"premises\" in options:\n         plugs.append(plugins.Proofs(\n            proof=\"proofs\" in options,\n            premises=\"premises\" in options,\n            flatten=\"flatten\" in options,\n            compress=\"compress\" in options,\n         ))\n      default(setup, \"plugins\", plugs)\n   return setup\n</code></pre>"},{"location":"api/solverpy/setups/common/#solverpy.setups.common.make_solver","title":"<code>make_solver(setup: Setup, mk_solver: SolverMaker, plugins: list | None = None, reloader: bool = False, options: list[str] | None = None)</code>","text":"<p>Build a solver via <code>mk_solver(setup[\"limit\"], ...)</code>, wrapping it in a <code>Reloader</code> if requested.</p> Source code in <code>packages/solverpy/src/solverpy/setups/common.py</code> <pre><code>def make_solver(\n   setup: Setup,\n   mk_solver: \"SolverMaker\",\n   plugins: list | None = None,\n   reloader: bool = False,\n   options: list[str] | None = None,\n):\n   \"\"\"Build a solver via `mk_solver(setup[\"limit\"], ...)`, wrapping it in a `Reloader` if requested.\"\"\"\n   assert \"static\" in setup\n   assert \"limit\" in setup\n   kwargs = {x: setup[x] for x in setup if x in GENERICS}\n   if plugins is not None:\n      kwargs[\"plugins\"] = plugins\n   kwargs[\"static\"] = \" \".join(setup[\"static\"])\n   _solver = mk_solver(setup[\"limit\"], **kwargs)\n   if reloader:\n      from ..solver.reloader import Reloader\n      options = options or []\n      _solver = Reloader(\n         _solver,\n         flatten=\"flatten\" in options,\n         compress=\"compress\" in options,\n      )\n   return _solver\n</code></pre>"},{"location":"api/solverpy/setups/evalset/","title":"module evalset","text":""},{"location":"api/solverpy/setups/evalset/#solverpy.setups.evalset","title":"<code>solverpy.setups.evalset</code>","text":""},{"location":"api/solverpy/setups/evalset/#solverpy.setups.evalset.Evalset","title":"<code>Evalset</code>","text":"<p>               Bases: <code>TypedDict</code></p> <p>Per-dataset configuration: problems, strategies, and ML training state.</p> Source code in <code>packages/solverpy/src/solverpy/setups/evalset.py</code> <pre><code>class Evalset(TypedDict, total=False):\n   \"\"\"Per-dataset configuration: problems, strategies, and ML training state.\"\"\"\n   label: str\n   benchmarks: list[str]\n   strategies: list[str]\n   bidfile: str\n   sidfile: str\n   refs: list[str]\n   ref: bool | int | str | None\n   limit: str\n   cores: int\n   binary: str\n   static: list[str]\n   complete: bool\n   force: bool\n   shuffle: bool\n   solvedby: str\n   max_proofs: int\n   pool_context: str\n   plugin: Any  # SvmTrains\n   plugins: list[\"Plugin\"]\n   solver: \"SolverPy\"\n   dataname: str\n   basedataname: str\n   previous_trains: str | tuple[str]\n   proofs: dict[str, int] | None\n   start_dataname: str\n</code></pre>"},{"location":"api/solverpy/setups/loop/","title":"module loop","text":""},{"location":"api/solverpy/setups/loop/#solverpy.setups.loop","title":"<code>solverpy.setups.loop</code>","text":""},{"location":"api/solverpy/setups/loop/#solverpy.setups.loop.evaluation","title":"<code>evaluation(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> for benchmark evaluation: DB, cores, strategies, and benchmarks.</p> Source code in <code>packages/solverpy/src/solverpy/setups/loop.py</code> <pre><code>def evaluation(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` for benchmark evaluation: DB, cores, strategies, and benchmarks.\"\"\"\n   def configure_evalset(evalset: Evalset) -&gt; None:\n      default(evalset, \"ref\", True)\n      if \"strategies\" not in evalset:\n         default(evalset, \"sidfile\", \"sids\")\n         with open(evalset[\"sidfile\"]) as f:\n            evalset[\"strategies\"] = f.read().strip().split(\"\\n\")\n      if \"benchmarks\" not in evalset:\n         default(evalset, \"bidfile\", \"bids\")\n         with open(evalset[\"bidfile\"]) as f:\n            evalset[\"benchmarks\"] = f.read().strip().split(\"\\n\")\n\n   def check_list(evalset: Evalset, key: str, fun: Callable) -&gt; None:\n      if key not in evalset:\n         return\n      for id in evalset[key]:\n         try:\n            fun(id)\n         except Exception as e:\n            logger.error(f\"Wrong id in {key}: {id}\")\n            logger.error(f\"Exception: {e}\")\n            sys.exit(0)\n\n   default(setup, \"cores\", 4)\n   default(setup, \"delfix\", None)\n   assert \"delfix\" in setup\n   default(setup, \"db\", db.default(delfix=setup[\"delfix\"]))\n   default(setup, \"ntfy\", None)\n\n   for key in (\"evals\", \"devels\"):\n      if key not in setup:\n         continue\n      evalset = setup[key]\n      configure_evalset(evalset)\n      check_list(evalset, \"strategies\", sids.load)\n      check_list(evalset, \"refs\", sids.load)\n      check_list(evalset, \"benchmarks\", bids.problems)\n   return setup\n</code></pre>"},{"location":"api/solverpy/setups/loop/#solverpy.setups.loop.experiment","title":"<code>experiment(setup: Setup) -&gt; Setup</code>","text":"<p>Normalize a user-facing setup before evaluation or looping starts.</p> Source code in <code>packages/solverpy/src/solverpy/setups/loop.py</code> <pre><code>def experiment(setup: Setup) -&gt; Setup:\n   \"\"\"Normalize a user-facing setup before evaluation or looping starts.\"\"\"\n   common = setup.pop(\"common\", {})\n   if \"evals\" in setup and \"trains\" in setup:\n      if setup[\"evals\"] != setup[\"trains\"]:\n         raise ValueError(\"Use either 'evals' or 'trains', not both.\")\n      setup.pop(\"trains\")\n   elif \"trains\" in setup:\n      setup[\"evals\"] = setup.pop(\"trains\")\n\n   for key in (\"evals\", \"devels\"):\n      if key not in setup:\n         continue\n      evalset = setup[key]\n      setup[key] = Evalset(common, **evalset)\n   return setup\n</code></pre>"},{"location":"api/solverpy/setups/loop/#solverpy.setups.loop.launch","title":"<code>launch(setup: Setup) -&gt; Setup | None</code>","text":"<p>Run the configured evaluation to completion, then shut down the runtime.</p> Source code in <code>packages/solverpy/src/solverpy/setups/loop.py</code> <pre><code>def launch(setup: Setup) -&gt; Setup | None:\n   \"\"\"Run the configured evaluation to completion, then shut down the runtime.\"\"\"\n   runtime = None\n   try:\n      log.ntfy(setup, \"solverpy: init\")\n      evaluator.init(setup)\n      runtime = boot(setup)\n      assert \"evals\" in setup\n      evaluator.launch(setup[\"evals\"], **setup)\n      log.ntfy(setup, \"solverpy: done\")\n      return setup\n   except KeyboardInterrupt:\n      logger.warning(\"Terminated (keyboard interrupt)\")\n      print(\"Terminated (keyboard interrupt)\")\n      sys.exit(0)\n   finally:\n      if runtime:\n         setup[\"talker\"].log_stop()\n         runtime.shutdown()\n</code></pre>"},{"location":"api/solverpy/setups/runtime/","title":"module runtime","text":""},{"location":"api/solverpy/setups/runtime/#solverpy.setups.runtime","title":"<code>solverpy.setups.runtime</code>","text":""},{"location":"api/solverpy/setups/runtime/#solverpy.setups.runtime.Runtime","title":"<code>Runtime</code>","text":"<p>Process resources owned by one complete learning-loop session.</p> Source code in <code>packages/solverpy/src/solverpy/setups/runtime.py</code> <pre><code>class Runtime:\n   \"\"\"Process resources owned by one complete learning-loop session.\"\"\"\n\n   def __init__(self, plugins: Iterable[Managed]) -&gt; None:\n      unique = {id(p): p for p in plugins}\n      self._plugins = list(unique.values())\n      self._manager = multiprocessing.get_context(\"forkserver\").Manager()\n      try:\n         self.log_queue = self._manager.Queue()\n         for p in self._plugins:\n            p.connect(self._manager)\n      except BaseException:\n         self.shutdown()\n         raise\n\n   def shutdown(self) -&gt; None:\n      \"\"\"Disconnect proxies and stop the shared Manager process.\"\"\"\n      if self._manager is None:\n         return\n      try:\n         for p in self._plugins:\n            p.disconnect()\n      finally:\n         self._manager.shutdown()\n         self._manager = None\n</code></pre>"},{"location":"api/solverpy/setups/runtime/#solverpy.setups.runtime.Runtime.shutdown","title":"<code>shutdown() -&gt; None</code>","text":"<p>Disconnect proxies and stop the shared Manager process.</p> Source code in <code>packages/solverpy/src/solverpy/setups/runtime.py</code> <pre><code>def shutdown(self) -&gt; None:\n   \"\"\"Disconnect proxies and stop the shared Manager process.\"\"\"\n   if self._manager is None:\n      return\n   try:\n      for p in self._plugins:\n         p.disconnect()\n   finally:\n      self._manager.shutdown()\n      self._manager = None\n</code></pre>"},{"location":"api/solverpy/setups/runtime/#solverpy.setups.runtime.initialize","title":"<code>initialize(setup: Setup) -&gt; Runtime</code>","text":"<p>Initialize all Managed plugins in the setup with a shared Manager.</p> Source code in <code>packages/solverpy/src/solverpy/setups/runtime.py</code> <pre><code>def initialize(setup: Setup) -&gt; Runtime:\n   \"\"\"Initialize all Managed plugins in the setup with a shared Manager.\"\"\"\n   configured = list(setup.get(\"plugins\", []))\n   for key in (\"evals\", \"devels\"):\n      if key in setup:\n         configured.extend(setup[key].get(\"plugins\", []))\n   plugins = [p for p in configured if isinstance(p, Managed)]\n   return Runtime(plugins)\n</code></pre>"},{"location":"api/solverpy/setups/setup/","title":"module setup","text":""},{"location":"api/solverpy/setups/setup/#solverpy.setups.setup","title":"<code>solverpy.setups.setup</code>","text":"<p>This module defines a (typed) dictionary <code>Setup</code> to store experiment parameters.</p>"},{"location":"api/solverpy/setups/setup/#solverpy.setups.setup.Setup","title":"<code>Setup</code>","text":"<p>               Bases: <code>TypedDict</code></p> <p>A (typed) dictionary summarizing the configuration of the experiment. An ordinary dictionary can be used in its place but type annotations provide additional safety.</p> <p>Some parameters should be set by the user, others are automatically filled in by functions in the <code>setups</code> module.</p>"},{"location":"api/solverpy/setups/setup/#solverpy.setups.setup.Setup--evaluation-parameters","title":"Evaluation parameters","text":"<p>The following are the most important parameters to be set by the user for an evaluation run:</p> <p>Attributes:</p> Name Type Description <code>limit</code> <code>str</code> <p>Resource limit for solvers.</p> <code>cores</code> <code>int</code> <p>Number of CPU cores for parallel evaluation.</p> <code>benchmarks</code> <code>int</code> <p>List of benchmark ids. </p> <code>strategies</code> <code>int</code> <p>List of strategy ids.</p> <code>complete</code> <code>bool</code> <p>Complete use of the solver (sat/Satisfiable are successes).</p> <p>The following parameters additional parameters for an evaluation run:</p> <p>Attributes:</p> Name Type Description <code>ref</code> <p>Reference strategy id.</p> <code>binary</code> <code>str</code> <p>Custom solver binary.  Must be in <code>PATH</code>.</p> <code>static</code> <code>list[str]</code> <p>Fixed solver options.</p> <code>ntfy</code> <code>str</code> <p>Address for user push notifications via ntfy.</p> <code>options</code> <code>list[str]</code> <p>Experiment boolean options. An option is identified by its string name, and represents a boolean yes/no value. Use <code>no-</code>option, like <code>no-compress</code>, to set the option to <code>no</code>. Currently supported options are:</p> option description default <code>outputs</code> Keep raw solver output files from all runs. no <code>compress</code> Compress output files (outputs, trains, results). yes <code>flatten</code> Put all output files in a single directory (replace <code>/</code> with <code>_._</code>). yes <code>compress-trains</code> Compress trains. yes <code>debug-trains</code> Dump training data for each file separately. no <code>proofs</code> Store proof objects in the database. no <code>premises</code> Store premise selections in the database. no <code>headless</code> Suppress progress bars and interactive UI (for non-terminal use). no <code>plugins</code> <code>list[Plugin]</code> <p>List of additional solver plugins.</p> <code>delfix</code> <code>int | str | None</code> <p>Prefix to remove from problem names.</p> <ul> <li>If the value is of type <code>str</code>, then this prefix is removed from the beginning of every problem name.</li> <li>If the value is of type <code>int</code>, then the problem name is expected to be a <code>/</code>-separated path, and the first <code>delfix</code> components are removed.</li> <li>If the value <code>None</code>, nothing is removed.</li> </ul> <p>The typical use is to remove the prefix <code>problems/</code> from problem names when the problems are stored inside <code>problems/</code> subdirectory. In that case, set <code>delfix=1</code>.</p> <p>This applies to DB providers that support <code>delfix</code> (e.g. <code>Solved</code> and <code>Status</code>). The default value is <code>None</code>.</p>"},{"location":"api/solverpy/setups/setup/#solverpy.setups.setup.Setup--ml-parameters","title":"ML parameters","text":"<p>The following are user options for ML experiments.</p> <p>Attributes:</p> Name Type Description <code>loops</code> <code>int</code> <p>Number of iterations of the eval/ML loop.</p> <code>force</code> <code>bool</code> <p>Recompute everything.</p> <code>shuffle</code> <code>bool</code> <p>Shuffle problem order.</p> <code>e_training_examples</code> <code>str</code> <p>Output format of training samples for <code>eprover</code>.</p> <code>gen_features</code> <code>str</code> <p>ENIGMA features for generation filtering.</p> <code>sel_features</code> <code>str</code> <p>ENIGMA features for clause selection.</p> <code>posneg_ratio</code> <code>float</code> <p>Maximum ratio of negative to positive examples.</p> <code>templates</code> <code>list[str]</code> <p>Templates for strategy generation.</p> <code>evals</code> <code>Evalset</code> <p>Primary evaluation dataset configuration (Evalset with benchmarks, strategies, plugin, etc.).</p> <code>devels</code> <code>Evalset</code> <p>Development dataset configuration (Evalset).</p>"},{"location":"api/solverpy/setups/setup/#solverpy.setups.setup.Setup--internal-parameters","title":"Internal parameters","text":"<p>The following parameters are used internally and should not be set directly by the user.</p> <p>Attributes:</p> Name Type Description <code>it</code> <code>int</code> <p>Current iteration number.</p> <code>news</code> <code>list[str]</code> <p>New ML strategies.</p> <code>db</code> <code>DB</code> <p>Database object.</p> <code>builder</code> <code>Any</code> <p>Builder object.</p> Source code in <code>packages/solverpy/src/solverpy/setups/setup.py</code> <pre><code>class Setup(TypedDict, total=False):\n   \"\"\"\n   A (typed) dictionary summarizing the configuration of the experiment.\n   An ordinary dictionary can be used in its place but type annotations\n   provide additional safety.\n\n   Some parameters should be set by the user, others are automatically\n   filled in by functions in the [`setups`][solverpy.setups] module.\n\n   # Evaluation parameters\n\n   The following are the most important parameters to be set by the user for an evaluation run:\n\n   Attributes: \n       limit: Resource limit for solvers.\n       cores: Number of CPU cores for parallel evaluation.\n       benchmarks: List of benchmark ids. \n       strategies: List of strategy ids.\n       complete: Complete use of the solver (sat/Satisfiable are successes).\n\n   The following parameters additional parameters for an evaluation run:\n\n   Attributes:\n       ref: Reference strategy id.\n       binary: Custom solver binary.  Must be in `PATH`.\n       static: Fixed solver options.\n       ntfy: Address for user push notifications via [ntfy](https://ntfy.sh).\n       options: Experiment boolean options.\n           An *option* is identified by its string name, and represents a boolean yes/no value.\n           Use `no-`option, like `no-compress`, to set the option to `no`.\n           Currently supported options are:\n\n           |option|description|default|\n           |------|-----------|-------|\n           |`outputs`|Keep raw solver output files from all runs.|no|\n           |`compress`|Compress output files (outputs, trains, results).|yes|\n           |`flatten`|Put all output files in a single directory (replace `/` with `_._`).|yes|\n           |`compress-trains`|Compress trains.|yes|\n           |`debug-trains`|Dump training data for each file separately.|no|\n           |`proofs`|Store proof objects in the database.|no|\n           |`premises`|Store premise selections in the database.|no|\n           |`headless`|Suppress progress bars and interactive UI (for non-terminal use).|no|\n\n       plugins: List of additional solver plugins.\n       delfix: \n           Prefix to remove from problem names.\n\n           + If the value is of type `str`, then this prefix is removed from the\n           beginning of every problem name.\n           + If the value is of type `int`, then the problem name is expected to\n           be a `/`-separated path, and the first `delfix` components are\n           removed.\n           + If the value `None`, nothing is removed.\n\n           The typical use is to remove the prefix `problems/` from problem\n           names when the problems are stored inside `problems/` subdirectory.\n           In that case, set `delfix=1`.\n\n           This applies to DB providers that support `delfix` (e.g.\n           [`Solved`][solverpy.benchmark.db.providers.solved] and\n           [`Status`][solverpy.benchmark.db.providers.status]).\n           The default value is `None`.\n\n\n\n\n\n   # ML parameters\n\n   The following are user options for ML experiments.\n\n   Attributes:\n       loops: Number of iterations of the eval/ML loop.\n       force: Recompute everything.\n       shuffle: Shuffle problem order.\n       e_training_examples: Output format of training samples for `eprover`.\n       gen_features: ENIGMA features for generation filtering.\n       sel_features: ENIGMA features for clause selection.\n       posneg_ratio: Maximum ratio of negative to positive examples.\n       templates: Templates for strategy generation.\n       evals: Primary evaluation dataset configuration (Evalset with benchmarks, strategies, plugin, etc.).\n       devels: Development dataset configuration (Evalset).\n\n   # Internal parameters\n\n   The following parameters are used internally and should not be set\n   directly by the user.\n\n   Attributes:\n       it: Current iteration number.\n       news: New ML strategies.\n       db: Database object.\n       builder: Builder object.\n   \"\"\"\n   limit: str\n   cores: int\n   binary: str\n   static: list[str]\n   ntfy: str\n   it: int\n   loops: int\n   news: list[str]\n   options: list[str]\n   common: dict[str, Any]\n   delfix: (int | str | None)\n   complete: bool\n\n   force: bool\n   shuffle: bool\n\n   db: \"DB\"\n   builder: Any  # set by solverpy_learn\n   talker: \"Talker\"\n   reloader: bool\n   evals: \"Evalset\"  # primary evaluation dataset (set by solverpy_learn or evaluation())\n   devels: \"Evalset\"  # development dataset (set by solverpy_learn)\n   plugins: list[\"Plugin\"]\n   e_training_examples: str\n   gen_features: str\n   sel_features: str\n   posneg_ratio: float\n   posneg_weight: float\n   templates: list[str]\n   chunk_size: int  # rows per NPZ chunk when compressing SVM-Light trains (default 1_000_000)\n   pool_context: str  # multiprocessing start method for the evaluation pool (default \"forkserver\")\n</code></pre>"},{"location":"api/solverpy/setups/solver/","title":"module solver","text":""},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver","title":"<code>solverpy.setups.solver</code>","text":""},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver--solver-setup-functions","title":"Solver setup functions","text":"<p>Each function here selects and configures one solver for a <code>Setup</code>, filling in its <code>evals</code>/<code>devels</code> solver instances and static options.</p>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.bitwuzla","title":"<code>bitwuzla(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Bitwuzla</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def bitwuzla(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Bitwuzla`][solverpy.solver.smt.bitwuzla.Bitwuzla] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Bitwuzla, BWZ_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.cvc5","title":"<code>cvc5(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Cvc5</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def cvc5(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Cvc5`][solverpy.solver.smt.cvc5.Cvc5] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Cvc5, CVC5_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.eprover","title":"<code>eprover(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>E</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def eprover(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`E`][solverpy.solver.atp.eprover.E] solver.\"\"\"\n   assert \"evals\" in setup\n   init(setup)\n   assert \"options\" in setup\n   assert \"plugins\" in setup\n   plugins = [\n      EProverSid() if isinstance(p, Sid) else p\n      for p in setup[\"plugins\"]\n   ]\n   setup[\"plugins\"] = plugins\n   for key in (\"evals\", \"devels\"):\n      if key not in setup:\n         continue\n      evalset = setup[key]\n      default(evalset, \"limit\", \"T1\")\n      default(evalset, \"static\", E_STATIC.split())\n      evalset[\"plugins\"] = copy.deepcopy(plugins)\n      evalset[\"solver\"] = make_solver(\n         evalset,\n         E,\n         evalset[\"plugins\"],\n         reloader=setup.get(\"reloader\", False),\n         options=setup[\"options\"],\n      )\n   setup.pop(\"solver\", None)\n   return setup\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.llm2smt","title":"<code>llm2smt(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Llm2smt</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def llm2smt(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Llm2smt`][solverpy.solver.smt.llm2smt.Llm2smt] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Llm2smt, LLM2SMT_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.opensmt","title":"<code>opensmt(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Opensmt</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def opensmt(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Opensmt`][solverpy.solver.smt.opensmt.Opensmt] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Opensmt, OPENSMT_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.primo","title":"<code>primo(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Primo</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def primo(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Primo`][solverpy.solver.smt.primo.Primo] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Primo, PRIMO_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.prover9","title":"<code>prover9(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Prover9</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def prover9(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Prover9`][solverpy.solver.atp.prover9.Prover9] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Prover9)\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.spasssatt","title":"<code>spasssatt(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>SpassSatt</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def spasssatt(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`SpassSatt`][solverpy.solver.smt.spasssatt.SpassSatt] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, SpassSatt, SPASSSATT_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.vampire","title":"<code>vampire(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Vampire</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def vampire(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Vampire`][solverpy.solver.atp.vampire.Vampire] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Vampire, V_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.yices","title":"<code>yices(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Yices</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def yices(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Yices`][solverpy.solver.smt.yices.Yices] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Yices, YICES_STATIC.split())\n</code></pre>"},{"location":"api/solverpy/setups/solver/#solverpy.setups.solver.z3","title":"<code>z3(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Z3</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def z3(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Z3`][solverpy.solver.smt.z3.Z3] solver.\"\"\"\n   init(setup)\n   # No static: `Z3` is a `StdinSolver`, where `static` is the prefix of the\n   # stdin payload and not the command line.  `Z3_STATIC` is already baked\n   # into the command by `Z3.__init__`, and passing it here as well sent\n   # `-smt2 -st` down the SMT-LIB2 stream, so every z3 run answered with a\n   # leading `(error \"line 1 column 1: invalid command, '(' expected\")`.\n   return _evalset_solvers(setup, Z3)\n</code></pre>"},{"location":"api/solverpy/solver/","title":"Solver Overview","text":""},{"location":"api/solverpy/solver/#solverpy.solver","title":"<code>solverpy.solver</code>","text":""},{"location":"api/solverpy/solver/#solverpy.solver--single-problem-solving","title":"Single problem solving","text":"<p>To call the solver on one problem instance, start by creating the solver object.</p> <pre><code>from solverpy.solver.smt.cvc5 import Cvc5\n\ncvc5 = Cvc5(\"T5\")  # time limit of 5 seconds\n</code></pre> <p>The constructor argument is a resource limit string, in this case, a time limit <code>T</code> in seconds.  All solvers support <code>T</code> and additional resource limits might be available depending on the solver.  Multiple resource limits can be used (separated by <code>-</code>, like <code>T10-R50000</code>).  The limit string must, however, always start with <code>T</code>.</p> <p>Then call the <code>solve</code> method:</p> <pre><code>result = cvc5.solve(\"myproblem.smt2\", \"--enum-inst\")\n</code></pre> <p>The first argument is the problem filename, the second is the solver-dependent strategy description (typically command line options as a string).</p> <p>The result is a Python <code>dict</code> with results and statistics.  The keys and values are solver-specific.  Nevertheless, the result always contains keys <code>status</code> (with the value of type <code>str</code>) and <code>runtime</code> (type <code>float</code>).</p> <p>Hint: Call <code>cvc5.run(p,s)</code> instead of <code>cvc5.solve(p,s)</code> to get the raw solver output without any processing.  Call <code>cvc5.command(p,s)</code> to output the shell command that is going to be executed to launch the solver.</p>"},{"location":"api/solverpy/solver/object/","title":"module object","text":""},{"location":"api/solverpy/solver/object/#solverpy.solver.object","title":"<code>solverpy.solver.object</code>","text":""},{"location":"api/solverpy/solver/object/#solverpy.solver.object--base-object-for-repr-and-logging","title":"Base object for repr and logging","text":"<p><code>SolverPyObj</code> is the root base class for all SolverPy objects. It provides a uniform <code>__repr__</code> built from the keyword arguments passed at construction time, and a <code>represent</code> method used for YAML serialisation.</p>"},{"location":"api/solverpy/solver/object/#solverpy.solver.object.SolverPyObj","title":"<code>SolverPyObj</code>","text":"<p>Root base class for all SolverPy objects providing uniform repr and YAML serialisation.</p> Source code in <code>packages/solverpy/src/solverpy/solver/object.py</code> <pre><code>class SolverPyObj:\n   \"\"\"Root base class for all SolverPy objects providing uniform repr and YAML serialisation.\"\"\"\n\n   def __init__(\n      self,\n      cls_name: (str | None) = None,\n      **kwargs: Any,\n   ):\n      \"\"\"\n      Args:\n          cls_name: override the class name used in `repr`. Defaults to `self.__class__.__name__`.\n          **kwargs: keyword arguments stored for use in `__repr__`.\n      \"\"\"\n      self._repr_args: dict[str, Any] = dict(kwargs)\n      self._cls_name = cls_name or self.__class__.__name__\n\n   def __repr__(self) -&gt; str:\n      \"\"\"Return `ClassName(key=value,...)` built from the kwargs passed at construction.\"\"\"\n      if hasattr(self, \"_repr_args\") and self._repr_args is not None:\n         ias = [f\"{x}={y}\" for (x, y) in self._repr_args.items()]\n         ias = \",\".join(sorted(ias))\n         return f\"{self._cls_name}({ias})\"\n      return object.__repr__(self)\n\n   def represent(self) -&gt; (str | list[Any] | dict[str, Any]):\n      \"\"\"Default yaml represeneter\"\"\"\n      return repr(self)\n</code></pre>"},{"location":"api/solverpy/solver/object/#solverpy.solver.object.SolverPyObj.__init__","title":"<code>__init__(cls_name: str | None = None, **kwargs: Any)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>cls_name</code> <code>str | None</code> <p>override the class name used in <code>repr</code>. Defaults to <code>self.__class__.__name__</code>.</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>keyword arguments stored for use in <code>__repr__</code>.</p> <code>{}</code> Source code in <code>packages/solverpy/src/solverpy/solver/object.py</code> <pre><code>def __init__(\n   self,\n   cls_name: (str | None) = None,\n   **kwargs: Any,\n):\n   \"\"\"\n   Args:\n       cls_name: override the class name used in `repr`. Defaults to `self.__class__.__name__`.\n       **kwargs: keyword arguments stored for use in `__repr__`.\n   \"\"\"\n   self._repr_args: dict[str, Any] = dict(kwargs)\n   self._cls_name = cls_name or self.__class__.__name__\n</code></pre>"},{"location":"api/solverpy/solver/object/#solverpy.solver.object.SolverPyObj.__repr__","title":"<code>__repr__() -&gt; str</code>","text":"<p>Return <code>ClassName(key=value,...)</code> built from the kwargs passed at construction.</p> Source code in <code>packages/solverpy/src/solverpy/solver/object.py</code> <pre><code>def __repr__(self) -&gt; str:\n   \"\"\"Return `ClassName(key=value,...)` built from the kwargs passed at construction.\"\"\"\n   if hasattr(self, \"_repr_args\") and self._repr_args is not None:\n      ias = [f\"{x}={y}\" for (x, y) in self._repr_args.items()]\n      ias = \",\".join(sorted(ias))\n      return f\"{self._cls_name}({ias})\"\n   return object.__repr__(self)\n</code></pre>"},{"location":"api/solverpy/solver/object/#solverpy.solver.object.SolverPyObj.represent","title":"<code>represent() -&gt; str | list[Any] | dict[str, Any]</code>","text":"<p>Default yaml represeneter</p> Source code in <code>packages/solverpy/src/solverpy/solver/object.py</code> <pre><code>def represent(self) -&gt; (str | list[Any] | dict[str, Any]):\n   \"\"\"Default yaml represeneter\"\"\"\n   return repr(self)\n</code></pre>"},{"location":"api/solverpy/solver/pluginsolver/","title":"module pluginsolver","text":""},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver","title":"<code>solverpy.solver.pluginsolver</code>","text":""},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver--solver-extension-plugins","title":"Solver extension plugins","text":"<p>The class <code>PluginSolver</code> extends <code>Solver</code> to add support for plugins.  <code>Plugin</code>'s can extend or modify the behavior of the solver. There are two types of plugins: decorators and translators. </p> <ul> <li><code>Decorator</code>'s are used to modify the command and the output result of the solver. Typically decorators are used to add timing information to the solver output, and parse standardized solver output, like TPTP or SMT2. </li> <li><code>Translator</code>'s are used to translate the instance and strategy for the <code>Solver.solve</code> method, for example, translating a benchmark-problem pair to a filename.</li> </ul> <p>A single solver can register multiple plugins. The order of registration might matter. The basic abstract class for plugins is <code>Plugin</code>.</p> <p> </p> <p>For more on plugins, check the <code>plugins</code> module. The direct superclass of <code>PluginSolver</code>  is <code>SolverPy</code> which adds database support.</p>"},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver.PluginSolver","title":"<code>PluginSolver</code>","text":"<p>               Bases: <code>Solver</code></p> <p> </p> Source code in <code>packages/solverpy/src/solverpy/solver/pluginsolver.py</code> <pre><code>class PluginSolver(Solver):\n   \"\"\"\n   ```plantuml name=\"solver-pluginsolver\"\n   abstract class solverpy.solver.solver.Solver\n   abstract class solverpy.solver.pluginsolver.PluginSolver extends solverpy.solver.solver.Solver {\n      # decorators : list[Decorator]\n      # translators : list[Translator]\n      + init(plugins) \n      + decorate(cmd, instance, strategy) : str\n      + translate(instance, strategy) : tuple\n      + update(instance, strategy, output, result) \n      + call(pid, method, arguments)\n   }\n   abstract class solverpy.solver.solverpy.SolverPy extends solverpy.solver.pluginsolver.PluginSolver\n   ```\n   \"\"\"\n\n   def __init__(self, plugins: list[\"Plugin\"] = [], **kwargs: Any):\n      \"\"\"\n      Initialize the solver and register all provided plugins.\n\n      Args:\n          plugins: list of `Plugin` instances to register on construction.\n          **kwargs: forwarded to `Solver.__init__`.\n      \"\"\"\n      Solver.__init__(self, **kwargs)\n      self.decorators: list[\"Decorator\"] = []\n      self.translators: list[\"Translator\"] = []\n      self.init(plugins)\n\n   def represent(self):\n      \"\"\"Return a dict representation listing the class name, decorators, and translators.\"\"\"\n      cls = f\"{self.__class__.__module__}.{self.__class__.__name__}\"\n      return dict(\n         cls=cls,\n         decorators=[repr(x) for x in self.decorators],\n         translators=[repr(x) for x in self.translators],\n      )\n\n   def solve(self, instance: Any, strategy: Any) -&gt; \"Result\":\n      \"\"\"\n      Run the solver with the strategy on the instatance. Update the \n      solver result and announce the final version. Return the updated\n      result.\n      \"\"\"\n      result = Solver.solve(self, instance, strategy)\n      output = self._output\n      self.update(instance, strategy, output, result)\n      if not self.valid(result):\n         lines = output.split(\"\\n\")\n         if len(lines) &gt; 3:\n            msg = f\"{lines[2]}\\n{lines[3]}\"  # command and first output line\n         else:\n            msg = output\n         logger.debug(\n            f\"failed solver run: {self}:{strategy} @ {instance}\\nresult: {result}\\n{msg}\"\n         )\n      return result\n\n   def init(self, plugins: list[\"Plugin\"]) -&gt; None:\n      \"\"\"Plugins initialization.\"\"\"\n      for plugin in plugins:\n         plugin.register(self)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Decorate the command for the solver.\"\"\"\n      for plugin in self.decorators:\n         cmd = plugin.decorate(cmd, instance, strategy)\n      return cmd\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Update the solver result and announce the final version.\"\"\"\n      for plugin in self.decorators:\n         plugin.update(instance, strategy, output, result)\n      for plugin in self.decorators:\n         plugin.finished(instance, strategy, output, result)\n\n   def translate(\n      self,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; tuple[Any, Any]:\n      \"\"\"Translate the instance and strategy for the solver.\"\"\"\n      for plugin in self.translators:\n         (instance, strategy) = plugin.translate(instance, strategy)\n      return (instance, strategy)\n\n   def call(\n      self,\n      pid: str,\n      method: str,\n      *args,\n      **kwargs,\n   ):\n      \"\"\"Call `method` of the plugin(s) `pid`.\"\"\" \n      for plugin in self.translators + self.decorators:\n         if (plugin.id == pid):\n            if hasattr(plugin, method):\n               handler = getattr(plugin, method)\n               handler(*args, **kwargs)\n            else:\n               logger.warning(f\"Unknown method {method} of plugin {pid}\")\n</code></pre>"},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver.PluginSolver.__init__","title":"<code>__init__(plugins: list[Plugin] = [], **kwargs: Any)</code>","text":"<p>Initialize the solver and register all provided plugins.</p> <p>Parameters:</p> Name Type Description Default <code>plugins</code> <code>list[Plugin]</code> <p>list of <code>Plugin</code> instances to register on construction.</p> <code>[]</code> <code>**kwargs</code> <code>Any</code> <p>forwarded to <code>Solver.__init__</code>.</p> <code>{}</code> Source code in <code>packages/solverpy/src/solverpy/solver/pluginsolver.py</code> <pre><code>def __init__(self, plugins: list[\"Plugin\"] = [], **kwargs: Any):\n   \"\"\"\n   Initialize the solver and register all provided plugins.\n\n   Args:\n       plugins: list of `Plugin` instances to register on construction.\n       **kwargs: forwarded to `Solver.__init__`.\n   \"\"\"\n   Solver.__init__(self, **kwargs)\n   self.decorators: list[\"Decorator\"] = []\n   self.translators: list[\"Translator\"] = []\n   self.init(plugins)\n</code></pre>"},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver.PluginSolver.call","title":"<code>call(pid: str, method: str, *args, **kwargs)</code>","text":"<p>Call <code>method</code> of the plugin(s) <code>pid</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/pluginsolver.py</code> <pre><code>def call(\n   self,\n   pid: str,\n   method: str,\n   *args,\n   **kwargs,\n):\n   \"\"\"Call `method` of the plugin(s) `pid`.\"\"\" \n   for plugin in self.translators + self.decorators:\n      if (plugin.id == pid):\n         if hasattr(plugin, method):\n            handler = getattr(plugin, method)\n            handler(*args, **kwargs)\n         else:\n            logger.warning(f\"Unknown method {method} of plugin {pid}\")\n</code></pre>"},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver.PluginSolver.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Decorate the command for the solver.</p> Source code in <code>packages/solverpy/src/solverpy/solver/pluginsolver.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Decorate the command for the solver.\"\"\"\n   for plugin in self.decorators:\n      cmd = plugin.decorate(cmd, instance, strategy)\n   return cmd\n</code></pre>"},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver.PluginSolver.init","title":"<code>init(plugins: list[Plugin]) -&gt; None</code>","text":"<p>Plugins initialization.</p> Source code in <code>packages/solverpy/src/solverpy/solver/pluginsolver.py</code> <pre><code>def init(self, plugins: list[\"Plugin\"]) -&gt; None:\n   \"\"\"Plugins initialization.\"\"\"\n   for plugin in plugins:\n      plugin.register(self)\n</code></pre>"},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver.PluginSolver.represent","title":"<code>represent()</code>","text":"<p>Return a dict representation listing the class name, decorators, and translators.</p> Source code in <code>packages/solverpy/src/solverpy/solver/pluginsolver.py</code> <pre><code>def represent(self):\n   \"\"\"Return a dict representation listing the class name, decorators, and translators.\"\"\"\n   cls = f\"{self.__class__.__module__}.{self.__class__.__name__}\"\n   return dict(\n      cls=cls,\n      decorators=[repr(x) for x in self.decorators],\n      translators=[repr(x) for x in self.translators],\n   )\n</code></pre>"},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver.PluginSolver.solve","title":"<code>solve(instance: Any, strategy: Any) -&gt; Result</code>","text":"<p>Run the solver with the strategy on the instatance. Update the  solver result and announce the final version. Return the updated result.</p> Source code in <code>packages/solverpy/src/solverpy/solver/pluginsolver.py</code> <pre><code>def solve(self, instance: Any, strategy: Any) -&gt; \"Result\":\n   \"\"\"\n   Run the solver with the strategy on the instatance. Update the \n   solver result and announce the final version. Return the updated\n   result.\n   \"\"\"\n   result = Solver.solve(self, instance, strategy)\n   output = self._output\n   self.update(instance, strategy, output, result)\n   if not self.valid(result):\n      lines = output.split(\"\\n\")\n      if len(lines) &gt; 3:\n         msg = f\"{lines[2]}\\n{lines[3]}\"  # command and first output line\n      else:\n         msg = output\n      logger.debug(\n         f\"failed solver run: {self}:{strategy} @ {instance}\\nresult: {result}\\n{msg}\"\n      )\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver.PluginSolver.translate","title":"<code>translate(instance: Any, strategy: Any) -&gt; tuple[Any, Any]</code>","text":"<p>Translate the instance and strategy for the solver.</p> Source code in <code>packages/solverpy/src/solverpy/solver/pluginsolver.py</code> <pre><code>def translate(\n   self,\n   instance: Any,\n   strategy: Any,\n) -&gt; tuple[Any, Any]:\n   \"\"\"Translate the instance and strategy for the solver.\"\"\"\n   for plugin in self.translators:\n      (instance, strategy) = plugin.translate(instance, strategy)\n   return (instance, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/pluginsolver/#solverpy.solver.pluginsolver.PluginSolver.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Update the solver result and announce the final version.</p> Source code in <code>packages/solverpy/src/solverpy/solver/pluginsolver.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Update the solver result and announce the final version.\"\"\"\n   for plugin in self.decorators:\n      plugin.update(instance, strategy, output, result)\n   for plugin in self.decorators:\n      plugin.finished(instance, strategy, output, result)\n</code></pre>"},{"location":"api/solverpy/solver/reloader/","title":"module reloader","text":""},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader","title":"<code>solverpy.solver.reloader</code>","text":""},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader--reloader-replay-solver-runs-from-cached-outputs","title":"Reloader \u2014 replay solver runs from cached outputs","text":"<p><code>Reloader</code> wraps an existing <code>SolverPy</code> solver and replays previously recorded runs by reading the raw output from the <code>outputs/</code> cache directory instead of re-invoking the binary.  All plugin calls (<code>decorate</code>, <code>update</code>, <code>translate</code>) are delegated to the wrapped solver so that result processing is identical to a live run.</p> <p>Typical use: re-process cached outputs with updated parsing logic without re-running the (expensive) solver.</p>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader","title":"<code>Reloader</code>","text":"<p>               Bases: <code>SolverPy</code></p> <p>A <code>SolverPy</code> that replays runs from cached output files instead of invoking the solver binary.  Delegates all processing to the wrapped solver.</p> Source code in <code>packages/solverpy/src/solverpy/solver/reloader.py</code> <pre><code>class Reloader(SolverPy):\n   \"\"\"\n   A `SolverPy` that replays runs from cached output files instead of\n   invoking the solver binary.  Delegates all processing to the wrapped solver.\n   \"\"\"\n\n   def __init__(\n      self,\n      solver: SolverPy,\n      plugins: list[\"Plugin\"] = [],\n      flatten: bool = True,\n      compress: bool = True,\n   ):\n      \"\"\"\n      Args:\n          solver: the wrapped solver whose output cache and processing logic to reuse.\n          plugins: additional plugins registered on this reloader instance.\n          flatten: passed to `Outputs` \u2014 replace `/` in problem paths with `_._`.\n          compress: passed to `Outputs` \u2014 read `.gz` files.\n      \"\"\"\n      self.solver = solver\n      SolverPy.__init__(\n         self,\n         solver._limits,\n         plugins,\n      )\n      self.outputs = Outputs(flatten=flatten, compress=compress)\n      self.outputs.register(self.solver)\n      self.solver.call(\"outputs\", \"disable\")\n      self.outputs.disable()\n\n   def run(self, instance: Any, strategy: Any) -&gt; str:\n      \"\"\"Read and return the cached raw output for `(instance, strategy)`.\"\"\"\n      # triggers Time.decorate() to snapshot start time; no subprocess is launched\n      self.decorate(\"\", instance, strategy)\n      try:\n         return self.outputs.read(instance, strategy)\n      except FileNotFoundError:\n         logger.warning(f\"No cached output for {instance} / {strategy}\")\n         return \"\"\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Delegate output parsing to the wrapped solver.\"\"\"\n      return self.solver.process(output)\n\n   def valid(self, result: \"Result\") -&gt; bool:\n      \"\"\"Delegate result validation to the wrapped solver.\"\"\"\n      return self.solver.valid(result)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Delegate command decoration to the wrapped solver.\"\"\"\n      return self.solver.decorate(cmd, instance, strategy)\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Delegate result update/announcement to the wrapped solver.\"\"\"\n      self.solver.update(instance, strategy, output, result)\n\n   def translate(\n      self,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; tuple[Any, Any]:\n      \"\"\"Delegate instance/strategy translation to the wrapped solver.\"\"\"\n      return self.solver.translate(instance, strategy)\n\n   @property\n   def name(self) -&gt; str:\n      \"\"\"Reloader name including the wrapped solver name, e.g. `Reloader(E:T10)`.\"\"\"\n      return f\"{super().name}({self.solver.name})\"\n\n   @property\n   def success(self) -&gt; frozenset[str]:\n      \"\"\"Delegate to the wrapped solver's success statuses.\"\"\"\n      return self.solver.success\n\n   @property\n   def timeouts(self) -&gt; frozenset[str]:\n      \"\"\"Delegate to the wrapped solver's timeout statuses.\"\"\"\n      return self.solver.timeouts\n</code></pre>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.name","title":"<code>name: str</code>  <code>property</code>","text":"<p>Reloader name including the wrapped solver name, e.g. <code>Reloader(E:T10)</code>.</p>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.success","title":"<code>success: frozenset[str]</code>  <code>property</code>","text":"<p>Delegate to the wrapped solver's success statuses.</p>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.timeouts","title":"<code>timeouts: frozenset[str]</code>  <code>property</code>","text":"<p>Delegate to the wrapped solver's timeout statuses.</p>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.__init__","title":"<code>__init__(solver: SolverPy, plugins: list[Plugin] = [], flatten: bool = True, compress: bool = True)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>solver</code> <code>SolverPy</code> <p>the wrapped solver whose output cache and processing logic to reuse.</p> required <code>plugins</code> <code>list[Plugin]</code> <p>additional plugins registered on this reloader instance.</p> <code>[]</code> <code>flatten</code> <code>bool</code> <p>passed to <code>Outputs</code> \u2014 replace <code>/</code> in problem paths with <code>_._</code>.</p> <code>True</code> <code>compress</code> <code>bool</code> <p>passed to <code>Outputs</code> \u2014 read <code>.gz</code> files.</p> <code>True</code> Source code in <code>packages/solverpy/src/solverpy/solver/reloader.py</code> <pre><code>def __init__(\n   self,\n   solver: SolverPy,\n   plugins: list[\"Plugin\"] = [],\n   flatten: bool = True,\n   compress: bool = True,\n):\n   \"\"\"\n   Args:\n       solver: the wrapped solver whose output cache and processing logic to reuse.\n       plugins: additional plugins registered on this reloader instance.\n       flatten: passed to `Outputs` \u2014 replace `/` in problem paths with `_._`.\n       compress: passed to `Outputs` \u2014 read `.gz` files.\n   \"\"\"\n   self.solver = solver\n   SolverPy.__init__(\n      self,\n      solver._limits,\n      plugins,\n   )\n   self.outputs = Outputs(flatten=flatten, compress=compress)\n   self.outputs.register(self.solver)\n   self.solver.call(\"outputs\", \"disable\")\n   self.outputs.disable()\n</code></pre>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Delegate command decoration to the wrapped solver.</p> Source code in <code>packages/solverpy/src/solverpy/solver/reloader.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Delegate command decoration to the wrapped solver.\"\"\"\n   return self.solver.decorate(cmd, instance, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Delegate output parsing to the wrapped solver.</p> Source code in <code>packages/solverpy/src/solverpy/solver/reloader.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Delegate output parsing to the wrapped solver.\"\"\"\n   return self.solver.process(output)\n</code></pre>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.run","title":"<code>run(instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Read and return the cached raw output for <code>(instance, strategy)</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/reloader.py</code> <pre><code>def run(self, instance: Any, strategy: Any) -&gt; str:\n   \"\"\"Read and return the cached raw output for `(instance, strategy)`.\"\"\"\n   # triggers Time.decorate() to snapshot start time; no subprocess is launched\n   self.decorate(\"\", instance, strategy)\n   try:\n      return self.outputs.read(instance, strategy)\n   except FileNotFoundError:\n      logger.warning(f\"No cached output for {instance} / {strategy}\")\n      return \"\"\n</code></pre>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.translate","title":"<code>translate(instance: Any, strategy: Any) -&gt; tuple[Any, Any]</code>","text":"<p>Delegate instance/strategy translation to the wrapped solver.</p> Source code in <code>packages/solverpy/src/solverpy/solver/reloader.py</code> <pre><code>def translate(\n   self,\n   instance: Any,\n   strategy: Any,\n) -&gt; tuple[Any, Any]:\n   \"\"\"Delegate instance/strategy translation to the wrapped solver.\"\"\"\n   return self.solver.translate(instance, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Delegate result update/announcement to the wrapped solver.</p> Source code in <code>packages/solverpy/src/solverpy/solver/reloader.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Delegate result update/announcement to the wrapped solver.\"\"\"\n   self.solver.update(instance, strategy, output, result)\n</code></pre>"},{"location":"api/solverpy/solver/reloader/#solverpy.solver.reloader.Reloader.valid","title":"<code>valid(result: Result) -&gt; bool</code>","text":"<p>Delegate result validation to the wrapped solver.</p> Source code in <code>packages/solverpy/src/solverpy/solver/reloader.py</code> <pre><code>def valid(self, result: \"Result\") -&gt; bool:\n   \"\"\"Delegate result validation to the wrapped solver.\"\"\"\n   return self.solver.valid(result)\n</code></pre>"},{"location":"api/solverpy/solver/shellsolver/","title":"module shellsolver","text":""},{"location":"api/solverpy/solver/shellsolver/#solverpy.solver.shellsolver","title":"<code>solverpy.solver.shellsolver</code>","text":""},{"location":"api/solverpy/solver/shellsolver/#solverpy.solver.shellsolver--shell-based-solver-execution","title":"Shell-based solver execution","text":"<p><code>ShellSolver</code> runs the solver as a subprocess via the shell.  The solver binary is invoked as <code>{cmd} {strategy} {instance}</code> after all decorator plugins have had a chance to wrap the command (e.g. prepend <code>timeout</code>). The raw stdout/stderr is returned as a single string and passed to <code>process</code>.</p> <p>Concrete ATP/SMT solvers (E, Vampire, cvc5 \u2026) subclass <code>ShellSolver</code> and only need to supply <code>_binary</code>, <code>process</code>, and their plugin list.</p>"},{"location":"api/solverpy/solver/shellsolver/#solverpy.solver.shellsolver.ShellSolver","title":"<code>ShellSolver</code>","text":"<p>               Bases: <code>SolverPy</code></p> <p>Concrete <code>SolverPy</code> that runs the solver binary as a shell subprocess.</p> <p>Subclasses set the <code>_binary</code> class variable to the default executable name and implement <code>process</code> to parse the solver output.</p> Source code in <code>packages/solverpy/src/solverpy/solver/shellsolver.py</code> <pre><code>class ShellSolver(SolverPy):\n   \"\"\"\n   Concrete `SolverPy` that runs the solver binary as a shell subprocess.\n\n   Subclasses set the `_binary` class variable to the default executable name\n   and implement `process` to parse the solver output.\n   \"\"\"\n\n   _binary: str = \"\"\n   \"\"\"Default solver binary name looked up via `shutil.which`.\"\"\"\n\n   @classmethod\n   def available(cls) -&gt; bool:\n      \"\"\"Return `True` if the default solver binary is found in `PATH`.\"\"\"\n      return shutil.which(cls._binary) is not None\n\n   def isinstalled(self) -&gt; bool:\n      \"\"\"Return `True` if this instance's binary is found in `PATH`.\"\"\"\n      return shutil.which(self._binary) is not None\n\n   def __init__(\n      self,\n      cmd: str,\n      limit: str,\n      builder: \"LimitBuilder\" = {},\n      plugins: list[\"Plugin\"] = [],\n      wait: (int | None) = None,\n      unspace: bool = True,\n      binary: str = \"\",\n   ):\n      \"\"\"\n      Args:\n          cmd: base shell command for the solver (e.g. `/usr/bin/eprover`).\n          limit: resource limit string (e.g. `\"T10\"` for 10-second timeout).\n          builder: mapping used to derive additional limit flags from the limit string.\n          plugins: extra plugins registered before the built-in limit plugins.\n          wait: extra seconds added to the wall-clock `timeout` wrapper on top\n              of the solver's own time limit, giving it time to shut down cleanly.\n          unspace: if `True`, replace spaces in the command with `%20` via\n              `sids.unspace` before passing to the shell.\n          binary: override the default `_binary` for this instance.\n      \"\"\"\n      self._unspace = unspace\n      self._binary = binary\n      limits = Limits(limit, builder)\n      new: list[\"Plugin\"] = [limits]\n      if wait is not None:\n         new.append(Timeout(limits.timeout + wait))\n      if limits.memory:\n         new.append(Memory(limits.memory))\n      SolverPy.__init__(\n         self,\n         limits=limits,\n         plugins=plugins + new,\n      )\n      self._cmd = cmd\n\n   @property\n   def name(self) -&gt; str:\n      \"\"\"Solver name including the resource limit, e.g. `E(limit=T10)`.\"\"\"\n      return f\"{super().name}(limit={self._limits.limit})\"\n\n   def run(self, instance: Any, strategy: Any) -&gt; str:\n      \"\"\"\n      Build the shell command and run it, returning combined stdout/stderr.\n\n      The output is prefixed with header lines recording the instance, strategy,\n      and full command for later parsing by `process`.\n      \"\"\"\n      cmd = self.command(instance, strategy)\n      env0 = dict(os.environ)\n      env0[\"OMP_NUM_THREADS\"] = \"1\"\n      #env0[\"CUDA_VISIBLE_DEVICES\"] = \"-1\"\n      if self._unspace:\n         cmd = sids.unspace(cmd)\n      self._exitcode = 0\n      try:\n         output = subprocess.check_output(\n            cmd,\n            shell=True,\n            stderr=subprocess.STDOUT,\n            env=env0,\n         )\n      except subprocess.CalledProcessError as e:\n         output = e.output\n         self._exitcode = e.returncode\n      return f\"%%% INSTANCE {instance}\\n%%% STRATEGY {strategy}\\n%%% COMMAND: {cmd}\\n\" + output.decode()\n\n   def command(self, instance: Any, strategy: Any) -&gt; str:\n      \"\"\"\n      Build the full shell command string: decorate the base command, translate\n      instance/strategy, then assemble as `{cmd} {strategy} {instance}`.\n      \"\"\"\n      cmd = self.decorate(self._cmd, instance, strategy)\n      (instance, strategy) = self.translate(instance, strategy)\n      return f\"{cmd} {strategy} {instance}\"\n</code></pre>"},{"location":"api/solverpy/solver/shellsolver/#solverpy.solver.shellsolver.ShellSolver.name","title":"<code>name: str</code>  <code>property</code>","text":"<p>Solver name including the resource limit, e.g. <code>E(limit=T10)</code>.</p>"},{"location":"api/solverpy/solver/shellsolver/#solverpy.solver.shellsolver.ShellSolver.__init__","title":"<code>__init__(cmd: str, limit: str, builder: LimitBuilder = {}, plugins: list[Plugin] = [], wait: int | None = None, unspace: bool = True, binary: str = '')</code>","text":"<p>Parameters:</p> Name Type Description Default <code>cmd</code> <code>str</code> <p>base shell command for the solver (e.g. <code>/usr/bin/eprover</code>).</p> required <code>limit</code> <code>str</code> <p>resource limit string (e.g. <code>\"T10\"</code> for 10-second timeout).</p> required <code>builder</code> <code>LimitBuilder</code> <p>mapping used to derive additional limit flags from the limit string.</p> <code>{}</code> <code>plugins</code> <code>list[Plugin]</code> <p>extra plugins registered before the built-in limit plugins.</p> <code>[]</code> <code>wait</code> <code>int | None</code> <p>extra seconds added to the wall-clock <code>timeout</code> wrapper on top of the solver's own time limit, giving it time to shut down cleanly.</p> <code>None</code> <code>unspace</code> <code>bool</code> <p>if <code>True</code>, replace spaces in the command with <code>%20</code> via <code>sids.unspace</code> before passing to the shell.</p> <code>True</code> <code>binary</code> <code>str</code> <p>override the default <code>_binary</code> for this instance.</p> <code>''</code> Source code in <code>packages/solverpy/src/solverpy/solver/shellsolver.py</code> <pre><code>def __init__(\n   self,\n   cmd: str,\n   limit: str,\n   builder: \"LimitBuilder\" = {},\n   plugins: list[\"Plugin\"] = [],\n   wait: (int | None) = None,\n   unspace: bool = True,\n   binary: str = \"\",\n):\n   \"\"\"\n   Args:\n       cmd: base shell command for the solver (e.g. `/usr/bin/eprover`).\n       limit: resource limit string (e.g. `\"T10\"` for 10-second timeout).\n       builder: mapping used to derive additional limit flags from the limit string.\n       plugins: extra plugins registered before the built-in limit plugins.\n       wait: extra seconds added to the wall-clock `timeout` wrapper on top\n           of the solver's own time limit, giving it time to shut down cleanly.\n       unspace: if `True`, replace spaces in the command with `%20` via\n           `sids.unspace` before passing to the shell.\n       binary: override the default `_binary` for this instance.\n   \"\"\"\n   self._unspace = unspace\n   self._binary = binary\n   limits = Limits(limit, builder)\n   new: list[\"Plugin\"] = [limits]\n   if wait is not None:\n      new.append(Timeout(limits.timeout + wait))\n   if limits.memory:\n      new.append(Memory(limits.memory))\n   SolverPy.__init__(\n      self,\n      limits=limits,\n      plugins=plugins + new,\n   )\n   self._cmd = cmd\n</code></pre>"},{"location":"api/solverpy/solver/shellsolver/#solverpy.solver.shellsolver.ShellSolver.available","title":"<code>available() -&gt; bool</code>  <code>classmethod</code>","text":"<p>Return <code>True</code> if the default solver binary is found in <code>PATH</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/shellsolver.py</code> <pre><code>@classmethod\ndef available(cls) -&gt; bool:\n   \"\"\"Return `True` if the default solver binary is found in `PATH`.\"\"\"\n   return shutil.which(cls._binary) is not None\n</code></pre>"},{"location":"api/solverpy/solver/shellsolver/#solverpy.solver.shellsolver.ShellSolver.command","title":"<code>command(instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Build the full shell command string: decorate the base command, translate instance/strategy, then assemble as <code>{cmd} {strategy} {instance}</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/shellsolver.py</code> <pre><code>def command(self, instance: Any, strategy: Any) -&gt; str:\n   \"\"\"\n   Build the full shell command string: decorate the base command, translate\n   instance/strategy, then assemble as `{cmd} {strategy} {instance}`.\n   \"\"\"\n   cmd = self.decorate(self._cmd, instance, strategy)\n   (instance, strategy) = self.translate(instance, strategy)\n   return f\"{cmd} {strategy} {instance}\"\n</code></pre>"},{"location":"api/solverpy/solver/shellsolver/#solverpy.solver.shellsolver.ShellSolver.isinstalled","title":"<code>isinstalled() -&gt; bool</code>","text":"<p>Return <code>True</code> if this instance's binary is found in <code>PATH</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/shellsolver.py</code> <pre><code>def isinstalled(self) -&gt; bool:\n   \"\"\"Return `True` if this instance's binary is found in `PATH`.\"\"\"\n   return shutil.which(self._binary) is not None\n</code></pre>"},{"location":"api/solverpy/solver/shellsolver/#solverpy.solver.shellsolver.ShellSolver.run","title":"<code>run(instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Build the shell command and run it, returning combined stdout/stderr.</p> <p>The output is prefixed with header lines recording the instance, strategy, and full command for later parsing by <code>process</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/shellsolver.py</code> <pre><code>def run(self, instance: Any, strategy: Any) -&gt; str:\n   \"\"\"\n   Build the shell command and run it, returning combined stdout/stderr.\n\n   The output is prefixed with header lines recording the instance, strategy,\n   and full command for later parsing by `process`.\n   \"\"\"\n   cmd = self.command(instance, strategy)\n   env0 = dict(os.environ)\n   env0[\"OMP_NUM_THREADS\"] = \"1\"\n   #env0[\"CUDA_VISIBLE_DEVICES\"] = \"-1\"\n   if self._unspace:\n      cmd = sids.unspace(cmd)\n   self._exitcode = 0\n   try:\n      output = subprocess.check_output(\n         cmd,\n         shell=True,\n         stderr=subprocess.STDOUT,\n         env=env0,\n      )\n   except subprocess.CalledProcessError as e:\n      output = e.output\n      self._exitcode = e.returncode\n   return f\"%%% INSTANCE {instance}\\n%%% STRATEGY {strategy}\\n%%% COMMAND: {cmd}\\n\" + output.decode()\n</code></pre>"},{"location":"api/solverpy/solver/solver/","title":"module solver","text":""},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver","title":"<code>solverpy.solver.solver</code>","text":""},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver--abstract-solver-interface","title":"Abstract solver interface","text":"<p>The class <code>Solver</code> encapsulates the very basic solver functionality --- solving a single problem instance and processing the output. The class defines solver-specific set of statuses, which are typically strings representing TPTP statuses (like <code>Theorem</code>, <code>Unsatisfiable</code>, etc), or SMT statuses (like <code>sat</code>, <code>unsat</code>, etc). The main method <code>solve</code> runs the solver and returns a processed result. The result is the SolverPy <code>Result</code> dictionary containing at least keys <code>status</code> and <code>runtime</code>. Although the result can contain solver-specific keys, the basic result structure is shared between all solvers. This makes the result comparable between solvers. The plugin functionality is added in the superclass class <code>PluginSolver</code>.</p>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver","title":"<code>Solver</code>","text":"<p>               Bases: <code>SolverPyObj</code></p> <p>Base abstract class for all solvers encapsulating the very basic solver functionality. The implementing class must provide the set of statuses, and implement methods <code>run</code> and <code>process</code> which run the solver on a single problem instance and process the solver output.</p> <p> </p> <p>The main method is <code>solve</code> which calls the solver on a problem instance and returns a processed result --- by default it calls <code>run</code> and <code>process</code> in sequence. Result query methods valid and solved are implemented to recognize a valid and successfully solved results.</p> <p>The values of <code>instance</code> and <code>strategy</code> in <code>solve</code> are completely solver-specific at this level. The <code>Result</code> is, however, always a dictionary guaranteed to contain at least the keys <code>status</code> and <code>runtime</code>. The value of <code>status</code> is one of <code>statuses</code> and the value of <code>runtime</code> is the solver runtime in seconds.</p> <p>The statuses are also solver-specific. The set <code>success</code> contains all successful statuses, and similarly <code>timeouts</code> contains timeouting statuses. Both sets <code>timeouts</code> and <code>success</code> are expected to be subsets of <code>statuses</code> which contain all valid statuses.</p> Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>class Solver(SolverPyObj):\n   \"\"\"\n   Base abstract class for all solvers encapsulating the very basic solver\n   functionality. The implementing class must provide the set of statuses, and\n   implement methods [`run`][solverpy.solver.solver.Solver.run] and\n   [`process`][solverpy.solver.solver.Solver.process] which run the solver on a\n   single problem instance and process the solver output.\n\n   ```plantuml name=\"solver-solver\"\n   abstract class solverpy.solver.solver.Solver extends solverpy.solver.object.SolverPyObj {\n      {abstract} # success : frozenset[Status]\n      {abstract} # timeouts : frozenset[Status]\n      {abstract} # statuses : frozenset[Status]\n      {abstract} # run(instance, strategy) : str\n      {abstract} # process(output) : Result\n      + solve(instance, strategy) : Result\n      + valid(result) : bool\n      + solved(result) : bool\n   }\n   abstract class solverpy.solver.pluginsolver.PluginSolver extends solverpy.solver.solver.Solver \n   ```\n\n   The main method is [`solve`][solverpy.solver.solver.Solver.solve] which\n   calls the solver on a problem instance and returns a processed result --- by\n   default it calls `run` and `process` in sequence. Result query methods\n   [valid][solverpy.solver.solver.Solver.valid] and\n   [solved][solverpy.solver.solver.Solver.solved] are implemented to recognize\n   a valid and successfully solved results.\n\n   The values of `instance` and `strategy` in `solve` are completely\n   solver-specific at this level. The [`Result`][solverpy.tools.typing.Result]\n   is, however, always a dictionary guaranteed to contain at least the keys\n   `status` and `runtime`. The value of `status` is one of\n   [`statuses`][solverpy.solver.solver.Solver.statuses] and the value of\n   `runtime` is the solver runtime in seconds.\n\n   The statuses are also solver-specific. The set\n   [`success`][solverpy.solver.solver.Solver.success] contains all successful\n   statuses, and similarly [`timeouts`][solverpy.solver.solver.Solver.timeouts]\n   contains timeouting statuses. Both sets\n   [`timeouts`][solverpy.solver.solver.Solver.timeouts] and\n   [`success`][solverpy.solver.solver.Solver.success] are expected to be\n   subsets of [`statuses`][solverpy.solver.solver.Solver.statuses] which\n   contain all valid statuses.\n   \"\"\"\n\n   def __init__(self, **kwargs: Any):\n      \"\"\"Initialize the solver, passing keyword arguments to `SolverPyObj`.\"\"\"\n      SolverPyObj.__init__(self, **kwargs)\n\n   def __str__(self) -&gt; str:\n      \"\"\"Return the solver name.\"\"\"\n      return self.name\n\n   def solve(self, instance: Any, strategy: Any) -&gt; \"Result\":\n      \"\"\"\n      Run the solver with the strategy on the instatance. Process the output\n      and create the result.\n\n      Args:\n          instance: solver problem instance (filename, or a bid-problem\n              pair, or custom). \n          strategy: solver strategy (filename, sid, or custom).\n\n      Returns: \n          The SolverPy result dictionary.\n      \"\"\"\n      output = self.run(instance, strategy)\n      self._output = output\n      result = self.process(output)\n      return result\n\n   def valid(self, result: \"Result\") -&gt; bool:\n      \"\"\"\n      A valid status contains at least keys `status` and `runtime`. The status\n      must be a valid status.\n\n      Args:\n          result: the result\n      \"\"\"\n      return bool(result) and \\\n         (\"status\" in result) and \\\n         (\"runtime\" in result) and \\\n         (result[\"status\"] in self.statuses)\n\n   def solved(self, result: \"Result\") -&gt; bool:\n      \"\"\"\n      The result is solved if the status is in the success set.\n\n      Args:\n          result: the result\n      \"\"\"\n      return bool(result) and \\\n         (\"status\" in result) and \\\n         (result[\"status\"] in self.success)\n\n   def run(self, instance: Any, strategy: Any) -&gt; str:\n      \"\"\"\n      Run the solver with the strategy on the instatnce.\n\n      Args:\n          instance: solver problem instance\n          strategy: solver strategy\n\n      Returns: \n          raw solver output\n      \"\"\"\n      del instance, strategy  # unused arguments\n      raise NotImplementedError()\n\n   def process(self, output: Any) -&gt; \"Result\":\n      \"\"\"\n      Process the solver output and create the result.\n\n      Args:\n          output: raw solver output\n\n      Returns: \n          processed result dictionary\n      \"\"\"\n      del output  # unused argument\n      raise NotImplementedError()\n\n   @property\n   def name(self) -&gt; str:\n      \"\"\"\n      Solver name.  The default name is the class name.\n      \"\"\"\n      return self.__class__.__name__\n\n   @property\n   def success(self) -&gt; frozenset[str]:\n      \"\"\"\n      The set of successful statuses.\n      \"\"\"\n      raise NotImplementedError()\n\n   @property\n   def timeouts(self) -&gt; frozenset[str]:\n      \"\"\"\n      The set of timeout statuses.\n      \"\"\"\n      raise NotImplementedError()\n\n   @property\n   def statuses(self) -&gt; frozenset[str]:\n      \"\"\"\n      The set of all valid statuses.\n      \"\"\"\n      raise NotImplementedError()\n\n   @classmethod\n   def available(cls) -&gt; bool:\n      \"\"\"\n      Check whether the default solver binary is available in PATH.\n      Can be called on the class: E.available()\n      \"\"\"\n      raise NotImplementedError()\n\n   def isinstalled(self) -&gt; bool:\n      \"\"\"\n      Check whether this instance's solver binary is available in PATH.\n      Respects a custom binary passed at construction time.\n      \"\"\"\n      raise NotImplementedError()\n</code></pre>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.name","title":"<code>name: str</code>  <code>property</code>","text":"<p>Solver name.  The default name is the class name.</p>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.statuses","title":"<code>statuses: frozenset[str]</code>  <code>property</code>","text":"<p>The set of all valid statuses.</p>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.success","title":"<code>success: frozenset[str]</code>  <code>property</code>","text":"<p>The set of successful statuses.</p>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.timeouts","title":"<code>timeouts: frozenset[str]</code>  <code>property</code>","text":"<p>The set of timeout statuses.</p>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.__init__","title":"<code>__init__(**kwargs: Any)</code>","text":"<p>Initialize the solver, passing keyword arguments to <code>SolverPyObj</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>def __init__(self, **kwargs: Any):\n   \"\"\"Initialize the solver, passing keyword arguments to `SolverPyObj`.\"\"\"\n   SolverPyObj.__init__(self, **kwargs)\n</code></pre>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.__str__","title":"<code>__str__() -&gt; str</code>","text":"<p>Return the solver name.</p> Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>def __str__(self) -&gt; str:\n   \"\"\"Return the solver name.\"\"\"\n   return self.name\n</code></pre>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.available","title":"<code>available() -&gt; bool</code>  <code>classmethod</code>","text":"<p>Check whether the default solver binary is available in PATH. Can be called on the class: E.available()</p> Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>@classmethod\ndef available(cls) -&gt; bool:\n   \"\"\"\n   Check whether the default solver binary is available in PATH.\n   Can be called on the class: E.available()\n   \"\"\"\n   raise NotImplementedError()\n</code></pre>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.isinstalled","title":"<code>isinstalled() -&gt; bool</code>","text":"<p>Check whether this instance's solver binary is available in PATH. Respects a custom binary passed at construction time.</p> Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>def isinstalled(self) -&gt; bool:\n   \"\"\"\n   Check whether this instance's solver binary is available in PATH.\n   Respects a custom binary passed at construction time.\n   \"\"\"\n   raise NotImplementedError()\n</code></pre>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.process","title":"<code>process(output: Any) -&gt; Result</code>","text":"<p>Process the solver output and create the result.</p> <p>Parameters:</p> Name Type Description Default <code>output</code> <code>Any</code> <p>raw solver output</p> required <p>Returns:</p> Type Description <code>Result</code> <p>processed result dictionary</p> Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>def process(self, output: Any) -&gt; \"Result\":\n   \"\"\"\n   Process the solver output and create the result.\n\n   Args:\n       output: raw solver output\n\n   Returns: \n       processed result dictionary\n   \"\"\"\n   del output  # unused argument\n   raise NotImplementedError()\n</code></pre>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.run","title":"<code>run(instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Run the solver with the strategy on the instatnce.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Any</code> <p>solver problem instance</p> required <code>strategy</code> <code>Any</code> <p>solver strategy</p> required <p>Returns:</p> Type Description <code>str</code> <p>raw solver output</p> Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>def run(self, instance: Any, strategy: Any) -&gt; str:\n   \"\"\"\n   Run the solver with the strategy on the instatnce.\n\n   Args:\n       instance: solver problem instance\n       strategy: solver strategy\n\n   Returns: \n       raw solver output\n   \"\"\"\n   del instance, strategy  # unused arguments\n   raise NotImplementedError()\n</code></pre>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.solve","title":"<code>solve(instance: Any, strategy: Any) -&gt; Result</code>","text":"<p>Run the solver with the strategy on the instatance. Process the output and create the result.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Any</code> <p>solver problem instance (filename, or a bid-problem pair, or custom). </p> required <code>strategy</code> <code>Any</code> <p>solver strategy (filename, sid, or custom).</p> required <p>Returns:</p> Type Description <code>Result</code> <p>The SolverPy result dictionary.</p> Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>def solve(self, instance: Any, strategy: Any) -&gt; \"Result\":\n   \"\"\"\n   Run the solver with the strategy on the instatance. Process the output\n   and create the result.\n\n   Args:\n       instance: solver problem instance (filename, or a bid-problem\n           pair, or custom). \n       strategy: solver strategy (filename, sid, or custom).\n\n   Returns: \n       The SolverPy result dictionary.\n   \"\"\"\n   output = self.run(instance, strategy)\n   self._output = output\n   result = self.process(output)\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.solved","title":"<code>solved(result: Result) -&gt; bool</code>","text":"<p>The result is solved if the status is in the success set.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>Result</code> <p>the result</p> required Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>def solved(self, result: \"Result\") -&gt; bool:\n   \"\"\"\n   The result is solved if the status is in the success set.\n\n   Args:\n       result: the result\n   \"\"\"\n   return bool(result) and \\\n      (\"status\" in result) and \\\n      (result[\"status\"] in self.success)\n</code></pre>"},{"location":"api/solverpy/solver/solver/#solverpy.solver.solver.Solver.valid","title":"<code>valid(result: Result) -&gt; bool</code>","text":"<p>A valid status contains at least keys <code>status</code> and <code>runtime</code>. The status must be a valid status.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>Result</code> <p>the result</p> required Source code in <code>packages/solverpy/src/solverpy/solver/solver.py</code> <pre><code>def valid(self, result: \"Result\") -&gt; bool:\n   \"\"\"\n   A valid status contains at least keys `status` and `runtime`. The status\n   must be a valid status.\n\n   Args:\n       result: the result\n   \"\"\"\n   return bool(result) and \\\n      (\"status\" in result) and \\\n      (\"runtime\" in result) and \\\n      (result[\"status\"] in self.statuses)\n</code></pre>"},{"location":"api/solverpy/solver/solverpy/","title":"module solverpy","text":""},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy","title":"<code>solverpy.solver.solverpy</code>","text":""},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy--resource-limits-and-result-caching","title":"Resource limits and result caching","text":"<p><code>SolverPy</code> extends <code>PluginSolver</code> with two concerns:</p> <ul> <li>Resource limits \u2014 a <code>Limits</code>   object (time/memory) is created at construction and a   <code>Limiter</code> decorator is   automatically registered to translate exit codes into timeout statuses.</li> <li>Result simulation \u2014 <code>simulate</code>   checks whether a cached result from a previous run is still applicable under   the current resource limits, avoiding redundant solver invocations.</li> </ul>"},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy.SolverPy","title":"<code>SolverPy</code>","text":"<p>               Bases: <code>PluginSolver</code></p> <p>Extends <code>PluginSolver</code> with resource limits and cached-result simulation.</p> <p>Concrete solver subclasses (e.g. <code>ShellSolver</code>, <code>StdinSolver</code>) build on this class and supply the actual solver binary invocation.</p> Source code in <code>packages/solverpy/src/solverpy/solver/solverpy.py</code> <pre><code>class SolverPy(PluginSolver):\n   \"\"\"\n   Extends `PluginSolver` with resource limits and cached-result simulation.\n\n   Concrete solver subclasses (e.g. `ShellSolver`, `StdinSolver`) build on\n   this class and supply the actual solver binary invocation.\n   \"\"\"\n\n   def __init__(\n      self,\n      limits: Limits,\n      plugins: list[\"Plugin\"] = [],\n      **kwargs: Any,\n   ):\n      \"\"\"\n      Args:\n          limits: resource limits (time/memory) for solver runs.\n          plugins: additional plugins to register (a `Limiter` is always appended).\n          **kwargs: forwarded to `PluginSolver.__init__`.\n      \"\"\"\n      assert limits.limit.startswith(\"T\")\n      self._limits: Limits = limits\n      self._exitcode: int = -1\n      self._timeouts = frozenset()\n      self._success = frozenset()\n      self._statuses = frozenset()\n      plugins = plugins + [\n         Limiter(),\n      ]\n      PluginSolver.__init__(self, plugins=plugins, **kwargs)\n\n   def __hash__(self) -&gt; int:\n      \"\"\"Hash based on the solver's string representation.\"\"\"\n      return hash(str(self))\n\n   def __eq__(self, other: object) -&gt; bool:\n      \"\"\"Two solvers are equal when their string representations match.\"\"\"\n      if not isinstance(other, SolverPy):\n         return False\n      return str(self) == str(other)\n\n   def simulate(self, result: \"Result\") -&gt; \"Result | None\":\n      \"Simulate run from the past result.\"\n      if result[\"status\"] in self.timeouts:\n         #if result[\"status\"] not in self.success: # we might want this?\n         oldlimits = Limits(result[\"limit\"], {})\n         # the cached result is timeout\n         if oldlimits &lt; self._limits:\n            #if result[\"runtime\"] &lt; self.timeout:\n            # recompute since we have more time or/and space\n            return None\n      elif result[\"status\"] in self.success:\n         # the cached result is solved\n         if result[\"runtime\"] &gt; self._limits.timeout:\n            #if result[\"runtime\"] &gt; self.timeout:\n            # simulated timeout\n            return dict(\n               result,\n               status=\"TIMEOUT\",\n               runtime=self._limits.timeout,\n            )\n      elif result[\"status\"] in self.statuses:\n         # the cached result is unknow/GaveUp\n         oldlimits = Limits(result[\"limit\"], {})\n         if oldlimits &gt; self._limits:\n            # recompute if we have less resources (maybe timeout?)\n            # TODO: is this correct?\n            return None\n      else:\n         # recompute unknown results (GaveUp, unknown)\n         # TODO: do we want to always recompute?\n         return None\n      # the result is applicable without changes\n      return result\n\n   @property\n   def timeouts(self) -&gt; frozenset[str]:\n      \"\"\"The set of timeout statuses, populated by the solver's status plugin.\"\"\"\n      return self._timeouts\n\n   @property\n   def success(self) -&gt; frozenset[str]:\n      \"\"\"The set of successful statuses, populated by the solver's status plugin.\"\"\"\n      return self._success\n\n   @property\n   def statuses(self) -&gt; frozenset[str]:\n      \"\"\"The set of all valid statuses, populated by the solver's status plugin.\"\"\"\n      return self._statuses\n</code></pre>"},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy.SolverPy.statuses","title":"<code>statuses: frozenset[str]</code>  <code>property</code>","text":"<p>The set of all valid statuses, populated by the solver's status plugin.</p>"},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy.SolverPy.success","title":"<code>success: frozenset[str]</code>  <code>property</code>","text":"<p>The set of successful statuses, populated by the solver's status plugin.</p>"},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy.SolverPy.timeouts","title":"<code>timeouts: frozenset[str]</code>  <code>property</code>","text":"<p>The set of timeout statuses, populated by the solver's status plugin.</p>"},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy.SolverPy.__eq__","title":"<code>__eq__(other: object) -&gt; bool</code>","text":"<p>Two solvers are equal when their string representations match.</p> Source code in <code>packages/solverpy/src/solverpy/solver/solverpy.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n   \"\"\"Two solvers are equal when their string representations match.\"\"\"\n   if not isinstance(other, SolverPy):\n      return False\n   return str(self) == str(other)\n</code></pre>"},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy.SolverPy.__hash__","title":"<code>__hash__() -&gt; int</code>","text":"<p>Hash based on the solver's string representation.</p> Source code in <code>packages/solverpy/src/solverpy/solver/solverpy.py</code> <pre><code>def __hash__(self) -&gt; int:\n   \"\"\"Hash based on the solver's string representation.\"\"\"\n   return hash(str(self))\n</code></pre>"},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy.SolverPy.__init__","title":"<code>__init__(limits: Limits, plugins: list[Plugin] = [], **kwargs: Any)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>limits</code> <code>Limits</code> <p>resource limits (time/memory) for solver runs.</p> required <code>plugins</code> <code>list[Plugin]</code> <p>additional plugins to register (a <code>Limiter</code> is always appended).</p> <code>[]</code> <code>**kwargs</code> <code>Any</code> <p>forwarded to <code>PluginSolver.__init__</code>.</p> <code>{}</code> Source code in <code>packages/solverpy/src/solverpy/solver/solverpy.py</code> <pre><code>def __init__(\n   self,\n   limits: Limits,\n   plugins: list[\"Plugin\"] = [],\n   **kwargs: Any,\n):\n   \"\"\"\n   Args:\n       limits: resource limits (time/memory) for solver runs.\n       plugins: additional plugins to register (a `Limiter` is always appended).\n       **kwargs: forwarded to `PluginSolver.__init__`.\n   \"\"\"\n   assert limits.limit.startswith(\"T\")\n   self._limits: Limits = limits\n   self._exitcode: int = -1\n   self._timeouts = frozenset()\n   self._success = frozenset()\n   self._statuses = frozenset()\n   plugins = plugins + [\n      Limiter(),\n   ]\n   PluginSolver.__init__(self, plugins=plugins, **kwargs)\n</code></pre>"},{"location":"api/solverpy/solver/solverpy/#solverpy.solver.solverpy.SolverPy.simulate","title":"<code>simulate(result: Result) -&gt; Result | None</code>","text":"<p>Simulate run from the past result.</p> Source code in <code>packages/solverpy/src/solverpy/solver/solverpy.py</code> <pre><code>def simulate(self, result: \"Result\") -&gt; \"Result | None\":\n   \"Simulate run from the past result.\"\n   if result[\"status\"] in self.timeouts:\n      #if result[\"status\"] not in self.success: # we might want this?\n      oldlimits = Limits(result[\"limit\"], {})\n      # the cached result is timeout\n      if oldlimits &lt; self._limits:\n         #if result[\"runtime\"] &lt; self.timeout:\n         # recompute since we have more time or/and space\n         return None\n   elif result[\"status\"] in self.success:\n      # the cached result is solved\n      if result[\"runtime\"] &gt; self._limits.timeout:\n         #if result[\"runtime\"] &gt; self.timeout:\n         # simulated timeout\n         return dict(\n            result,\n            status=\"TIMEOUT\",\n            runtime=self._limits.timeout,\n         )\n   elif result[\"status\"] in self.statuses:\n      # the cached result is unknow/GaveUp\n      oldlimits = Limits(result[\"limit\"], {})\n      if oldlimits &gt; self._limits:\n         # recompute if we have less resources (maybe timeout?)\n         # TODO: is this correct?\n         return None\n   else:\n      # recompute unknown results (GaveUp, unknown)\n      # TODO: do we want to always recompute?\n      return None\n   # the result is applicable without changes\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/stdinsolver/","title":"module stdinsolver","text":""},{"location":"api/solverpy/solver/stdinsolver/#solverpy.solver.stdinsolver","title":"<code>solverpy.solver.stdinsolver</code>","text":""},{"location":"api/solverpy/solver/stdinsolver/#solverpy.solver.stdinsolver--stdin-based-solver-execution","title":"Stdin-based solver execution","text":"<p><code>StdinSolver</code> runs the solver binary as a subprocess and feeds the problem to it via stdin rather than as a command-line filename argument.  This is used for solvers like cvc5 that accept SMT-LIB input on stdin.</p> <p>The input stream is assembled as: <code>static_prefix + strategy_options + newline + problem_file_contents</code>.  Resource limits are applied identically to <code>ShellSolver</code> via the <code>Limits</code> / <code>Timeout</code> / <code>Memory</code> plugin chain.</p>"},{"location":"api/solverpy/solver/stdinsolver/#solverpy.solver.stdinsolver.StdinSolver","title":"<code>StdinSolver</code>","text":"<p>               Bases: <code>SolverPy</code></p> <p>Concrete <code>SolverPy</code> that pipes problem input to the solver via stdin.</p> <p>Used for solvers that read SMT-LIB (or similar) input from stdin rather than accepting a filename on the command line.</p> Source code in <code>packages/solverpy/src/solverpy/solver/stdinsolver.py</code> <pre><code>class StdinSolver(SolverPy):\n   \"\"\"\n   Concrete `SolverPy` that pipes problem input to the solver via stdin.\n\n   Used for solvers that read SMT-LIB (or similar) input from stdin rather\n   than accepting a filename on the command line.\n   \"\"\"\n\n   _binary: str = \"\"\n   \"\"\"Default solver binary name looked up via `shutil.which`.\"\"\"\n\n   @classmethod\n   def available(cls) -&gt; bool:\n      \"\"\"Return `True` if the default solver binary is found in `PATH`.\"\"\"\n      return shutil.which(cls._binary) is not None\n\n   def isinstalled(self) -&gt; bool:\n      \"\"\"Return `True` if this instance's binary is found in `PATH`.\"\"\"\n      return shutil.which(self._binary) is not None\n\n   def __init__(\n      self,\n      cmd: str,\n      limit: str,\n      builder: \"LimitBuilder\" = {},\n      plugins: list[\"Plugin\"] = [],\n      wait: (int | None) = None,\n      static: str = \"\", # TODO: rename to `prefix`\n      cmdline: bool = False, # set limits using command line arguments?\n      inputfile: bool = False,\n      binary: str = \"\",\n   ):\n      \"\"\"\n      Args:\n          cmd: base shell command for the solver (e.g. `cvc5`).\n          limit: resource limit string (e.g. `\"T10\"`).\n          builder: mapping used to derive additional limit flags.\n          plugins: extra plugins registered before the built-in limit plugins.\n          wait: extra wall-clock seconds added on top of the solver time limit.\n          static: fixed prefix prepended to every stdin payload (e.g. solver options).\n          cmdline: if `True`, pass limits as command-line arguments instead of\n              relying on the `timeout` wrapper.\n          inputfile: if `True`, write stdin to a temp file and pass the path\n              instead (for solvers that require a file argument even in stdin mode).\n          binary: override the default `_binary` for this instance.\n      \"\"\"\n      self._binary = binary\n      limits = Limits(limit, builder, cmdline=cmdline, inputfile=inputfile)\n      new: list[\"Plugin\"] = [limits]\n      if wait is not None:\n         new.append(Timeout(limits.timeout + wait))\n      if limits.memory:\n         new.append(Memory(limits.memory))\n      SolverPy.__init__(\n         self,\n         limits=limits,\n         plugins=plugins + new,\n      )\n      self._static = static # TODO: rename to `prefix`\n      self._cmd = cmd\n\n   @property\n   def name(self) -&gt; str:\n      \"\"\"Solver name including the resource limit, e.g. `Cvc5(limit=T10)`.\"\"\"\n      return f\"{super().name}(limit={self._limits.limit})\"\n\n   def run(self, instance: Any, strategy: Any) -&gt; str:\n      \"\"\"\n      Build the stdin payload, run the solver, and return combined stdout/stderr.\n\n      The output is prefixed with header lines recording the instance, strategy,\n      and full command for later parsing by `process`.\n      \"\"\"\n      inputstr = self.input(instance, strategy)\n      env0 = dict(os.environ)\n      env0[\"OMP_NUM_THREADS\"] = \"1\"\n      #env0[\"CUDA_VISIBLE_DEVICES\"] = \"-1\"\n      cmd = self.decorate(self._cmd, instance, strategy)\n      try:\n         output = subprocess.check_output(\n            cmd,\n            input=inputstr,\n            shell=True,\n            stderr=subprocess.STDOUT,\n            env=env0,\n         )\n         self._exitcode = 0\n      except subprocess.CalledProcessError as e:\n         output = e.output\n         self._exitcode = e.returncode\n      return f\"%%% INSTANCE {instance}\\n%%% STRATEGY {strategy}\\n%%% COMMAND: {cmd}\\n\" + output.decode()\n\n   def input(self, instance: Any, strategy: Any) -&gt; bytes:\n      \"\"\"\n      Assemble the stdin bytes: `static + strategy + newline + problem_file_contents`.\n      Translators are applied to `(instance, strategy)` before assembly.\n      \"\"\"\n      (instance, strategy) = self.translate(instance, strategy)\n      inputstr = self._static.encode()\n      inputstr += strategy.encode()\n      inputstr += b\"\\n\"\n      inputstr += open(instance, \"rb\").read()\n      #open(f\"/home/yan/tmp.smt2\", \"wb\").write(inputstr)\n      return inputstr\n</code></pre>"},{"location":"api/solverpy/solver/stdinsolver/#solverpy.solver.stdinsolver.StdinSolver.name","title":"<code>name: str</code>  <code>property</code>","text":"<p>Solver name including the resource limit, e.g. <code>Cvc5(limit=T10)</code>.</p>"},{"location":"api/solverpy/solver/stdinsolver/#solverpy.solver.stdinsolver.StdinSolver.__init__","title":"<code>__init__(cmd: str, limit: str, builder: LimitBuilder = {}, plugins: list[Plugin] = [], wait: int | None = None, static: str = '', cmdline: bool = False, inputfile: bool = False, binary: str = '')</code>","text":"<p>Parameters:</p> Name Type Description Default <code>cmd</code> <code>str</code> <p>base shell command for the solver (e.g. <code>cvc5</code>).</p> required <code>limit</code> <code>str</code> <p>resource limit string (e.g. <code>\"T10\"</code>).</p> required <code>builder</code> <code>LimitBuilder</code> <p>mapping used to derive additional limit flags.</p> <code>{}</code> <code>plugins</code> <code>list[Plugin]</code> <p>extra plugins registered before the built-in limit plugins.</p> <code>[]</code> <code>wait</code> <code>int | None</code> <p>extra wall-clock seconds added on top of the solver time limit.</p> <code>None</code> <code>static</code> <code>str</code> <p>fixed prefix prepended to every stdin payload (e.g. solver options).</p> <code>''</code> <code>cmdline</code> <code>bool</code> <p>if <code>True</code>, pass limits as command-line arguments instead of relying on the <code>timeout</code> wrapper.</p> <code>False</code> <code>inputfile</code> <code>bool</code> <p>if <code>True</code>, write stdin to a temp file and pass the path instead (for solvers that require a file argument even in stdin mode).</p> <code>False</code> <code>binary</code> <code>str</code> <p>override the default <code>_binary</code> for this instance.</p> <code>''</code> Source code in <code>packages/solverpy/src/solverpy/solver/stdinsolver.py</code> <pre><code>def __init__(\n   self,\n   cmd: str,\n   limit: str,\n   builder: \"LimitBuilder\" = {},\n   plugins: list[\"Plugin\"] = [],\n   wait: (int | None) = None,\n   static: str = \"\", # TODO: rename to `prefix`\n   cmdline: bool = False, # set limits using command line arguments?\n   inputfile: bool = False,\n   binary: str = \"\",\n):\n   \"\"\"\n   Args:\n       cmd: base shell command for the solver (e.g. `cvc5`).\n       limit: resource limit string (e.g. `\"T10\"`).\n       builder: mapping used to derive additional limit flags.\n       plugins: extra plugins registered before the built-in limit plugins.\n       wait: extra wall-clock seconds added on top of the solver time limit.\n       static: fixed prefix prepended to every stdin payload (e.g. solver options).\n       cmdline: if `True`, pass limits as command-line arguments instead of\n           relying on the `timeout` wrapper.\n       inputfile: if `True`, write stdin to a temp file and pass the path\n           instead (for solvers that require a file argument even in stdin mode).\n       binary: override the default `_binary` for this instance.\n   \"\"\"\n   self._binary = binary\n   limits = Limits(limit, builder, cmdline=cmdline, inputfile=inputfile)\n   new: list[\"Plugin\"] = [limits]\n   if wait is not None:\n      new.append(Timeout(limits.timeout + wait))\n   if limits.memory:\n      new.append(Memory(limits.memory))\n   SolverPy.__init__(\n      self,\n      limits=limits,\n      plugins=plugins + new,\n   )\n   self._static = static # TODO: rename to `prefix`\n   self._cmd = cmd\n</code></pre>"},{"location":"api/solverpy/solver/stdinsolver/#solverpy.solver.stdinsolver.StdinSolver.available","title":"<code>available() -&gt; bool</code>  <code>classmethod</code>","text":"<p>Return <code>True</code> if the default solver binary is found in <code>PATH</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/stdinsolver.py</code> <pre><code>@classmethod\ndef available(cls) -&gt; bool:\n   \"\"\"Return `True` if the default solver binary is found in `PATH`.\"\"\"\n   return shutil.which(cls._binary) is not None\n</code></pre>"},{"location":"api/solverpy/solver/stdinsolver/#solverpy.solver.stdinsolver.StdinSolver.input","title":"<code>input(instance: Any, strategy: Any) -&gt; bytes</code>","text":"<p>Assemble the stdin bytes: <code>static + strategy + newline + problem_file_contents</code>. Translators are applied to <code>(instance, strategy)</code> before assembly.</p> Source code in <code>packages/solverpy/src/solverpy/solver/stdinsolver.py</code> <pre><code>def input(self, instance: Any, strategy: Any) -&gt; bytes:\n   \"\"\"\n   Assemble the stdin bytes: `static + strategy + newline + problem_file_contents`.\n   Translators are applied to `(instance, strategy)` before assembly.\n   \"\"\"\n   (instance, strategy) = self.translate(instance, strategy)\n   inputstr = self._static.encode()\n   inputstr += strategy.encode()\n   inputstr += b\"\\n\"\n   inputstr += open(instance, \"rb\").read()\n   #open(f\"/home/yan/tmp.smt2\", \"wb\").write(inputstr)\n   return inputstr\n</code></pre>"},{"location":"api/solverpy/solver/stdinsolver/#solverpy.solver.stdinsolver.StdinSolver.isinstalled","title":"<code>isinstalled() -&gt; bool</code>","text":"<p>Return <code>True</code> if this instance's binary is found in <code>PATH</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/stdinsolver.py</code> <pre><code>def isinstalled(self) -&gt; bool:\n   \"\"\"Return `True` if this instance's binary is found in `PATH`.\"\"\"\n   return shutil.which(self._binary) is not None\n</code></pre>"},{"location":"api/solverpy/solver/stdinsolver/#solverpy.solver.stdinsolver.StdinSolver.run","title":"<code>run(instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Build the stdin payload, run the solver, and return combined stdout/stderr.</p> <p>The output is prefixed with header lines recording the instance, strategy, and full command for later parsing by <code>process</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/stdinsolver.py</code> <pre><code>def run(self, instance: Any, strategy: Any) -&gt; str:\n   \"\"\"\n   Build the stdin payload, run the solver, and return combined stdout/stderr.\n\n   The output is prefixed with header lines recording the instance, strategy,\n   and full command for later parsing by `process`.\n   \"\"\"\n   inputstr = self.input(instance, strategy)\n   env0 = dict(os.environ)\n   env0[\"OMP_NUM_THREADS\"] = \"1\"\n   #env0[\"CUDA_VISIBLE_DEVICES\"] = \"-1\"\n   cmd = self.decorate(self._cmd, instance, strategy)\n   try:\n      output = subprocess.check_output(\n         cmd,\n         input=inputstr,\n         shell=True,\n         stderr=subprocess.STDOUT,\n         env=env0,\n      )\n      self._exitcode = 0\n   except subprocess.CalledProcessError as e:\n      output = e.output\n      self._exitcode = e.returncode\n   return f\"%%% INSTANCE {instance}\\n%%% STRATEGY {strategy}\\n%%% COMMAND: {cmd}\\n\" + output.decode()\n</code></pre>"},{"location":"api/solverpy/solver/atp/","title":"Atp Overview","text":""},{"location":"api/solverpy/solver/atp/#solverpy.solver.atp","title":"<code>solverpy.solver.atp</code>","text":""},{"location":"api/solverpy/solver/atp/#solverpy.solver.atp--atp-solvers","title":"ATP solvers","text":"<p>Automated theorem provers (ATPs) for classical first-order and higher-order logic.  All ATP solvers accept problems in TPTP format (FOF, HOF/THF), except <code>Prover9</code> which uses the LADR format.</p> <p> </p> Solver Binary Format Notes <code>E</code> <code>eprover</code> TPTP FOF Pass <code>binary=\"eprover-ho\"</code> for higher-order (THF) <code>Vampire</code> <code>vampire</code> TPTP FOF/THF Supports both first and higher order <code>Lash</code> <code>lash</code> TPTP THF Higher-order only; requires a modes directory via <code>-M</code> <code>Prover9</code> <code>prover9</code> LADR Reads problem from stdin; stdin-based solver"},{"location":"api/solverpy/solver/atp/#solverpy.solver.atp.Cvc5","title":"<code>Cvc5</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>Deprecated: runs <code>cvc5</code> in its legacy <code>--lang=tptp</code> mode against TPTP problems, reusing the SMT <code>Cvc5</code>'s statistics parsing. Prefer the SMT-LIB2 <code>Cvc5</code> for new work.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/cvc5.py</code> <pre><code>class Cvc5(ShellSolver):\n   \"\"\"\n   Deprecated: runs `cvc5` in its legacy `--lang=tptp` mode against TPTP\n   problems, reusing the SMT\n   [`Cvc5`][solverpy.solver.smt.cvc5.Cvc5]'s statistics parsing. Prefer\n   the SMT-LIB2 [`Cvc5`][solverpy.solver.smt.cvc5.Cvc5] for new work.\n   \"\"\"\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = CVC5_BINARY,\n      static: str = CVC5_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      keys: list[str] = CVC5_KEYS,\n      complete: bool = True,\n   ):\n      cmd = f\"{binary} --lang=tptp {static}\"\n      plugins = plugins + [\n         Time(),\n         Tptp(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         CVC5_BUILDER,\n         plugins,\n         wait=1,\n      )\n      self.pattern: Pattern = re.compile(\n         r\"^(%s) = (.*)$\" % \"|\".join(keys),\n         flags=re.MULTILINE,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      result = SmtCvc5.process(self, output)  # type: ignore\n      if (\"status\" in result) and (result[\"status\"] == \"timeout\"):\n         result[\"status\"] = \"Timeout\"\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/#solverpy.solver.atp.E","title":"<code>E</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>eprover</code> automated theorem prover for first-order logic with equality.</p> <p>The strategy (sid) supplies E's command-line options, appended after the static options (<code>--print-statistics --proof-statistics --tstp-format</code>, ...). TSTP-format statistics output is parsed by <code>process</code>. Status is decided by the <code>Tptp</code> plugin from the TSTP <code>SZS status</code> line.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/eprover.py</code> <pre><code>class E(ShellSolver):\n   \"\"\"\n   The `eprover` automated theorem prover for first-order logic with\n   equality.\n\n   The strategy (sid) supplies E's command-line options, appended after the\n   static options (`--print-statistics --proof-statistics --tstp-format`,\n   ...). TSTP-format statistics output is parsed by\n   [`process`][solverpy.solver.atp.eprover.E.process]. Status is decided by\n   the [`Tptp`][solverpy.solver.plugins.status.tptp.Tptp] plugin from the\n   TSTP `SZS status` line.\n   \"\"\"\n\n   _binary = E_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = E_BINARY,\n      static: str = E_STATIC,\n      complete: bool = True,\n      plugins: list[\"Plugin\"] = [],\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Tptp(complete=complete),\n         RunHash(E_RUNHASH_GEN),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         E_BUILDER,\n         plugins,\n         15,\n         complete,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse E's TSTP statistics block into a result dict.\"\"\"\n      result = patterns.keyval(E_PAT, output, E_TABLE)\n      result = patterns.mapval(result, human.numeric)\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/#solverpy.solver.atp.E.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse E's TSTP statistics block into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/eprover.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse E's TSTP statistics block into a result dict.\"\"\"\n   result = patterns.keyval(E_PAT, output, E_TABLE)\n   result = patterns.mapval(result, human.numeric)\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/#solverpy.solver.atp.Lash","title":"<code>Lash</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>lash</code> higher-order automated theorem prover.</p> <p>The strategy (sid) supplies Lash's command-line options, appended after the static option <code>-p tstp</code> (TSTP output). Output is parsed for <code>Steps</code> and <code>Mode</code> by <code>process</code>. Status is decided by the <code>Tptp</code> plugin from the TSTP <code>SZS status</code> line.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/lash.py</code> <pre><code>class Lash(ShellSolver):\n   \"\"\"\n   The `lash` higher-order automated theorem prover.\n\n   The strategy (sid) supplies Lash's command-line options, appended after\n   the static option `-p tstp` (TSTP output). Output is parsed for `Steps`\n   and `Mode` by\n   [`process`][solverpy.solver.atp.lash.Lash.process]. Status is decided by\n   the [`Tptp`][solverpy.solver.plugins.status.tptp.Tptp] plugin from the\n   TSTP `SZS status` line.\n   \"\"\"\n\n   _binary = L_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = L_BINARY,\n      static: str = L_STATIC,\n      complete: bool = True,\n      plugins: list[\"Plugin\"] = [],\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Tptp(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         L_BUILDER,\n         plugins,\n         0,\n         complete,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse Lash's `Steps`/`Mode` output lines into a result dict.\"\"\"\n      result = patterns.keyval(L_PAT, output, L_TABLE)\n      result = patterns.mapval(result, human.numeric)\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/#solverpy.solver.atp.Lash.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse Lash's <code>Steps</code>/<code>Mode</code> output lines into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/lash.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse Lash's `Steps`/`Mode` output lines into a result dict.\"\"\"\n   result = patterns.keyval(L_PAT, output, L_TABLE)\n   result = patterns.mapval(result, human.numeric)\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/#solverpy.solver.atp.Vampire","title":"<code>Vampire</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>vampire</code> automated theorem prover for first-order logic.</p> <p>The strategy (sid) supplies Vampire's command-line options, appended after the static options. Output includes a TPTP proof and a <code>--stat full</code> statistics block, parsed by <code>process</code>. Status is decided by the <code>Tptp</code> plugin from the TSTP <code>SZS status</code> line.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/vampire.py</code> <pre><code>class Vampire(ShellSolver):\n   \"\"\"\n   The `vampire` automated theorem prover for first-order logic.\n\n   The strategy (sid) supplies Vampire's command-line options, appended\n   after the static options. Output includes a TPTP proof and a `--stat\n   full` statistics block, parsed by\n   [`process`][solverpy.solver.atp.vampire.Vampire.process]. Status is\n   decided by the [`Tptp`][solverpy.solver.plugins.status.tptp.Tptp] plugin\n   from the TSTP `SZS status` line.\n   \"\"\"\n\n   _binary = V_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = V_BINARY,\n      static: str = V_STATIC,\n      complete: bool = True,\n      plugins: list[\"Plugin\"] = [],\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Tptp(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         V_BUILDER,\n         plugins,\n         1,\n         complete,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse Vampire's `--stat full` statistics block into a result dict.\"\"\"\n      result = patterns.keyval(V_PAT, output, V_TABLE)\n      result = patterns.mapval(result, human.numeric)\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/#solverpy.solver.atp.Vampire.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse Vampire's <code>--stat full</code> statistics block into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/vampire.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse Vampire's `--stat full` statistics block into a result dict.\"\"\"\n   result = patterns.keyval(V_PAT, output, V_TABLE)\n   result = patterns.mapval(result, human.numeric)\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/cvc5/","title":"module cvc5","text":""},{"location":"api/solverpy/solver/atp/cvc5/#solverpy.solver.atp.cvc5","title":"<code>solverpy.solver.atp.cvc5</code>","text":""},{"location":"api/solverpy/solver/atp/cvc5/#solverpy.solver.atp.cvc5.Cvc5","title":"<code>Cvc5</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>Deprecated: runs <code>cvc5</code> in its legacy <code>--lang=tptp</code> mode against TPTP problems, reusing the SMT <code>Cvc5</code>'s statistics parsing. Prefer the SMT-LIB2 <code>Cvc5</code> for new work.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/cvc5.py</code> <pre><code>class Cvc5(ShellSolver):\n   \"\"\"\n   Deprecated: runs `cvc5` in its legacy `--lang=tptp` mode against TPTP\n   problems, reusing the SMT\n   [`Cvc5`][solverpy.solver.smt.cvc5.Cvc5]'s statistics parsing. Prefer\n   the SMT-LIB2 [`Cvc5`][solverpy.solver.smt.cvc5.Cvc5] for new work.\n   \"\"\"\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = CVC5_BINARY,\n      static: str = CVC5_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      keys: list[str] = CVC5_KEYS,\n      complete: bool = True,\n   ):\n      cmd = f\"{binary} --lang=tptp {static}\"\n      plugins = plugins + [\n         Time(),\n         Tptp(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         CVC5_BUILDER,\n         plugins,\n         wait=1,\n      )\n      self.pattern: Pattern = re.compile(\n         r\"^(%s) = (.*)$\" % \"|\".join(keys),\n         flags=re.MULTILINE,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      result = SmtCvc5.process(self, output)  # type: ignore\n      if (\"status\" in result) and (result[\"status\"] == \"timeout\"):\n         result[\"status\"] = \"Timeout\"\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/eprover/","title":"module eprover","text":""},{"location":"api/solverpy/solver/atp/eprover/#solverpy.solver.atp.eprover","title":"<code>solverpy.solver.atp.eprover</code>","text":""},{"location":"api/solverpy/solver/atp/eprover/#solverpy.solver.atp.eprover.E","title":"<code>E</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>eprover</code> automated theorem prover for first-order logic with equality.</p> <p>The strategy (sid) supplies E's command-line options, appended after the static options (<code>--print-statistics --proof-statistics --tstp-format</code>, ...). TSTP-format statistics output is parsed by <code>process</code>. Status is decided by the <code>Tptp</code> plugin from the TSTP <code>SZS status</code> line.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/eprover.py</code> <pre><code>class E(ShellSolver):\n   \"\"\"\n   The `eprover` automated theorem prover for first-order logic with\n   equality.\n\n   The strategy (sid) supplies E's command-line options, appended after the\n   static options (`--print-statistics --proof-statistics --tstp-format`,\n   ...). TSTP-format statistics output is parsed by\n   [`process`][solverpy.solver.atp.eprover.E.process]. Status is decided by\n   the [`Tptp`][solverpy.solver.plugins.status.tptp.Tptp] plugin from the\n   TSTP `SZS status` line.\n   \"\"\"\n\n   _binary = E_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = E_BINARY,\n      static: str = E_STATIC,\n      complete: bool = True,\n      plugins: list[\"Plugin\"] = [],\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Tptp(complete=complete),\n         RunHash(E_RUNHASH_GEN),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         E_BUILDER,\n         plugins,\n         15,\n         complete,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse E's TSTP statistics block into a result dict.\"\"\"\n      result = patterns.keyval(E_PAT, output, E_TABLE)\n      result = patterns.mapval(result, human.numeric)\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/eprover/#solverpy.solver.atp.eprover.E.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse E's TSTP statistics block into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/eprover.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse E's TSTP statistics block into a result dict.\"\"\"\n   result = patterns.keyval(E_PAT, output, E_TABLE)\n   result = patterns.mapval(result, human.numeric)\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/lash/","title":"module lash","text":""},{"location":"api/solverpy/solver/atp/lash/#solverpy.solver.atp.lash","title":"<code>solverpy.solver.atp.lash</code>","text":""},{"location":"api/solverpy/solver/atp/lash/#solverpy.solver.atp.lash.Lash","title":"<code>Lash</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>lash</code> higher-order automated theorem prover.</p> <p>The strategy (sid) supplies Lash's command-line options, appended after the static option <code>-p tstp</code> (TSTP output). Output is parsed for <code>Steps</code> and <code>Mode</code> by <code>process</code>. Status is decided by the <code>Tptp</code> plugin from the TSTP <code>SZS status</code> line.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/lash.py</code> <pre><code>class Lash(ShellSolver):\n   \"\"\"\n   The `lash` higher-order automated theorem prover.\n\n   The strategy (sid) supplies Lash's command-line options, appended after\n   the static option `-p tstp` (TSTP output). Output is parsed for `Steps`\n   and `Mode` by\n   [`process`][solverpy.solver.atp.lash.Lash.process]. Status is decided by\n   the [`Tptp`][solverpy.solver.plugins.status.tptp.Tptp] plugin from the\n   TSTP `SZS status` line.\n   \"\"\"\n\n   _binary = L_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = L_BINARY,\n      static: str = L_STATIC,\n      complete: bool = True,\n      plugins: list[\"Plugin\"] = [],\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Tptp(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         L_BUILDER,\n         plugins,\n         0,\n         complete,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse Lash's `Steps`/`Mode` output lines into a result dict.\"\"\"\n      result = patterns.keyval(L_PAT, output, L_TABLE)\n      result = patterns.mapval(result, human.numeric)\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/lash/#solverpy.solver.atp.lash.Lash.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse Lash's <code>Steps</code>/<code>Mode</code> output lines into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/lash.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse Lash's `Steps`/`Mode` output lines into a result dict.\"\"\"\n   result = patterns.keyval(L_PAT, output, L_TABLE)\n   result = patterns.mapval(result, human.numeric)\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/prover9/","title":"module prover9","text":""},{"location":"api/solverpy/solver/atp/prover9/#solverpy.solver.atp.prover9","title":"<code>solverpy.solver.atp.prover9</code>","text":""},{"location":"api/solverpy/solver/atp/prover9/#solverpy.solver.atp.prover9.Prover9","title":"<code>Prover9</code>","text":"<p>               Bases: <code>StdinSolver</code></p> <p>The <code>prover9</code> automated theorem prover for first-order logic.</p> <p>Unlike the other ATP solvers, Prover9 is a <code>StdinSolver</code>: the strategy (sid) supplies Prover9 input-language commands (e.g. <code>assign(max_seconds, ...)</code>) fed on stdin, not command-line options. Output is parsed by <code>process</code>, which reads the termination reason and the <code>STATISTICS</code> block, and maps the reason to a TPTP-compatible status.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/prover9.py</code> <pre><code>class Prover9(StdinSolver):\n   \"\"\"\n   The `prover9` automated theorem prover for first-order logic.\n\n   Unlike the other ATP solvers, Prover9 is a\n   [`StdinSolver`][solverpy.solver.stdinsolver.StdinSolver]: the strategy\n   (sid) supplies Prover9 input-language commands (e.g.\n   `assign(max_seconds, ...)`) fed on stdin, not command-line options. Output\n   is parsed by [`process`][solverpy.solver.atp.prover9.Prover9.process],\n   which reads the termination reason and the `STATISTICS` block, and maps\n   the reason to a TPTP-compatible status.\n   \"\"\"\n\n   _binary = P9_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = P9_BINARY,\n      static: str = P9_STATIC,\n      complete: bool = False,\n      plugins: list[\"Plugin\"] = [],\n   ):\n      plugins = plugins + [Time()]\n      StdinSolver.__init__(\n         self,\n         binary,\n         limit,\n         P9_BUILDER,\n         plugins,\n         1,\n         static,\n         binary=binary,\n      )\n      self._complete = complete\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse the termination reason and `STATISTICS` block into a result dict.\"\"\"\n      reason = P9_REASON.search(output)\n      if not reason:\n         return {}\n      reason = reason.group(1)\n\n      stats = P9_STATS.search(output)\n      if stats:\n         result = patterns.keyval(P9_SINGLE, stats.group(1))\n      else:\n         result = {}\n\n      result[\"reason\"] = reason\n      if reason in P9_STATUS:\n         result[\"status\"] = P9_STATUS[reason]\n      elif reason.startswith(\"max_\"):  # some limit reached\n         result[\"status\"] = \"ResourceOut\"\n\n      result = patterns.mapval(result, human.numeric)\n\n      if \"User_CPU\" in result:\n         result[\"runtime\"] = result[\"User_CPU\"]\n\n      return result\n\n   def valid(self, result: \"Result\") -&gt; bool:\n      return super().valid(result) and result[\"status\"] in TPTP_ALL\n\n   @property\n   def success(self) -&gt; frozenset[str]:\n      return TPTP_OK if self._complete else TPTP_INC_OK\n\n   @property\n   def timeouts(self) -&gt; frozenset[str]:\n      return TPTP_TIMEOUT\n\n   @property\n   def statuses(self) -&gt; frozenset[str]:\n      return TPTP_ALL\n</code></pre>"},{"location":"api/solverpy/solver/atp/prover9/#solverpy.solver.atp.prover9.Prover9.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse the termination reason and <code>STATISTICS</code> block into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/prover9.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse the termination reason and `STATISTICS` block into a result dict.\"\"\"\n   reason = P9_REASON.search(output)\n   if not reason:\n      return {}\n   reason = reason.group(1)\n\n   stats = P9_STATS.search(output)\n   if stats:\n      result = patterns.keyval(P9_SINGLE, stats.group(1))\n   else:\n      result = {}\n\n   result[\"reason\"] = reason\n   if reason in P9_STATUS:\n      result[\"status\"] = P9_STATUS[reason]\n   elif reason.startswith(\"max_\"):  # some limit reached\n      result[\"status\"] = \"ResourceOut\"\n\n   result = patterns.mapval(result, human.numeric)\n\n   if \"User_CPU\" in result:\n      result[\"runtime\"] = result[\"User_CPU\"]\n\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/vampire/","title":"module vampire","text":""},{"location":"api/solverpy/solver/atp/vampire/#solverpy.solver.atp.vampire","title":"<code>solverpy.solver.atp.vampire</code>","text":""},{"location":"api/solverpy/solver/atp/vampire/#solverpy.solver.atp.vampire.Vampire","title":"<code>Vampire</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>vampire</code> automated theorem prover for first-order logic.</p> <p>The strategy (sid) supplies Vampire's command-line options, appended after the static options. Output includes a TPTP proof and a <code>--stat full</code> statistics block, parsed by <code>process</code>. Status is decided by the <code>Tptp</code> plugin from the TSTP <code>SZS status</code> line.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/vampire.py</code> <pre><code>class Vampire(ShellSolver):\n   \"\"\"\n   The `vampire` automated theorem prover for first-order logic.\n\n   The strategy (sid) supplies Vampire's command-line options, appended\n   after the static options. Output includes a TPTP proof and a `--stat\n   full` statistics block, parsed by\n   [`process`][solverpy.solver.atp.vampire.Vampire.process]. Status is\n   decided by the [`Tptp`][solverpy.solver.plugins.status.tptp.Tptp] plugin\n   from the TSTP `SZS status` line.\n   \"\"\"\n\n   _binary = V_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = V_BINARY,\n      static: str = V_STATIC,\n      complete: bool = True,\n      plugins: list[\"Plugin\"] = [],\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Tptp(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         V_BUILDER,\n         plugins,\n         1,\n         complete,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse Vampire's `--stat full` statistics block into a result dict.\"\"\"\n      result = patterns.keyval(V_PAT, output, V_TABLE)\n      result = patterns.mapval(result, human.numeric)\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/atp/vampire/#solverpy.solver.atp.vampire.Vampire.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse Vampire's <code>--stat full</code> statistics block into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/atp/vampire.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse Vampire's `--stat full` statistics block into a result dict.\"\"\"\n   result = patterns.keyval(V_PAT, output, V_TABLE)\n   result = patterns.mapval(result, human.numeric)\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/plugins/","title":"Plugins Overview","text":""},{"location":"api/solverpy/solver/plugins/#solverpy.solver.plugins","title":"<code>solverpy.solver.plugins</code>","text":""},{"location":"api/solverpy/solver/plugins/#solverpy.solver.plugins--plugins","title":"Plugins","text":"<p>Plugins are lightweight extensions that modify how a solver processes its inputs and outputs without requiring subclassing.  They are attached to a <code>PluginSolver</code> at construction time and called at fixed points in the solve lifecycle.</p> <p> </p>"},{"location":"api/solverpy/solver/plugins/#solverpy.solver.plugins--plugin-roles","title":"Plugin roles","text":"<p>Two abstract base classes define the two roles a plugin can play:</p> Role Base class Called during <code>Decorator</code> wraps the shell command; post-processes the solver output <code>Translator</code> transforms <code>(instance, strategy)</code> inputs before the solver runs"},{"location":"api/solverpy/solver/plugins/#solverpy.solver.plugins--solve-lifecycle","title":"Solve lifecycle","text":"<p>Each call to <code>solver.solve(instance, strategy)</code> follows these steps:</p> <ol> <li>All translators transform <code>(instance, strategy)</code> in registration order.</li> <li>The solver builds its shell command and each decorator wraps it via <code>decorate()</code>.</li> <li>The subprocess runs.</li> <li>Each decorator's <code>update()</code> is called to extract data from the raw output.</li> <li>Each decorator's <code>finished()</code> is called for any post-update side-effects (e.g. writing files).</li> </ol>"},{"location":"api/solverpy/solver/plugins/#solverpy.solver.plugins--connecting-plugins-to-a-solver","title":"Connecting plugins to a solver","text":"<p>Pass plugins as a list to the solver constructor; <code>init()</code> registers each one:</p> <pre><code>from solverpy.solver.plugins import db\nsolver = E(\"T10\", plugins=db())     # Bid and Sid are registered\n</code></pre> <p>To call a method on a specific plugin by id, use <code>solver.call()</code>:</p> <pre><code>solver.call(\"trains\", \"disable\")    # disable training data collection\n</code></pre>"},{"location":"api/solverpy/solver/plugins/#solverpy.solver.plugins--standalone-db-access","title":"Standalone DB access","text":"<p><code>Bid</code> and <code>Sid</code> can be used outside a solver to resolve identifiers independently:</p> <pre><code>from solverpy.benchmark.path import bids, sids\npath    = bids.path(bid, problem)   # (bid, problem) -&gt; file path\noptions = sids.load(sid)            # sid -&gt; strategy string\n</code></pre> <p><code>Outputs</code> and <code>Errors</code> write directly to the <code>solverpy_db/outputs/</code> and <code>solverpy_db/errors/</code> directories and can be instantiated standalone to inspect or write cached output files.</p>"},{"location":"api/solverpy/solver/plugins/#solverpy.solver.plugins--submodules","title":"Submodules","text":"Submodule Plugins Purpose <code>shell</code> Limits, Timeout, Time, Memory subprocess launch control and resource limits <code>status</code> Tptp, Smt, Limiter solver output parsing; result status population <code>db</code> Bid, Sid, Outputs, Errors benchmark database bridge"},{"location":"api/solverpy/solver/plugins/#solverpy.solver.plugins.outputs","title":"<code>outputs(flatten: bool = True, compress: bool = True)</code>","text":"<p>Return DB plugins plus output-capture plugins for debugging.</p> <p>Returns <code>[Bid(), Sid(), Outputs(...), Errors(...)]</code>.  Solver stdout is written to <code>solverpy_db/outputs/</code> for successful runs and to <code>solverpy_db/errors/</code> for failed or incomplete runs.</p> <p>Parameters:</p> Name Type Description Default <code>flatten</code> <code>bool</code> <p>Replace <code>/</code> in problem paths with <code>_._</code> to avoid nested directories in the output tree.</p> <code>True</code> <code>compress</code> <code>bool</code> <p>Gzip-compress the written output files.</p> <code>True</code> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/__init__.py</code> <pre><code>def outputs(flatten: bool = True, compress: bool = True):\n   \"\"\"Return DB plugins plus output-capture plugins for debugging.\n\n   Returns `[Bid(), Sid(), Outputs(...), Errors(...)]`.  Solver stdout is\n   written to `solverpy_db/outputs/` for successful runs and to\n   `solverpy_db/errors/` for failed or incomplete runs.\n\n   Args:\n      flatten: Replace `/` in problem paths with `_._` to avoid nested\n         directories in the output tree.\n      compress: Gzip-compress the written output files.\n   \"\"\"\n   return [\n      Bid(),\n      Sid(),\n      Outputs(flatten, compress),\n      Errors(flatten, compress),\n   ]\n</code></pre>"},{"location":"api/solverpy/solver/plugins/#solverpy.solver.plugins.proofs","title":"<code>proofs(proof=True, premises=False)</code>","text":"<p>Return DB plugins plus proof/premises-capture plugin.</p> <p>Returns <code>[Bid(), Sid(), Proofs(...)]</code>.  The proof block and/or premise names are written to <code>solverpy_db/proofs/</code> and <code>solverpy_db/premises/&lt;bid&gt;/&lt;problem&gt;</code> for successful TPTP solves.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/__init__.py</code> <pre><code>def proofs(proof=True, premises=False):\n   \"\"\"Return DB plugins plus proof/premises-capture plugin.\n\n   Returns `[Bid(), Sid(), Proofs(...)]`.  The proof block and/or premise\n   names are written to ``solverpy_db/proofs/`` and\n   ``solverpy_db/premises/&lt;bid&gt;/&lt;problem&gt;`` for successful TPTP solves.\n   \"\"\"\n   return [\n      Bid(),\n      Sid(),\n      Proofs(proof=proof, premises=premises),\n   ]\n</code></pre>"},{"location":"api/solverpy/solver/plugins/apply/","title":"module apply","text":""},{"location":"api/solverpy/solver/plugins/apply/#solverpy.solver.plugins.apply","title":"<code>solverpy.solver.plugins.apply</code>","text":""},{"location":"api/solverpy/solver/plugins/apply/#solverpy.solver.plugins.apply.Apply","title":"<code>Apply</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Decorator that applies a function to the result dict after each solve.</p> <p>The callable receives the current result dict and returns a dict of key/value pairs to merge into it.  This is a lightweight way to compute and store derived result keys without writing a full <code>Decorator</code> subclass.</p> <p>The function is called during <code>update()</code>, after all earlier decorators have run, so keys populated by plugins registered before this one (e.g. <code>status</code>, <code>runtime</code>) are already available in result.</p> <p>Parameters:</p> Name Type Description Default <code>fn</code> <code>Callable[[Result], dict]</code> <p>Callable <code>(result) -&gt; dict</code> whose return value is merged into result in place.</p> required <code>**kwargs</code> <code>Any</code> <p>Forwarded to :class:<code>Decorator</code>.</p> <code>{}</code> <p>Example::</p> <p>solver.init([Apply(lambda r: {\"solved\": r.get(\"status\") == \"Theorem\"})])</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/apply.py</code> <pre><code>class Apply(Decorator):\n   \"\"\"Decorator that applies a function to the result dict after each solve.\n\n   The callable receives the current result dict and returns a dict of\n   key/value pairs to merge into it.  This is a lightweight way to compute\n   and store derived result keys without writing a full\n   [`Decorator`][solverpy.solver.plugins.decorator.Decorator] subclass.\n\n   The function is called during `update()`, after all earlier decorators\n   have run, so keys populated by plugins registered before this one\n   (e.g. `status`, `runtime`) are already available in *result*.\n\n   Args:\n      fn: Callable ``(result) -&gt; dict`` whose return value is merged into\n         *result* in place.\n      **kwargs: Forwarded to :class:`Decorator`.\n\n   Example::\n\n      solver.init([Apply(lambda r: {\"solved\": r.get(\"status\") == \"Theorem\"})])\n   \"\"\"\n\n   def __init__(self, fn: \"Callable[[Result], dict]\", **kwargs: Any):\n      Decorator.__init__(self, fn=fn.__name__, **kwargs)\n      self._fn = fn\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Call `fn(result)` and merge the returned dict into *result*.\"\"\"\n      del instance, strategy, output\n      result.update(self._fn(result))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/apply/#solverpy.solver.plugins.apply.Apply.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Call <code>fn(result)</code> and merge the returned dict into result.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/apply.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Call `fn(result)` and merge the returned dict into *result*.\"\"\"\n   del instance, strategy, output\n   result.update(self._fn(result))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/decorator/","title":"module decorator","text":""},{"location":"api/solverpy/solver/plugins/decorator/#solverpy.solver.plugins.decorator","title":"<code>solverpy.solver.plugins.decorator</code>","text":""},{"location":"api/solverpy/solver/plugins/decorator/#solverpy.solver.plugins.decorator.Decorator","title":"<code>Decorator</code>","text":"<p>               Bases: <code>Plugin</code></p> <p>Plugin that wraps the solver command and post-processes the result.</p> <p>Decorators are applied in registration order by <code>PluginSolver</code> at three points in the solve lifecycle:</p> <ol> <li><code>decorate()</code> \u2014 called before the subprocess launches; each decorator may    wrap or extend the command string.</li> <li><code>update()</code> \u2014 called after the subprocess exits; reads raw output and    populates the <code>result</code> dict.</li> <li><code>finished()</code> \u2014 called after all <code>update()</code> calls complete; used for    side-effects (e.g. writing output files) that depend on the final result    state.</li> </ol> <p>All three methods are no-ops by default; subclasses override only what they need.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/decorator.py</code> <pre><code>class Decorator(Plugin):\n   \"\"\"Plugin that wraps the solver command and post-processes the result.\n\n   Decorators are applied in registration order by\n   [`PluginSolver`][solverpy.solver.pluginsolver.PluginSolver] at three points\n   in the solve lifecycle:\n\n   1. `decorate()` \u2014 called before the subprocess launches; each decorator may\n      wrap or extend the command string.\n   2. `update()` \u2014 called after the subprocess exits; reads raw output and\n      populates the `result` dict.\n   3. `finished()` \u2014 called after all `update()` calls complete; used for\n      side-effects (e.g. writing output files) that depend on the final result\n      state.\n\n   All three methods are no-ops by default; subclasses override only what they\n   need.\n   \"\"\"\n\n   def __init__(self, **kwargs):\n      Plugin.__init__(self, **kwargs)\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Append this decorator to `solver.decorators`.\"\"\"\n      solver.decorators.append(self)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Wrap or extend the shell command before the subprocess launches.\n\n      Args:\n         cmd: The command string built so far.\n         instance: The (translated) problem instance.\n         strategy: The (translated) strategy string.\n\n      Returns:\n         The modified command string.  The default implementation returns\n         *cmd* unchanged.\n      \"\"\"\n      del instance, strategy  # unused arguments\n      return cmd\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Extract data from *output* and update *result* in place.\n\n      Called after the subprocess exits, in decorator registration order.\n\n      Args:\n         instance: The problem instance that was solved.\n         strategy: The strategy that was used.\n         output: Combined stdout (and stderr, if captured) from the subprocess.\n         result: The result dict being constructed; modify it in place.\n      \"\"\"\n      del instance, strategy, output, result  # unused arguments\n      return\n\n   def finished(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Perform side-effects after all `update()` calls have completed.\n\n      Called once the result dict is fully populated.  Useful for writing\n      files or logging that depends on the final status (e.g.\n      [`Outputs`][solverpy.solver.plugins.db.outputs.Outputs]).\n\n      Args:\n         instance: The problem instance that was solved.\n         strategy: The strategy that was used.\n         output: Raw subprocess output.\n         result: The completed result dict.\n      \"\"\"\n      del instance, strategy, output, result  # unused arguments\n      return\n</code></pre>"},{"location":"api/solverpy/solver/plugins/decorator/#solverpy.solver.plugins.decorator.Decorator.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Wrap or extend the shell command before the subprocess launches.</p> <p>Parameters:</p> Name Type Description Default <code>cmd</code> <code>str</code> <p>The command string built so far.</p> required <code>instance</code> <code>Any</code> <p>The (translated) problem instance.</p> required <code>strategy</code> <code>Any</code> <p>The (translated) strategy string.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The modified command string.  The default implementation returns</p> <code>str</code> <p>cmd unchanged.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/decorator.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Wrap or extend the shell command before the subprocess launches.\n\n   Args:\n      cmd: The command string built so far.\n      instance: The (translated) problem instance.\n      strategy: The (translated) strategy string.\n\n   Returns:\n      The modified command string.  The default implementation returns\n      *cmd* unchanged.\n   \"\"\"\n   del instance, strategy  # unused arguments\n   return cmd\n</code></pre>"},{"location":"api/solverpy/solver/plugins/decorator/#solverpy.solver.plugins.decorator.Decorator.finished","title":"<code>finished(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Perform side-effects after all <code>update()</code> calls have completed.</p> <p>Called once the result dict is fully populated.  Useful for writing files or logging that depends on the final status (e.g. <code>Outputs</code>).</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Any</code> <p>The problem instance that was solved.</p> required <code>strategy</code> <code>Any</code> <p>The strategy that was used.</p> required <code>output</code> <code>str</code> <p>Raw subprocess output.</p> required <code>result</code> <code>Result</code> <p>The completed result dict.</p> required Source code in <code>packages/solverpy/src/solverpy/solver/plugins/decorator.py</code> <pre><code>def finished(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Perform side-effects after all `update()` calls have completed.\n\n   Called once the result dict is fully populated.  Useful for writing\n   files or logging that depends on the final status (e.g.\n   [`Outputs`][solverpy.solver.plugins.db.outputs.Outputs]).\n\n   Args:\n      instance: The problem instance that was solved.\n      strategy: The strategy that was used.\n      output: Raw subprocess output.\n      result: The completed result dict.\n   \"\"\"\n   del instance, strategy, output, result  # unused arguments\n   return\n</code></pre>"},{"location":"api/solverpy/solver/plugins/decorator/#solverpy.solver.plugins.decorator.Decorator.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Append this decorator to <code>solver.decorators</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/decorator.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Append this decorator to `solver.decorators`.\"\"\"\n   solver.decorators.append(self)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/decorator/#solverpy.solver.plugins.decorator.Decorator.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Extract data from output and update result in place.</p> <p>Called after the subprocess exits, in decorator registration order.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Any</code> <p>The problem instance that was solved.</p> required <code>strategy</code> <code>Any</code> <p>The strategy that was used.</p> required <code>output</code> <code>str</code> <p>Combined stdout (and stderr, if captured) from the subprocess.</p> required <code>result</code> <code>Result</code> <p>The result dict being constructed; modify it in place.</p> required Source code in <code>packages/solverpy/src/solverpy/solver/plugins/decorator.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Extract data from *output* and update *result* in place.\n\n   Called after the subprocess exits, in decorator registration order.\n\n   Args:\n      instance: The problem instance that was solved.\n      strategy: The strategy that was used.\n      output: Combined stdout (and stderr, if captured) from the subprocess.\n      result: The result dict being constructed; modify it in place.\n   \"\"\"\n   del instance, strategy, output, result  # unused arguments\n   return\n</code></pre>"},{"location":"api/solverpy/solver/plugins/managed/","title":"module managed","text":""},{"location":"api/solverpy/solver/plugins/managed/#solverpy.solver.plugins.managed","title":"<code>solverpy.solver.plugins.managed</code>","text":""},{"location":"api/solverpy/solver/plugins/managed/#solverpy.solver.plugins.managed.Managed","title":"<code>Managed</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Decorator that holds process-shared state connected via a Manager.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/managed.py</code> <pre><code>class Managed(Decorator):\n   \"\"\"Decorator that holds process-shared state connected via a Manager.\"\"\"\n\n   def connect(self, manager: \"SyncManager\") -&gt; None:\n      \"\"\"Create process-shared state from the session-owned Manager.\"\"\"\n\n   def disconnect(self) -&gt; None:\n      \"\"\"Discard process-shared proxies before their Manager is shut down.\"\"\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/managed/#solverpy.solver.plugins.managed.Managed.connect","title":"<code>connect(manager: SyncManager) -&gt; None</code>","text":"<p>Create process-shared state from the session-owned Manager.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/managed.py</code> <pre><code>def connect(self, manager: \"SyncManager\") -&gt; None:\n   \"\"\"Create process-shared state from the session-owned Manager.\"\"\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/managed/#solverpy.solver.plugins.managed.Managed.disconnect","title":"<code>disconnect() -&gt; None</code>","text":"<p>Discard process-shared proxies before their Manager is shut down.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/managed.py</code> <pre><code>def disconnect(self) -&gt; None:\n   \"\"\"Discard process-shared proxies before their Manager is shut down.\"\"\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/plugin/","title":"module plugin","text":""},{"location":"api/solverpy/solver/plugins/plugin/#solverpy.solver.plugins.plugin","title":"<code>solverpy.solver.plugins.plugin</code>","text":""},{"location":"api/solverpy/solver/plugins/plugin/#solverpy.solver.plugins.plugin.Plugin","title":"<code>Plugin</code>","text":"<p>               Bases: <code>SolverPyObj</code></p> <p>Base class for all solver plugins.</p> <p>A plugin is attached to a <code>PluginSolver</code> at construction time via the <code>plugins</code> list.  When <code>init()</code> is called, each plugin's <code>register()</code> method fires, giving the plugin a reference to the solver and placing it on either <code>solver.decorators</code> or <code>solver.translators</code>.</p> <p>Plugins can be toggled at runtime via <code>enable()</code> / <code>disable()</code>.  To target a specific plugin from outside the solver, assign a string <code>pid</code> at construction and call <code>solver.call(pid, method)</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/plugin.py</code> <pre><code>class Plugin(SolverPyObj):\n   \"\"\"Base class for all solver plugins.\n\n   A plugin is attached to a\n   [`PluginSolver`][solverpy.solver.pluginsolver.PluginSolver] at construction\n   time via the `plugins` list.  When `init()` is called, each plugin's\n   `register()` method fires, giving the plugin a reference to the solver and\n   placing it on either `solver.decorators` or `solver.translators`.\n\n   Plugins can be toggled at runtime via `enable()` / `disable()`.  To target a\n   specific plugin from outside the solver, assign a string `pid` at construction\n   and call `solver.call(pid, method)`.\n   \"\"\"\n\n   def __init__(self, pid: str | None = None, **kwargs: Any):\n      \"\"\"Initialise the plugin.\n\n      Args:\n         pid: Optional string identifier used by\n            [`PluginSolver.call`][solverpy.solver.pluginsolver.PluginSolver.call]\n            to dispatch method calls to this plugin.\n         **kwargs: Forwarded to `SolverPyObj` for repr support.\n      \"\"\"\n      if pid is not None:\n         kwargs[\"pid\"] = pid\n      SolverPyObj.__init__(self, **kwargs)\n      self._enabled = True\n      self._id = pid\n\n   def register(self, solver: Any) -&gt; None:\n      \"\"\"Register this plugin with *solver*.\n\n      Called once by\n      [`PluginSolver.init`][solverpy.solver.pluginsolver.PluginSolver.init].\n      Subclasses append themselves to `solver.decorators` or\n      `solver.translators` (or both, in the case of\n      [`Limits`][solverpy.solver.plugins.shell.limits.Limits]).\n\n      Raises:\n         NotImplementedError: Must be overridden by every concrete subclass.\n      \"\"\"\n      del solver # unused argument\n      raise NotImplementedError()\n\n   def enable(self) -&gt; None:\n      \"\"\"Re-enable this plugin after a previous `disable()` call.\"\"\"\n      self._enabled = True\n\n   def disable(self) -&gt; None:\n      \"\"\"Disable this plugin so its hooks become no-ops until re-enabled.\"\"\"\n      self._enabled = False\n\n   @property\n   def id(self) -&gt; str | None:\n      \"\"\"The optional string identifier assigned at construction time.\"\"\"\n      return self._id\n</code></pre>"},{"location":"api/solverpy/solver/plugins/plugin/#solverpy.solver.plugins.plugin.Plugin.id","title":"<code>id: str | None</code>  <code>property</code>","text":"<p>The optional string identifier assigned at construction time.</p>"},{"location":"api/solverpy/solver/plugins/plugin/#solverpy.solver.plugins.plugin.Plugin.__init__","title":"<code>__init__(pid: str | None = None, **kwargs: Any)</code>","text":"<p>Initialise the plugin.</p> <p>Parameters:</p> Name Type Description Default <code>pid</code> <code>str | None</code> <p>Optional string identifier used by <code>PluginSolver.call</code> to dispatch method calls to this plugin.</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Forwarded to <code>SolverPyObj</code> for repr support.</p> <code>{}</code> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/plugin.py</code> <pre><code>def __init__(self, pid: str | None = None, **kwargs: Any):\n   \"\"\"Initialise the plugin.\n\n   Args:\n      pid: Optional string identifier used by\n         [`PluginSolver.call`][solverpy.solver.pluginsolver.PluginSolver.call]\n         to dispatch method calls to this plugin.\n      **kwargs: Forwarded to `SolverPyObj` for repr support.\n   \"\"\"\n   if pid is not None:\n      kwargs[\"pid\"] = pid\n   SolverPyObj.__init__(self, **kwargs)\n   self._enabled = True\n   self._id = pid\n</code></pre>"},{"location":"api/solverpy/solver/plugins/plugin/#solverpy.solver.plugins.plugin.Plugin.disable","title":"<code>disable() -&gt; None</code>","text":"<p>Disable this plugin so its hooks become no-ops until re-enabled.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/plugin.py</code> <pre><code>def disable(self) -&gt; None:\n   \"\"\"Disable this plugin so its hooks become no-ops until re-enabled.\"\"\"\n   self._enabled = False\n</code></pre>"},{"location":"api/solverpy/solver/plugins/plugin/#solverpy.solver.plugins.plugin.Plugin.enable","title":"<code>enable() -&gt; None</code>","text":"<p>Re-enable this plugin after a previous <code>disable()</code> call.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/plugin.py</code> <pre><code>def enable(self) -&gt; None:\n   \"\"\"Re-enable this plugin after a previous `disable()` call.\"\"\"\n   self._enabled = True\n</code></pre>"},{"location":"api/solverpy/solver/plugins/plugin/#solverpy.solver.plugins.plugin.Plugin.register","title":"<code>register(solver: Any) -&gt; None</code>","text":"<p>Register this plugin with solver.</p> <p>Called once by <code>PluginSolver.init</code>. Subclasses append themselves to <code>solver.decorators</code> or <code>solver.translators</code> (or both, in the case of <code>Limits</code>).</p> <p>Raises:</p> Type Description <code>NotImplementedError</code> <p>Must be overridden by every concrete subclass.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/plugin.py</code> <pre><code>def register(self, solver: Any) -&gt; None:\n   \"\"\"Register this plugin with *solver*.\n\n   Called once by\n   [`PluginSolver.init`][solverpy.solver.pluginsolver.PluginSolver.init].\n   Subclasses append themselves to `solver.decorators` or\n   `solver.translators` (or both, in the case of\n   [`Limits`][solverpy.solver.plugins.shell.limits.Limits]).\n\n   Raises:\n      NotImplementedError: Must be overridden by every concrete subclass.\n   \"\"\"\n   del solver # unused argument\n   raise NotImplementedError()\n</code></pre>"},{"location":"api/solverpy/solver/plugins/translator/","title":"module translator","text":""},{"location":"api/solverpy/solver/plugins/translator/#solverpy.solver.plugins.translator","title":"<code>solverpy.solver.plugins.translator</code>","text":""},{"location":"api/solverpy/solver/plugins/translator/#solverpy.solver.plugins.translator.Translator","title":"<code>Translator</code>","text":"<p>               Bases: <code>Plugin</code></p> <p>Plugin that transforms solver inputs before each solve call.</p> <p>The single hook <code>translate(instance, strategy)</code> receives the current <code>(instance, strategy)</code> pair and returns a new pair.  Translators are chained in registration order: the output of each becomes the input of the next.</p> <p>The two built-in translators handle the benchmark database identifiers:</p> <ul> <li><code>Bid</code> converts a <code>(bid, problem)</code>   tuple to the full problem file path.</li> <li><code>Sid</code> loads the strategy definition   string from <code>solverpy_db/strats/</code>, expanding any parametric placeholders.</li> </ul> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/translator.py</code> <pre><code>class Translator(Plugin):\n   \"\"\"Plugin that transforms solver inputs before each solve call.\n\n   The single hook `translate(instance, strategy)` receives the current\n   `(instance, strategy)` pair and returns a new pair.  Translators are chained\n   in registration order: the output of each becomes the input of the next.\n\n   The two built-in translators handle the benchmark database identifiers:\n\n   - [`Bid`][solverpy.solver.plugins.db.bid.Bid] converts a `(bid, problem)`\n     tuple to the full problem file path.\n   - [`Sid`][solverpy.solver.plugins.db.sid.Sid] loads the strategy definition\n     string from `solverpy_db/strats/`, expanding any parametric placeholders.\n   \"\"\"\n\n   def __init__(self, **kwargs):\n      Plugin.__init__(self, **kwargs)\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Append this translator to `solver.translators`.\"\"\"\n      solver.translators.append(self)\n\n   def translate(\n      self,\n      instance: Any,\n      strategy: Any\n   ) -&gt; tuple[Any, Any]:\n      \"\"\"Transform the `(instance, strategy)` pair.\n\n      Args:\n         instance: The current problem instance (often a `(bid, problem)` tuple\n            early in the chain, a file path later).\n         strategy: The current strategy value (often a sid string early in the\n            chain, a full CLI options string later).\n\n      Returns:\n         A `(instance, strategy)` tuple to be passed to the next translator or,\n         ultimately, to the solver's run method.  The default implementation\n         returns the inputs unchanged.\n      \"\"\"\n      return (instance, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/translator/#solverpy.solver.plugins.translator.Translator.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Append this translator to <code>solver.translators</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/translator.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Append this translator to `solver.translators`.\"\"\"\n   solver.translators.append(self)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/translator/#solverpy.solver.plugins.translator.Translator.translate","title":"<code>translate(instance: Any, strategy: Any) -&gt; tuple[Any, Any]</code>","text":"<p>Transform the <code>(instance, strategy)</code> pair.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Any</code> <p>The current problem instance (often a <code>(bid, problem)</code> tuple early in the chain, a file path later).</p> required <code>strategy</code> <code>Any</code> <p>The current strategy value (often a sid string early in the chain, a full CLI options string later).</p> required <p>Returns:</p> Type Description <code>Any</code> <p>A <code>(instance, strategy)</code> tuple to be passed to the next translator or,</p> <code>Any</code> <p>ultimately, to the solver's run method.  The default implementation</p> <code>tuple[Any, Any]</code> <p>returns the inputs unchanged.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/translator.py</code> <pre><code>def translate(\n   self,\n   instance: Any,\n   strategy: Any\n) -&gt; tuple[Any, Any]:\n   \"\"\"Transform the `(instance, strategy)` pair.\n\n   Args:\n      instance: The current problem instance (often a `(bid, problem)` tuple\n         early in the chain, a file path later).\n      strategy: The current strategy value (often a sid string early in the\n         chain, a full CLI options string later).\n\n   Returns:\n      A `(instance, strategy)` tuple to be passed to the next translator or,\n      ultimately, to the solver's run method.  The default implementation\n      returns the inputs unchanged.\n   \"\"\"\n   return (instance, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/","title":"Db Overview","text":""},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db","title":"<code>solverpy.solver.plugins.db</code>","text":""},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db--db-plugins","title":"DB plugins","text":"<p>DB plugins bridge the solver and the benchmark database.  They translate abstract identifiers \u2014 benchmark ids and strategy ids \u2014 into concrete inputs for the solver, and write solver outputs back to the database.</p> <p> </p> Plugin Description <code>Bid</code> Translates a <code>(bid, problem)</code> instance pair to the full problem file path <code>Sid</code> Loads a strategy definition from <code>solverpy_db/strats/</code> by strategy id; handles parametric strategies <code>Outputs</code> Writes raw solver output to <code>solverpy_db/outputs/</code> for successful runs <code>Errors</code> Writes raw solver output to <code>solverpy_db/errors/</code> for failed/incomplete runs"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.Bid","title":"<code>Bid</code>","text":"<p>               Bases: <code>Translator</code></p> <p>Translate a <code>(bid, problem)</code> instance pair to the full problem file path.</p> <p>The instance passed into the solver during benchmark evaluation is a tuple <code>(bid, problem)</code> where <code>bid</code> is a benchmark directory id and <code>problem</code> is a relative problem name within that benchmark.  This translator unwraps the tuple and resolves it to an absolute file path via <code>bids.path</code>.</p> <p><code>Bid</code> must run before <code>Sid</code> in the translator chain (the default order established by <code>plugins.db()</code>).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/bid.py</code> <pre><code>class Bid(Translator):\n   \"\"\"Translate a `(bid, problem)` instance pair to the full problem file path.\n\n   The instance passed into the solver during benchmark evaluation is a tuple\n   `(bid, problem)` where `bid` is a benchmark directory id and `problem` is a\n   relative problem name within that benchmark.  This translator unwraps the\n   tuple and resolves it to an absolute file path via\n   [`bids.path`][solverpy.benchmark.path.bids.path].\n\n   `Bid` must run **before** `Sid` in the translator chain (the default order\n   established by [`plugins.db()`][solverpy.solver.plugins.db]).\n   \"\"\"\n\n   def __init__(self, **kwargs):\n      Translator.__init__(self, **kwargs)\n\n   def translate(\n      self,\n      instance: tuple[str, str],\n      strategy: Any\n   ) -&gt; tuple[str, Any]:\n      \"\"\"Resolve `(bid, problem)` \u2192 absolute file path.\"\"\"\n      (bid, problem) = instance\n      instance0 = bids.path(bid, problem)\n      return (instance0, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.Bid.translate","title":"<code>translate(instance: tuple[str, str], strategy: Any) -&gt; tuple[str, Any]</code>","text":"<p>Resolve <code>(bid, problem)</code> \u2192 absolute file path.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/bid.py</code> <pre><code>def translate(\n   self,\n   instance: tuple[str, str],\n   strategy: Any\n) -&gt; tuple[str, Any]:\n   \"\"\"Resolve `(bid, problem)` \u2192 absolute file path.\"\"\"\n   (bid, problem) = instance\n   instance0 = bids.path(bid, problem)\n   return (instance0, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.EProverSid","title":"<code>EProverSid</code>","text":"<p>               Bases: <code>Translator</code></p> <p>Sid translator for E Prover supporting both old and new strategy formats.</p> <p>Old format (CLI options string): behaves identically to <code>Sid</code> \u2014 loads the file contents and returns them as the strategy string.</p> <p>New format (file starts with <code>{</code>): the file contains an E Prover strategy block produced by <code>--print-strategy</code>.  In this case the file path is passed to E via <code>--parse-strategy=&lt;path&gt;</code> instead of inlining the options.</p> <p>Parametric strategies (<code>@@@</code>) are not supported for new-format esids.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/eprovesid.py</code> <pre><code>class EProverSid(Translator):\n   \"\"\"Sid translator for E Prover supporting both old and new strategy formats.\n\n   Old format (CLI options string): behaves identically to\n   [`Sid`][solverpy.solver.plugins.db.sid.Sid] \u2014 loads the file contents and\n   returns them as the strategy string.\n\n   New format (file starts with `{`): the file contains an E Prover strategy\n   block produced by `--print-strategy`.  In this case the file path is passed\n   to E via `--parse-strategy=&lt;path&gt;` instead of inlining the options.\n\n   Parametric strategies (`@@@`) are not supported for new-format esids.\n   \"\"\"\n\n   def translate(\n      self,\n      instance: Any,\n      strategy: str,\n   ) -&gt; tuple[Any, str]:\n      \"\"\"Load strategy and return CLI options or `--parse-strategy=&lt;path&gt;`.\"\"\"\n      strategy0 = sids.load(strategy)\n      if strategy0.startswith(\"{\"):\n         assert \"@@@\" not in strategy0, \\\n            f\"Parametric strategies not supported for esid: {strategy}\"\n         return (instance, f\"--parse-strategy={sids.path(strategy)}\")\n      return (instance, sids.resolve(strategy, strategy0))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.EProverSid.translate","title":"<code>translate(instance: Any, strategy: str) -&gt; tuple[Any, str]</code>","text":"<p>Load strategy and return CLI options or <code>--parse-strategy=&lt;path&gt;</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/eprovesid.py</code> <pre><code>def translate(\n   self,\n   instance: Any,\n   strategy: str,\n) -&gt; tuple[Any, str]:\n   \"\"\"Load strategy and return CLI options or `--parse-strategy=&lt;path&gt;`.\"\"\"\n   strategy0 = sids.load(strategy)\n   if strategy0.startswith(\"{\"):\n      assert \"@@@\" not in strategy0, \\\n         f\"Parametric strategies not supported for esid: {strategy}\"\n      return (instance, f\"--parse-strategy={sids.path(strategy)}\")\n   return (instance, sids.resolve(strategy, strategy0))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.Proofs","title":"<code>Proofs</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Write TPTP proof and/or premise names to <code>solverpy_db/</code> subdirectories.</p> <p>Fires whenever the solver output contains an SZS proof block (between <code>% SZS output start</code> and <code>% SZS output end</code>).  Requires the instance to be a <code>(bid, problem)</code> tuple (i.e. used together with :class:<code>~solverpy.solver.plugins.db.bid.Bid</code>).</p> <p>Directory structure mirrors :class:<code>~solverpy.solver.plugins.db.outputs.Outputs</code>: <code>solverpy_db/{proofs,premises}/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;</code>.</p> <p>Parameters:</p> Name Type Description Default <code>proof</code> <code>bool</code> <p>Write the raw proof block to <code>solverpy_db/proofs/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;</code>.</p> <code>True</code> <code>premises</code> <code>bool</code> <p>Write deduplicated formula names (one per line) from <code>file(&lt;filename&gt;, &lt;name&gt;)</code> annotations to <code>solverpy_db/premises/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;</code>.</p> <code>False</code> <code>flatten</code> <code>bool | str</code> <p>Replace <code>/</code> in problem paths with <code>_._</code> (or a custom string) so all files live in a single flat directory.</p> <code>True</code> <code>compress</code> <code>bool</code> <p>Gzip-compress proof files (appends <code>.gz</code>).  Premises files are never compressed.</p> <code>True</code> <code>pid</code> <code>str</code> <p>Plugin id for use with <code>solver.call()</code>.</p> <code>'proofs'</code> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/proofs.py</code> <pre><code>class Proofs(Decorator):\n   \"\"\"Write TPTP proof and/or premise names to ``solverpy_db/`` subdirectories.\n\n   Fires whenever the solver output contains an SZS proof block (between\n   ``% SZS output start`` and ``% SZS output end``).  Requires the instance to be a ``(bid, problem)`` tuple (i.e. used\n   together with :class:`~solverpy.solver.plugins.db.bid.Bid`).\n\n   Directory structure mirrors\n   :class:`~solverpy.solver.plugins.db.outputs.Outputs`:\n   ``solverpy_db/{proofs,premises}/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;``.\n\n   Args:\n      proof: Write the raw proof block to ``solverpy_db/proofs/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;``.\n      premises: Write deduplicated formula names (one per line) from\n         ``file(&lt;filename&gt;, &lt;name&gt;)`` annotations to\n         ``solverpy_db/premises/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;``.\n      flatten: Replace ``/`` in problem paths with ``_._`` (or a custom\n         string) so all files live in a single flat directory.\n      compress: Gzip-compress proof files (appends ``.gz``).  Premises files\n         are never compressed.\n      pid: Plugin id for use with ``solver.call()``.\n   \"\"\"\n\n   def __init__(\n      self,\n      proof: bool = True,\n      premises: bool = False,\n      flatten: bool | str = True,\n      compress: bool = True,\n      pid: str = \"proofs\",\n   ):\n      Decorator.__init__(\n         self,\n         proof=proof,\n         premises=premises,\n         flatten=flatten,\n         compress=compress,\n         pid=pid,\n      )\n      self._proof = proof\n      self._premises = premises\n      self._flatten = flatten\n      self._compress = compress\n      self._proofs_path = bids.dbpath(\"proofs\")\n      self._premises_path = bids.dbpath(\"premises\")\n      self._limit: str = \"\"\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      solver.decorators.append(self)\n      self._limit = solver._limits.limit\n\n   def proof_path(self, instance: tuple[str, str], strategy: str) -&gt; str:\n      \"\"\"Return the proof file path (without ``.gz`` when compress is on).\"\"\"\n      (bid, problem) = instance\n      if self._flatten:\n         sep = \"_._\" if self._flatten is True else self._flatten\n         problem = problem.replace(\"/\", sep)\n      return os.path.join(\n         self._proofs_path,\n         bids.name(bid, limit=self._limit),\n         sids.name(strategy),\n         problem,\n      )\n\n   def premises_path(self, instance: tuple[str, str], strategy: str) -&gt; str:\n      \"\"\"Return the premises file path.\"\"\"\n      (bid, problem) = instance\n      if self._flatten:\n         sep = \"_._\" if self._flatten is True else self._flatten\n         problem = problem.replace(\"/\", sep)\n      return os.path.join(\n         self._premises_path,\n         bids.name(bid, limit=self._limit),\n         sids.name(strategy),\n         problem,\n      )\n\n   def update(\n      self,\n      instance,\n      strategy: str,\n      output: str,\n      result: dict,\n   ) -&gt; None:\n      if not self._enabled:\n         return\n      if not isinstance(instance, tuple):\n         return\n      block = _szs_block(output or \"\")\n      if not block:\n         return\n      if self._proof:\n         p = self.proof_path(instance, strategy)\n         if self._compress:\n            _write_gz(p, block)\n         else:\n            _write_plain(p, block)\n      if self._premises:\n         names = _names_from_block(block)\n         if names:\n            _write_plain(self.premises_path(instance, strategy),\n                         \"\\n\".join(sorted(names)) + \"\\n\")\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.Proofs.premises_path","title":"<code>premises_path(instance: tuple[str, str], strategy: str) -&gt; str</code>","text":"<p>Return the premises file path.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/proofs.py</code> <pre><code>def premises_path(self, instance: tuple[str, str], strategy: str) -&gt; str:\n   \"\"\"Return the premises file path.\"\"\"\n   (bid, problem) = instance\n   if self._flatten:\n      sep = \"_._\" if self._flatten is True else self._flatten\n      problem = problem.replace(\"/\", sep)\n   return os.path.join(\n      self._premises_path,\n      bids.name(bid, limit=self._limit),\n      sids.name(strategy),\n      problem,\n   )\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.Proofs.proof_path","title":"<code>proof_path(instance: tuple[str, str], strategy: str) -&gt; str</code>","text":"<p>Return the proof file path (without <code>.gz</code> when compress is on).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/proofs.py</code> <pre><code>def proof_path(self, instance: tuple[str, str], strategy: str) -&gt; str:\n   \"\"\"Return the proof file path (without ``.gz`` when compress is on).\"\"\"\n   (bid, problem) = instance\n   if self._flatten:\n      sep = \"_._\" if self._flatten is True else self._flatten\n      problem = problem.replace(\"/\", sep)\n   return os.path.join(\n      self._proofs_path,\n      bids.name(bid, limit=self._limit),\n      sids.name(strategy),\n      problem,\n   )\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.Sid","title":"<code>Sid</code>","text":"<p>               Bases: <code>Translator</code></p> <p>Load a strategy definition from <code>solverpy_db/strats/</code> by strategy id.</p> <p>The strategy passed into the solver during benchmark evaluation is a strategy id (sid) \u2014 a filename inside <code>solverpy_db/strats/</code>.  This translator loads the file contents (the actual solver CLI options string) via <code>sids.load</code>.</p> <p>Parametric strategies: if the loaded definition contains <code>@@@ var : default @@@</code> placeholders, the sid may carry arguments in the form <code>base_sid@var=val:var2=val2</code>.  The translator splits the sid, extracts the argument assignments, and calls <code>sids.instatiate</code> to expand the placeholders before returning the final strategy string.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/sid.py</code> <pre><code>class Sid(Translator):\n   \"\"\"Load a strategy definition from `solverpy_db/strats/` by strategy id.\n\n   The *strategy* passed into the solver during benchmark evaluation is a\n   strategy id (sid) \u2014 a filename inside `solverpy_db/strats/`.  This\n   translator loads the file contents (the actual solver CLI options string)\n   via [`sids.load`][solverpy.benchmark.path.sids.load].\n\n   **Parametric strategies**: if the loaded definition contains `@@@ var :\n   default @@@` placeholders, the sid may carry arguments in the form\n   `base_sid@var=val:var2=val2`.  The translator splits the sid, extracts the\n   argument assignments, and calls\n   [`sids.instatiate`][solverpy.benchmark.path.sids.instatiate] to expand the\n   placeholders before returning the final strategy string.\n   \"\"\"\n\n   def translate(\n      self,\n      instance: Any,\n      strategy: str,\n   ) -&gt; tuple[Any, str]:\n      \"\"\"Load and (if parametric) instantiate the strategy definition.\"\"\"\n      return (instance, sids.resolve(strategy, sids.load(strategy)))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.Sid.translate","title":"<code>translate(instance: Any, strategy: str) -&gt; tuple[Any, str]</code>","text":"<p>Load and (if parametric) instantiate the strategy definition.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/sid.py</code> <pre><code>def translate(\n   self,\n   instance: Any,\n   strategy: str,\n) -&gt; tuple[Any, str]:\n   \"\"\"Load and (if parametric) instantiate the strategy definition.\"\"\"\n   return (instance, sids.resolve(strategy, sids.load(strategy)))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/#solverpy.solver.plugins.db.extract","title":"<code>extract(output: str) -&gt; list[str]</code>","text":"<p>Extract unique TPTP formula names from a TPTP proof output.</p> <p>Scans the block between <code>SZS output start</code> and <code>SZS output end</code> for <code>file(&lt;filename&gt;, &lt;name&gt;)</code> annotations and returns the deduplicated list of formula names in first-occurrence order.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/proofs.py</code> <pre><code>def extract(output: str) -&gt; list[str]:\n   \"\"\"Extract unique TPTP formula names from a TPTP proof output.\n\n   Scans the block between ``SZS output start`` and ``SZS output end`` for\n   ``file(&lt;filename&gt;, &lt;name&gt;)`` annotations and returns the deduplicated list\n   of formula names in first-occurrence order.\n   \"\"\"\n   block = _szs_block(output)\n   return _names_from_block(block) if block else []\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/bid/","title":"module bid","text":""},{"location":"api/solverpy/solver/plugins/db/bid/#solverpy.solver.plugins.db.bid","title":"<code>solverpy.solver.plugins.db.bid</code>","text":""},{"location":"api/solverpy/solver/plugins/db/bid/#solverpy.solver.plugins.db.bid.Bid","title":"<code>Bid</code>","text":"<p>               Bases: <code>Translator</code></p> <p>Translate a <code>(bid, problem)</code> instance pair to the full problem file path.</p> <p>The instance passed into the solver during benchmark evaluation is a tuple <code>(bid, problem)</code> where <code>bid</code> is a benchmark directory id and <code>problem</code> is a relative problem name within that benchmark.  This translator unwraps the tuple and resolves it to an absolute file path via <code>bids.path</code>.</p> <p><code>Bid</code> must run before <code>Sid</code> in the translator chain (the default order established by <code>plugins.db()</code>).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/bid.py</code> <pre><code>class Bid(Translator):\n   \"\"\"Translate a `(bid, problem)` instance pair to the full problem file path.\n\n   The instance passed into the solver during benchmark evaluation is a tuple\n   `(bid, problem)` where `bid` is a benchmark directory id and `problem` is a\n   relative problem name within that benchmark.  This translator unwraps the\n   tuple and resolves it to an absolute file path via\n   [`bids.path`][solverpy.benchmark.path.bids.path].\n\n   `Bid` must run **before** `Sid` in the translator chain (the default order\n   established by [`plugins.db()`][solverpy.solver.plugins.db]).\n   \"\"\"\n\n   def __init__(self, **kwargs):\n      Translator.__init__(self, **kwargs)\n\n   def translate(\n      self,\n      instance: tuple[str, str],\n      strategy: Any\n   ) -&gt; tuple[str, Any]:\n      \"\"\"Resolve `(bid, problem)` \u2192 absolute file path.\"\"\"\n      (bid, problem) = instance\n      instance0 = bids.path(bid, problem)\n      return (instance0, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/bid/#solverpy.solver.plugins.db.bid.Bid.translate","title":"<code>translate(instance: tuple[str, str], strategy: Any) -&gt; tuple[str, Any]</code>","text":"<p>Resolve <code>(bid, problem)</code> \u2192 absolute file path.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/bid.py</code> <pre><code>def translate(\n   self,\n   instance: tuple[str, str],\n   strategy: Any\n) -&gt; tuple[str, Any]:\n   \"\"\"Resolve `(bid, problem)` \u2192 absolute file path.\"\"\"\n   (bid, problem) = instance\n   instance0 = bids.path(bid, problem)\n   return (instance0, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/eprovesid/","title":"module eprovesid","text":""},{"location":"api/solverpy/solver/plugins/db/eprovesid/#solverpy.solver.plugins.db.eprovesid","title":"<code>solverpy.solver.plugins.db.eprovesid</code>","text":""},{"location":"api/solverpy/solver/plugins/db/eprovesid/#solverpy.solver.plugins.db.eprovesid.EProverSid","title":"<code>EProverSid</code>","text":"<p>               Bases: <code>Translator</code></p> <p>Sid translator for E Prover supporting both old and new strategy formats.</p> <p>Old format (CLI options string): behaves identically to <code>Sid</code> \u2014 loads the file contents and returns them as the strategy string.</p> <p>New format (file starts with <code>{</code>): the file contains an E Prover strategy block produced by <code>--print-strategy</code>.  In this case the file path is passed to E via <code>--parse-strategy=&lt;path&gt;</code> instead of inlining the options.</p> <p>Parametric strategies (<code>@@@</code>) are not supported for new-format esids.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/eprovesid.py</code> <pre><code>class EProverSid(Translator):\n   \"\"\"Sid translator for E Prover supporting both old and new strategy formats.\n\n   Old format (CLI options string): behaves identically to\n   [`Sid`][solverpy.solver.plugins.db.sid.Sid] \u2014 loads the file contents and\n   returns them as the strategy string.\n\n   New format (file starts with `{`): the file contains an E Prover strategy\n   block produced by `--print-strategy`.  In this case the file path is passed\n   to E via `--parse-strategy=&lt;path&gt;` instead of inlining the options.\n\n   Parametric strategies (`@@@`) are not supported for new-format esids.\n   \"\"\"\n\n   def translate(\n      self,\n      instance: Any,\n      strategy: str,\n   ) -&gt; tuple[Any, str]:\n      \"\"\"Load strategy and return CLI options or `--parse-strategy=&lt;path&gt;`.\"\"\"\n      strategy0 = sids.load(strategy)\n      if strategy0.startswith(\"{\"):\n         assert \"@@@\" not in strategy0, \\\n            f\"Parametric strategies not supported for esid: {strategy}\"\n         return (instance, f\"--parse-strategy={sids.path(strategy)}\")\n      return (instance, sids.resolve(strategy, strategy0))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/eprovesid/#solverpy.solver.plugins.db.eprovesid.EProverSid.translate","title":"<code>translate(instance: Any, strategy: str) -&gt; tuple[Any, str]</code>","text":"<p>Load strategy and return CLI options or <code>--parse-strategy=&lt;path&gt;</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/eprovesid.py</code> <pre><code>def translate(\n   self,\n   instance: Any,\n   strategy: str,\n) -&gt; tuple[Any, str]:\n   \"\"\"Load strategy and return CLI options or `--parse-strategy=&lt;path&gt;`.\"\"\"\n   strategy0 = sids.load(strategy)\n   if strategy0.startswith(\"{\"):\n      assert \"@@@\" not in strategy0, \\\n         f\"Parametric strategies not supported for esid: {strategy}\"\n      return (instance, f\"--parse-strategy={sids.path(strategy)}\")\n   return (instance, sids.resolve(strategy, strategy0))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/errors/","title":"module errors","text":""},{"location":"api/solverpy/solver/plugins/db/errors/#solverpy.solver.plugins.db.errors","title":"<code>solverpy.solver.plugins.db.errors</code>","text":""},{"location":"api/solverpy/solver/plugins/db/errors/#solverpy.solver.plugins.db.errors.Errors","title":"<code>Errors</code>","text":"<p>               Bases: <code>Outputs</code></p> <p>Write raw solver output to <code>solverpy_db/errors/</code> for failed runs.</p> <p>Mirror of <code>Outputs</code> with the validity check inverted: output is written when <code>solver.valid(result)</code> is <code>False</code> (timeouts, errors, unknown results, etc.).</p> <p>Useful for debugging \u2014 inspect <code>solverpy_db/errors/</code> to see what the solver printed for problems it could not solve.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/errors.py</code> <pre><code>class Errors(Outputs):\n   \"\"\"Write raw solver output to `solverpy_db/errors/` for failed runs.\n\n   Mirror of [`Outputs`][solverpy.solver.plugins.db.outputs.Outputs] with the\n   validity check **inverted**: output is written when `solver.valid(result)` is\n   `False` (timeouts, errors, unknown results, etc.).\n\n   Useful for debugging \u2014 inspect `solverpy_db/errors/` to see what the solver\n   printed for problems it could not solve.\n   \"\"\"\n\n   def __init__(\n      self,\n      flatten: bool = True,\n      compress: bool = True,\n   ):\n      \"\"\"Args:\n         flatten: Replace `/` in problem paths with `_._`.\n         compress: Gzip-compress the written file.\n      \"\"\"\n      Outputs.__init__(self, flatten, compress)\n      self._path: str = bids.dbpath(NAME)\n\n   def finished(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Write *output* to disk if the run failed (validity check inverted).\"\"\"\n      if output and not self.solver.valid(result):\n         self.write(instance, strategy, output)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/errors/#solverpy.solver.plugins.db.errors.Errors.__init__","title":"<code>__init__(flatten: bool = True, compress: bool = True)</code>","text":"<p>Args: flatten: Replace <code>/</code> in problem paths with <code>_._</code>. compress: Gzip-compress the written file.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/errors.py</code> <pre><code>def __init__(\n   self,\n   flatten: bool = True,\n   compress: bool = True,\n):\n   \"\"\"Args:\n      flatten: Replace `/` in problem paths with `_._`.\n      compress: Gzip-compress the written file.\n   \"\"\"\n   Outputs.__init__(self, flatten, compress)\n   self._path: str = bids.dbpath(NAME)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/errors/#solverpy.solver.plugins.db.errors.Errors.finished","title":"<code>finished(instance: tuple[str, str], strategy: str, output: str, result: Result) -&gt; None</code>","text":"<p>Write output to disk if the run failed (validity check inverted).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/errors.py</code> <pre><code>def finished(\n   self,\n   instance: tuple[str, str],\n   strategy: str,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Write *output* to disk if the run failed (validity check inverted).\"\"\"\n   if output and not self.solver.valid(result):\n      self.write(instance, strategy, output)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/outputs/","title":"module outputs","text":""},{"location":"api/solverpy/solver/plugins/db/outputs/#solverpy.solver.plugins.db.outputs","title":"<code>solverpy.solver.plugins.db.outputs</code>","text":""},{"location":"api/solverpy/solver/plugins/db/outputs/#solverpy.solver.plugins.db.outputs.Outputs","title":"<code>Outputs</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Write raw solver output to <code>solverpy_db/outputs/</code> for successful runs.</p> <p>Activated in the <code>finished()</code> hook \u2014 after the result dict is complete \u2014 so the write only happens when <code>solver.valid(result)</code> is <code>True</code> (i.e. the solver actually solved the problem).</p> <p>The output file path is structured as: <code>solverpy_db/outputs/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;</code> (optionally <code>.gz</code>).</p> <p>When <code>flatten=True</code> (default), forward slashes in the problem path are replaced with <code>_._</code> to avoid deep directory nesting.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/outputs.py</code> <pre><code>class Outputs(Decorator):\n   \"\"\"Write raw solver output to `solverpy_db/outputs/` for successful runs.\n\n   Activated in the `finished()` hook \u2014 after the result dict is complete \u2014\n   so the write only happens when `solver.valid(result)` is `True` (i.e. the\n   solver actually solved the problem).\n\n   The output file path is structured as:\n   `solverpy_db/outputs/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;` (optionally `.gz`).\n\n   When `flatten=True` (default), forward slashes in the problem path are\n   replaced with `_._` to avoid deep directory nesting.\n   \"\"\"\n\n   def __init__(\n      self,\n      flatten: bool = True,\n      compress: bool = True,\n      pid: str | None = \"outputs\",\n   ):\n      \"\"\"Args:\n         flatten: Replace `/` in problem paths with `_._` so all output files\n            live in a single flat directory.  Pass a custom string to use a\n            different separator.\n         compress: Gzip-compress the written file (appends `.gz` to path).\n         pid: Optional plugin id for use with `solver.call()`.\n      \"\"\"\n      Decorator.__init__(\n         self,\n         flatten=flatten,\n         compress=compress,\n         pid=pid,\n      )\n      self._path = bids.dbpath(NAME)\n      self._flatten = flatten\n      self._compress = compress\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Append to decorators and store a reference to the solver.\"\"\"\n      solver.decorators.append(self)\n      self.solver = solver\n\n   def path(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n   ) -&gt; str:\n      \"\"\"Return the output file path (without `.gz` extension).\"\"\"\n      (bid, problem) = instance\n      bs = bids.name(bid, limit=self.solver._limits.limit)\n      if self._flatten:\n         slash = \"_._\" if (self._flatten is True) else self._flatten\n         problem = problem.replace(\"/\", slash)\n      p = os.path.join(self._path, bs, sids.name(strategy), problem)\n      return p\n\n   def finished(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n      output: str,\n      result: dict,\n   ) -&gt; None:\n      \"\"\"Write *output* to disk if the plugin is enabled and the run succeeded.\"\"\"\n      if not self._enabled:\n         return\n      if output and self.solver.valid(result):\n         self.write(instance, strategy, output)\n\n   def read(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n   ) -&gt; str:\n      \"\"\"Read and return the cached output for *(instance, strategy)*.\"\"\"\n      f = self.path(instance, strategy)\n      if self._compress:\n         f += \".gz\"\n      if not os.path.isfile(f):\n         raise FileNotFoundError(f\"No cached output: {f!r}\")\n      if self._compress:\n         with gzip.open(f, \"rt\") as fr:\n            return fr.read()\n      with open(f) as fr:\n         return fr.read()\n\n   def write(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n      content: str,\n   ) -&gt; None:\n      \"\"\"Write *content* to the output file, creating directories as needed.\"\"\"\n      if not self._enabled:\n         return\n      f = self.path(instance, strategy)\n      os.makedirs(os.path.dirname(f), exist_ok=True)\n      if self._compress:\n         with gzip.open(f + \".gz\", \"wb\") as fw:\n            fw.write(content.encode())\n      else:\n         with open(f, \"wb\") as fw:\n            fw.write(content.encode())\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/outputs/#solverpy.solver.plugins.db.outputs.Outputs.__init__","title":"<code>__init__(flatten: bool = True, compress: bool = True, pid: str | None = 'outputs')</code>","text":"<p>flatten: Replace <code>/</code> in problem paths with <code>_._</code> so all output files    live in a single flat directory.  Pass a custom string to use a    different separator. compress: Gzip-compress the written file (appends <code>.gz</code> to path). pid: Optional plugin id for use with <code>solver.call()</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/outputs.py</code> <pre><code>def __init__(\n   self,\n   flatten: bool = True,\n   compress: bool = True,\n   pid: str | None = \"outputs\",\n):\n   \"\"\"Args:\n      flatten: Replace `/` in problem paths with `_._` so all output files\n         live in a single flat directory.  Pass a custom string to use a\n         different separator.\n      compress: Gzip-compress the written file (appends `.gz` to path).\n      pid: Optional plugin id for use with `solver.call()`.\n   \"\"\"\n   Decorator.__init__(\n      self,\n      flatten=flatten,\n      compress=compress,\n      pid=pid,\n   )\n   self._path = bids.dbpath(NAME)\n   self._flatten = flatten\n   self._compress = compress\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/outputs/#solverpy.solver.plugins.db.outputs.Outputs.finished","title":"<code>finished(instance: tuple[str, str], strategy: str, output: str, result: dict) -&gt; None</code>","text":"<p>Write output to disk if the plugin is enabled and the run succeeded.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/outputs.py</code> <pre><code>def finished(\n   self,\n   instance: tuple[str, str],\n   strategy: str,\n   output: str,\n   result: dict,\n) -&gt; None:\n   \"\"\"Write *output* to disk if the plugin is enabled and the run succeeded.\"\"\"\n   if not self._enabled:\n      return\n   if output and self.solver.valid(result):\n      self.write(instance, strategy, output)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/outputs/#solverpy.solver.plugins.db.outputs.Outputs.path","title":"<code>path(instance: tuple[str, str], strategy: str) -&gt; str</code>","text":"<p>Return the output file path (without <code>.gz</code> extension).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/outputs.py</code> <pre><code>def path(\n   self,\n   instance: tuple[str, str],\n   strategy: str,\n) -&gt; str:\n   \"\"\"Return the output file path (without `.gz` extension).\"\"\"\n   (bid, problem) = instance\n   bs = bids.name(bid, limit=self.solver._limits.limit)\n   if self._flatten:\n      slash = \"_._\" if (self._flatten is True) else self._flatten\n      problem = problem.replace(\"/\", slash)\n   p = os.path.join(self._path, bs, sids.name(strategy), problem)\n   return p\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/outputs/#solverpy.solver.plugins.db.outputs.Outputs.read","title":"<code>read(instance: tuple[str, str], strategy: str) -&gt; str</code>","text":"<p>Read and return the cached output for (instance, strategy).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/outputs.py</code> <pre><code>def read(\n   self,\n   instance: tuple[str, str],\n   strategy: str,\n) -&gt; str:\n   \"\"\"Read and return the cached output for *(instance, strategy)*.\"\"\"\n   f = self.path(instance, strategy)\n   if self._compress:\n      f += \".gz\"\n   if not os.path.isfile(f):\n      raise FileNotFoundError(f\"No cached output: {f!r}\")\n   if self._compress:\n      with gzip.open(f, \"rt\") as fr:\n         return fr.read()\n   with open(f) as fr:\n      return fr.read()\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/outputs/#solverpy.solver.plugins.db.outputs.Outputs.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Append to decorators and store a reference to the solver.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/outputs.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Append to decorators and store a reference to the solver.\"\"\"\n   solver.decorators.append(self)\n   self.solver = solver\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/outputs/#solverpy.solver.plugins.db.outputs.Outputs.write","title":"<code>write(instance: tuple[str, str], strategy: str, content: str) -&gt; None</code>","text":"<p>Write content to the output file, creating directories as needed.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/outputs.py</code> <pre><code>def write(\n   self,\n   instance: tuple[str, str],\n   strategy: str,\n   content: str,\n) -&gt; None:\n   \"\"\"Write *content* to the output file, creating directories as needed.\"\"\"\n   if not self._enabled:\n      return\n   f = self.path(instance, strategy)\n   os.makedirs(os.path.dirname(f), exist_ok=True)\n   if self._compress:\n      with gzip.open(f + \".gz\", \"wb\") as fw:\n         fw.write(content.encode())\n   else:\n      with open(f, \"wb\") as fw:\n         fw.write(content.encode())\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/proofs/","title":"module proofs","text":""},{"location":"api/solverpy/solver/plugins/db/proofs/#solverpy.solver.plugins.db.proofs","title":"<code>solverpy.solver.plugins.db.proofs</code>","text":""},{"location":"api/solverpy/solver/plugins/db/proofs/#solverpy.solver.plugins.db.proofs.Proofs","title":"<code>Proofs</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Write TPTP proof and/or premise names to <code>solverpy_db/</code> subdirectories.</p> <p>Fires whenever the solver output contains an SZS proof block (between <code>% SZS output start</code> and <code>% SZS output end</code>).  Requires the instance to be a <code>(bid, problem)</code> tuple (i.e. used together with :class:<code>~solverpy.solver.plugins.db.bid.Bid</code>).</p> <p>Directory structure mirrors :class:<code>~solverpy.solver.plugins.db.outputs.Outputs</code>: <code>solverpy_db/{proofs,premises}/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;</code>.</p> <p>Parameters:</p> Name Type Description Default <code>proof</code> <code>bool</code> <p>Write the raw proof block to <code>solverpy_db/proofs/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;</code>.</p> <code>True</code> <code>premises</code> <code>bool</code> <p>Write deduplicated formula names (one per line) from <code>file(&lt;filename&gt;, &lt;name&gt;)</code> annotations to <code>solverpy_db/premises/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;</code>.</p> <code>False</code> <code>flatten</code> <code>bool | str</code> <p>Replace <code>/</code> in problem paths with <code>_._</code> (or a custom string) so all files live in a single flat directory.</p> <code>True</code> <code>compress</code> <code>bool</code> <p>Gzip-compress proof files (appends <code>.gz</code>).  Premises files are never compressed.</p> <code>True</code> <code>pid</code> <code>str</code> <p>Plugin id for use with <code>solver.call()</code>.</p> <code>'proofs'</code> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/proofs.py</code> <pre><code>class Proofs(Decorator):\n   \"\"\"Write TPTP proof and/or premise names to ``solverpy_db/`` subdirectories.\n\n   Fires whenever the solver output contains an SZS proof block (between\n   ``% SZS output start`` and ``% SZS output end``).  Requires the instance to be a ``(bid, problem)`` tuple (i.e. used\n   together with :class:`~solverpy.solver.plugins.db.bid.Bid`).\n\n   Directory structure mirrors\n   :class:`~solverpy.solver.plugins.db.outputs.Outputs`:\n   ``solverpy_db/{proofs,premises}/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;``.\n\n   Args:\n      proof: Write the raw proof block to ``solverpy_db/proofs/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;``.\n      premises: Write deduplicated formula names (one per line) from\n         ``file(&lt;filename&gt;, &lt;name&gt;)`` annotations to\n         ``solverpy_db/premises/&lt;bid+limit&gt;/&lt;sid&gt;/&lt;problem&gt;``.\n      flatten: Replace ``/`` in problem paths with ``_._`` (or a custom\n         string) so all files live in a single flat directory.\n      compress: Gzip-compress proof files (appends ``.gz``).  Premises files\n         are never compressed.\n      pid: Plugin id for use with ``solver.call()``.\n   \"\"\"\n\n   def __init__(\n      self,\n      proof: bool = True,\n      premises: bool = False,\n      flatten: bool | str = True,\n      compress: bool = True,\n      pid: str = \"proofs\",\n   ):\n      Decorator.__init__(\n         self,\n         proof=proof,\n         premises=premises,\n         flatten=flatten,\n         compress=compress,\n         pid=pid,\n      )\n      self._proof = proof\n      self._premises = premises\n      self._flatten = flatten\n      self._compress = compress\n      self._proofs_path = bids.dbpath(\"proofs\")\n      self._premises_path = bids.dbpath(\"premises\")\n      self._limit: str = \"\"\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      solver.decorators.append(self)\n      self._limit = solver._limits.limit\n\n   def proof_path(self, instance: tuple[str, str], strategy: str) -&gt; str:\n      \"\"\"Return the proof file path (without ``.gz`` when compress is on).\"\"\"\n      (bid, problem) = instance\n      if self._flatten:\n         sep = \"_._\" if self._flatten is True else self._flatten\n         problem = problem.replace(\"/\", sep)\n      return os.path.join(\n         self._proofs_path,\n         bids.name(bid, limit=self._limit),\n         sids.name(strategy),\n         problem,\n      )\n\n   def premises_path(self, instance: tuple[str, str], strategy: str) -&gt; str:\n      \"\"\"Return the premises file path.\"\"\"\n      (bid, problem) = instance\n      if self._flatten:\n         sep = \"_._\" if self._flatten is True else self._flatten\n         problem = problem.replace(\"/\", sep)\n      return os.path.join(\n         self._premises_path,\n         bids.name(bid, limit=self._limit),\n         sids.name(strategy),\n         problem,\n      )\n\n   def update(\n      self,\n      instance,\n      strategy: str,\n      output: str,\n      result: dict,\n   ) -&gt; None:\n      if not self._enabled:\n         return\n      if not isinstance(instance, tuple):\n         return\n      block = _szs_block(output or \"\")\n      if not block:\n         return\n      if self._proof:\n         p = self.proof_path(instance, strategy)\n         if self._compress:\n            _write_gz(p, block)\n         else:\n            _write_plain(p, block)\n      if self._premises:\n         names = _names_from_block(block)\n         if names:\n            _write_plain(self.premises_path(instance, strategy),\n                         \"\\n\".join(sorted(names)) + \"\\n\")\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/proofs/#solverpy.solver.plugins.db.proofs.Proofs.premises_path","title":"<code>premises_path(instance: tuple[str, str], strategy: str) -&gt; str</code>","text":"<p>Return the premises file path.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/proofs.py</code> <pre><code>def premises_path(self, instance: tuple[str, str], strategy: str) -&gt; str:\n   \"\"\"Return the premises file path.\"\"\"\n   (bid, problem) = instance\n   if self._flatten:\n      sep = \"_._\" if self._flatten is True else self._flatten\n      problem = problem.replace(\"/\", sep)\n   return os.path.join(\n      self._premises_path,\n      bids.name(bid, limit=self._limit),\n      sids.name(strategy),\n      problem,\n   )\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/proofs/#solverpy.solver.plugins.db.proofs.Proofs.proof_path","title":"<code>proof_path(instance: tuple[str, str], strategy: str) -&gt; str</code>","text":"<p>Return the proof file path (without <code>.gz</code> when compress is on).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/proofs.py</code> <pre><code>def proof_path(self, instance: tuple[str, str], strategy: str) -&gt; str:\n   \"\"\"Return the proof file path (without ``.gz`` when compress is on).\"\"\"\n   (bid, problem) = instance\n   if self._flatten:\n      sep = \"_._\" if self._flatten is True else self._flatten\n      problem = problem.replace(\"/\", sep)\n   return os.path.join(\n      self._proofs_path,\n      bids.name(bid, limit=self._limit),\n      sids.name(strategy),\n      problem,\n   )\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/proofs/#solverpy.solver.plugins.db.proofs.extract","title":"<code>extract(output: str) -&gt; list[str]</code>","text":"<p>Extract unique TPTP formula names from a TPTP proof output.</p> <p>Scans the block between <code>SZS output start</code> and <code>SZS output end</code> for <code>file(&lt;filename&gt;, &lt;name&gt;)</code> annotations and returns the deduplicated list of formula names in first-occurrence order.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/proofs.py</code> <pre><code>def extract(output: str) -&gt; list[str]:\n   \"\"\"Extract unique TPTP formula names from a TPTP proof output.\n\n   Scans the block between ``SZS output start`` and ``SZS output end`` for\n   ``file(&lt;filename&gt;, &lt;name&gt;)`` annotations and returns the deduplicated list\n   of formula names in first-occurrence order.\n   \"\"\"\n   block = _szs_block(output)\n   return _names_from_block(block) if block else []\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/sid/","title":"module sid","text":""},{"location":"api/solverpy/solver/plugins/db/sid/#solverpy.solver.plugins.db.sid","title":"<code>solverpy.solver.plugins.db.sid</code>","text":""},{"location":"api/solverpy/solver/plugins/db/sid/#solverpy.solver.plugins.db.sid.Sid","title":"<code>Sid</code>","text":"<p>               Bases: <code>Translator</code></p> <p>Load a strategy definition from <code>solverpy_db/strats/</code> by strategy id.</p> <p>The strategy passed into the solver during benchmark evaluation is a strategy id (sid) \u2014 a filename inside <code>solverpy_db/strats/</code>.  This translator loads the file contents (the actual solver CLI options string) via <code>sids.load</code>.</p> <p>Parametric strategies: if the loaded definition contains <code>@@@ var : default @@@</code> placeholders, the sid may carry arguments in the form <code>base_sid@var=val:var2=val2</code>.  The translator splits the sid, extracts the argument assignments, and calls <code>sids.instatiate</code> to expand the placeholders before returning the final strategy string.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/sid.py</code> <pre><code>class Sid(Translator):\n   \"\"\"Load a strategy definition from `solverpy_db/strats/` by strategy id.\n\n   The *strategy* passed into the solver during benchmark evaluation is a\n   strategy id (sid) \u2014 a filename inside `solverpy_db/strats/`.  This\n   translator loads the file contents (the actual solver CLI options string)\n   via [`sids.load`][solverpy.benchmark.path.sids.load].\n\n   **Parametric strategies**: if the loaded definition contains `@@@ var :\n   default @@@` placeholders, the sid may carry arguments in the form\n   `base_sid@var=val:var2=val2`.  The translator splits the sid, extracts the\n   argument assignments, and calls\n   [`sids.instatiate`][solverpy.benchmark.path.sids.instatiate] to expand the\n   placeholders before returning the final strategy string.\n   \"\"\"\n\n   def translate(\n      self,\n      instance: Any,\n      strategy: str,\n   ) -&gt; tuple[Any, str]:\n      \"\"\"Load and (if parametric) instantiate the strategy definition.\"\"\"\n      return (instance, sids.resolve(strategy, sids.load(strategy)))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/db/sid/#solverpy.solver.plugins.db.sid.Sid.translate","title":"<code>translate(instance: Any, strategy: str) -&gt; tuple[Any, str]</code>","text":"<p>Load and (if parametric) instantiate the strategy definition.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/db/sid.py</code> <pre><code>def translate(\n   self,\n   instance: Any,\n   strategy: str,\n) -&gt; tuple[Any, str]:\n   \"\"\"Load and (if parametric) instantiate the strategy definition.\"\"\"\n   return (instance, sids.resolve(strategy, sids.load(strategy)))\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/","title":"Shell Overview","text":""},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell","title":"<code>solverpy.solver.plugins.shell</code>","text":""},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell--shell-plugins","title":"Shell plugins","text":"<p>Shell plugins control how the solver subprocess is launched: building resource-limit arguments, wrapping the command with <code>timeout</code>, and measuring wall-clock time with <code>/usr/bin/env time</code>.</p> <p> </p> Plugin Description <code>Limits</code> Parses the limit string (e.g. <code>T5-M2048</code>) and appends solver-specific CLI flags; can also prepend limits to the strategy string <code>Timeout</code> Prepends <code>timeout --kill-after=15 --foreground N</code> to enforce a hard wall-clock cutoff <code>Time</code> Prepends <code>/usr/bin/env time -p</code> to measure real/user/sys time; populates <code>runtime</code> in the result <code>Memory</code> Prepends <code>ulimit -Sv N</code> to cap virtual memory before the solver process starts"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Limits","title":"<code>Limits</code>","text":"<p>               Bases: <code>Decorator</code>, <code>Translator</code></p> <p>Parse a resource-limit string and apply solver-specific CLI flags.</p> <p><code>Limits</code> can operate as either a <code>Decorator</code> or a <code>Translator</code> depending on the <code>cmdline</code> flag:</p> <ul> <li><code>cmdline=True</code> (default) \u2014 registers as a decorator; <code>decorate()</code> appends   the constructed flags to the shell command (e.g. <code>--cpu-limit=10</code>).</li> <li><code>cmdline=False</code> \u2014 registers as a translator; <code>translate()</code> prepends the   flags to the strategy string so the solver receives them as part of its   options input.</li> </ul> <p>The limit string uses the format <code>T&lt;seconds&gt;[-M&lt;gigabytes&gt;]</code>, e.g.:</p> <ul> <li><code>\"T10\"</code> \u2014 10-second CPU/wall-clock limit</li> <li><code>\"T10-M4\"</code> \u2014 10 seconds and 4 GB memory limit</li> </ul> <p>Each flag letter is mapped to a CLI template via the <code>builder</code> dict provided by the concrete solver subclass.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/limits.py</code> <pre><code>class Limits(Decorator, Translator):\n   \"\"\"Parse a resource-limit string and apply solver-specific CLI flags.\n\n   `Limits` can operate as either a\n   [`Decorator`][solverpy.solver.plugins.decorator.Decorator] or a\n   [`Translator`][solverpy.solver.plugins.translator.Translator] depending on\n   the `cmdline` flag:\n\n   - `cmdline=True` *(default)* \u2014 registers as a decorator; `decorate()` appends\n     the constructed flags to the shell command (e.g. `--cpu-limit=10`).\n   - `cmdline=False` \u2014 registers as a translator; `translate()` prepends the\n     flags to the strategy string so the solver receives them as part of its\n     options input.\n\n   The limit string uses the format `T&lt;seconds&gt;[-M&lt;gigabytes&gt;]`, e.g.:\n\n   - `\"T10\"` \u2014 10-second CPU/wall-clock limit\n   - `\"T10-M4\"` \u2014 10 seconds and 4 GB memory limit\n\n   Each flag letter is mapped to a CLI template via the `builder` dict provided\n   by the concrete solver subclass.\n   \"\"\"\n\n   def __init__(\n      self,\n      limit: str,\n      builder: \"LimitBuilder\",\n      cmdline: bool = True,\n      inputfile: bool = False,\n   ):\n      Plugin.__init__(\n         self,\n         limit=limit,\n         cmdline=cmdline,\n      )\n      lims = {x[0]: x[1:] for x in limit.split(\"-\") if x}\n      assert \"T\" in lims\n      self.timeout = int(lims[\"T\"])\n      self.memory = float(lims[\"M\"]) if \"M\" in lims else None\n      try:\n         lims = [\n            build(builder[x], y) for (x, y) in lims.items() if x in builder\n         ]\n      except Exception as e:\n         print(e)\n         raise Exception(f\"solverpy: Invalid limit string: {limit}\")\n\n      delim = \" \" if cmdline else \"\"\n      self.strat = delim.join(lims)\n      self.cmdline = cmdline\n\n      #self.args = \" \".join(lims)\n      self.limit = limit\n      self._inputfile = inputfile\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Register as a decorator or translator depending on `cmdline`.\"\"\"\n      if self.cmdline:\n         solver.decorators.append(self)\n      else:\n         solver.translators.append(self)\n\n   def __str__(self) -&gt; str:\n      return self.limit\n\n   def __lt__(self, other: \"Limits\") -&gt; bool | None:\n      if self.limit and not other.limit:\n         return None\n      if self.memory and not other.memory:\n         return None\n      if not self.memory:\n         return (self.timeout &lt; other.timeout)\n      else:\n         return (self.timeout &lt; other.timeout) or (self.memory &lt; other.memory)\n\n   #def __le__(self, other):\n   #   return (self.key == other.key) or (self &lt; other)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Append limit flags to *cmd* when `cmdline=True` (no-op otherwise).\n\n      An optional `/dev/stdin` suffix is appended when `inputfile=True` so that\n      solvers that read the problem from stdin receive the correct filename.\n      \"\"\"\n      del instance, strategy  # unused arguments\n      if self.cmdline:\n         input = \" /dev/stdin\" if self._inputfile else \"\"\n         return f\"{cmd} {self.strategy}{input}\" if self.strategy else cmd\n      else:\n         return cmd\n\n   def translate(\n      self,\n      instance: Any,\n      strategy: str,\n   ) -&gt; tuple[Any, str]:\n      \"\"\"Prepend limit flags to *strategy* when `cmdline=False` (no-op otherwise).\"\"\"\n      if not self.cmdline:\n         return (instance, self.strategy + strategy)\n      else:\n         return (instance, strategy)\n\n   @property\n   def strategy(self) -&gt; str:\n      \"\"\"The constructed CLI flag string derived from the limit string.\"\"\"\n      return self.strat\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Limits.strategy","title":"<code>strategy: str</code>  <code>property</code>","text":"<p>The constructed CLI flag string derived from the limit string.</p>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Limits.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Append limit flags to cmd when <code>cmdline=True</code> (no-op otherwise).</p> <p>An optional <code>/dev/stdin</code> suffix is appended when <code>inputfile=True</code> so that solvers that read the problem from stdin receive the correct filename.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/limits.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Append limit flags to *cmd* when `cmdline=True` (no-op otherwise).\n\n   An optional `/dev/stdin` suffix is appended when `inputfile=True` so that\n   solvers that read the problem from stdin receive the correct filename.\n   \"\"\"\n   del instance, strategy  # unused arguments\n   if self.cmdline:\n      input = \" /dev/stdin\" if self._inputfile else \"\"\n      return f\"{cmd} {self.strategy}{input}\" if self.strategy else cmd\n   else:\n      return cmd\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Limits.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Register as a decorator or translator depending on <code>cmdline</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/limits.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Register as a decorator or translator depending on `cmdline`.\"\"\"\n   if self.cmdline:\n      solver.decorators.append(self)\n   else:\n      solver.translators.append(self)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Limits.translate","title":"<code>translate(instance: Any, strategy: str) -&gt; tuple[Any, str]</code>","text":"<p>Prepend limit flags to strategy when <code>cmdline=False</code> (no-op otherwise).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/limits.py</code> <pre><code>def translate(\n   self,\n   instance: Any,\n   strategy: str,\n) -&gt; tuple[Any, str]:\n   \"\"\"Prepend limit flags to *strategy* when `cmdline=False` (no-op otherwise).\"\"\"\n   if not self.cmdline:\n      return (instance, self.strategy + strategy)\n   else:\n      return (instance, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Memory","title":"<code>Memory</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Limit virtual memory using <code>ulimit -Sv</code> before launching the solver.</p> <p>Prepends <code>ulimit -Sv &lt;kilobytes&gt; &amp;&amp;</code> to the shell command.  The limit is set in kilobytes as <code>giga * 1024 * 1024 + 1024</code> to allow a small overhead above the nominal gigabyte value.</p> <p>Note: <code>ulimit -Sv</code> caps virtual memory (VSZ), not resident memory (RSS). For stricter RSS limits consider a <code>cgroup</code>-based approach instead.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/memory.py</code> <pre><code>class Memory(Decorator):\n   \"\"\"Limit virtual memory using `ulimit -Sv` before launching the solver.\n\n   Prepends `ulimit -Sv &lt;kilobytes&gt; &amp;&amp;` to the shell command.  The limit is\n   set in kilobytes as `giga * 1024 * 1024 + 1024` to allow a small overhead\n   above the nominal gigabyte value.\n\n   Note: `ulimit -Sv` caps *virtual* memory (VSZ), not resident memory (RSS).\n   For stricter RSS limits consider a `cgroup`-based approach instead.\n   \"\"\"\n\n   def __init__(self, giga: float = 4):\n      \"\"\"Args:\n         giga: Virtual memory limit in gigabytes (default 4 GB).\n      \"\"\"\n      Decorator.__init__(self, giga=giga)\n      self.prefix = ULIMIT_CMD % (int(giga * 1024 * 1024) + 1024)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Prepend `ulimit -Sv &lt;N&gt; &amp;&amp;` to *cmd*.\"\"\"\n      del instance, strategy\n      return f\"{self.prefix} &amp;&amp; {cmd}\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Memory.__init__","title":"<code>__init__(giga: float = 4)</code>","text":"<p>Args: giga: Virtual memory limit in gigabytes (default 4 GB).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/memory.py</code> <pre><code>def __init__(self, giga: float = 4):\n   \"\"\"Args:\n      giga: Virtual memory limit in gigabytes (default 4 GB).\n   \"\"\"\n   Decorator.__init__(self, giga=giga)\n   self.prefix = ULIMIT_CMD % (int(giga * 1024 * 1024) + 1024)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Memory.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Prepend <code>ulimit -Sv &lt;N&gt; &amp;&amp;</code> to cmd.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/memory.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Prepend `ulimit -Sv &lt;N&gt; &amp;&amp;` to *cmd*.\"\"\"\n   del instance, strategy\n   return f\"{self.prefix} &amp;&amp; {cmd}\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Time","title":"<code>Time</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Measure wall-clock, user, and sys time using Python's stdlib.</p> <p>Snapshots <code>time.perf_counter()</code> and <code>resource.getrusage(RUSAGE_CHILDREN)</code> in <code>decorate()</code> (just before the subprocess) and computes deltas in <code>update()</code> (just after).  Three keys are added to the result:</p> <ul> <li><code>realtime</code> \u2014 elapsed wall-clock time (seconds)</li> <li><code>usertime</code> \u2014 user-mode CPU time of child processes (seconds)</li> <li><code>systime</code>  \u2014 kernel-mode CPU time of child processes (seconds)</li> <li><code>runtime</code>  \u2014 <code>realtime - systime</code> (canonical solve time)</li> </ul> <p>The solver command is not modified, so no timing lines appear in the raw solver output.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/time.py</code> <pre><code>class Time(Decorator):\n   \"\"\"Measure wall-clock, user, and sys time using Python's stdlib.\n\n   Snapshots `time.perf_counter()` and `resource.getrusage(RUSAGE_CHILDREN)`\n   in `decorate()` (just before the subprocess) and computes deltas in\n   `update()` (just after).  Three keys are added to the result:\n\n   - `realtime` \u2014 elapsed wall-clock time (seconds)\n   - `usertime` \u2014 user-mode CPU time of child processes (seconds)\n   - `systime`  \u2014 kernel-mode CPU time of child processes (seconds)\n   - `runtime`  \u2014 `realtime - systime` (canonical solve time)\n\n   The solver command is not modified, so no timing lines appear in the\n   raw solver output.\n   \"\"\"\n\n   def __init__(self, **kwargs):\n      Decorator.__init__(self, **kwargs)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Snapshot start times; return *cmd* unchanged.\"\"\"\n      del instance, strategy  # unused arguments\n      self._t0_wall = _time.perf_counter()\n      r = resource.getrusage(resource.RUSAGE_CHILDREN)\n      self._t0_user = r.ru_utime\n      self._t0_sys = r.ru_stime\n      return cmd\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Compute time deltas and populate *result*.\"\"\"\n      del instance, strategy, output  # unused arguments\n      wall = _time.perf_counter() - self._t0_wall\n      r = resource.getrusage(resource.RUSAGE_CHILDREN)\n      user = r.ru_utime - self._t0_user\n      sys_ = r.ru_stime - self._t0_sys\n      result[\"realtime\"] = wall\n      result[\"usertime\"] = user\n      result[\"systime\"] = sys_\n      result[\"runtime\"] = wall - sys_\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Time.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Snapshot start times; return cmd unchanged.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/time.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Snapshot start times; return *cmd* unchanged.\"\"\"\n   del instance, strategy  # unused arguments\n   self._t0_wall = _time.perf_counter()\n   r = resource.getrusage(resource.RUSAGE_CHILDREN)\n   self._t0_user = r.ru_utime\n   self._t0_sys = r.ru_stime\n   return cmd\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Time.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Compute time deltas and populate result.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/time.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Compute time deltas and populate *result*.\"\"\"\n   del instance, strategy, output  # unused arguments\n   wall = _time.perf_counter() - self._t0_wall\n   r = resource.getrusage(resource.RUSAGE_CHILDREN)\n   user = r.ru_utime - self._t0_user\n   sys_ = r.ru_stime - self._t0_sys\n   result[\"realtime\"] = wall\n   result[\"usertime\"] = user\n   result[\"systime\"] = sys_\n   result[\"runtime\"] = wall - sys_\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Timeout","title":"<code>Timeout</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Enforce a hard wall-clock cutoff using the GNU <code>timeout</code> command.</p> <p>Prepends <code>timeout --kill-after=15 --foreground &lt;N&gt;</code> to the solver command. If the solver does not finish within <code>N</code> seconds, <code>timeout</code> sends <code>SIGTERM</code>; after a further 15 seconds it sends <code>SIGKILL</code>.</p> <p>This decorator inserts itself at position 0 so it becomes the outermost wrapper \u2014 it fires before any other decorator modifies the command.</p> <p>In <code>update()</code> it sets <code>result[\"status\"] = \"TIMEOUT\"</code> when the subprocess exits with code 124 (killed by <code>timeout</code>) or 137 (killed by <code>SIGKILL</code>).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>class Timeout(Decorator):\n   \"\"\"Enforce a hard wall-clock cutoff using the GNU `timeout` command.\n\n   Prepends `timeout --kill-after=15 --foreground &lt;N&gt;` to the solver command.\n   If the solver does not finish within `N` seconds, `timeout` sends `SIGTERM`;\n   after a further 15 seconds it sends `SIGKILL`.\n\n   This decorator inserts itself at **position 0** so it becomes the outermost\n   wrapper \u2014 it fires before any other decorator modifies the command.\n\n   In `update()` it sets `result[\"status\"] = \"TIMEOUT\"` when the subprocess\n   exits with code 124 (killed by `timeout`) or 137 (killed by `SIGKILL`).\n   \"\"\"\n\n   def __init__(self, timeout: int):\n      \"\"\"Args:\n         timeout: Wall-clock limit in seconds.\n      \"\"\"\n      Decorator.__init__(self, timeout=timeout)\n      self.timeout = timeout\n      self.prefix = TIMEOUT_CMD % timeout\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Insert at position 0 of `solver.decorators` (outermost wrapper).\"\"\"\n      self.solver = solver\n      solver.decorators.insert(0, self)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Prepend the `timeout` command to *cmd*.\"\"\"\n      del instance, strategy # unused arguments\n      return f\"{self.prefix} {cmd}\"\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Set `status = \"TIMEOUT\"` if the process was killed by the timeout.\"\"\"\n      del instance, strategy, output # unused arguments\n      if self.solver._exitcode in [124, 137]:\n         # killed by timeout or SIGKILL\n         result[\"status\"] = \"TIMEOUT\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Timeout.__init__","title":"<code>__init__(timeout: int)</code>","text":"<p>Args: timeout: Wall-clock limit in seconds.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>def __init__(self, timeout: int):\n   \"\"\"Args:\n      timeout: Wall-clock limit in seconds.\n   \"\"\"\n   Decorator.__init__(self, timeout=timeout)\n   self.timeout = timeout\n   self.prefix = TIMEOUT_CMD % timeout\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Timeout.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Prepend the <code>timeout</code> command to cmd.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Prepend the `timeout` command to *cmd*.\"\"\"\n   del instance, strategy # unused arguments\n   return f\"{self.prefix} {cmd}\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Timeout.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Insert at position 0 of <code>solver.decorators</code> (outermost wrapper).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Insert at position 0 of `solver.decorators` (outermost wrapper).\"\"\"\n   self.solver = solver\n   solver.decorators.insert(0, self)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/#solverpy.solver.plugins.shell.Timeout.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Set <code>status = \"TIMEOUT\"</code> if the process was killed by the timeout.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Set `status = \"TIMEOUT\"` if the process was killed by the timeout.\"\"\"\n   del instance, strategy, output # unused arguments\n   if self.solver._exitcode in [124, 137]:\n      # killed by timeout or SIGKILL\n      result[\"status\"] = \"TIMEOUT\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/limits/","title":"module limits","text":""},{"location":"api/solverpy/solver/plugins/shell/limits/#solverpy.solver.plugins.shell.limits","title":"<code>solverpy.solver.plugins.shell.limits</code>","text":""},{"location":"api/solverpy/solver/plugins/shell/limits/#solverpy.solver.plugins.shell.limits.Limits","title":"<code>Limits</code>","text":"<p>               Bases: <code>Decorator</code>, <code>Translator</code></p> <p>Parse a resource-limit string and apply solver-specific CLI flags.</p> <p><code>Limits</code> can operate as either a <code>Decorator</code> or a <code>Translator</code> depending on the <code>cmdline</code> flag:</p> <ul> <li><code>cmdline=True</code> (default) \u2014 registers as a decorator; <code>decorate()</code> appends   the constructed flags to the shell command (e.g. <code>--cpu-limit=10</code>).</li> <li><code>cmdline=False</code> \u2014 registers as a translator; <code>translate()</code> prepends the   flags to the strategy string so the solver receives them as part of its   options input.</li> </ul> <p>The limit string uses the format <code>T&lt;seconds&gt;[-M&lt;gigabytes&gt;]</code>, e.g.:</p> <ul> <li><code>\"T10\"</code> \u2014 10-second CPU/wall-clock limit</li> <li><code>\"T10-M4\"</code> \u2014 10 seconds and 4 GB memory limit</li> </ul> <p>Each flag letter is mapped to a CLI template via the <code>builder</code> dict provided by the concrete solver subclass.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/limits.py</code> <pre><code>class Limits(Decorator, Translator):\n   \"\"\"Parse a resource-limit string and apply solver-specific CLI flags.\n\n   `Limits` can operate as either a\n   [`Decorator`][solverpy.solver.plugins.decorator.Decorator] or a\n   [`Translator`][solverpy.solver.plugins.translator.Translator] depending on\n   the `cmdline` flag:\n\n   - `cmdline=True` *(default)* \u2014 registers as a decorator; `decorate()` appends\n     the constructed flags to the shell command (e.g. `--cpu-limit=10`).\n   - `cmdline=False` \u2014 registers as a translator; `translate()` prepends the\n     flags to the strategy string so the solver receives them as part of its\n     options input.\n\n   The limit string uses the format `T&lt;seconds&gt;[-M&lt;gigabytes&gt;]`, e.g.:\n\n   - `\"T10\"` \u2014 10-second CPU/wall-clock limit\n   - `\"T10-M4\"` \u2014 10 seconds and 4 GB memory limit\n\n   Each flag letter is mapped to a CLI template via the `builder` dict provided\n   by the concrete solver subclass.\n   \"\"\"\n\n   def __init__(\n      self,\n      limit: str,\n      builder: \"LimitBuilder\",\n      cmdline: bool = True,\n      inputfile: bool = False,\n   ):\n      Plugin.__init__(\n         self,\n         limit=limit,\n         cmdline=cmdline,\n      )\n      lims = {x[0]: x[1:] for x in limit.split(\"-\") if x}\n      assert \"T\" in lims\n      self.timeout = int(lims[\"T\"])\n      self.memory = float(lims[\"M\"]) if \"M\" in lims else None\n      try:\n         lims = [\n            build(builder[x], y) for (x, y) in lims.items() if x in builder\n         ]\n      except Exception as e:\n         print(e)\n         raise Exception(f\"solverpy: Invalid limit string: {limit}\")\n\n      delim = \" \" if cmdline else \"\"\n      self.strat = delim.join(lims)\n      self.cmdline = cmdline\n\n      #self.args = \" \".join(lims)\n      self.limit = limit\n      self._inputfile = inputfile\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Register as a decorator or translator depending on `cmdline`.\"\"\"\n      if self.cmdline:\n         solver.decorators.append(self)\n      else:\n         solver.translators.append(self)\n\n   def __str__(self) -&gt; str:\n      return self.limit\n\n   def __lt__(self, other: \"Limits\") -&gt; bool | None:\n      if self.limit and not other.limit:\n         return None\n      if self.memory and not other.memory:\n         return None\n      if not self.memory:\n         return (self.timeout &lt; other.timeout)\n      else:\n         return (self.timeout &lt; other.timeout) or (self.memory &lt; other.memory)\n\n   #def __le__(self, other):\n   #   return (self.key == other.key) or (self &lt; other)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Append limit flags to *cmd* when `cmdline=True` (no-op otherwise).\n\n      An optional `/dev/stdin` suffix is appended when `inputfile=True` so that\n      solvers that read the problem from stdin receive the correct filename.\n      \"\"\"\n      del instance, strategy  # unused arguments\n      if self.cmdline:\n         input = \" /dev/stdin\" if self._inputfile else \"\"\n         return f\"{cmd} {self.strategy}{input}\" if self.strategy else cmd\n      else:\n         return cmd\n\n   def translate(\n      self,\n      instance: Any,\n      strategy: str,\n   ) -&gt; tuple[Any, str]:\n      \"\"\"Prepend limit flags to *strategy* when `cmdline=False` (no-op otherwise).\"\"\"\n      if not self.cmdline:\n         return (instance, self.strategy + strategy)\n      else:\n         return (instance, strategy)\n\n   @property\n   def strategy(self) -&gt; str:\n      \"\"\"The constructed CLI flag string derived from the limit string.\"\"\"\n      return self.strat\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/limits/#solverpy.solver.plugins.shell.limits.Limits.strategy","title":"<code>strategy: str</code>  <code>property</code>","text":"<p>The constructed CLI flag string derived from the limit string.</p>"},{"location":"api/solverpy/solver/plugins/shell/limits/#solverpy.solver.plugins.shell.limits.Limits.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Append limit flags to cmd when <code>cmdline=True</code> (no-op otherwise).</p> <p>An optional <code>/dev/stdin</code> suffix is appended when <code>inputfile=True</code> so that solvers that read the problem from stdin receive the correct filename.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/limits.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Append limit flags to *cmd* when `cmdline=True` (no-op otherwise).\n\n   An optional `/dev/stdin` suffix is appended when `inputfile=True` so that\n   solvers that read the problem from stdin receive the correct filename.\n   \"\"\"\n   del instance, strategy  # unused arguments\n   if self.cmdline:\n      input = \" /dev/stdin\" if self._inputfile else \"\"\n      return f\"{cmd} {self.strategy}{input}\" if self.strategy else cmd\n   else:\n      return cmd\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/limits/#solverpy.solver.plugins.shell.limits.Limits.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Register as a decorator or translator depending on <code>cmdline</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/limits.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Register as a decorator or translator depending on `cmdline`.\"\"\"\n   if self.cmdline:\n      solver.decorators.append(self)\n   else:\n      solver.translators.append(self)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/limits/#solverpy.solver.plugins.shell.limits.Limits.translate","title":"<code>translate(instance: Any, strategy: str) -&gt; tuple[Any, str]</code>","text":"<p>Prepend limit flags to strategy when <code>cmdline=False</code> (no-op otherwise).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/limits.py</code> <pre><code>def translate(\n   self,\n   instance: Any,\n   strategy: str,\n) -&gt; tuple[Any, str]:\n   \"\"\"Prepend limit flags to *strategy* when `cmdline=False` (no-op otherwise).\"\"\"\n   if not self.cmdline:\n      return (instance, self.strategy + strategy)\n   else:\n      return (instance, strategy)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/memory/","title":"module memory","text":""},{"location":"api/solverpy/solver/plugins/shell/memory/#solverpy.solver.plugins.shell.memory","title":"<code>solverpy.solver.plugins.shell.memory</code>","text":""},{"location":"api/solverpy/solver/plugins/shell/memory/#solverpy.solver.plugins.shell.memory.Memory","title":"<code>Memory</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Limit virtual memory using <code>ulimit -Sv</code> before launching the solver.</p> <p>Prepends <code>ulimit -Sv &lt;kilobytes&gt; &amp;&amp;</code> to the shell command.  The limit is set in kilobytes as <code>giga * 1024 * 1024 + 1024</code> to allow a small overhead above the nominal gigabyte value.</p> <p>Note: <code>ulimit -Sv</code> caps virtual memory (VSZ), not resident memory (RSS). For stricter RSS limits consider a <code>cgroup</code>-based approach instead.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/memory.py</code> <pre><code>class Memory(Decorator):\n   \"\"\"Limit virtual memory using `ulimit -Sv` before launching the solver.\n\n   Prepends `ulimit -Sv &lt;kilobytes&gt; &amp;&amp;` to the shell command.  The limit is\n   set in kilobytes as `giga * 1024 * 1024 + 1024` to allow a small overhead\n   above the nominal gigabyte value.\n\n   Note: `ulimit -Sv` caps *virtual* memory (VSZ), not resident memory (RSS).\n   For stricter RSS limits consider a `cgroup`-based approach instead.\n   \"\"\"\n\n   def __init__(self, giga: float = 4):\n      \"\"\"Args:\n         giga: Virtual memory limit in gigabytes (default 4 GB).\n      \"\"\"\n      Decorator.__init__(self, giga=giga)\n      self.prefix = ULIMIT_CMD % (int(giga * 1024 * 1024) + 1024)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Prepend `ulimit -Sv &lt;N&gt; &amp;&amp;` to *cmd*.\"\"\"\n      del instance, strategy\n      return f\"{self.prefix} &amp;&amp; {cmd}\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/memory/#solverpy.solver.plugins.shell.memory.Memory.__init__","title":"<code>__init__(giga: float = 4)</code>","text":"<p>Args: giga: Virtual memory limit in gigabytes (default 4 GB).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/memory.py</code> <pre><code>def __init__(self, giga: float = 4):\n   \"\"\"Args:\n      giga: Virtual memory limit in gigabytes (default 4 GB).\n   \"\"\"\n   Decorator.__init__(self, giga=giga)\n   self.prefix = ULIMIT_CMD % (int(giga * 1024 * 1024) + 1024)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/memory/#solverpy.solver.plugins.shell.memory.Memory.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Prepend <code>ulimit -Sv &lt;N&gt; &amp;&amp;</code> to cmd.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/memory.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Prepend `ulimit -Sv &lt;N&gt; &amp;&amp;` to *cmd*.\"\"\"\n   del instance, strategy\n   return f\"{self.prefix} &amp;&amp; {cmd}\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/shelltime/","title":"module shelltime","text":""},{"location":"api/solverpy/solver/plugins/shell/shelltime/#solverpy.solver.plugins.shell.shelltime","title":"<code>solverpy.solver.plugins.shell.shelltime</code>","text":""},{"location":"api/solverpy/solver/plugins/shell/shelltime/#solverpy.solver.plugins.shell.shelltime.ShellTime","title":"<code>ShellTime</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Measure wall-clock time using <code>/usr/bin/env time -p</code>.</p> <p>Prepends <code>/usr/bin/env time -p</code> to the solver command.  The POSIX output written to stderr is parsed in <code>update()</code> and three keys are added to the result:</p> <ul> <li><code>realtime</code> \u2014 elapsed wall-clock time (seconds)</li> <li><code>usertime</code> \u2014 user-mode CPU time (seconds)</li> <li><code>systime</code>  \u2014 kernel-mode CPU time (seconds)</li> <li><code>runtime</code>  \u2014 <code>realtime - systime</code> (used as the canonical solve time)</li> </ul> <p>Note: the <code>real</code>, <code>user</code>, <code>sys</code> lines appear verbatim in the raw solver output (merged via <code>stderr=subprocess.STDOUT</code>), which makes the output invalid TPTP.  Use <code>Time</code> from <code>time.py</code> to avoid this.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/shelltime.py</code> <pre><code>class ShellTime(Decorator):\n   \"\"\"Measure wall-clock time using `/usr/bin/env time -p`.\n\n   Prepends `/usr/bin/env time -p` to the solver command.  The POSIX output\n   written to *stderr* is parsed in `update()` and three keys are added to the\n   result:\n\n   - `realtime` \u2014 elapsed wall-clock time (seconds)\n   - `usertime` \u2014 user-mode CPU time (seconds)\n   - `systime`  \u2014 kernel-mode CPU time (seconds)\n   - `runtime`  \u2014 `realtime - systime` (used as the canonical solve time)\n\n   Note: the `real`, `user`, `sys` lines appear verbatim in the raw solver\n   output (merged via `stderr=subprocess.STDOUT`), which makes the output\n   invalid TPTP.  Use `Time` from `time.py` to avoid this.\n   \"\"\"\n\n   def __init__(self, **kwargs):\n      Decorator.__init__(self, **kwargs)\n      self.prefix = TIME_CMD\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Prepend `/usr/bin/env time -p` to *cmd*.\"\"\"\n      del instance, strategy  # unused arguments\n      return f\"{self.prefix} {cmd}\"\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Parse `real / user / sys` lines from *output* and populate *result*.\"\"\"\n      del instance, strategy  # unused arguments\n      res = patterns.keyval(TIME_PAT, output, TIME_TABLE)\n      res = patterns.mapval(res, float)\n      result.update(res)\n      if (\"realtime\" in res) and (\"systime\" in res):\n         result[\"runtime\"] = res[\"realtime\"] - res[\"systime\"]\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/shelltime/#solverpy.solver.plugins.shell.shelltime.ShellTime.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Prepend <code>/usr/bin/env time -p</code> to cmd.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/shelltime.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Prepend `/usr/bin/env time -p` to *cmd*.\"\"\"\n   del instance, strategy  # unused arguments\n   return f\"{self.prefix} {cmd}\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/shelltime/#solverpy.solver.plugins.shell.shelltime.ShellTime.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Parse <code>real / user / sys</code> lines from output and populate result.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/shelltime.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Parse `real / user / sys` lines from *output* and populate *result*.\"\"\"\n   del instance, strategy  # unused arguments\n   res = patterns.keyval(TIME_PAT, output, TIME_TABLE)\n   res = patterns.mapval(res, float)\n   result.update(res)\n   if (\"realtime\" in res) and (\"systime\" in res):\n      result[\"runtime\"] = res[\"realtime\"] - res[\"systime\"]\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/time/","title":"module time","text":""},{"location":"api/solverpy/solver/plugins/shell/time/#solverpy.solver.plugins.shell.time","title":"<code>solverpy.solver.plugins.shell.time</code>","text":""},{"location":"api/solverpy/solver/plugins/shell/time/#solverpy.solver.plugins.shell.time.Time","title":"<code>Time</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Measure wall-clock, user, and sys time using Python's stdlib.</p> <p>Snapshots <code>time.perf_counter()</code> and <code>resource.getrusage(RUSAGE_CHILDREN)</code> in <code>decorate()</code> (just before the subprocess) and computes deltas in <code>update()</code> (just after).  Three keys are added to the result:</p> <ul> <li><code>realtime</code> \u2014 elapsed wall-clock time (seconds)</li> <li><code>usertime</code> \u2014 user-mode CPU time of child processes (seconds)</li> <li><code>systime</code>  \u2014 kernel-mode CPU time of child processes (seconds)</li> <li><code>runtime</code>  \u2014 <code>realtime - systime</code> (canonical solve time)</li> </ul> <p>The solver command is not modified, so no timing lines appear in the raw solver output.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/time.py</code> <pre><code>class Time(Decorator):\n   \"\"\"Measure wall-clock, user, and sys time using Python's stdlib.\n\n   Snapshots `time.perf_counter()` and `resource.getrusage(RUSAGE_CHILDREN)`\n   in `decorate()` (just before the subprocess) and computes deltas in\n   `update()` (just after).  Three keys are added to the result:\n\n   - `realtime` \u2014 elapsed wall-clock time (seconds)\n   - `usertime` \u2014 user-mode CPU time of child processes (seconds)\n   - `systime`  \u2014 kernel-mode CPU time of child processes (seconds)\n   - `runtime`  \u2014 `realtime - systime` (canonical solve time)\n\n   The solver command is not modified, so no timing lines appear in the\n   raw solver output.\n   \"\"\"\n\n   def __init__(self, **kwargs):\n      Decorator.__init__(self, **kwargs)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Snapshot start times; return *cmd* unchanged.\"\"\"\n      del instance, strategy  # unused arguments\n      self._t0_wall = _time.perf_counter()\n      r = resource.getrusage(resource.RUSAGE_CHILDREN)\n      self._t0_user = r.ru_utime\n      self._t0_sys = r.ru_stime\n      return cmd\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Compute time deltas and populate *result*.\"\"\"\n      del instance, strategy, output  # unused arguments\n      wall = _time.perf_counter() - self._t0_wall\n      r = resource.getrusage(resource.RUSAGE_CHILDREN)\n      user = r.ru_utime - self._t0_user\n      sys_ = r.ru_stime - self._t0_sys\n      result[\"realtime\"] = wall\n      result[\"usertime\"] = user\n      result[\"systime\"] = sys_\n      result[\"runtime\"] = wall - sys_\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/time/#solverpy.solver.plugins.shell.time.Time.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Snapshot start times; return cmd unchanged.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/time.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Snapshot start times; return *cmd* unchanged.\"\"\"\n   del instance, strategy  # unused arguments\n   self._t0_wall = _time.perf_counter()\n   r = resource.getrusage(resource.RUSAGE_CHILDREN)\n   self._t0_user = r.ru_utime\n   self._t0_sys = r.ru_stime\n   return cmd\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/time/#solverpy.solver.plugins.shell.time.Time.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Compute time deltas and populate result.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/time.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Compute time deltas and populate *result*.\"\"\"\n   del instance, strategy, output  # unused arguments\n   wall = _time.perf_counter() - self._t0_wall\n   r = resource.getrusage(resource.RUSAGE_CHILDREN)\n   user = r.ru_utime - self._t0_user\n   sys_ = r.ru_stime - self._t0_sys\n   result[\"realtime\"] = wall\n   result[\"usertime\"] = user\n   result[\"systime\"] = sys_\n   result[\"runtime\"] = wall - sys_\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/timeout/","title":"module timeout","text":""},{"location":"api/solverpy/solver/plugins/shell/timeout/#solverpy.solver.plugins.shell.timeout","title":"<code>solverpy.solver.plugins.shell.timeout</code>","text":""},{"location":"api/solverpy/solver/plugins/shell/timeout/#solverpy.solver.plugins.shell.timeout.Timeout","title":"<code>Timeout</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Enforce a hard wall-clock cutoff using the GNU <code>timeout</code> command.</p> <p>Prepends <code>timeout --kill-after=15 --foreground &lt;N&gt;</code> to the solver command. If the solver does not finish within <code>N</code> seconds, <code>timeout</code> sends <code>SIGTERM</code>; after a further 15 seconds it sends <code>SIGKILL</code>.</p> <p>This decorator inserts itself at position 0 so it becomes the outermost wrapper \u2014 it fires before any other decorator modifies the command.</p> <p>In <code>update()</code> it sets <code>result[\"status\"] = \"TIMEOUT\"</code> when the subprocess exits with code 124 (killed by <code>timeout</code>) or 137 (killed by <code>SIGKILL</code>).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>class Timeout(Decorator):\n   \"\"\"Enforce a hard wall-clock cutoff using the GNU `timeout` command.\n\n   Prepends `timeout --kill-after=15 --foreground &lt;N&gt;` to the solver command.\n   If the solver does not finish within `N` seconds, `timeout` sends `SIGTERM`;\n   after a further 15 seconds it sends `SIGKILL`.\n\n   This decorator inserts itself at **position 0** so it becomes the outermost\n   wrapper \u2014 it fires before any other decorator modifies the command.\n\n   In `update()` it sets `result[\"status\"] = \"TIMEOUT\"` when the subprocess\n   exits with code 124 (killed by `timeout`) or 137 (killed by `SIGKILL`).\n   \"\"\"\n\n   def __init__(self, timeout: int):\n      \"\"\"Args:\n         timeout: Wall-clock limit in seconds.\n      \"\"\"\n      Decorator.__init__(self, timeout=timeout)\n      self.timeout = timeout\n      self.prefix = TIMEOUT_CMD % timeout\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Insert at position 0 of `solver.decorators` (outermost wrapper).\"\"\"\n      self.solver = solver\n      solver.decorators.insert(0, self)\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      \"\"\"Prepend the `timeout` command to *cmd*.\"\"\"\n      del instance, strategy # unused arguments\n      return f\"{self.prefix} {cmd}\"\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Set `status = \"TIMEOUT\"` if the process was killed by the timeout.\"\"\"\n      del instance, strategy, output # unused arguments\n      if self.solver._exitcode in [124, 137]:\n         # killed by timeout or SIGKILL\n         result[\"status\"] = \"TIMEOUT\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/timeout/#solverpy.solver.plugins.shell.timeout.Timeout.__init__","title":"<code>__init__(timeout: int)</code>","text":"<p>Args: timeout: Wall-clock limit in seconds.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>def __init__(self, timeout: int):\n   \"\"\"Args:\n      timeout: Wall-clock limit in seconds.\n   \"\"\"\n   Decorator.__init__(self, timeout=timeout)\n   self.timeout = timeout\n   self.prefix = TIMEOUT_CMD % timeout\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/timeout/#solverpy.solver.plugins.shell.timeout.Timeout.decorate","title":"<code>decorate(cmd: str, instance: Any, strategy: Any) -&gt; str</code>","text":"<p>Prepend the <code>timeout</code> command to cmd.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>def decorate(\n   self,\n   cmd: str,\n   instance: Any,\n   strategy: Any,\n) -&gt; str:\n   \"\"\"Prepend the `timeout` command to *cmd*.\"\"\"\n   del instance, strategy # unused arguments\n   return f\"{self.prefix} {cmd}\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/timeout/#solverpy.solver.plugins.shell.timeout.Timeout.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Insert at position 0 of <code>solver.decorators</code> (outermost wrapper).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Insert at position 0 of `solver.decorators` (outermost wrapper).\"\"\"\n   self.solver = solver\n   solver.decorators.insert(0, self)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/shell/timeout/#solverpy.solver.plugins.shell.timeout.Timeout.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Set <code>status = \"TIMEOUT\"</code> if the process was killed by the timeout.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/shell/timeout.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Set `status = \"TIMEOUT\"` if the process was killed by the timeout.\"\"\"\n   del instance, strategy, output # unused arguments\n   if self.solver._exitcode in [124, 137]:\n      # killed by timeout or SIGKILL\n      result[\"status\"] = \"TIMEOUT\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/","title":"Status Overview","text":""},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status","title":"<code>solverpy.solver.plugins.status</code>","text":""},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status--status-plugins","title":"Status plugins","text":"<p>Status plugins parse the raw solver output and populate the <code>status</code> key of the result.  They also register the solver's valid, successful and timeout statuses so that <code>valid</code> and <code>solved</code> work correctly.</p> <p> </p> Plugin Used by Description <code>Tptp</code> E, Vampire, Lash, Prover9 Parses TPTP SZS status line from solver output <code>Smt</code> Cvc5, Z3, Bitwuzla, Llm2smt Parses SMT-LIB2 status (<code>sat</code>/<code>unsat</code>/<code>unknown</code>) <code>Limiter</code> optional Caps <code>runtime</code> at the time limit; adds <code>limit</code> key to result"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Limiter","title":"<code>Limiter</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Cap <code>runtime</code> at the time limit and stamp <code>limit</code> onto the result.</p> <p>Two jobs in <code>update()</code>:</p> <ol> <li>If <code>result[\"status\"]</code> is a timeout status or <code>result[\"runtime\"]</code>    already exceeds the time limit, clamp <code>result[\"runtime\"]</code> to the    limit value so that cached timeouts always report a consistent duration.</li> <li>Set <code>result[\"limit\"]</code> to the limit string (e.g. <code>\"T10\"</code>).</li> </ol> <p>The <code>limit</code> key is required by <code>SolverPy.simulate</code> for cached-result reuse under different time limits.  <code>Limiter</code> is registered automatically by every <code>SolverPy</code> instance, so results always carry this key.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/limiter.py</code> <pre><code>class Limiter(Decorator):\n   \"\"\"Cap `runtime` at the time limit and stamp `limit` onto the result.\n\n   Two jobs in `update()`:\n\n   1. If `result[\"status\"]` is a timeout status *or* `result[\"runtime\"]`\n      already exceeds the time limit, clamp `result[\"runtime\"]` to the\n      limit value so that cached timeouts always report a consistent duration.\n   2. Set `result[\"limit\"]` to the limit string (e.g. `\"T10\"`).\n\n   The `limit` key is **required** by\n   [`SolverPy.simulate`][solverpy.solver.solverpy.SolverPy.simulate] for\n   cached-result reuse under different time limits.  `Limiter` is registered\n   automatically by every `SolverPy` instance, so results always carry this key.\n   \"\"\"\n\n   def __init__(self, **kwargs):\n      Decorator.__init__(self, **kwargs)\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Append to decorators and cache references to the solver's limit info.\"\"\"\n      super().register(solver)\n      self.timeouts = solver.timeouts\n      self.timeout = solver._limits.timeout\n      self.limit = solver._limits.limit\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: Any,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Clamp `runtime` and set `result[\"limit\"]`.\"\"\"\n      del instance, strategy, output # unused arguments\n      if (not result) or (\"status\" not in result) or (\"runtime\" not in result):\n         return\n      if (result[\"status\"] in self.timeouts) or \\\n            (result[\"runtime\"] &gt; self.timeout):\n         result[\"runtime\"] = self.timeout\n      result[\"limit\"] = self.limit\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Limiter.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Append to decorators and cache references to the solver's limit info.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/limiter.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Append to decorators and cache references to the solver's limit info.\"\"\"\n   super().register(solver)\n   self.timeouts = solver.timeouts\n   self.timeout = solver._limits.timeout\n   self.limit = solver._limits.limit\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Limiter.update","title":"<code>update(instance: Any, strategy: Any, output: Any, result: Result) -&gt; None</code>","text":"<p>Clamp <code>runtime</code> and set <code>result[\"limit\"]</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/limiter.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: Any,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Clamp `runtime` and set `result[\"limit\"]`.\"\"\"\n   del instance, strategy, output # unused arguments\n   if (not result) or (\"status\" not in result) or (\"runtime\" not in result):\n      return\n   if (result[\"status\"] in self.timeouts) or \\\n         (result[\"runtime\"] &gt; self.timeout):\n      result[\"runtime\"] = self.timeout\n   result[\"limit\"] = self.limit\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Smt","title":"<code>Smt</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Parse the SMT-LIB2 status token from solver output.</p> <p>Matches one of <code>sat</code>, <code>unsat</code>, <code>unknown</code>, or <code>timeout</code> on its own line and writes it to <code>result[\"status\"]</code>.  Falls back to <code>\"error\"</code> if no token is found and <code>result[\"status\"]</code> is not yet set.</p> <p>On <code>register()</code> it also updates the solver's status sets so that <code>Solver.solved</code> and <code>Solver.valid</code> work correctly:</p> <ul> <li><code>solver._success</code> \u2190 <code>SMT_OK</code> (or <code>SMT_INC_OK</code> for incomplete solvers)</li> <li><code>solver._timeouts</code> \u2190 <code>SMT_TIMEOUT</code></li> <li><code>solver._statuses</code> \u2190 <code>SMT_ALL</code></li> </ul> <p>Used by: Cvc5, Z3, Bitwuzla, Llm2smt.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/smt.py</code> <pre><code>class Smt(Decorator):\n   \"\"\"Parse the SMT-LIB2 status token from solver output.\n\n   Matches one of `sat`, `unsat`, `unknown`, or `timeout` on its own line\n   and writes it to `result[\"status\"]`.  Falls back to `\"error\"` if no\n   token is found and `result[\"status\"]` is not yet set.\n\n   On `register()` it also updates the solver's status sets so that\n   [`Solver.solved`][solverpy.solver.solver.Solver.solved] and\n   [`Solver.valid`][solverpy.solver.solver.Solver.valid] work correctly:\n\n   - `solver._success` \u2190 `SMT_OK` (or `SMT_INC_OK` for incomplete solvers)\n   - `solver._timeouts` \u2190 `SMT_TIMEOUT`\n   - `solver._statuses` \u2190 `SMT_ALL`\n\n   Used by: Cvc5, Z3, Bitwuzla, Llm2smt.\n   \"\"\"\n\n   def __init__(self, complete=True, **kwargs):\n      \"\"\"Args:\n         complete: If `True` (default), `sat` is counted as success.  Set to\n            `False` for incomplete solvers where `sat` may be unreliable.\n      \"\"\"\n      Decorator.__init__(self, complete=complete, **kwargs)\n      self._complete = complete\n\n   def register(self, solver: \"SolverPy\"):\n      \"\"\"Append to decorators and update the solver's status sets.\"\"\"\n      super().register(solver)\n      solver._success |= SMT_OK if self._complete else SMT_INC_OK\n      solver._timeouts |= SMT_TIMEOUT\n      solver._statuses |= SMT_ALL\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      del instance, strategy  # unused arguments\n      return cmd\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Extract the SMT status token and write it to `result[\"status\"]`.\"\"\"\n      del instance, strategy  # unused arguments\n      status = patterns.single(SMT_STATUS, output, \"\")\n      if status:\n         result[\"status\"] = status\n      elif \"status\" not in result:\n         result[\"status\"] = \"error\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Smt.__init__","title":"<code>__init__(complete=True, **kwargs)</code>","text":"<p>complete: If <code>True</code> (default), <code>sat</code> is counted as success.  Set to    <code>False</code> for incomplete solvers where <code>sat</code> may be unreliable.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/smt.py</code> <pre><code>def __init__(self, complete=True, **kwargs):\n   \"\"\"Args:\n      complete: If `True` (default), `sat` is counted as success.  Set to\n         `False` for incomplete solvers where `sat` may be unreliable.\n   \"\"\"\n   Decorator.__init__(self, complete=complete, **kwargs)\n   self._complete = complete\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Smt.register","title":"<code>register(solver: SolverPy)</code>","text":"<p>Append to decorators and update the solver's status sets.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/smt.py</code> <pre><code>def register(self, solver: \"SolverPy\"):\n   \"\"\"Append to decorators and update the solver's status sets.\"\"\"\n   super().register(solver)\n   solver._success |= SMT_OK if self._complete else SMT_INC_OK\n   solver._timeouts |= SMT_TIMEOUT\n   solver._statuses |= SMT_ALL\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Smt.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Extract the SMT status token and write it to <code>result[\"status\"]</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/smt.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Extract the SMT status token and write it to `result[\"status\"]`.\"\"\"\n   del instance, strategy  # unused arguments\n   status = patterns.single(SMT_STATUS, output, \"\")\n   if status:\n      result[\"status\"] = status\n   elif \"status\" not in result:\n      result[\"status\"] = \"error\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Tptp","title":"<code>Tptp</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Parse the TPTP SZS status line from solver output.</p> <p>Matches the pattern <code># SZS status &lt;STATUS&gt;</code> (or <code>%</code> prefix) in the solver output and writes the status token to <code>result[\"status\"]</code>.  Falls back to <code>\"ERROR\"</code> if no status line is found and <code>result[\"status\"]</code> is not yet set.</p> <p>On <code>register()</code> it also updates the solver's status sets so that <code>Solver.solved</code> and <code>Solver.valid</code> work correctly:</p> <ul> <li><code>solver._success</code> \u2190 <code>TPTP_OK</code> (or <code>TPTP_INC_OK</code> for incomplete solvers)</li> <li><code>solver._timeouts</code> \u2190 <code>TPTP_TIMEOUT</code></li> <li><code>solver._statuses</code> \u2190 <code>TPTP_ALL</code></li> </ul> <p>Used by: E, Vampire, Lash, Prover9.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/tptp.py</code> <pre><code>class Tptp(Decorator):\n   \"\"\"Parse the TPTP SZS status line from solver output.\n\n   Matches the pattern `# SZS status &lt;STATUS&gt;` (or `%` prefix) in the solver\n   output and writes the status token to `result[\"status\"]`.  Falls back to\n   `\"ERROR\"` if no status line is found and `result[\"status\"]` is not yet set.\n\n   On `register()` it also updates the solver's status sets so that\n   [`Solver.solved`][solverpy.solver.solver.Solver.solved] and\n   [`Solver.valid`][solverpy.solver.solver.Solver.valid] work correctly:\n\n   - `solver._success` \u2190 `TPTP_OK` (or `TPTP_INC_OK` for incomplete solvers)\n   - `solver._timeouts` \u2190 `TPTP_TIMEOUT`\n   - `solver._statuses` \u2190 `TPTP_ALL`\n\n   Used by: E, Vampire, Lash, Prover9.\n   \"\"\"\n\n   def __init__(self, complete=True, **kwargs):\n      \"\"\"Args:\n         complete: If `True` (default), `Satisfiable` and `CounterSatisfiable`\n            are counted as success.  Set to `False` for incomplete solvers\n            where these statuses are unreliable.\n      \"\"\"\n      Decorator.__init__(self, complete=complete, **kwargs)\n      self._complete = complete\n\n   def register(self, solver: \"SolverPy\"):\n      \"\"\"Append to decorators and update the solver's status sets.\"\"\"\n      super().register(solver)\n      solver._success |= TPTP_OK if self._complete else TPTP_INC_OK\n      solver._timeouts |= TPTP_TIMEOUT\n      solver._statuses |= TPTP_ALL\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      del instance, strategy\n      return cmd\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Extract the SZS status token and write it to `result[\"status\"]`.\"\"\"\n      del instance, strategy\n      status = patterns.single(TPTP_STATUS, output, \"\")\n      if status:\n         result[\"status\"] = status\n      elif \"status\" not in result:\n         result[\"status\"] = \"ERROR\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Tptp.__init__","title":"<code>__init__(complete=True, **kwargs)</code>","text":"<p>complete: If <code>True</code> (default), <code>Satisfiable</code> and <code>CounterSatisfiable</code>    are counted as success.  Set to <code>False</code> for incomplete solvers    where these statuses are unreliable.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/tptp.py</code> <pre><code>def __init__(self, complete=True, **kwargs):\n   \"\"\"Args:\n      complete: If `True` (default), `Satisfiable` and `CounterSatisfiable`\n         are counted as success.  Set to `False` for incomplete solvers\n         where these statuses are unreliable.\n   \"\"\"\n   Decorator.__init__(self, complete=complete, **kwargs)\n   self._complete = complete\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Tptp.register","title":"<code>register(solver: SolverPy)</code>","text":"<p>Append to decorators and update the solver's status sets.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/tptp.py</code> <pre><code>def register(self, solver: \"SolverPy\"):\n   \"\"\"Append to decorators and update the solver's status sets.\"\"\"\n   super().register(solver)\n   solver._success |= TPTP_OK if self._complete else TPTP_INC_OK\n   solver._timeouts |= TPTP_TIMEOUT\n   solver._statuses |= TPTP_ALL\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/#solverpy.solver.plugins.status.Tptp.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Extract the SZS status token and write it to <code>result[\"status\"]</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/tptp.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Extract the SZS status token and write it to `result[\"status\"]`.\"\"\"\n   del instance, strategy\n   status = patterns.single(TPTP_STATUS, output, \"\")\n   if status:\n      result[\"status\"] = status\n   elif \"status\" not in result:\n      result[\"status\"] = \"ERROR\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/limiter/","title":"module limiter","text":""},{"location":"api/solverpy/solver/plugins/status/limiter/#solverpy.solver.plugins.status.limiter","title":"<code>solverpy.solver.plugins.status.limiter</code>","text":""},{"location":"api/solverpy/solver/plugins/status/limiter/#solverpy.solver.plugins.status.limiter.Limiter","title":"<code>Limiter</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Cap <code>runtime</code> at the time limit and stamp <code>limit</code> onto the result.</p> <p>Two jobs in <code>update()</code>:</p> <ol> <li>If <code>result[\"status\"]</code> is a timeout status or <code>result[\"runtime\"]</code>    already exceeds the time limit, clamp <code>result[\"runtime\"]</code> to the    limit value so that cached timeouts always report a consistent duration.</li> <li>Set <code>result[\"limit\"]</code> to the limit string (e.g. <code>\"T10\"</code>).</li> </ol> <p>The <code>limit</code> key is required by <code>SolverPy.simulate</code> for cached-result reuse under different time limits.  <code>Limiter</code> is registered automatically by every <code>SolverPy</code> instance, so results always carry this key.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/limiter.py</code> <pre><code>class Limiter(Decorator):\n   \"\"\"Cap `runtime` at the time limit and stamp `limit` onto the result.\n\n   Two jobs in `update()`:\n\n   1. If `result[\"status\"]` is a timeout status *or* `result[\"runtime\"]`\n      already exceeds the time limit, clamp `result[\"runtime\"]` to the\n      limit value so that cached timeouts always report a consistent duration.\n   2. Set `result[\"limit\"]` to the limit string (e.g. `\"T10\"`).\n\n   The `limit` key is **required** by\n   [`SolverPy.simulate`][solverpy.solver.solverpy.SolverPy.simulate] for\n   cached-result reuse under different time limits.  `Limiter` is registered\n   automatically by every `SolverPy` instance, so results always carry this key.\n   \"\"\"\n\n   def __init__(self, **kwargs):\n      Decorator.__init__(self, **kwargs)\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Append to decorators and cache references to the solver's limit info.\"\"\"\n      super().register(solver)\n      self.timeouts = solver.timeouts\n      self.timeout = solver._limits.timeout\n      self.limit = solver._limits.limit\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: Any,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Clamp `runtime` and set `result[\"limit\"]`.\"\"\"\n      del instance, strategy, output # unused arguments\n      if (not result) or (\"status\" not in result) or (\"runtime\" not in result):\n         return\n      if (result[\"status\"] in self.timeouts) or \\\n            (result[\"runtime\"] &gt; self.timeout):\n         result[\"runtime\"] = self.timeout\n      result[\"limit\"] = self.limit\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/limiter/#solverpy.solver.plugins.status.limiter.Limiter.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Append to decorators and cache references to the solver's limit info.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/limiter.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Append to decorators and cache references to the solver's limit info.\"\"\"\n   super().register(solver)\n   self.timeouts = solver.timeouts\n   self.timeout = solver._limits.timeout\n   self.limit = solver._limits.limit\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/limiter/#solverpy.solver.plugins.status.limiter.Limiter.update","title":"<code>update(instance: Any, strategy: Any, output: Any, result: Result) -&gt; None</code>","text":"<p>Clamp <code>runtime</code> and set <code>result[\"limit\"]</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/limiter.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: Any,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Clamp `runtime` and set `result[\"limit\"]`.\"\"\"\n   del instance, strategy, output # unused arguments\n   if (not result) or (\"status\" not in result) or (\"runtime\" not in result):\n      return\n   if (result[\"status\"] in self.timeouts) or \\\n         (result[\"runtime\"] &gt; self.timeout):\n      result[\"runtime\"] = self.timeout\n   result[\"limit\"] = self.limit\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/runhash/","title":"module runhash","text":""},{"location":"api/solverpy/solver/plugins/status/runhash/#solverpy.solver.plugins.status.runhash","title":"<code>solverpy.solver.plugins.status.runhash</code>","text":""},{"location":"api/solverpy/solver/plugins/status/runhash/#solverpy.solver.plugins.status.runhash.RunHash","title":"<code>RunHash</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Compute a runhash for each run, capturing the solver's internal behaviour independent of its options.</p> <p>Hashes a fixed subset of <code>result</code> -- the solver's own internal counters (e.g. decisions, propagations, preprocessing pass counts), not wall-clock time or its command-line options -- into <code>result[\"runhash\"]</code> via <code>runhash</code>.</p> <p>Two runs of the same instance that produce the same runhash did byte-identical work internally, even if they were launched with different strategies. That makes the runhash a cheap way to catch an option that parses, reaches the command line, and is reachable, yet changes nothing about what the solver actually does (an \"inert\" parameter) -- a failure mode that a plateau in the score alone cannot distinguish from a genuinely flat region of the search space.</p> <p>Which keys count as \"internal behaviour\" is solver-specific and supplied by the caller as <code>gens</code>; see <code>PRIMO_RUNHASH_GEN</code> in <code>Primo</code> for the primo selection.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/runhash.py</code> <pre><code>class RunHash(Decorator):\n   \"\"\"Compute a runhash for each run, capturing the solver's internal\n   behaviour independent of its options.\n\n   Hashes a fixed subset of `result` -- the solver's own internal counters\n   (e.g. decisions, propagations, preprocessing pass counts), not wall-clock\n   time or its command-line options -- into `result[\"runhash\"]` via\n   [`runhash`][solverpy.solver.plugins.status.runhash.runhash].\n\n   Two runs of the *same instance* that produce the same runhash did\n   byte-identical work internally, even if they were launched with different\n   strategies. That makes the runhash a cheap way to catch an option that\n   parses, reaches the command line, and is reachable, yet changes nothing\n   about what the solver actually does (an \"inert\" parameter) -- a failure\n   mode that a plateau in the score alone cannot distinguish from a\n   genuinely flat region of the search space.\n\n   Which keys count as \"internal behaviour\" is solver-specific and supplied\n   by the caller as `gens`; see `PRIMO_RUNHASH_GEN` in\n   [`Primo`][solverpy.solver.smt.primo.Primo] for the primo selection.\n   \"\"\"\n\n   def __init__(self, gens: Iterable[str], **kwargs: Any):\n      \"\"\"Args:\n         gens: An iterable of `result` keys to include in the runhash. Keys\n            not present in `result` for a given run are simply skipped, so\n            the same `gens` works across instances that populate different\n            subsets of counters (e.g. a problem solved during preprocessing\n            never emits `sat.*` keys).\n      \"\"\"\n      self._gens = gens\n      Decorator.__init__(self, **kwargs)\n\n   def selected(self, key: str) -&gt; bool:\n      \"\"\"Return whether result key `key` is included in the runhash.\"\"\"\n      return key in self._gens\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      \"\"\"Append to decorators. Does not touch the solver's status sets.\"\"\"\n      super().register(solver)\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Compute the runhash of the selected subset of `result` and write it\n      to `result[\"runhash\"]`.\"\"\"\n      del instance, strategy, output  # unused arguments\n      #runhash = {x:result[x] for x in result if not x.endswith(\"-us\")}\n      sels = {k: result[k] for k in result if self.selected(k)}\n      result[\"runhash\"] = runhash(sels)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/runhash/#solverpy.solver.plugins.status.runhash.RunHash.__init__","title":"<code>__init__(gens: Iterable[str], **kwargs: Any)</code>","text":"<p>gens: An iterable of <code>result</code> keys to include in the runhash. Keys    not present in <code>result</code> for a given run are simply skipped, so    the same <code>gens</code> works across instances that populate different    subsets of counters (e.g. a problem solved during preprocessing    never emits <code>sat.*</code> keys).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/runhash.py</code> <pre><code>def __init__(self, gens: Iterable[str], **kwargs: Any):\n   \"\"\"Args:\n      gens: An iterable of `result` keys to include in the runhash. Keys\n         not present in `result` for a given run are simply skipped, so\n         the same `gens` works across instances that populate different\n         subsets of counters (e.g. a problem solved during preprocessing\n         never emits `sat.*` keys).\n   \"\"\"\n   self._gens = gens\n   Decorator.__init__(self, **kwargs)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/runhash/#solverpy.solver.plugins.status.runhash.RunHash.register","title":"<code>register(solver: SolverPy) -&gt; None</code>","text":"<p>Append to decorators. Does not touch the solver's status sets.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/runhash.py</code> <pre><code>def register(self, solver: \"SolverPy\") -&gt; None:\n   \"\"\"Append to decorators. Does not touch the solver's status sets.\"\"\"\n   super().register(solver)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/runhash/#solverpy.solver.plugins.status.runhash.RunHash.selected","title":"<code>selected(key: str) -&gt; bool</code>","text":"<p>Return whether result key <code>key</code> is included in the runhash.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/runhash.py</code> <pre><code>def selected(self, key: str) -&gt; bool:\n   \"\"\"Return whether result key `key` is included in the runhash.\"\"\"\n   return key in self._gens\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/runhash/#solverpy.solver.plugins.status.runhash.RunHash.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Compute the runhash of the selected subset of <code>result</code> and write it to <code>result[\"runhash\"]</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/runhash.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Compute the runhash of the selected subset of `result` and write it\n   to `result[\"runhash\"]`.\"\"\"\n   del instance, strategy, output  # unused arguments\n   #runhash = {x:result[x] for x in result if not x.endswith(\"-us\")}\n   sels = {k: result[k] for k in result if self.selected(k)}\n   result[\"runhash\"] = runhash(sels)\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/runhash/#solverpy.solver.plugins.status.runhash.runhash","title":"<code>runhash(res: Result) -&gt; int</code>","text":"<p>Compute the runhash of a dict of result values: a short, deterministic fingerprint.</p> <p><code>res</code> is JSON-serialized with sorted keys and no incidental whitespace, so two dicts with the same keys and values always produce the same runhash regardless of insertion order. Truncated to 8 bytes (64 bits) of SHA-256 -- collisions are not a concern here, since the runhash is used to notice equal runs among a handful of candidates, not as a cryptographic or globally-unique id.</p> <p>Returned as a plain integer rather than a hex string: it is used as a number everywhere it is consumed (XOR-combined across a batch, compared for equality), and <code>result[\"runhash\"]</code> round-trips through <code>json.dumps</code> as a bare number just as well as any other numeric result field. Format it as <code>f\"{h:016x}\"</code> at the point it needs to look like a fingerprint (a log line, the wrapper's result-line field) rather than storing it pre-formatted.</p> <p>Parameters:</p> Name Type Description Default <code>res</code> <code>Result</code> <p>A JSON-serializable dict, typically a subset of a solver's result.</p> required <p>Returns:</p> Type Description <code>int</code> <p>The runhash, as an unsigned 64-bit integer (<code>0</code> to <code>2**64 - 1</code>).</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/runhash.py</code> <pre><code>def runhash(res: \"Result\") -&gt; int:\n   \"\"\"Compute the runhash of a dict of result values: a short, deterministic\n   fingerprint.\n\n   `res` is JSON-serialized with sorted keys and no incidental whitespace, so\n   two dicts with the same keys and values always produce the same runhash\n   regardless of insertion order. Truncated to 8 bytes (64 bits) of SHA-256 --\n   collisions are not a concern here, since the runhash is used to *notice*\n   equal runs among a handful of candidates, not as a cryptographic or\n   globally-unique id.\n\n   Returned as a plain integer rather than a hex string: it is used as a\n   number everywhere it is consumed (XOR-combined across a batch, compared\n   for equality), and `result[\"runhash\"]` round-trips through `json.dumps` as\n   a bare number just as well as any other numeric result field. Format it as\n   `f\"{h:016x}\"` at the point it needs to look like a fingerprint (a log line,\n   the wrapper's result-line field) rather than storing it pre-formatted.\n\n   Args:\n      res: A JSON-serializable dict, typically a subset of a solver's result.\n\n   Returns:\n      The runhash, as an unsigned 64-bit integer (`0` to `2**64 - 1`).\n   \"\"\"\n   data = json.dumps(\n      res,\n      sort_keys=True,\n      separators=(\",\", \":\"),\n      ensure_ascii=False,\n   ).encode(\"utf-8\")\n\n   return int.from_bytes(hashlib.sha256(data).digest()[:8], \"big\")\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/smt/","title":"module smt","text":""},{"location":"api/solverpy/solver/plugins/status/smt/#solverpy.solver.plugins.status.smt","title":"<code>solverpy.solver.plugins.status.smt</code>","text":""},{"location":"api/solverpy/solver/plugins/status/smt/#solverpy.solver.plugins.status.smt.Smt","title":"<code>Smt</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Parse the SMT-LIB2 status token from solver output.</p> <p>Matches one of <code>sat</code>, <code>unsat</code>, <code>unknown</code>, or <code>timeout</code> on its own line and writes it to <code>result[\"status\"]</code>.  Falls back to <code>\"error\"</code> if no token is found and <code>result[\"status\"]</code> is not yet set.</p> <p>On <code>register()</code> it also updates the solver's status sets so that <code>Solver.solved</code> and <code>Solver.valid</code> work correctly:</p> <ul> <li><code>solver._success</code> \u2190 <code>SMT_OK</code> (or <code>SMT_INC_OK</code> for incomplete solvers)</li> <li><code>solver._timeouts</code> \u2190 <code>SMT_TIMEOUT</code></li> <li><code>solver._statuses</code> \u2190 <code>SMT_ALL</code></li> </ul> <p>Used by: Cvc5, Z3, Bitwuzla, Llm2smt.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/smt.py</code> <pre><code>class Smt(Decorator):\n   \"\"\"Parse the SMT-LIB2 status token from solver output.\n\n   Matches one of `sat`, `unsat`, `unknown`, or `timeout` on its own line\n   and writes it to `result[\"status\"]`.  Falls back to `\"error\"` if no\n   token is found and `result[\"status\"]` is not yet set.\n\n   On `register()` it also updates the solver's status sets so that\n   [`Solver.solved`][solverpy.solver.solver.Solver.solved] and\n   [`Solver.valid`][solverpy.solver.solver.Solver.valid] work correctly:\n\n   - `solver._success` \u2190 `SMT_OK` (or `SMT_INC_OK` for incomplete solvers)\n   - `solver._timeouts` \u2190 `SMT_TIMEOUT`\n   - `solver._statuses` \u2190 `SMT_ALL`\n\n   Used by: Cvc5, Z3, Bitwuzla, Llm2smt.\n   \"\"\"\n\n   def __init__(self, complete=True, **kwargs):\n      \"\"\"Args:\n         complete: If `True` (default), `sat` is counted as success.  Set to\n            `False` for incomplete solvers where `sat` may be unreliable.\n      \"\"\"\n      Decorator.__init__(self, complete=complete, **kwargs)\n      self._complete = complete\n\n   def register(self, solver: \"SolverPy\"):\n      \"\"\"Append to decorators and update the solver's status sets.\"\"\"\n      super().register(solver)\n      solver._success |= SMT_OK if self._complete else SMT_INC_OK\n      solver._timeouts |= SMT_TIMEOUT\n      solver._statuses |= SMT_ALL\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      del instance, strategy  # unused arguments\n      return cmd\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Extract the SMT status token and write it to `result[\"status\"]`.\"\"\"\n      del instance, strategy  # unused arguments\n      status = patterns.single(SMT_STATUS, output, \"\")\n      if status:\n         result[\"status\"] = status\n      elif \"status\" not in result:\n         result[\"status\"] = \"error\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/smt/#solverpy.solver.plugins.status.smt.Smt.__init__","title":"<code>__init__(complete=True, **kwargs)</code>","text":"<p>complete: If <code>True</code> (default), <code>sat</code> is counted as success.  Set to    <code>False</code> for incomplete solvers where <code>sat</code> may be unreliable.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/smt.py</code> <pre><code>def __init__(self, complete=True, **kwargs):\n   \"\"\"Args:\n      complete: If `True` (default), `sat` is counted as success.  Set to\n         `False` for incomplete solvers where `sat` may be unreliable.\n   \"\"\"\n   Decorator.__init__(self, complete=complete, **kwargs)\n   self._complete = complete\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/smt/#solverpy.solver.plugins.status.smt.Smt.register","title":"<code>register(solver: SolverPy)</code>","text":"<p>Append to decorators and update the solver's status sets.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/smt.py</code> <pre><code>def register(self, solver: \"SolverPy\"):\n   \"\"\"Append to decorators and update the solver's status sets.\"\"\"\n   super().register(solver)\n   solver._success |= SMT_OK if self._complete else SMT_INC_OK\n   solver._timeouts |= SMT_TIMEOUT\n   solver._statuses |= SMT_ALL\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/smt/#solverpy.solver.plugins.status.smt.Smt.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Extract the SMT status token and write it to <code>result[\"status\"]</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/smt.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Extract the SMT status token and write it to `result[\"status\"]`.\"\"\"\n   del instance, strategy  # unused arguments\n   status = patterns.single(SMT_STATUS, output, \"\")\n   if status:\n      result[\"status\"] = status\n   elif \"status\" not in result:\n      result[\"status\"] = \"error\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/tptp/","title":"module tptp","text":""},{"location":"api/solverpy/solver/plugins/status/tptp/#solverpy.solver.plugins.status.tptp","title":"<code>solverpy.solver.plugins.status.tptp</code>","text":""},{"location":"api/solverpy/solver/plugins/status/tptp/#solverpy.solver.plugins.status.tptp.Tptp","title":"<code>Tptp</code>","text":"<p>               Bases: <code>Decorator</code></p> <p>Parse the TPTP SZS status line from solver output.</p> <p>Matches the pattern <code># SZS status &lt;STATUS&gt;</code> (or <code>%</code> prefix) in the solver output and writes the status token to <code>result[\"status\"]</code>.  Falls back to <code>\"ERROR\"</code> if no status line is found and <code>result[\"status\"]</code> is not yet set.</p> <p>On <code>register()</code> it also updates the solver's status sets so that <code>Solver.solved</code> and <code>Solver.valid</code> work correctly:</p> <ul> <li><code>solver._success</code> \u2190 <code>TPTP_OK</code> (or <code>TPTP_INC_OK</code> for incomplete solvers)</li> <li><code>solver._timeouts</code> \u2190 <code>TPTP_TIMEOUT</code></li> <li><code>solver._statuses</code> \u2190 <code>TPTP_ALL</code></li> </ul> <p>Used by: E, Vampire, Lash, Prover9.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/tptp.py</code> <pre><code>class Tptp(Decorator):\n   \"\"\"Parse the TPTP SZS status line from solver output.\n\n   Matches the pattern `# SZS status &lt;STATUS&gt;` (or `%` prefix) in the solver\n   output and writes the status token to `result[\"status\"]`.  Falls back to\n   `\"ERROR\"` if no status line is found and `result[\"status\"]` is not yet set.\n\n   On `register()` it also updates the solver's status sets so that\n   [`Solver.solved`][solverpy.solver.solver.Solver.solved] and\n   [`Solver.valid`][solverpy.solver.solver.Solver.valid] work correctly:\n\n   - `solver._success` \u2190 `TPTP_OK` (or `TPTP_INC_OK` for incomplete solvers)\n   - `solver._timeouts` \u2190 `TPTP_TIMEOUT`\n   - `solver._statuses` \u2190 `TPTP_ALL`\n\n   Used by: E, Vampire, Lash, Prover9.\n   \"\"\"\n\n   def __init__(self, complete=True, **kwargs):\n      \"\"\"Args:\n         complete: If `True` (default), `Satisfiable` and `CounterSatisfiable`\n            are counted as success.  Set to `False` for incomplete solvers\n            where these statuses are unreliable.\n      \"\"\"\n      Decorator.__init__(self, complete=complete, **kwargs)\n      self._complete = complete\n\n   def register(self, solver: \"SolverPy\"):\n      \"\"\"Append to decorators and update the solver's status sets.\"\"\"\n      super().register(solver)\n      solver._success |= TPTP_OK if self._complete else TPTP_INC_OK\n      solver._timeouts |= TPTP_TIMEOUT\n      solver._statuses |= TPTP_ALL\n\n   def decorate(\n      self,\n      cmd: str,\n      instance: Any,\n      strategy: Any,\n   ) -&gt; str:\n      del instance, strategy\n      return cmd\n\n   def update(\n      self,\n      instance: Any,\n      strategy: Any,\n      output: str,\n      result: \"Result\",\n   ) -&gt; None:\n      \"\"\"Extract the SZS status token and write it to `result[\"status\"]`.\"\"\"\n      del instance, strategy\n      status = patterns.single(TPTP_STATUS, output, \"\")\n      if status:\n         result[\"status\"] = status\n      elif \"status\" not in result:\n         result[\"status\"] = \"ERROR\"\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/tptp/#solverpy.solver.plugins.status.tptp.Tptp.__init__","title":"<code>__init__(complete=True, **kwargs)</code>","text":"<p>complete: If <code>True</code> (default), <code>Satisfiable</code> and <code>CounterSatisfiable</code>    are counted as success.  Set to <code>False</code> for incomplete solvers    where these statuses are unreliable.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/tptp.py</code> <pre><code>def __init__(self, complete=True, **kwargs):\n   \"\"\"Args:\n      complete: If `True` (default), `Satisfiable` and `CounterSatisfiable`\n         are counted as success.  Set to `False` for incomplete solvers\n         where these statuses are unreliable.\n   \"\"\"\n   Decorator.__init__(self, complete=complete, **kwargs)\n   self._complete = complete\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/tptp/#solverpy.solver.plugins.status.tptp.Tptp.register","title":"<code>register(solver: SolverPy)</code>","text":"<p>Append to decorators and update the solver's status sets.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/tptp.py</code> <pre><code>def register(self, solver: \"SolverPy\"):\n   \"\"\"Append to decorators and update the solver's status sets.\"\"\"\n   super().register(solver)\n   solver._success |= TPTP_OK if self._complete else TPTP_INC_OK\n   solver._timeouts |= TPTP_TIMEOUT\n   solver._statuses |= TPTP_ALL\n</code></pre>"},{"location":"api/solverpy/solver/plugins/status/tptp/#solverpy.solver.plugins.status.tptp.Tptp.update","title":"<code>update(instance: Any, strategy: Any, output: str, result: Result) -&gt; None</code>","text":"<p>Extract the SZS status token and write it to <code>result[\"status\"]</code>.</p> Source code in <code>packages/solverpy/src/solverpy/solver/plugins/status/tptp.py</code> <pre><code>def update(\n   self,\n   instance: Any,\n   strategy: Any,\n   output: str,\n   result: \"Result\",\n) -&gt; None:\n   \"\"\"Extract the SZS status token and write it to `result[\"status\"]`.\"\"\"\n   del instance, strategy\n   status = patterns.single(TPTP_STATUS, output, \"\")\n   if status:\n      result[\"status\"] = status\n   elif \"status\" not in result:\n      result[\"status\"] = \"ERROR\"\n</code></pre>"},{"location":"api/solverpy/solver/smt/","title":"Smt Overview","text":""},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt","title":"<code>solverpy.solver.smt</code>","text":""},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt--smt-solvers","title":"SMT solvers","text":"<p>SMT (Satisfiability Modulo Theories) solvers for the SMT-LIB2 format.</p> <p> </p> Solver Binary Notes <code>Cvc5</code> <code>cvc5</code> Leading SMT solver; broad theory support <code>Z3</code> <code>z3</code> Microsoft's SMT solver; wide theory coverage <code>Bitwuzla</code> <code>bitwuzla</code> Specialised for bit-vectors and floating point <code>Llm2smt</code> <code>llm2smt</code> QF_EUF solver <code>Primo</code> <code>primo</code> QF_LRA and QF_UF solver <code>Opensmt</code> <code>opensmt</code> SMT solver with interpolation and proofs <code>SpassSatt</code> <code>SPASS-SATT</code> CDCL(LA) solver for QF_LRA/QF_LIA/QF_LIRA <code>Yices</code> <code>yices-smt2</code> Yices 2; SMT-LIB2 front end of <code>yices</code>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Bitwuzla","title":"<code>Bitwuzla</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>bitwuzla</code> SMT solver for bit-vectors, floating-point, arrays, and uninterpreted functions.</p> <p>The strategy (sid) supplies Bitwuzla's command-line options, appended after the static option <code>-v</code> (verbose, needed for statistics). Verbose core statistics are parsed by <code>process</code>; status is decided by the <code>Smt</code> plugin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/bitwuzla.py</code> <pre><code>class Bitwuzla(ShellSolver):\n   \"\"\"\n   The `bitwuzla` SMT solver for bit-vectors, floating-point, arrays, and\n   uninterpreted functions.\n\n   The strategy (sid) supplies Bitwuzla's command-line options, appended\n   after the static option `-v` (verbose, needed for statistics). Verbose\n   core statistics are parsed by\n   [`process`][solverpy.solver.smt.bitwuzla.Bitwuzla.process]; status is\n   decided by the [`Smt`][solverpy.solver.plugins.status.smt.Smt] plugin.\n   \"\"\"\n\n   _binary = BWZ_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = BWZ_BINARY,\n      static: str = BWZ_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         BWZ_BUILDER,\n         plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse Bitwuzla's verbose core statistics into a result dict.\"\"\"\n      result = patterns.valkey(BWZ_PAT, output, BWZ_TABLE)\n      result = patterns.mapval(result, human.numeric)\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Bitwuzla.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse Bitwuzla's verbose core statistics into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/bitwuzla.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse Bitwuzla's verbose core statistics into a result dict.\"\"\"\n   result = patterns.valkey(BWZ_PAT, output, BWZ_TABLE)\n   result = patterns.mapval(result, human.numeric)\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Cvc5","title":"<code>Cvc5</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>cvc5</code> SMT solver.</p> <p>The strategy (sid) supplies cvc5's command-line options, appended after the static options <code>-Lsmt2 --stats --stats-internal</code>. Statistics matching <code>CVC5_KEYS</code> are parsed by <code>process</code>; status is decided by the <code>Smt</code> plugin, with an explicit <code>timeout</code> status set when cvc5 reports being interrupted by a timeout.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/cvc5.py</code> <pre><code>class Cvc5(ShellSolver):\n   \"\"\"\n   The `cvc5` SMT solver.\n\n   The strategy (sid) supplies cvc5's command-line options, appended after\n   the static options `-Lsmt2 --stats --stats-internal`. Statistics matching\n   `CVC5_KEYS` are parsed by\n   [`process`][solverpy.solver.smt.cvc5.Cvc5.process]; status is decided by\n   the [`Smt`][solverpy.solver.plugins.status.smt.Smt] plugin, with an\n   explicit `timeout` status set when cvc5 reports being interrupted by a\n   timeout.\n   \"\"\"\n\n   _binary = CVC5_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = CVC5_BINARY,\n      static: str = CVC5_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      keys: list[str] = CVC5_KEYS,\n      complete: bool = True,\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         CVC5_BUILDER,\n         plugins,\n         wait=1,\n         binary=binary,\n      )\n      self.pattern: \"Pattern\" = re.compile(\n         r\"^(%s) = (.*)$\" % \"|\".join(keys),\n         flags=re.MULTILINE,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse cvc5's `--stats` output into a result dict.\"\"\"\n\n      def parseval(val: str) -&gt; Any:  # value or dict of values\n         if val.startswith(\"{\") and val.endswith(\"}\"):\n            val0 = val.strip(\" {}\")\n            val0 = val0.split(\",\")\n            val0 = [x.split(\":\") for x in val0]\n            return {x.strip(): human.numeric(y.strip()) for (x, y) in val0}\n         return human.numeric(val)\n\n      result = patterns.keyval(self.pattern, output)\n      result = patterns.mapval(result, parseval)\n      timeouted = patterns.single(CVC5_TIMEOUT, output, \"\")\n      if timeouted:\n         result[\"status\"] = timeouted  # timeouted == \"timeout\"\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Cvc5.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse cvc5's <code>--stats</code> output into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/cvc5.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse cvc5's `--stats` output into a result dict.\"\"\"\n\n   def parseval(val: str) -&gt; Any:  # value or dict of values\n      if val.startswith(\"{\") and val.endswith(\"}\"):\n         val0 = val.strip(\" {}\")\n         val0 = val0.split(\",\")\n         val0 = [x.split(\":\") for x in val0]\n         return {x.strip(): human.numeric(y.strip()) for (x, y) in val0}\n      return human.numeric(val)\n\n   result = patterns.keyval(self.pattern, output)\n   result = patterns.mapval(result, parseval)\n   timeouted = patterns.single(CVC5_TIMEOUT, output, \"\")\n   if timeouted:\n      result[\"status\"] = timeouted  # timeouted == \"timeout\"\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Llm2smt","title":"<code>Llm2smt</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>llm2smt</code> SMT solver.</p> <p>The strategy (sid) supplies llm2smt's command-line options, appended after the static options <code>--quiet --stats</code>, as for <code>Opensmt</code>. Output statistics are parsed by <code>process</code>; status is decided by the <code>Smt</code> plugin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/llm2smt.py</code> <pre><code>class Llm2smt(ShellSolver):\n   \"\"\"\n   The `llm2smt` SMT solver.\n\n   The strategy (sid) supplies llm2smt's command-line options, appended\n   after the static options `--quiet --stats`, as for\n   [`Opensmt`][solverpy.solver.smt.opensmt.Opensmt]. Output statistics are\n   parsed by\n   [`process`][solverpy.solver.smt.llm2smt.Llm2smt.process]; status is\n   decided by the [`Smt`][solverpy.solver.plugins.status.smt.Smt] plugin.\n   \"\"\"\n\n   _binary = LLM2SMT_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = LLM2SMT_BINARY,\n      static: str = LLM2SMT_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\",\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse llm2smt's `--stats` output into a result dict.\"\"\"\n      result = patterns.keyval(LLM2SMT_STATS, output)\n      return patterns.mapval(result, human.numeric)\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Llm2smt.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse llm2smt's <code>--stats</code> output into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/llm2smt.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse llm2smt's `--stats` output into a result dict.\"\"\"\n   result = patterns.keyval(LLM2SMT_STATS, output)\n   return patterns.mapval(result, human.numeric)\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Opensmt","title":"<code>Opensmt</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>opensmt</code> SMT solver.</p> <p>The strategy (sid) supplies opensmt's command-line options, as for <code>Primo</code> and <code>Llm2smt</code>.  An empty sid means opensmt's own defaults.</p> <p>opensmt has no time or memory limit options of its own, so the limit is enforced by the shell <code>timeout</code> wrapper and the memory plugin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/opensmt.py</code> <pre><code>class Opensmt(ShellSolver):\n   \"\"\"\n   The `opensmt` SMT solver.\n\n   The strategy (sid) supplies opensmt's command-line options, as for\n   [`Primo`][solverpy.solver.smt.primo.Primo] and\n   [`Llm2smt`][solverpy.solver.smt.llm2smt.Llm2smt].  An empty sid means\n   opensmt's own defaults.\n\n   opensmt has no time or memory limit options of its own, so the limit is\n   enforced by the shell `timeout` wrapper and the memory plugin.\n   \"\"\"\n\n   _binary = OPENSMT_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = OPENSMT_BINARY,\n      static: str = OPENSMT_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\".rstrip(),\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      # A release opensmt emits no statistics, so there is nothing to parse:\n      # `status` comes from the Smt plugin (opensmt prints `sat`, `unsat` or\n      # `unknown` on its own line) and `runtime` from Time.\n      #\n      # Statistics exist in the sources but are compiled out.  They are behind\n      # the `STATISTICS` cmake option, which defaults to OFF, and only then do\n      # `(set-option :produce-stats 1)` and the `; Label....: value` dumps in\n      # CoreSMTSolver/TSolver/Egraph do anything.  Enabling it would need a\n      # section-aware parser rather than a flat one, because each solver\n      # repeats the same labels under its own `; STATISTICS FOR &lt;name&gt;`\n      # heading.  `-v` is not a substitute: it traces restart progress to\n      # stderr during the search rather than reporting final counters.\n      del output  # no statistics in a release build\n      return {}\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Primo","title":"<code>Primo</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>primo</code> QF_LRA/QF_UF SMT solver.</p> <p>The strategy (sid) supplies primo's command-line options, appended after the static options, as for <code>Llm2smt</code>.</p> <p>primo has no time or memory limit options of its own, so the limit is enforced by the shell <code>timeout</code> wrapper and the memory plugin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/primo.py</code> <pre><code>class Primo(ShellSolver):\n   \"\"\"\n   The `primo` QF_LRA/QF_UF SMT solver.\n\n   The strategy (sid) supplies primo's command-line options, appended after\n   the static options, as for [`Llm2smt`][solverpy.solver.smt.llm2smt.Llm2smt].\n\n   primo has no time or memory limit options of its own, so the limit is\n   enforced by the shell `timeout` wrapper and the memory plugin.\n   \"\"\"\n\n   _binary = PRIMO_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = PRIMO_BINARY,\n      static: str = PRIMO_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n         RunHash(PRIMO_RUNHASH_GEN),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\",\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      result = patterns.keyval(PRIMO_STATS, output)\n      result = patterns.mapval(result, human.numeric)\n      # primo prints every counter it tracks, most of which are zero for any\n      # one instance -- around 86% of the 220 keys on the QF_LRA benchmarks.\n      # Zero and absent already mean the same thing here, because primo also\n      # omits whole families it never reached (a problem settled during\n      # preprocessing emits no `sat.*` or `lra.*` keys at all), so read a\n      # missing key as zero.\n      return {key: val for (key, val) in result.items() if val != 0}\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.SpassSatt","title":"<code>SpassSatt</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>SPASS-SATT</code> CDCL(LA) solver for linear rational and linear mixed/integer arithmetic (QF_LRA, QF_LIA, QF_LIRA).</p> <p>The strategy (sid) supplies SPASS-SATT's command-line options, as for <code>Opensmt</code>. An empty sid means SPASS-SATT's own defaults.</p> <p>SPASS-SATT has no time or memory limit options of its own, so the limit is enforced by the shell <code>timeout</code> wrapper and the memory plugin. It also reads only from a file argument, not stdin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/spasssatt.py</code> <pre><code>class SpassSatt(ShellSolver):\n   \"\"\"\n   The `SPASS-SATT` CDCL(LA) solver for linear rational and linear\n   mixed/integer arithmetic (QF_LRA, QF_LIA, QF_LIRA).\n\n   The strategy (sid) supplies SPASS-SATT's command-line options, as for\n   [`Opensmt`][solverpy.solver.smt.opensmt.Opensmt]. An empty sid means\n   SPASS-SATT's own defaults.\n\n   SPASS-SATT has no time or memory limit options of its own, so the limit\n   is enforced by the shell `timeout` wrapper and the memory plugin. It also\n   reads only from a file argument, not stdin.\n   \"\"\"\n\n   _binary = SPASSSATT_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = SPASSSATT_BINARY,\n      static: str = SPASSSATT_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\".rstrip(),\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      # With no flags, SPASS-SATT emits no statistics: `status` comes from\n      # the Smt plugin (SPASS-SATT prints `sat` or `unsat` on its own line)\n      # and `runtime` from Time. `-t`/`-S` add timing and LA-statistics\n      # blocks, but their format is a multi-section dump (sharing stats, SAT\n      # solver stats, simplex stats, branch-and-bound stats, phase timings)\n      # rather than flat key/value lines, so it is not parsed here.\n      del output  # no statistics parsed\n      return {}\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Yices","title":"<code>Yices</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>yices-smt2</code> SMT solver (Yices 2).</p> <p>The strategy (sid) supplies yices' command-line options, appended after the static options, as for <code>Primo</code>.</p> <p>yices does have a <code>--timeout=&lt;seconds&gt;</code> option, but it is deliberately unused, so the limit is left to the shell <code>timeout</code> wrapper as for <code>Primo</code>.  On its own timeout yices answers <code>unknown</code> and exits 0, which at the default verbosity is indistinguishable from a genuine <code>unknown</code> -- the <code>(check-sat: interrupted)</code> marker appears only at <code>-v 2</code>, which also floods the output with a line per restart.  <code>unknown</code> counts as a failure and not as a timeout for <code>Smt</code>, so such a result would never be recomputed at a longer cutoff.  Letting <code>timeout</code> kill yices instead yields exit code 124 and the correct <code>TIMEOUT</code> status, at the cost of the statistics of runs that time out.</p> <p>yices has no memory limit option either; that is the memory plugin's job.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/yices.py</code> <pre><code>class Yices(ShellSolver):\n   \"\"\"\n   The `yices-smt2` SMT solver (Yices 2).\n\n   The strategy (sid) supplies yices' command-line options, appended after\n   the static options, as for [`Primo`][solverpy.solver.smt.primo.Primo].\n\n   yices does have a `--timeout=&lt;seconds&gt;` option, but it is deliberately\n   unused, so the limit is left to the shell `timeout` wrapper as for\n   [`Primo`][solverpy.solver.smt.primo.Primo].  On its own timeout yices\n   answers `unknown` and exits 0, which at the default verbosity is\n   indistinguishable from a genuine `unknown` -- the `(check-sat:\n   interrupted)` marker appears only at `-v 2`, which also floods the output\n   with a line per restart.  `unknown` counts as a failure and not as a\n   timeout for [`Smt`][solverpy.solver.plugins.status.smt.Smt], so such a\n   result would never be recomputed at a longer cutoff.  Letting `timeout`\n   kill yices instead yields exit code 124 and the correct `TIMEOUT` status,\n   at the cost of the statistics of runs that time out.\n\n   yices has no memory limit option either; that is the memory plugin's job.\n   \"\"\"\n\n   _binary = YICES_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = YICES_BINARY,\n      static: str = YICES_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\",\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      result = patterns.keyval(YICES_STATS, output)\n      result = patterns.mapval(result, human.numeric)\n      # Drop the zero counters, as for `Primo`.  Zero and absent already mean\n      # the same thing here: yices prints only the counters of the solver\n      # components it built, so a QF_AUFBV run reports four keys and no\n      # `simplex-*` or `egraph-*` family at all, and a missing key had to be\n      # read as zero anyway.  It saves the most under `--mcsat`, which emits\n      # over sixty keys of which almost all stay zero.\n      return {key: val for (key, val) in result.items() if val != 0}\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Z3","title":"<code>Z3</code>","text":"<p>               Bases: <code>StdinSolver</code></p> <p>The <code>z3</code> SMT solver.</p> <p>A <code>StdinSolver</code>: the strategy (sid) is SMT-LIB2 fed on stdin along with the problem instance, after the static options <code>-smt2 -st</code>. If the strategy contains a <code>check-sat-using</code> command, it replaces the instance's own <code>check-sat</code>. Output is parsed by <code>process</code>; status is decided by the <code>Smt</code> plugin, with an explicit <code>memout</code> status set on an \"out of memory\" error.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/z3.py</code> <pre><code>class Z3(StdinSolver):\n   \"\"\"\n   The `z3` SMT solver.\n\n   A [`StdinSolver`][solverpy.solver.stdinsolver.StdinSolver]: the strategy\n   (sid) is SMT-LIB2 fed on stdin along with the problem instance, after\n   the static options `-smt2 -st`. If the strategy contains a\n   `check-sat-using` command, it replaces the instance's own `check-sat`.\n   Output is parsed by\n   [`process`][solverpy.solver.smt.z3.Z3.process]; status is decided by the\n   [`Smt`][solverpy.solver.plugins.status.smt.Smt] plugin, with an explicit\n   `memout` status set on an \"out of memory\" error.\n   \"\"\"\n\n   _binary = Z3_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = Z3_BINARY,\n      static: str = \"\",\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      cmd = f\"{binary} {Z3_STATIC}\"\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      StdinSolver.__init__(\n         self,\n         cmd,\n         limit,\n         Z3_BUILDER,\n         plugins,\n         1,\n         static,\n         True,\n         True,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse z3's `-st` statistics into a result dict; sets `memout` on out-of-memory.\"\"\"\n      result = patterns.keyval(Z3_PAT, output)\n      result = patterns.mapval(result, human.numeric)\n      if re.search(Z3_MEMOUT, output):\n         result[\"status\"] = \"memout\"\n      return result\n\n   def input(self, instance: Any, strategy: Any) -&gt; bytes:\n      (instance, strategy) = self.translate(instance, strategy)\n      inputstr = self._static.encode()\n      inputstr += strategy.encode()\n      inputstr += b\"\\n\"\n      mo = Z3_USING.search(strategy)\n      if mo:\n         check = mo.group(1)\n         in0 = open(instance, \"r\").read()\n         (in0, count) = Z3_CHECKSAT.subn(check, in0)\n         if count != 1:\n            logger.warning(f\"Unexpected number ({count}) of 'check-sat' patterns in instance '{instance}' for strategy '{strategy}'.\")\n         inputstr += in0.encode()\n      else:\n         inputstr += open(instance, \"rb\").read()\n      #open(f\"/tmp/solverpy-tmp.smt2\", \"wb\").write(inputstr)\n      return inputstr\n</code></pre>"},{"location":"api/solverpy/solver/smt/#solverpy.solver.smt.Z3.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse z3's <code>-st</code> statistics into a result dict; sets <code>memout</code> on out-of-memory.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/z3.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse z3's `-st` statistics into a result dict; sets `memout` on out-of-memory.\"\"\"\n   result = patterns.keyval(Z3_PAT, output)\n   result = patterns.mapval(result, human.numeric)\n   if re.search(Z3_MEMOUT, output):\n      result[\"status\"] = \"memout\"\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/smt/bitwuzla/","title":"module bitwuzla","text":""},{"location":"api/solverpy/solver/smt/bitwuzla/#solverpy.solver.smt.bitwuzla","title":"<code>solverpy.solver.smt.bitwuzla</code>","text":""},{"location":"api/solverpy/solver/smt/bitwuzla/#solverpy.solver.smt.bitwuzla.Bitwuzla","title":"<code>Bitwuzla</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>bitwuzla</code> SMT solver for bit-vectors, floating-point, arrays, and uninterpreted functions.</p> <p>The strategy (sid) supplies Bitwuzla's command-line options, appended after the static option <code>-v</code> (verbose, needed for statistics). Verbose core statistics are parsed by <code>process</code>; status is decided by the <code>Smt</code> plugin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/bitwuzla.py</code> <pre><code>class Bitwuzla(ShellSolver):\n   \"\"\"\n   The `bitwuzla` SMT solver for bit-vectors, floating-point, arrays, and\n   uninterpreted functions.\n\n   The strategy (sid) supplies Bitwuzla's command-line options, appended\n   after the static option `-v` (verbose, needed for statistics). Verbose\n   core statistics are parsed by\n   [`process`][solverpy.solver.smt.bitwuzla.Bitwuzla.process]; status is\n   decided by the [`Smt`][solverpy.solver.plugins.status.smt.Smt] plugin.\n   \"\"\"\n\n   _binary = BWZ_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = BWZ_BINARY,\n      static: str = BWZ_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         BWZ_BUILDER,\n         plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse Bitwuzla's verbose core statistics into a result dict.\"\"\"\n      result = patterns.valkey(BWZ_PAT, output, BWZ_TABLE)\n      result = patterns.mapval(result, human.numeric)\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/smt/bitwuzla/#solverpy.solver.smt.bitwuzla.Bitwuzla.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse Bitwuzla's verbose core statistics into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/bitwuzla.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse Bitwuzla's verbose core statistics into a result dict.\"\"\"\n   result = patterns.valkey(BWZ_PAT, output, BWZ_TABLE)\n   result = patterns.mapval(result, human.numeric)\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/smt/cvc5/","title":"module cvc5","text":""},{"location":"api/solverpy/solver/smt/cvc5/#solverpy.solver.smt.cvc5","title":"<code>solverpy.solver.smt.cvc5</code>","text":""},{"location":"api/solverpy/solver/smt/cvc5/#solverpy.solver.smt.cvc5.Cvc5","title":"<code>Cvc5</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>cvc5</code> SMT solver.</p> <p>The strategy (sid) supplies cvc5's command-line options, appended after the static options <code>-Lsmt2 --stats --stats-internal</code>. Statistics matching <code>CVC5_KEYS</code> are parsed by <code>process</code>; status is decided by the <code>Smt</code> plugin, with an explicit <code>timeout</code> status set when cvc5 reports being interrupted by a timeout.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/cvc5.py</code> <pre><code>class Cvc5(ShellSolver):\n   \"\"\"\n   The `cvc5` SMT solver.\n\n   The strategy (sid) supplies cvc5's command-line options, appended after\n   the static options `-Lsmt2 --stats --stats-internal`. Statistics matching\n   `CVC5_KEYS` are parsed by\n   [`process`][solverpy.solver.smt.cvc5.Cvc5.process]; status is decided by\n   the [`Smt`][solverpy.solver.plugins.status.smt.Smt] plugin, with an\n   explicit `timeout` status set when cvc5 reports being interrupted by a\n   timeout.\n   \"\"\"\n\n   _binary = CVC5_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = CVC5_BINARY,\n      static: str = CVC5_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      keys: list[str] = CVC5_KEYS,\n      complete: bool = True,\n   ):\n      cmd = f\"{binary} {static}\"\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         cmd,\n         limit,\n         CVC5_BUILDER,\n         plugins,\n         wait=1,\n         binary=binary,\n      )\n      self.pattern: \"Pattern\" = re.compile(\n         r\"^(%s) = (.*)$\" % \"|\".join(keys),\n         flags=re.MULTILINE,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse cvc5's `--stats` output into a result dict.\"\"\"\n\n      def parseval(val: str) -&gt; Any:  # value or dict of values\n         if val.startswith(\"{\") and val.endswith(\"}\"):\n            val0 = val.strip(\" {}\")\n            val0 = val0.split(\",\")\n            val0 = [x.split(\":\") for x in val0]\n            return {x.strip(): human.numeric(y.strip()) for (x, y) in val0}\n         return human.numeric(val)\n\n      result = patterns.keyval(self.pattern, output)\n      result = patterns.mapval(result, parseval)\n      timeouted = patterns.single(CVC5_TIMEOUT, output, \"\")\n      if timeouted:\n         result[\"status\"] = timeouted  # timeouted == \"timeout\"\n      return result\n</code></pre>"},{"location":"api/solverpy/solver/smt/cvc5/#solverpy.solver.smt.cvc5.Cvc5.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse cvc5's <code>--stats</code> output into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/cvc5.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse cvc5's `--stats` output into a result dict.\"\"\"\n\n   def parseval(val: str) -&gt; Any:  # value or dict of values\n      if val.startswith(\"{\") and val.endswith(\"}\"):\n         val0 = val.strip(\" {}\")\n         val0 = val0.split(\",\")\n         val0 = [x.split(\":\") for x in val0]\n         return {x.strip(): human.numeric(y.strip()) for (x, y) in val0}\n      return human.numeric(val)\n\n   result = patterns.keyval(self.pattern, output)\n   result = patterns.mapval(result, parseval)\n   timeouted = patterns.single(CVC5_TIMEOUT, output, \"\")\n   if timeouted:\n      result[\"status\"] = timeouted  # timeouted == \"timeout\"\n   return result\n</code></pre>"},{"location":"api/solverpy/solver/smt/llm2smt/","title":"module llm2smt","text":""},{"location":"api/solverpy/solver/smt/llm2smt/#solverpy.solver.smt.llm2smt","title":"<code>solverpy.solver.smt.llm2smt</code>","text":""},{"location":"api/solverpy/solver/smt/llm2smt/#solverpy.solver.smt.llm2smt.Llm2smt","title":"<code>Llm2smt</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>llm2smt</code> SMT solver.</p> <p>The strategy (sid) supplies llm2smt's command-line options, appended after the static options <code>--quiet --stats</code>, as for <code>Opensmt</code>. Output statistics are parsed by <code>process</code>; status is decided by the <code>Smt</code> plugin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/llm2smt.py</code> <pre><code>class Llm2smt(ShellSolver):\n   \"\"\"\n   The `llm2smt` SMT solver.\n\n   The strategy (sid) supplies llm2smt's command-line options, appended\n   after the static options `--quiet --stats`, as for\n   [`Opensmt`][solverpy.solver.smt.opensmt.Opensmt]. Output statistics are\n   parsed by\n   [`process`][solverpy.solver.smt.llm2smt.Llm2smt.process]; status is\n   decided by the [`Smt`][solverpy.solver.plugins.status.smt.Smt] plugin.\n   \"\"\"\n\n   _binary = LLM2SMT_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = LLM2SMT_BINARY,\n      static: str = LLM2SMT_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\",\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse llm2smt's `--stats` output into a result dict.\"\"\"\n      result = patterns.keyval(LLM2SMT_STATS, output)\n      return patterns.mapval(result, human.numeric)\n</code></pre>"},{"location":"api/solverpy/solver/smt/llm2smt/#solverpy.solver.smt.llm2smt.Llm2smt.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse llm2smt's <code>--stats</code> output into a result dict.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/llm2smt.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse llm2smt's `--stats` output into a result dict.\"\"\"\n   result = patterns.keyval(LLM2SMT_STATS, output)\n   return patterns.mapval(result, human.numeric)\n</code></pre>"},{"location":"api/solverpy/solver/smt/opensmt/","title":"module opensmt","text":""},{"location":"api/solverpy/solver/smt/opensmt/#solverpy.solver.smt.opensmt","title":"<code>solverpy.solver.smt.opensmt</code>","text":""},{"location":"api/solverpy/solver/smt/opensmt/#solverpy.solver.smt.opensmt.Opensmt","title":"<code>Opensmt</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>opensmt</code> SMT solver.</p> <p>The strategy (sid) supplies opensmt's command-line options, as for <code>Primo</code> and <code>Llm2smt</code>.  An empty sid means opensmt's own defaults.</p> <p>opensmt has no time or memory limit options of its own, so the limit is enforced by the shell <code>timeout</code> wrapper and the memory plugin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/opensmt.py</code> <pre><code>class Opensmt(ShellSolver):\n   \"\"\"\n   The `opensmt` SMT solver.\n\n   The strategy (sid) supplies opensmt's command-line options, as for\n   [`Primo`][solverpy.solver.smt.primo.Primo] and\n   [`Llm2smt`][solverpy.solver.smt.llm2smt.Llm2smt].  An empty sid means\n   opensmt's own defaults.\n\n   opensmt has no time or memory limit options of its own, so the limit is\n   enforced by the shell `timeout` wrapper and the memory plugin.\n   \"\"\"\n\n   _binary = OPENSMT_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = OPENSMT_BINARY,\n      static: str = OPENSMT_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\".rstrip(),\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      # A release opensmt emits no statistics, so there is nothing to parse:\n      # `status` comes from the Smt plugin (opensmt prints `sat`, `unsat` or\n      # `unknown` on its own line) and `runtime` from Time.\n      #\n      # Statistics exist in the sources but are compiled out.  They are behind\n      # the `STATISTICS` cmake option, which defaults to OFF, and only then do\n      # `(set-option :produce-stats 1)` and the `; Label....: value` dumps in\n      # CoreSMTSolver/TSolver/Egraph do anything.  Enabling it would need a\n      # section-aware parser rather than a flat one, because each solver\n      # repeats the same labels under its own `; STATISTICS FOR &lt;name&gt;`\n      # heading.  `-v` is not a substitute: it traces restart progress to\n      # stderr during the search rather than reporting final counters.\n      del output  # no statistics in a release build\n      return {}\n</code></pre>"},{"location":"api/solverpy/solver/smt/primo/","title":"module primo","text":""},{"location":"api/solverpy/solver/smt/primo/#solverpy.solver.smt.primo","title":"<code>solverpy.solver.smt.primo</code>","text":""},{"location":"api/solverpy/solver/smt/primo/#solverpy.solver.smt.primo.Primo","title":"<code>Primo</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>primo</code> QF_LRA/QF_UF SMT solver.</p> <p>The strategy (sid) supplies primo's command-line options, appended after the static options, as for <code>Llm2smt</code>.</p> <p>primo has no time or memory limit options of its own, so the limit is enforced by the shell <code>timeout</code> wrapper and the memory plugin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/primo.py</code> <pre><code>class Primo(ShellSolver):\n   \"\"\"\n   The `primo` QF_LRA/QF_UF SMT solver.\n\n   The strategy (sid) supplies primo's command-line options, appended after\n   the static options, as for [`Llm2smt`][solverpy.solver.smt.llm2smt.Llm2smt].\n\n   primo has no time or memory limit options of its own, so the limit is\n   enforced by the shell `timeout` wrapper and the memory plugin.\n   \"\"\"\n\n   _binary = PRIMO_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = PRIMO_BINARY,\n      static: str = PRIMO_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n         RunHash(PRIMO_RUNHASH_GEN),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\",\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      result = patterns.keyval(PRIMO_STATS, output)\n      result = patterns.mapval(result, human.numeric)\n      # primo prints every counter it tracks, most of which are zero for any\n      # one instance -- around 86% of the 220 keys on the QF_LRA benchmarks.\n      # Zero and absent already mean the same thing here, because primo also\n      # omits whole families it never reached (a problem settled during\n      # preprocessing emits no `sat.*` or `lra.*` keys at all), so read a\n      # missing key as zero.\n      return {key: val for (key, val) in result.items() if val != 0}\n</code></pre>"},{"location":"api/solverpy/solver/smt/spasssatt/","title":"module spasssatt","text":""},{"location":"api/solverpy/solver/smt/spasssatt/#solverpy.solver.smt.spasssatt","title":"<code>solverpy.solver.smt.spasssatt</code>","text":""},{"location":"api/solverpy/solver/smt/spasssatt/#solverpy.solver.smt.spasssatt.SpassSatt","title":"<code>SpassSatt</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>SPASS-SATT</code> CDCL(LA) solver for linear rational and linear mixed/integer arithmetic (QF_LRA, QF_LIA, QF_LIRA).</p> <p>The strategy (sid) supplies SPASS-SATT's command-line options, as for <code>Opensmt</code>. An empty sid means SPASS-SATT's own defaults.</p> <p>SPASS-SATT has no time or memory limit options of its own, so the limit is enforced by the shell <code>timeout</code> wrapper and the memory plugin. It also reads only from a file argument, not stdin.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/spasssatt.py</code> <pre><code>class SpassSatt(ShellSolver):\n   \"\"\"\n   The `SPASS-SATT` CDCL(LA) solver for linear rational and linear\n   mixed/integer arithmetic (QF_LRA, QF_LIA, QF_LIRA).\n\n   The strategy (sid) supplies SPASS-SATT's command-line options, as for\n   [`Opensmt`][solverpy.solver.smt.opensmt.Opensmt]. An empty sid means\n   SPASS-SATT's own defaults.\n\n   SPASS-SATT has no time or memory limit options of its own, so the limit\n   is enforced by the shell `timeout` wrapper and the memory plugin. It also\n   reads only from a file argument, not stdin.\n   \"\"\"\n\n   _binary = SPASSSATT_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = SPASSSATT_BINARY,\n      static: str = SPASSSATT_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\".rstrip(),\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      # With no flags, SPASS-SATT emits no statistics: `status` comes from\n      # the Smt plugin (SPASS-SATT prints `sat` or `unsat` on its own line)\n      # and `runtime` from Time. `-t`/`-S` add timing and LA-statistics\n      # blocks, but their format is a multi-section dump (sharing stats, SAT\n      # solver stats, simplex stats, branch-and-bound stats, phase timings)\n      # rather than flat key/value lines, so it is not parsed here.\n      del output  # no statistics parsed\n      return {}\n</code></pre>"},{"location":"api/solverpy/solver/smt/yices/","title":"module yices","text":""},{"location":"api/solverpy/solver/smt/yices/#solverpy.solver.smt.yices","title":"<code>solverpy.solver.smt.yices</code>","text":""},{"location":"api/solverpy/solver/smt/yices/#solverpy.solver.smt.yices.Yices","title":"<code>Yices</code>","text":"<p>               Bases: <code>ShellSolver</code></p> <p>The <code>yices-smt2</code> SMT solver (Yices 2).</p> <p>The strategy (sid) supplies yices' command-line options, appended after the static options, as for <code>Primo</code>.</p> <p>yices does have a <code>--timeout=&lt;seconds&gt;</code> option, but it is deliberately unused, so the limit is left to the shell <code>timeout</code> wrapper as for <code>Primo</code>.  On its own timeout yices answers <code>unknown</code> and exits 0, which at the default verbosity is indistinguishable from a genuine <code>unknown</code> -- the <code>(check-sat: interrupted)</code> marker appears only at <code>-v 2</code>, which also floods the output with a line per restart.  <code>unknown</code> counts as a failure and not as a timeout for <code>Smt</code>, so such a result would never be recomputed at a longer cutoff.  Letting <code>timeout</code> kill yices instead yields exit code 124 and the correct <code>TIMEOUT</code> status, at the cost of the statistics of runs that time out.</p> <p>yices has no memory limit option either; that is the memory plugin's job.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/yices.py</code> <pre><code>class Yices(ShellSolver):\n   \"\"\"\n   The `yices-smt2` SMT solver (Yices 2).\n\n   The strategy (sid) supplies yices' command-line options, appended after\n   the static options, as for [`Primo`][solverpy.solver.smt.primo.Primo].\n\n   yices does have a `--timeout=&lt;seconds&gt;` option, but it is deliberately\n   unused, so the limit is left to the shell `timeout` wrapper as for\n   [`Primo`][solverpy.solver.smt.primo.Primo].  On its own timeout yices\n   answers `unknown` and exits 0, which at the default verbosity is\n   indistinguishable from a genuine `unknown` -- the `(check-sat:\n   interrupted)` marker appears only at `-v 2`, which also floods the output\n   with a line per restart.  `unknown` counts as a failure and not as a\n   timeout for [`Smt`][solverpy.solver.plugins.status.smt.Smt], so such a\n   result would never be recomputed at a longer cutoff.  Letting `timeout`\n   kill yices instead yields exit code 124 and the correct `TIMEOUT` status,\n   at the cost of the statistics of runs that time out.\n\n   yices has no memory limit option either; that is the memory plugin's job.\n   \"\"\"\n\n   _binary = YICES_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = YICES_BINARY,\n      static: str = YICES_STATIC,\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      ShellSolver.__init__(\n         self,\n         f\"{binary} {static}\",\n         limit,\n         plugins=plugins,\n         wait=1,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      result = patterns.keyval(YICES_STATS, output)\n      result = patterns.mapval(result, human.numeric)\n      # Drop the zero counters, as for `Primo`.  Zero and absent already mean\n      # the same thing here: yices prints only the counters of the solver\n      # components it built, so a QF_AUFBV run reports four keys and no\n      # `simplex-*` or `egraph-*` family at all, and a missing key had to be\n      # read as zero anyway.  It saves the most under `--mcsat`, which emits\n      # over sixty keys of which almost all stay zero.\n      return {key: val for (key, val) in result.items() if val != 0}\n</code></pre>"},{"location":"api/solverpy/solver/smt/z3/","title":"module z3","text":""},{"location":"api/solverpy/solver/smt/z3/#solverpy.solver.smt.z3","title":"<code>solverpy.solver.smt.z3</code>","text":""},{"location":"api/solverpy/solver/smt/z3/#solverpy.solver.smt.z3.Z3","title":"<code>Z3</code>","text":"<p>               Bases: <code>StdinSolver</code></p> <p>The <code>z3</code> SMT solver.</p> <p>A <code>StdinSolver</code>: the strategy (sid) is SMT-LIB2 fed on stdin along with the problem instance, after the static options <code>-smt2 -st</code>. If the strategy contains a <code>check-sat-using</code> command, it replaces the instance's own <code>check-sat</code>. Output is parsed by <code>process</code>; status is decided by the <code>Smt</code> plugin, with an explicit <code>memout</code> status set on an \"out of memory\" error.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/z3.py</code> <pre><code>class Z3(StdinSolver):\n   \"\"\"\n   The `z3` SMT solver.\n\n   A [`StdinSolver`][solverpy.solver.stdinsolver.StdinSolver]: the strategy\n   (sid) is SMT-LIB2 fed on stdin along with the problem instance, after\n   the static options `-smt2 -st`. If the strategy contains a\n   `check-sat-using` command, it replaces the instance's own `check-sat`.\n   Output is parsed by\n   [`process`][solverpy.solver.smt.z3.Z3.process]; status is decided by the\n   [`Smt`][solverpy.solver.plugins.status.smt.Smt] plugin, with an explicit\n   `memout` status set on an \"out of memory\" error.\n   \"\"\"\n\n   _binary = Z3_BINARY\n\n   def __init__(\n      self,\n      limit: str,\n      binary: str = Z3_BINARY,\n      static: str = \"\",\n      plugins: list[\"Plugin\"] = [],\n      complete: bool = True,\n   ):\n      cmd = f\"{binary} {Z3_STATIC}\"\n      plugins = plugins + [\n         Time(),\n         Smt(complete=complete),\n      ]\n      StdinSolver.__init__(\n         self,\n         cmd,\n         limit,\n         Z3_BUILDER,\n         plugins,\n         1,\n         static,\n         True,\n         True,\n         binary=binary,\n      )\n\n   def process(self, output: str) -&gt; \"Result\":\n      \"\"\"Parse z3's `-st` statistics into a result dict; sets `memout` on out-of-memory.\"\"\"\n      result = patterns.keyval(Z3_PAT, output)\n      result = patterns.mapval(result, human.numeric)\n      if re.search(Z3_MEMOUT, output):\n         result[\"status\"] = \"memout\"\n      return result\n\n   def input(self, instance: Any, strategy: Any) -&gt; bytes:\n      (instance, strategy) = self.translate(instance, strategy)\n      inputstr = self._static.encode()\n      inputstr += strategy.encode()\n      inputstr += b\"\\n\"\n      mo = Z3_USING.search(strategy)\n      if mo:\n         check = mo.group(1)\n         in0 = open(instance, \"r\").read()\n         (in0, count) = Z3_CHECKSAT.subn(check, in0)\n         if count != 1:\n            logger.warning(f\"Unexpected number ({count}) of 'check-sat' patterns in instance '{instance}' for strategy '{strategy}'.\")\n         inputstr += in0.encode()\n      else:\n         inputstr += open(instance, \"rb\").read()\n      #open(f\"/tmp/solverpy-tmp.smt2\", \"wb\").write(inputstr)\n      return inputstr\n</code></pre>"},{"location":"api/solverpy/solver/smt/z3/#solverpy.solver.smt.z3.Z3.process","title":"<code>process(output: str) -&gt; Result</code>","text":"<p>Parse z3's <code>-st</code> statistics into a result dict; sets <code>memout</code> on out-of-memory.</p> Source code in <code>packages/solverpy/src/solverpy/solver/smt/z3.py</code> <pre><code>def process(self, output: str) -&gt; \"Result\":\n   \"\"\"Parse z3's `-st` statistics into a result dict; sets `memout` on out-of-memory.\"\"\"\n   result = patterns.keyval(Z3_PAT, output)\n   result = patterns.mapval(result, human.numeric)\n   if re.search(Z3_MEMOUT, output):\n      result[\"status\"] = \"memout\"\n   return result\n</code></pre>"},{"location":"api/solverpy/task/","title":"Task Overview","text":""},{"location":"api/solverpy/task/#solverpy.task","title":"<code>solverpy.task</code>","text":""},{"location":"api/solverpy/task/#solverpy.task--task-management-and-parallel-execution","title":"Task management and parallel execution","text":"<p>The <code>task</code> module handles parallel execution of solver runs.  A <code>SolverTask</code> wraps a single <code>(solver, bid, sid, problem)</code> job.  The <code>Launcher</code> runs a pool of tasks in parallel using Python's <code>multiprocessing</code>.  Progress is reported through a <code>Talker</code> from the <code>solverpy.report.talker</code> package.</p> <p> </p>"},{"location":"api/solverpy/task/launcher/","title":"module launcher","text":""},{"location":"api/solverpy/task/launcher/#solverpy.task.launcher","title":"<code>solverpy.task.launcher</code>","text":""},{"location":"api/solverpy/task/launcher/#solverpy.task.launcher.run","title":"<code>run(tasks: list[Task], cores: int = 4, chunksize: int = 1) -&gt; list[Any]</code>","text":"<p>Launch <code>tasks</code> in parallel on multiple cores and return results.</p> <p>:param tasks:  list of task to be executed (instances of Task) :param cores:  number of worker threads (Default value = 4) :param chunksize:  chunksize for Pool.map_async (Default value = 1)</p> Source code in <code>packages/solverpy/src/solverpy/task/launcher.py</code> <pre><code>def run(\n   tasks: list[\"Task\"],\n   cores: int = 4,\n   chunksize: int = 1,\n) -&gt; list[Any]:\n   \"\"\"Launch `tasks` in parallel on multiple cores and return results.\n\n   :param tasks:  list of task to be executed (instances of Task)\n   :param cores:  number of worker threads (Default value = 4)\n   :param chunksize:  chunksize for Pool.map_async (Default value = 1)\n   \"\"\"\n   pool = mp.get_context(\"forkserver\").Pool(cores)\n   try:\n      runner = pool.map_async(runtask_single, tasks, chunksize=chunksize)\n      results = runner.get(WAIT)\n      pool.close()\n      pool.join()\n      return results\n   except (Exception, KeyboardInterrupt):\n      pool.terminate()\n      raise \n</code></pre>"},{"location":"api/solverpy/task/shelltask/","title":"module shelltask","text":""},{"location":"api/solverpy/task/shelltask/#solverpy.task.shelltask","title":"<code>solverpy.task.shelltask</code>","text":""},{"location":"api/solverpy/task/solvertask/","title":"module solvertask","text":""},{"location":"api/solverpy/task/solvertask/#solverpy.task.solvertask","title":"<code>solverpy.task.solvertask</code>","text":"<p>Defines the <code>SolverTask</code> class.</p>"},{"location":"api/solverpy/task/solvertask/#solverpy.task.solvertask.SolverTask","title":"<code>SolverTask</code>","text":"<p>               Bases: <code>Task</code></p> <p>Represents a task to be executed by a <code>SolverPy</code> solver.  This is a basic interface for the database which stores results of <code>SolverTask</code>.</p> Source code in <code>packages/solverpy/src/solverpy/task/solvertask.py</code> <pre><code>class SolverTask(Task):\n   \"\"\"\n   Represents a task to be executed by a\n   [`SolverPy`][solverpy.solver.solverpy.SolverPy] solver. \n   This is a basic interface for the database which stores results\n   of [`SolverTask`][solverpy.task.solvertask.SolverTask].\n   \"\"\"\n\n   def __init__(\n      self,\n      solver: \"SolverPy\",\n      bid: str,\n      sid: str,\n      problem: str,\n      calls: list[tuple[str, str, Any, Any]] | None = None,\n   ):\n      \"\"\"\n      Create a new task to execute `solver` on problem `problem` from benchmark\n      `bid` using strategy `sid`.\n\n      Args:\n          solver: the solver based on [`SolverPy`][solverpy.solver.solverpy.SolverPy]\n          bid: the benchmark id\n          sid: the strategy id\n          problem: the problem from the benchmark\n          calls: the plugin calls to be executed before solving the task\n      \"\"\"\n      Task.__init__(self)\n      self._solver = solver\n      self.bid = bid\n      self.sid = sid\n      self.problem = problem\n      self._calls = calls if calls is not None else []\n\n   def __str__(self) -&gt; str:\n      return f\"{self.solver}:{self.sid} @ {self.bid} / {self.problem}\"\n\n   def __eq__(self, other):\n      if not isinstance(other, self.__class__):\n         return False\n      return (f\"{self.solver}\" == f\"{other.solver}\" \\\n         and self.bid == other.bid \\\n         and self.sid == other.sid \\\n         and self.problem == other.problem)\n\n   def __hash__(self):\n      return hash((\n         str(self.solver),\n         self.bid,\n         self.sid,\n         self.problem,\n      ))\n\n   def run(self) -&gt; \"Result\":\n      \"\"\"\n      Run the task and return the result. \n\n      First, plugin calls from `calls` given to the constructor are executed on\n      the [`solver`][solverpy.task.solvertask.SolverTask.solver]. Then,\n      [`SolverPy.solve`][solverpy.solver.solver.Solver.solve] is called on\n      [`instance`][solverpy.task.solvertask.SolverTask.instance] and\n      [`strategy`][solverpy.task.solvertask.SolverTask.strategy].\n\n      Returns: \n         the result dictionary\n      \"\"\"\n      if self.logqueue:\n         Talker.log_config(self.logqueue)\n      for (pid, method, args, kwargs) in self._calls:\n         self.solver.call(pid, method, *args, **kwargs)\n      return self.solver.solve(self.instance, self.strategy)\n\n   def status(self, result: \"Result\") -&gt; bool | None:\n      \"\"\"\n      Translate the result to (typically smaller) status to send it over the\n      queue.\n\n      Args:\n          result: the result\n\n      Returns:\n          the status\n      \"\"\"\n      if not self.solver.valid(result):\n         return None\n      return self.solver.solved(result)\n\n   @property\n   def instance(self) -&gt; tuple[str, str]:\n      \"\"\"\n      Get the instance of this solver task as a benchmark-problem pair.\n\n      Returns:\n          the pair of benchmark id and problem name\n      \"\"\"\n      return (self.bid, self.problem)\n\n   @property\n   def strategy(self) -&gt; str:\n      \"\"\"\n      Get the strategy of this solver task as a strategy id.\n\n      Returns:\n          the strategy\n      \"\"\"\n      return self.sid\n\n   @property\n   def solver(self) -&gt; \"SolverPy\":\n      \"\"\"\n      Get the [`SolverPy`][solverpy.solver.solverpy.SolverPy] solver of this solver task.\n\n      Returns:\n          the solver\n      \"\"\"\n      return self._solver\n</code></pre>"},{"location":"api/solverpy/task/solvertask/#solverpy.task.solvertask.SolverTask.instance","title":"<code>instance: tuple[str, str]</code>  <code>property</code>","text":"<p>Get the instance of this solver task as a benchmark-problem pair.</p> <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>the pair of benchmark id and problem name</p>"},{"location":"api/solverpy/task/solvertask/#solverpy.task.solvertask.SolverTask.solver","title":"<code>solver: SolverPy</code>  <code>property</code>","text":"<p>Get the <code>SolverPy</code> solver of this solver task.</p> <p>Returns:</p> Type Description <code>SolverPy</code> <p>the solver</p>"},{"location":"api/solverpy/task/solvertask/#solverpy.task.solvertask.SolverTask.strategy","title":"<code>strategy: str</code>  <code>property</code>","text":"<p>Get the strategy of this solver task as a strategy id.</p> <p>Returns:</p> Type Description <code>str</code> <p>the strategy</p>"},{"location":"api/solverpy/task/solvertask/#solverpy.task.solvertask.SolverTask.__init__","title":"<code>__init__(solver: SolverPy, bid: str, sid: str, problem: str, calls: list[tuple[str, str, Any, Any]] | None = None)</code>","text":"<p>Create a new task to execute <code>solver</code> on problem <code>problem</code> from benchmark <code>bid</code> using strategy <code>sid</code>.</p> <p>Parameters:</p> Name Type Description Default <code>solver</code> <code>SolverPy</code> <p>the solver based on <code>SolverPy</code></p> required <code>bid</code> <code>str</code> <p>the benchmark id</p> required <code>sid</code> <code>str</code> <p>the strategy id</p> required <code>problem</code> <code>str</code> <p>the problem from the benchmark</p> required <code>calls</code> <code>list[tuple[str, str, Any, Any]] | None</code> <p>the plugin calls to be executed before solving the task</p> <code>None</code> Source code in <code>packages/solverpy/src/solverpy/task/solvertask.py</code> <pre><code>def __init__(\n   self,\n   solver: \"SolverPy\",\n   bid: str,\n   sid: str,\n   problem: str,\n   calls: list[tuple[str, str, Any, Any]] | None = None,\n):\n   \"\"\"\n   Create a new task to execute `solver` on problem `problem` from benchmark\n   `bid` using strategy `sid`.\n\n   Args:\n       solver: the solver based on [`SolverPy`][solverpy.solver.solverpy.SolverPy]\n       bid: the benchmark id\n       sid: the strategy id\n       problem: the problem from the benchmark\n       calls: the plugin calls to be executed before solving the task\n   \"\"\"\n   Task.__init__(self)\n   self._solver = solver\n   self.bid = bid\n   self.sid = sid\n   self.problem = problem\n   self._calls = calls if calls is not None else []\n</code></pre>"},{"location":"api/solverpy/task/solvertask/#solverpy.task.solvertask.SolverTask.run","title":"<code>run() -&gt; Result</code>","text":"<p>Run the task and return the result. </p> <p>First, plugin calls from <code>calls</code> given to the constructor are executed on the <code>solver</code>. Then, <code>SolverPy.solve</code> is called on <code>instance</code> and <code>strategy</code>.</p> <p>Returns:</p> Type Description <code>Result</code> <p>the result dictionary</p> Source code in <code>packages/solverpy/src/solverpy/task/solvertask.py</code> <pre><code>def run(self) -&gt; \"Result\":\n   \"\"\"\n   Run the task and return the result. \n\n   First, plugin calls from `calls` given to the constructor are executed on\n   the [`solver`][solverpy.task.solvertask.SolverTask.solver]. Then,\n   [`SolverPy.solve`][solverpy.solver.solver.Solver.solve] is called on\n   [`instance`][solverpy.task.solvertask.SolverTask.instance] and\n   [`strategy`][solverpy.task.solvertask.SolverTask.strategy].\n\n   Returns: \n      the result dictionary\n   \"\"\"\n   if self.logqueue:\n      Talker.log_config(self.logqueue)\n   for (pid, method, args, kwargs) in self._calls:\n      self.solver.call(pid, method, *args, **kwargs)\n   return self.solver.solve(self.instance, self.strategy)\n</code></pre>"},{"location":"api/solverpy/task/solvertask/#solverpy.task.solvertask.SolverTask.status","title":"<code>status(result: Result) -&gt; bool | None</code>","text":"<p>Translate the result to (typically smaller) status to send it over the queue.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>Result</code> <p>the result</p> required <p>Returns:</p> Type Description <code>bool | None</code> <p>the status</p> Source code in <code>packages/solverpy/src/solverpy/task/solvertask.py</code> <pre><code>def status(self, result: \"Result\") -&gt; bool | None:\n   \"\"\"\n   Translate the result to (typically smaller) status to send it over the\n   queue.\n\n   Args:\n       result: the result\n\n   Returns:\n       the status\n   \"\"\"\n   if not self.solver.valid(result):\n      return None\n   return self.solver.solved(result)\n</code></pre>"},{"location":"api/solverpy/task/task/","title":"module task","text":""},{"location":"api/solverpy/task/task/#solverpy.task.task","title":"<code>solverpy.task.task</code>","text":""},{"location":"api/solverpy/task/task/#solverpy.task.task.Task","title":"<code>Task</code>","text":"<p>A generic executable task/job.</p> <p>Represents a task that can be executed.  The result can be returned or communicated over a queue.  The abstract method <code>run</code> should run the task and return the result.</p> <p>Tasks are typically executed in different threads.  Hence modification to the Task instance might not be visible to the main thread.  Also the returned result should be as small as possible to limit inter-process communication.</p> <p>Alternatively, the communication queue can be used to send the result to the main thread.  If the queue is set, the result is transformed to the <code>status</code> and this status is pushed onto the queue (see <code>Task.runtask</code>).  </p> <p>Status is also propagated to the progress bar using the <code>status</code> bar handler (see <code>RunningBar.status</code>).  <code>DefaultBar</code> ignores the status.  <code>RunningBar</code> expects the status for failed tasks to be None, and non-None otherwise. <code>SolvingBar</code> expects <code>True</code> for solved tasks, <code>False</code> for unsolved, and <code>None</code> for failed tasks.</p> Source code in <code>packages/solverpy/src/solverpy/task/task.py</code> <pre><code>class Task:\n   \"\"\"A generic executable task/job.\n\n   Represents a task that can be executed.  The result can be returned or\n   communicated over a queue.  The abstract method `run` should run the task\n   and return the result.\n\n   Tasks are typically executed in different threads.  Hence modification to\n   the Task instance might not be visible to the main thread.  Also the\n   returned result should be as small as possible to limit inter-process\n   communication.\n\n   Alternatively, the communication queue can be used to send the result to the\n   main thread.  If the queue is set, the result is transformed to the `status`\n   and this status is pushed onto the queue (see `Task.runtask`).  \n\n   Status is also propagated to the progress bar using the `status` bar handler\n   (see `RunningBar.status`).  `DefaultBar` ignores the status.  `RunningBar`\n   expects the status for failed tasks to be None, and non-None otherwise.\n   `SolvingBar` expects `True` for solved tasks, `False` for unsolved, and\n   `None` for failed tasks.\n   \"\"\"\n\n   def __init__(\n      self,\n      queue: \"Queue[Any] | None\" = None,\n      talker: \"Talker | None\" = None,\n      tid: int = -1,\n   ):\n      \"\"\"Init the task.\n\n      :param queue: communication queue (optional) \n\n      \"\"\"\n      self._log_queue = queue\n      self._talker = talker\n      self._tid = tid\n\n   def run(self) -&gt; Any:\n      \"\"\"Run the task and return the result.\"\"\"\n      raise NotImplementedError(\"Task.run: abstract method not implemented.\")\n\n   @property\n   def tid(self) -&gt; int:\n      \"\"\"Get the task id.\"\"\"\n      return self._tid\n\n   @tid.setter\n   def tid(self, tid: int) -&gt; None:\n      \"\"\"Set the task id.\n\n      :param tid: the task id\n\n      \"\"\"\n      self._tid = tid\n\n   @property\n   def logqueue(self) -&gt; \"Queue[Any] | None\":\n      \"\"\"Get the queue.\"\"\"\n      return self._log_queue\n\n   @logqueue.setter\n   def logqueue(self, q: \"Queue[Any]\") -&gt; None:\n      \"\"\"Set the queue.\n\n      :param q: the queue\n\n      \"\"\"\n      self._log_queue = q\n\n   #@property\n   #def talker(self) -&gt; \"Talker | None\":\n   #   \"\"\"Get the talker.\"\"\"\n   #   return self._talker\n\n   #@talker.setter\n   #def talker(self, talker: \"Talker | None\"):\n   #   \"\"\"Set the talker.\n\n   #   :param t: the talker\n\n   #   \"\"\"\n   #   self._talker = talker\n\n   def status(\n      self,\n      result: Any,\n   ) -&gt; Any:\n      \"\"\"Translate the result to (typically smaller) status to send it over the\n      queue.\"\n\n      :param result: the result\n\n      \"\"\"\n      return (result is not None)\n\n   @staticmethod\n   def runtask(task: \"Task\") -&gt; Any:\n      \"\"\"Run the task andd announce the result over the queue.\n\n      :param task: the task to be ran\n\n      \"\"\"\n      try:\n         res = task.run()\n         status = task.status(res)\n      except Exception as e:\n         import traceback\n         logger.warning(f\"Exception:: {e}\")\n         logger.warning(f\"Task:: {task}\")\n         logger.warning(f\"Error:: {traceback.format_exc()}\")\n         status = None\n         res = None\n      if status is None:\n         logger.debug(f\"failed task: {task}\")\n      #if task.queue is not None:\n      #   task.queue.put(status)\n      return res\n</code></pre>"},{"location":"api/solverpy/task/task/#solverpy.task.task.Task.logqueue","title":"<code>logqueue: Queue[Any] | None</code>  <code>property</code> <code>writable</code>","text":"<p>Get the queue.</p>"},{"location":"api/solverpy/task/task/#solverpy.task.task.Task.tid","title":"<code>tid: int</code>  <code>property</code> <code>writable</code>","text":"<p>Get the task id.</p>"},{"location":"api/solverpy/task/task/#solverpy.task.task.Task.__init__","title":"<code>__init__(queue: Queue[Any] | None = None, talker: Talker | None = None, tid: int = -1)</code>","text":"<p>Init the task.</p> <p>:param queue: communication queue (optional)</p> Source code in <code>packages/solverpy/src/solverpy/task/task.py</code> <pre><code>def __init__(\n   self,\n   queue: \"Queue[Any] | None\" = None,\n   talker: \"Talker | None\" = None,\n   tid: int = -1,\n):\n   \"\"\"Init the task.\n\n   :param queue: communication queue (optional) \n\n   \"\"\"\n   self._log_queue = queue\n   self._talker = talker\n   self._tid = tid\n</code></pre>"},{"location":"api/solverpy/task/task/#solverpy.task.task.Task.run","title":"<code>run() -&gt; Any</code>","text":"<p>Run the task and return the result.</p> Source code in <code>packages/solverpy/src/solverpy/task/task.py</code> <pre><code>def run(self) -&gt; Any:\n   \"\"\"Run the task and return the result.\"\"\"\n   raise NotImplementedError(\"Task.run: abstract method not implemented.\")\n</code></pre>"},{"location":"api/solverpy/task/task/#solverpy.task.task.Task.runtask","title":"<code>runtask(task: Task) -&gt; Any</code>  <code>staticmethod</code>","text":"<p>Run the task andd announce the result over the queue.</p> <p>:param task: the task to be ran</p> Source code in <code>packages/solverpy/src/solverpy/task/task.py</code> <pre><code>@staticmethod\ndef runtask(task: \"Task\") -&gt; Any:\n   \"\"\"Run the task andd announce the result over the queue.\n\n   :param task: the task to be ran\n\n   \"\"\"\n   try:\n      res = task.run()\n      status = task.status(res)\n   except Exception as e:\n      import traceback\n      logger.warning(f\"Exception:: {e}\")\n      logger.warning(f\"Task:: {task}\")\n      logger.warning(f\"Error:: {traceback.format_exc()}\")\n      status = None\n      res = None\n   if status is None:\n      logger.debug(f\"failed task: {task}\")\n   #if task.queue is not None:\n   #   task.queue.put(status)\n   return res\n</code></pre>"},{"location":"api/solverpy/task/task/#solverpy.task.task.Task.status","title":"<code>status(result: Any) -&gt; Any</code>","text":"<p>Translate the result to (typically smaller) status to send it over the queue.\"</p> <p>:param result: the result</p> Source code in <code>packages/solverpy/src/solverpy/task/task.py</code> <pre><code>def status(\n   self,\n   result: Any,\n) -&gt; Any:\n   \"\"\"Translate the result to (typically smaller) status to send it over the\n   queue.\"\n\n   :param result: the result\n\n   \"\"\"\n   return (result is not None)\n</code></pre>"},{"location":"api/solverpy/task/task/#solverpy.task.task.runtask","title":"<code>runtask(task: Task) -&gt; tuple[int, Any]</code>","text":"<p>Run task and return the result.</p> <p>:param task:</p> Source code in <code>packages/solverpy/src/solverpy/task/task.py</code> <pre><code>def runtask(task: Task) -&gt; tuple[int, Any]:\n   \"\"\"Run task and return the result.\n\n   :param task: \n\n   \"\"\"\n   return (task.tid, task.runtask(task))\n</code></pre>"},{"location":"api/solverpy/task/task/#solverpy.task.task.runtask_single","title":"<code>runtask_single(task: Task) -&gt; Any</code>","text":"<p>Run task and return the result.</p> <p>:param task:</p> Source code in <code>packages/solverpy/src/solverpy/task/task.py</code> <pre><code>def runtask_single(task: Task) -&gt; Any:\n   \"\"\"Run task and return the result.\n\n   :param task: \n\n   \"\"\"\n   return task.runtask(task)\n</code></pre>"},{"location":"api/solverpy/tools/external/","title":"module external","text":""},{"location":"api/solverpy/tools/external/#solverpy.tools.external","title":"<code>solverpy.tools.external</code>","text":""},{"location":"api/solverpy/tools/external/#solverpy.tools.external.catching","title":"<code>catching(func: Callable) -&gt; Callable</code>","text":"<p>Decorator that catches exceptions and returns them as strings in the case an exception is raised.</p> Source code in <code>packages/solverpy/src/solverpy/tools/external.py</code> <pre><code>def catching(func: Callable) -&gt; Callable:\n   \"\"\"\n   Decorator that catches exceptions and returns them as strings\n   in the case an exception is raised.\n   \"\"\"\n\n   @wraps(func)\n   def wrapper(*args, **kwargs):\n      try:\n         return func(*args, **kwargs)\n      except Exception as e:\n         return str(e)\n\n   return wrapper\n</code></pre>"},{"location":"api/solverpy/tools/external/#solverpy.tools.external.external","title":"<code>external(func: Callable) -&gt; Callable</code>","text":"<p>Decorator that runs a function in a separate forked process to free memory on return.</p> Source code in <code>packages/solverpy/src/solverpy/tools/external.py</code> <pre><code>def external(func: Callable) -&gt; Callable:\n   \"\"\"\n   Decorator that runs a function in a separate forked process to free memory on return.\n   \"\"\"\n\n   @wraps(func)\n   def wrapper(*args, **kwargs):\n      ctx = multiprocessing.get_context(\"fork\")\n      queue = ctx.Queue()\n\n      def target():\n         try:\n            result = func(*args, **kwargs)\n            queue.put((True, result))\n         except Exception as e:\n            queue.put((False, e))\n\n      process = ctx.Process(target=target)\n      process.start()\n      process.join()\n\n      if process.exitcode != 0:\n         raise RuntimeError(\n            f\"Process failed with exit code {process.exitcode}\")\n\n      (success, data) = queue.get()\n      if success:\n         return data\n      else:\n         raise data\n\n   return wrapper\n</code></pre>"},{"location":"api/solverpy/tools/human/","title":"module human","text":""},{"location":"api/solverpy/tools/human/#solverpy.tools.human","title":"<code>solverpy.tools.human</code>","text":""},{"location":"api/solverpy/tools/human/#solverpy.tools.human.humanbytes","title":"<code>humanbytes(b: float, precision: int = 2, separator: str = ' ', binary_units: bool = False) -&gt; str</code>","text":"<p>Format a byte count using powers of 1024.</p> <p>Defaults preserve the established display format. Set <code>binary_units</code> for explicit IEC labels such as <code>KiB</code> and <code>MiB</code>.</p> Source code in <code>packages/solverpy/src/solverpy/tools/human.py</code> <pre><code>def humanbytes(\n   b: float,\n   precision: int = 2,\n   separator: str = \" \",\n   binary_units: bool = False,\n) -&gt; str:\n   \"\"\"Format a byte count using powers of 1024.\n\n   Defaults preserve the established display format. Set ``binary_units`` for\n   explicit IEC labels such as ``KiB`` and ``MiB``.\n   \"\"\"\n   units = (\n      (\"B\", \"KiB\", \"MiB\", \"GiB\", \"TiB\", \"PiB\", \"EiB\")\n      if binary_units else\n      (\"Bytes\", \"KB\", \"MB\", \"GB\", \"TB\", \"PB\", \"EB\")\n   )\n   value = float(b)\n   unit = 0\n   while abs(value) &gt;= 1024 and unit &lt; len(units) - 1:\n      value /= 1024\n      unit += 1\n   return f\"{value:.{precision}f}{separator}{units[unit]}\"\n</code></pre>"},{"location":"api/solverpy/tools/patterns/","title":"module patterns","text":""},{"location":"api/solverpy/tools/patterns/#solverpy.tools.patterns","title":"<code>solverpy.tools.patterns</code>","text":""},{"location":"api/solverpy/tools/redirect/","title":"module redirect","text":""},{"location":"api/solverpy/tools/redirect/#solverpy.tools.redirect","title":"<code>solverpy.tools.redirect</code>","text":""},{"location":"api/solverpy/tools/reporter/","title":"module reporter","text":""},{"location":"api/solverpy/tools/reporter/#solverpy.tools.reporter","title":"<code>solverpy.tools.reporter</code>","text":""},{"location":"api/solverpy/tools/reporter/#solverpy.tools.reporter.fence","title":"<code>fence(lang: str)</code>","text":"<p>Register a super-fence handler for the given language.</p> Source code in <code>packages/solverpy/src/solverpy/tools/reporter.py</code> <pre><code>def fence(lang: str):\n   \"\"\"Register a super-fence handler for the given language.\"\"\"\n   def decorator(fn):\n      _fences[lang] = fn\n      return fn\n   return decorator\n</code></pre>"},{"location":"api/solverpy/tools/resources/","title":"module resources","text":""},{"location":"api/solverpy/tools/resources/#solverpy.tools.resources","title":"<code>solverpy.tools.resources</code>","text":"<p>Lightweight process resource snapshots for debug logging.</p>"},{"location":"api/solverpy/tools/resources/#solverpy.tools.resources.summary","title":"<code>summary(label: str, started_at: float) -&gt; str</code>","text":"<p>Format current/peak memory and elapsed time for concise info logging.</p> Source code in <code>packages/solverpy/src/solverpy/tools/resources.py</code> <pre><code>def summary(label: str, started_at: float) -&gt; str:\n   \"\"\"Format current/peak memory and elapsed time for concise info logging.\"\"\"\n   current_rss, peak_rss, _ = _memory()\n   own = resource.getrusage(resource.RUSAGE_SELF)\n   peak_scale = 1 if sys.platform == \"darwin\" else 1024\n   if peak_rss is None:\n      peak_rss = int(own.ru_maxrss * peak_scale)\n   if current_rss is None:\n      current_rss = peak_rss\n   memory = humanbytes(\n      current_rss, precision=1, separator=\"\", binary_units=True)\n   peak = humanbytes(\n      peak_rss, precision=1, separator=\"\", binary_units=True)\n   elapsed = _duration(time.monotonic() - started_at)\n   return (\n      f\"Resources[{label}]: memory={memory} peak={peak} elapsed={elapsed}\"\n   )\n</code></pre>"},{"location":"api/solverpy/tools/resources/#solverpy.tools.resources.usage","title":"<code>usage(label: str) -&gt; str</code>","text":"<p>Format current memory and cumulative CPU usage for debug logs.</p> Source code in <code>packages/solverpy/src/solverpy/tools/resources.py</code> <pre><code>def usage(label: str) -&gt; str:\n   \"\"\"Format current memory and cumulative CPU usage for debug logs.\"\"\"\n   current_rss, peak_rss, virtual = _memory()\n   own = resource.getrusage(resource.RUSAGE_SELF)\n   children = resource.getrusage(resource.RUSAGE_CHILDREN)\n   peak_scale = 1 if sys.platform == \"darwin\" else 1024\n   fields = [f\"resources [{label}]\"]\n   if current_rss is not None:\n      fields.append(\n         \"rss=\" + humanbytes(\n            current_rss,\n            precision=1,\n            separator=\"\",\n            binary_units=True,\n         ))\n   fields.append(\n      \"peak_rss=\" + humanbytes(\n         peak_rss if peak_rss is not None else int(own.ru_maxrss * peak_scale),\n         precision=1,\n         separator=\"\",\n         binary_units=True,\n      ))\n   if virtual is not None:\n      fields.append(\n         \"vms=\" + humanbytes(\n            virtual,\n            precision=1,\n            separator=\"\",\n            binary_units=True,\n         ))\n   fields.extend([\n      f\"cpu={own.ru_utime + own.ru_stime:.2f}s\",\n      f\"child_cpu={children.ru_utime + children.ru_stime:.2f}s\",\n   ])\n   return \" \".join(fields)\n</code></pre>"},{"location":"api/solverpy/tools/timeme/","title":"module timeme","text":""},{"location":"api/solverpy/tools/timeme/#solverpy.tools.timeme","title":"<code>solverpy.tools.timeme</code>","text":""},{"location":"api/solverpy/tools/typing/","title":"module typing","text":""},{"location":"api/solverpy/tools/typing/#solverpy.tools.typing","title":"<code>solverpy.tools.typing</code>","text":""},{"location":"api/solverpy/tools/typing/#solverpy.tools.typing.Result","title":"<code>Result = dict[str, Any]</code>  <code>module-attribute</code>","text":"<p>Generic solver result.</p>"},{"location":"api/solverpy/tools/typing/#solverpy.tools.typing.Status","title":"<code>Status = str</code>  <code>module-attribute</code>","text":"<p>Solver task status.</p>"},{"location":"api/solverpy/tools/markdown/dia/","title":"module dia","text":""},{"location":"api/solverpy/tools/markdown/dia/#solverpy.tools.markdown.dia","title":"<code>solverpy.tools.markdown.dia</code>","text":""},{"location":"api/solverpy/tools/markdown/plantuml/","title":"module plantuml","text":""},{"location":"api/solverpy/tools/markdown/plantuml/#solverpy.tools.markdown.plantuml","title":"<code>solverpy.tools.markdown.plantuml</code>","text":""},{"location":"api/solverpy_learn/builder/","title":"Builder Overview","text":""},{"location":"api/solverpy_learn/builder/#solverpy_learn.builder","title":"<code>solverpy_learn.builder</code>","text":"<p>This module encapsulates the model builder for ML experiments.</p>"},{"location":"api/solverpy_learn/builder/autotuner/","title":"module autotuner","text":""},{"location":"api/solverpy_learn/builder/autotuner/#solverpy_learn.builder.autotuner","title":"<code>solverpy_learn.builder.autotuner</code>","text":""},{"location":"api/solverpy_learn/builder/autotuner/#solverpy_learn.builder.autotuner.AutoTuner","title":"<code>AutoTuner</code>","text":"<p>               Bases: <code>Builder</code></p> <p><code>Builder</code> that trains a model by running Optuna-based hyperparameter tuning (<code>autotune.prettytuner</code>) over the training/development strategy files, then saves the best model.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/autotuner.py</code> <pre><code>class AutoTuner(Builder):\n   \"\"\"\n   [`Builder`][solverpy_learn.builder.builder.Builder] that trains a model by\n   running Optuna-based hyperparameter tuning (`autotune.prettytuner`) over\n   the training/development strategy files, then saves the best model.\n   \"\"\"\n\n   def __init__(\n      self,\n      setup: Setup,\n      tuneargs: (dict[str, Any] | None) = None,\n      templates: (list[str] | None) = None,\n   ):\n      assert \"evals\" in setup\n      assert \"dataname\" in setup[\"evals\"]\n      Builder.__init__(self, setup[\"evals\"][\"dataname\"])\n      self._setup = setup\n      self._tuneargs: dict[str, Any] = TUNEARGS | (tuneargs or {})\n      self._templates = templates or []\n\n   def represent(self) -&gt; dict[str, Any]:\n      return dict(\n         cls=f\"{self.__class__.__module__}.{self.__class__.__name__}\",\n         dataname=self._dataname,\n         tuneargs=self._tuneargs,\n         templates=self._templates,\n      )\n\n   def path(self, modelfile: str = \"model.lgb\") -&gt; str:\n      if modelfile:\n         return os.path.join(super().path(), modelfile)\n      else:\n         return super().path()\n\n   def build(self, talker: Talker = Talker()) -&gt; None:\n      trains = self._setup[\"evals\"]\n      devels = self._setup[\"devels\"] if \"devels\" in self._setup else trains\n      assert \"plugin\" in trains\n      assert \"plugin\" in devels\n      assert \"refs\" in trains\n      report = markdown.newline() + markdown.heading(f\"Building model `{self._dataname}`\", level=2)\n      reporter.add(report)\n      logger.info(f\"Building model: {self._dataname}\")\n      logger.debug(f'using trains: {trains[\"plugin\"].path()}')\n\n      f_model = self.path()\n      if os.path.exists(f_model):\n         logger.info(f\"Skipped model building; model {self._dataname} exists.\")\n         self._strats = self.applies(trains[\"refs\"], self._dataname)\n         return\n\n      f_train = trains[\"plugin\"].path()\n      f_test = devels[\"plugin\"].path()\n\n      use_builder = (\"atpeval\" in self._tuneargs) and self._tuneargs[\"atpeval\"]\n      started_at = time.monotonic()\n\n      logger.info(f\"Tunning learning params: train={f_train} test={f_test}\")\n      logger.info(resource_summary(\"main\", started_at))\n      logger.debug(usage(f\"before tuning: {self._dataname}\"))\n      try:\n         ret = autotune.prettytuner(\n            talker=talker,\n            f_train=f_train,\n            f_test=f_test,\n            d_tmp=self.path(\"opt\"),\n            builder=self if use_builder else None,\n            **self._tuneargs,\n         )\n      except KeyboardInterrupt:\n         raise\n      finally:\n         logger.info(resource_summary(\"main\", started_at))\n         logger.debug(usage(f\"after tuning: {self._dataname}\"))\n\n      #f_best = ret[3]\n      (_, _, _, f_best, _, _, pos, neg) = ret\n      (pos, neg) = (int(pos), int(neg))\n      shutil.copyfile(f_best, f_model)\n      #self._models = [f_model]\n      self._strats = self.applies(trains[\"refs\"], self._dataname)\n\n      progress.build(self._dataname, *ret)\n      logger.info(f\"Model {self._dataname} built.\")\n</code></pre>"},{"location":"api/solverpy_learn/builder/builder/","title":"module builder","text":""},{"location":"api/solverpy_learn/builder/builder/#solverpy_learn.builder.builder","title":"<code>solverpy_learn.builder.builder</code>","text":""},{"location":"api/solverpy_learn/builder/builder/#solverpy_learn.builder.builder.Builder","title":"<code>Builder</code>","text":"<p>               Bases: <code>SolverPyObj</code></p> <p>Build the model from the training samples.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/builder.py</code> <pre><code>class Builder(SolverPyObj):\n   \"\"\"Build the model from the training samples.\"\"\"\n\n   def __init__(self, dataname: str):\n      \"\"\"Construct the builder and store the dataname.\"\"\"\n      SolverPyObj.__init__(self, dataname=dataname)\n      self._strats = []\n      self._dataname = dataname\n\n   def represent(self) -&gt; dict[str, Any]:\n      return dict(\n         cls=f\"{self.__class__.__module__}.{self.__class__.__name__}\",\n         dataname=self._dataname,\n      )\n\n   def path(self) -&gt; str:\n      \"\"\"Return the model filename.\"\"\"\n      return os.path.join(bids.dbpath(NAME), self._dataname)\n\n   def reset(self, dataname: str) -&gt; None:\n      \"\"\"Reset the dataname, for example, when a new loop is initiated.\"\"\"\n      self._dataname = dataname\n\n   def build(self, talker: Talker = Talker()) -&gt; None:\n      \"\"\"Build the model(s). Save the list of new strategies `self._strats`.\"\"\"\n      raise NotImplementedError()\n\n   def apply(self, sid: str, model: str) -&gt; list[str]:\n      \"\"\"Combine the `model` with strategy `sid`.\"\"\"\n      del sid, model\n      raise NotImplementedError()\n\n   @property\n   def strategies(self):\n      \"\"\"Return all created strategies.\"\"\"\n      return self._strats\n\n   def applies(self, strategies: list[str], model: str) -&gt; list[str]:\n      \"\"\"Combine the `model` with several strategies `strategies`.\"\"\"\n      new = []\n      for ref in strategies:\n         new.extend(self.apply(ref, model))\n      return new\n</code></pre>"},{"location":"api/solverpy_learn/builder/builder/#solverpy_learn.builder.builder.Builder.strategies","title":"<code>strategies</code>  <code>property</code>","text":"<p>Return all created strategies.</p>"},{"location":"api/solverpy_learn/builder/builder/#solverpy_learn.builder.builder.Builder.__init__","title":"<code>__init__(dataname: str)</code>","text":"<p>Construct the builder and store the dataname.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/builder.py</code> <pre><code>def __init__(self, dataname: str):\n   \"\"\"Construct the builder and store the dataname.\"\"\"\n   SolverPyObj.__init__(self, dataname=dataname)\n   self._strats = []\n   self._dataname = dataname\n</code></pre>"},{"location":"api/solverpy_learn/builder/builder/#solverpy_learn.builder.builder.Builder.applies","title":"<code>applies(strategies: list[str], model: str) -&gt; list[str]</code>","text":"<p>Combine the <code>model</code> with several strategies <code>strategies</code>.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/builder.py</code> <pre><code>def applies(self, strategies: list[str], model: str) -&gt; list[str]:\n   \"\"\"Combine the `model` with several strategies `strategies`.\"\"\"\n   new = []\n   for ref in strategies:\n      new.extend(self.apply(ref, model))\n   return new\n</code></pre>"},{"location":"api/solverpy_learn/builder/builder/#solverpy_learn.builder.builder.Builder.apply","title":"<code>apply(sid: str, model: str) -&gt; list[str]</code>","text":"<p>Combine the <code>model</code> with strategy <code>sid</code>.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/builder.py</code> <pre><code>def apply(self, sid: str, model: str) -&gt; list[str]:\n   \"\"\"Combine the `model` with strategy `sid`.\"\"\"\n   del sid, model\n   raise NotImplementedError()\n</code></pre>"},{"location":"api/solverpy_learn/builder/builder/#solverpy_learn.builder.builder.Builder.build","title":"<code>build(talker: Talker = Talker()) -&gt; None</code>","text":"<p>Build the model(s). Save the list of new strategies <code>self._strats</code>.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/builder.py</code> <pre><code>def build(self, talker: Talker = Talker()) -&gt; None:\n   \"\"\"Build the model(s). Save the list of new strategies `self._strats`.\"\"\"\n   raise NotImplementedError()\n</code></pre>"},{"location":"api/solverpy_learn/builder/builder/#solverpy_learn.builder.builder.Builder.path","title":"<code>path() -&gt; str</code>","text":"<p>Return the model filename.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/builder.py</code> <pre><code>def path(self) -&gt; str:\n   \"\"\"Return the model filename.\"\"\"\n   return os.path.join(bids.dbpath(NAME), self._dataname)\n</code></pre>"},{"location":"api/solverpy_learn/builder/builder/#solverpy_learn.builder.builder.Builder.reset","title":"<code>reset(dataname: str) -&gt; None</code>","text":"<p>Reset the dataname, for example, when a new loop is initiated.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/builder.py</code> <pre><code>def reset(self, dataname: str) -&gt; None:\n   \"\"\"Reset the dataname, for example, when a new loop is initiated.\"\"\"\n   self._dataname = dataname\n</code></pre>"},{"location":"api/solverpy_learn/builder/cvc5ml/","title":"module cvc5ml","text":""},{"location":"api/solverpy_learn/builder/cvc5ml/#solverpy_learn.builder.cvc5ml","title":"<code>solverpy_learn.builder.cvc5ml</code>","text":""},{"location":"api/solverpy_learn/builder/cvc5ml/#solverpy_learn.builder.cvc5ml.Cvc5ML","title":"<code>Cvc5ML</code>","text":"<p>               Bases: <code>AutoTuner</code></p> <p><code>AutoTuner</code> that trains an ML-enhanced cvc5 strategy: <code>apply</code>/<code>template</code> produce an <code>-ml</code>-suffixed sid pointing at the trained model.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/cvc5ml.py</code> <pre><code>class Cvc5ML(AutoTuner):\n   \"\"\"\n   [`AutoTuner`][solverpy_learn.builder.autotuner.AutoTuner] that trains an\n   ML-enhanced cvc5 strategy: `apply`/`template` produce an `-ml`-suffixed\n   sid pointing at the trained model.\n   \"\"\"\n\n   def __init__(\n      self,\n      setup: Setup,\n      tuneargs: (dict[str, Any] | None) = None,\n   ):\n      AutoTuner.__init__(\n         self,\n         setup,\n         tuneargs,\n      )\n\n   def template(self, sid : str) -&gt; str:\n      \"`sid` must be base strategy without parameters\"\n      if sid.endswith(\"-ml\"):\n         logger.debug(f\"strategy {sid} already ml-enhanced\")\n         return sid\n      sidml = f\"{sid}-ml\"\n      if os.path.exists(sids.path(sidml)):\n         logger.debug(f\"ml strategy {sidml} already exists\")\n         return sidml\n      dbpath = bids.dbpath(NAME)\n      mod = f\"{dbpath}/@@@model:default@@@/model.lgb\"\n      strat = sids.load(sid).rstrip()\n      strat = self.mlstrat(strat, mod)\n      sids.save(sidml, strat)\n      logger.debug(\n         f\"created parametric ml strategy {sidml} inherited from {sid}:\\n{strat}\"\n      )\n      return sidml\n\n   def mlstrat(self, strat: str, model: str) -&gt; str:\n      adds = \"\\n\".join([\n         f\"--ml-engine\",\n         f\"--ml-model={model}\",\n         f\"--ml-usage=@@@usage:1.0@@@\",\n         f\"--ml-fallback=@@@fallback:0@@@\",\n         f\"--ml-selector=@@@sel:orig@@@\",\n         f\"--ml-selector-value=@@@val:0.5@@@\",\n      ])\n      return f\"{strat}\\n{adds}\"\n\n   def apply(self, sid: str, model: str) -&gt; list[str]:\n      (base, args) = sids.split(sid)\n      tpl = self.template(base)\n      sidml = sids.fmt(tpl, dict(args, model=model))\n      logger.debug(f\"new strategy: {sidml}\")\n      return [sidml]\n</code></pre>"},{"location":"api/solverpy_learn/builder/cvc5ml/#solverpy_learn.builder.cvc5ml.Cvc5ML.template","title":"<code>template(sid: str) -&gt; str</code>","text":"<p><code>sid</code> must be base strategy without parameters</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/cvc5ml.py</code> <pre><code>def template(self, sid : str) -&gt; str:\n   \"`sid` must be base strategy without parameters\"\n   if sid.endswith(\"-ml\"):\n      logger.debug(f\"strategy {sid} already ml-enhanced\")\n      return sid\n   sidml = f\"{sid}-ml\"\n   if os.path.exists(sids.path(sidml)):\n      logger.debug(f\"ml strategy {sidml} already exists\")\n      return sidml\n   dbpath = bids.dbpath(NAME)\n   mod = f\"{dbpath}/@@@model:default@@@/model.lgb\"\n   strat = sids.load(sid).rstrip()\n   strat = self.mlstrat(strat, mod)\n   sids.save(sidml, strat)\n   logger.debug(\n      f\"created parametric ml strategy {sidml} inherited from {sid}:\\n{strat}\"\n   )\n   return sidml\n</code></pre>"},{"location":"api/solverpy_learn/builder/enigma/","title":"module enigma","text":""},{"location":"api/solverpy_learn/builder/enigma/#solverpy_learn.builder.enigma","title":"<code>solverpy_learn.builder.enigma</code>","text":""},{"location":"api/solverpy_learn/builder/enigma/#solverpy_learn.builder.enigma.Enigma","title":"<code>Enigma</code>","text":"<p>               Bases: <code>EnigmaModel</code></p> <p><code>EnigmaModel</code> combining the <code>sel</code> and <code>gen</code> feature sets, training either or both depending on which feature options are set in <code>setup</code>.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/enigma.py</code> <pre><code>class Enigma(EnigmaModel):\n   \"\"\"\n   [`EnigmaModel`][solverpy_learn.builder.enigma.EnigmaModel] combining the\n   `sel` and `gen` feature sets, training either or both depending on which\n   feature options are set in `setup`.\n   \"\"\"\n\n   def __init__(\n      self,\n      setup: Setup,\n      tunesel: (dict[str, Any] | None) = None,\n      tunegen: (dict[str, Any] | None) = None,\n      templates: (list[str] | None) = None,\n   ):\n      templates = templates or [\"coop\", \"solo\", \"gen\"]\n      AutoTuner.__init__(\n         self,\n         setup,\n         tunesel,\n         templates=templates,\n      )\n      assert \"evals\" in setup\n      trains_evalset = setup[\"evals\"]\n      assert \"plugin\" in trains_evalset\n      assert \"sel_features\" in setup\n      assert \"gen_features\" in setup\n      sel = setup[\"sel_features\"]\n      gen = setup[\"gen_features\"]\n      self._features = sel or gen or \"\"\n\n      setup_sel = setup\n      setup_gen = setup\n      if sel and gen:\n         # split the multi train data for sel and gen sub-builders\n         assert isinstance(trains_evalset[\"plugin\"], enigma.EnigmaMultiTrains)\n         plugin = trains_evalset[\"plugin\"]\n         setup_sel = Setup(setup)\n         setup_sel[\"evals\"] = Evalset(trains_evalset, plugin=plugin._sel)\n         setup_gen = Setup(setup)\n         setup_gen[\"evals\"] = Evalset(trains_evalset, plugin=plugin._gen)\n         if \"devels\" in setup:\n            devels_evalset = setup[\"devels\"]\n            assert \"plugin\" in devels_evalset\n            assert isinstance(devels_evalset[\"plugin\"], enigma.EnigmaMultiTrains)\n            setup_sel[\"devels\"] = Evalset(devels_evalset, plugin=devels_evalset[\"plugin\"]._sel)\n            setup_gen[\"devels\"] = Evalset(devels_evalset, plugin=devels_evalset[\"plugin\"]._gen)\n\n      self._sel = EnigmaSel(setup_sel, tunesel, templates) if sel else None\n      self._gen = EnigmaGen(setup_gen, tunegen, templates) if gen else None\n\n   def reset(self, dataname: str) -&gt; None:\n      if self._sel:\n         self._sel.reset(dataname)\n      if self._gen:\n         self._gen.reset(dataname)\n      Builder.reset(self, dataname)\n\n   def build(self, talker: Talker = Talker()) -&gt; None:\n      self._strats = []\n      if self._sel:\n         self._sel.build(talker)\n         self._strats.extend(self._sel.strategies)\n      if self._gen:\n         self._gen.build(talker)\n         self._strats.extend(self._gen.strategies)\n      if self._sel and self._gen:\n         trains = self._setup[\"evals\"]\n         assert \"refs\" in trains\n         refs = trains[\"refs\"]\n         self._strats.extend(self.applies(refs, self._dataname))\n\n   def apply(self, sid: str, model: str) -&gt; list[str]:\n      assert self._sel and self._gen\n      (base, args) = sids.split(sid)\n      sidsolo = f\"{base}-solo\"\n      sidcoop = f\"{base}-coop\"\n      sidsologen = self.template(sidsolo, \"gen\", gen)\n      sidcoopgen = self.template(sidcoop, \"gen\", gen)\n      args = dict(args, sel=self._sel._dataname, gen=self._gen._dataname)\n      sidsologen = sids.fmt(sidsologen, args)\n      sidcoopgen = sids.fmt(sidcoopgen, args)\n      news = []\n      if \"solo\" in self._templates:\n         news.append(sidsologen)\n      if \"coop\" in self._templates:\n         news.append(sidcoopgen)\n      logger.debug(f\"new strategies: {news}\")\n      self.makemap(os.path.join(bids.dbpath(NAME), model, \"enigma.map\"))\n      return news\n</code></pre>"},{"location":"api/solverpy_learn/builder/enigma/#solverpy_learn.builder.enigma.EnigmaGen","title":"<code>EnigmaGen</code>","text":"<p>               Bases: <code>EnigmaModel</code></p> <p><code>EnigmaModel</code> fixed to the <code>gen</code> (clause generation) feature variant.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/enigma.py</code> <pre><code>class EnigmaGen(EnigmaModel):\n   \"\"\"[`EnigmaModel`][solverpy_learn.builder.enigma.EnigmaModel] fixed to the `gen` (clause generation) feature variant.\"\"\"\n\n   def __init__(\n      self,\n      setup: Setup,\n      tuneargs: (dict[str, Any] | None) = None,\n      templates: (list[str] | None) = None,\n   ):\n      EnigmaModel.__init__(\n         self,\n         setup,\n         tuneargs,\n         \"gen\",\n         templates,\n      )\n\n   def apply(self, sid: str, model: str) -&gt; list[str]:\n      if \"gen\" not in self._templates:\n         return []\n      (base, args) = sids.split(sid)\n      sidgen = self.template(base, \"gen\", gen)\n      sidgen = sids.fmt(sidgen, dict(args, gen=model))\n      news = [sidgen]\n      self.makemap(os.path.join(bids.dbpath(NAME), model, \"enigma.map\"))\n      logger.debug(f\"new strategies: {news}\")\n      return news\n</code></pre>"},{"location":"api/solverpy_learn/builder/enigma/#solverpy_learn.builder.enigma.EnigmaModel","title":"<code>EnigmaModel</code>","text":"<p>               Bases: <code>AutoTuner</code></p> <p><code>AutoTuner</code> for one ENIGMA model variant (a feature set such as <code>sel</code> or <code>gen</code>). Subclasses <code>EnigmaSel</code> and <code>EnigmaGen</code> fix the variant; <code>Enigma</code> combines both.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/enigma.py</code> <pre><code>class EnigmaModel(AutoTuner):\n   \"\"\"\n   [`AutoTuner`][solverpy_learn.builder.autotuner.AutoTuner] for one ENIGMA\n   model variant (a feature set such as `sel` or `gen`). Subclasses\n   [`EnigmaSel`][solverpy_learn.builder.enigma.EnigmaSel] and\n   [`EnigmaGen`][solverpy_learn.builder.enigma.EnigmaGen] fix the variant;\n   [`Enigma`][solverpy_learn.builder.enigma.Enigma] combines both.\n   \"\"\"\n\n   def __init__(\n      self,\n      setup: Setup,\n      tuneargs: (dict[str, Any] | None),\n      variant: str,\n      templates: (list[str] | None) = None,\n   ):\n      AutoTuner.__init__(self, setup, tuneargs, templates)\n      self._variant = variant\n      assert f\"{variant}_features\" in setup\n      self._features: str = setup[f\"{variant}_features\"]\n      self.reset(self._dataname)\n\n   def featurepath(self) -&gt; str:\n      fpath = enigma.featurepath(self._features)\n      return f\"{self._variant}_{fpath}\"\n\n   def reset(self, dataname: str) -&gt; None:\n      dataname = os.path.join(dataname, self.featurepath())\n      super().reset(dataname)  # does: self._dataname = dataname\n\n   def template(\n      self,\n      sid: str,\n      name: str,\n      mk_strat: Callable[[str], str],\n   ) -&gt; str:\n      sidml = f\"{sid}-{name}\"\n      if os.path.exists(sids.path(sidml)):\n         logger.debug(f\"ml strategy {sidml} already exists\")\n         return sidml\n      strat = mk_strat(sid)\n      sids.save(sidml, strat)\n      logger.debug(\n         f\"created parametric ml strategy {sidml} inherited from {sid}:\\n{strat}\"\n      )\n      return sidml\n\n   def build(self, talker: Talker = Talker()) -&gt; None:\n      super().build(talker)\n      self.makemap()\n\n   def makemap(self, mapfile: str | None = None) -&gt; None:\n      f_map = mapfile or self.path(\"enigma.map\")\n      logger.debug(f\"creating enigma map: {f_map}\")\n      open(f_map, \"w\").write(f'features(\"{self._features}\").\\n')\n</code></pre>"},{"location":"api/solverpy_learn/builder/enigma/#solverpy_learn.builder.enigma.EnigmaSel","title":"<code>EnigmaSel</code>","text":"<p>               Bases: <code>EnigmaModel</code></p> <p><code>EnigmaModel</code> fixed to the <code>sel</code> (clause selection) feature variant.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/enigma.py</code> <pre><code>class EnigmaSel(EnigmaModel):\n   \"\"\"[`EnigmaModel`][solverpy_learn.builder.enigma.EnigmaModel] fixed to the `sel` (clause selection) feature variant.\"\"\"\n\n   def __init__(\n      self,\n      setup: Setup,\n      tuneargs: (dict[str, Any] | None) = None,\n      templates: (list[str] | None) = None,\n   ):\n      EnigmaModel.__init__(\n         self,\n         setup,\n         tuneargs,\n         \"sel\",\n         templates,\n      )\n\n   def apply(self, sid: str, model: str) -&gt; list[str]:\n      (base, args) = sids.split(sid)\n      sidsolo = self.template(base, \"solo\", solo)\n      sidsolo = sids.fmt(sidsolo, dict(args, sel=model))\n      sidcoop = self.template(base, \"coop\", coop)\n      sidcoop = sids.fmt(sidcoop, dict(args, sel=model))\n      news = []\n      if \"solo\" in self._templates:\n         news.append(sidsolo)\n      if \"coop\" in self._templates:\n         news.append(sidcoop)\n      self.makemap(os.path.join(bids.dbpath(NAME), model, \"enigma.map\"))\n      logger.debug(f\"new strategies: {news}\")\n      return news\n</code></pre>"},{"location":"api/solverpy_learn/builder/svm/","title":"module svm","text":""},{"location":"api/solverpy_learn/builder/svm/#solverpy_learn.builder.svm","title":"<code>solverpy_learn.builder.svm</code>","text":""},{"location":"api/solverpy_learn/builder/svm/#solverpy_learn.builder.svm.deconflict","title":"<code>deconflict(xs: spmatrix, ys: ndarray) -&gt; tuple[spmatrix, ndarray]</code>","text":"<p>Find conflicting positive and negative samples and remove the negative ones.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/svm.py</code> <pre><code>def deconflict(xs: \"spmatrix\", ys: \"ndarray\") -&gt; tuple[\"spmatrix\", \"ndarray\"]:\n   \"Find conflicting positive and negative samples and remove the negative ones.\"\n   assert xs.shape[0] == ys.shape[0]\n   logger.info(\"Looking up conflicting samples.\")\n   logger.debug(\"building samples map\")\n   dups = defaultdict(list)\n   for i in range(xs.shape[0]):\n      row = xs.getrow(i)\n      key = (tuple(row.indices), tuple(row.data))\n      dups[key].append(i)\n   logger.debug(\"marking conflicting negative samples\")\n   todel = set()\n   for ids in dups.values():\n      if len(ids) &lt; 2:\n         continue\n      td = []\n      onepos = False\n      for i in ids:\n         if ys[i] == 0:\n            td.append(i)\n         else:\n            onepos = True\n      if onepos:\n         todel.update(td)\n   logger.debug(\"deleting marked rows\")\n   keep = [i for i in range(xs.shape[0]) if i not in todel]\n   xs0 = xs[keep]  # type: ignore\n   ys0 = ys[keep]\n   logger.info(\"\\n\".join([\n      \"Data shape difference:\",\n      f\"\\t{xs.shape} --&gt; {xs0.shape}\",\n      f\"\\t{ys.shape} --&gt; {ys0.shape}\",\n   ]))\n   return (xs0, ys0)\n</code></pre>"},{"location":"api/solverpy_learn/builder/autotune/","title":"Autotune Overview","text":""},{"location":"api/solverpy_learn/builder/autotune/#solverpy_learn.builder.autotune","title":"<code>solverpy_learn.builder.autotune</code>","text":"<p>Meta-parameters tunner.</p> <p> </p>"},{"location":"api/solverpy_learn/builder/autotune/autotune/","title":"module autotune","text":""},{"location":"api/solverpy_learn/builder/autotune/autotune/#solverpy_learn.builder.autotune.autotune","title":"<code>solverpy_learn.builder.autotune.autotune</code>","text":""},{"location":"api/solverpy_learn/builder/autotune/build/","title":"module build","text":""},{"location":"api/solverpy_learn/builder/autotune/build/#solverpy_learn.builder.autotune.build","title":"<code>solverpy_learn.builder.autotune.build</code>","text":""},{"location":"api/solverpy_learn/builder/autotune/check/","title":"module check","text":""},{"location":"api/solverpy_learn/builder/autotune/check/#solverpy_learn.builder.autotune.check","title":"<code>solverpy_learn.builder.autotune.check</code>","text":""},{"location":"api/solverpy_learn/builder/autotune/tune/","title":"module tune","text":""},{"location":"api/solverpy_learn/builder/autotune/tune/#solverpy_learn.builder.autotune.tune","title":"<code>solverpy_learn.builder.autotune.tune</code>","text":""},{"location":"api/solverpy_learn/builder/plugins/","title":"Plugins Overview","text":""},{"location":"api/solverpy_learn/builder/plugins/#solverpy_learn.builder.plugins","title":"<code>solverpy_learn.builder.plugins</code>","text":""},{"location":"api/solverpy_learn/builder/plugins/cvc5/","title":"module cvc5","text":""},{"location":"api/solverpy_learn/builder/plugins/cvc5/#solverpy_learn.builder.plugins.cvc5","title":"<code>solverpy_learn.builder.plugins.cvc5</code>","text":""},{"location":"api/solverpy_learn/builder/plugins/enigma/","title":"module enigma","text":""},{"location":"api/solverpy_learn/builder/plugins/enigma/#solverpy_learn.builder.plugins.enigma","title":"<code>solverpy_learn.builder.plugins.enigma</code>","text":""},{"location":"api/solverpy_learn/builder/plugins/multi/","title":"module multi","text":""},{"location":"api/solverpy_learn/builder/plugins/multi/#solverpy_learn.builder.plugins.multi","title":"<code>solverpy_learn.builder.plugins.multi</code>","text":""},{"location":"api/solverpy_learn/builder/plugins/multi/#solverpy_learn.builder.plugins.multi.MultiTrains","title":"<code>MultiTrains</code>","text":"<p>               Bases: <code>SvmTrains</code></p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/multi.py</code> <pre><code>class MultiTrains(SvmTrains):\n\n   def __init__(self, dataname: str):\n      self._trains: list[\"SvmTrains\"] = []\n      self._dataname = dataname\n      self._pid = \"trains\"\n\n   def represent(self) -&gt; dict:\n      return dict(\n         cls=f\"{self.__class__.__module__}.{self.__class__.__name__}\",\n         dataname=self._dataname,\n         trains=[t.represent() for t in self._trains],\n      )\n\n   def dispatch(self, t: \"SvmTrains\"):\n      self._trains.append(t)\n\n   def apply(self, function: Callable[[\"SvmTrains\"], None]) -&gt; None:\n      for t in self._trains:\n         function(t)\n\n   def connect(self, manager: \"SyncManager\") -&gt; None:\n      \"\"\"Connect every underlying training-data collector.\"\"\"\n      self.apply(lambda x: x.connect(manager))\n\n   def disconnect(self) -&gt; None:\n      \"\"\"Disconnect every underlying training-data collector.\"\"\"\n      self.apply(lambda x: x.disconnect())\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      super().register(solver)\n      self._solver = solver\n      for t in self._trains:\n         t._solver = solver\n\n   def reset(\n      self,\n      dataname: (str | None) = None,\n      filename: str = \"train.in\",\n   ) -&gt; None:\n      if dataname:\n         self._dataname = dataname\n      self.apply(lambda x: x.reset(dataname=dataname, filename=filename))\n\n   def path(\n      self,\n      dataname: (str | None) = None,\n      filename: (str | None) = None,\n   ) -&gt; tuple[str, ...]:\n      return tuple(t.path(dataname, filename) for t in self._trains)\n\n   def exists(self) -&gt; bool:\n      return all(t.exists() for t in self._trains)\n\n   def link(self, src: str | tuple[str]):\n      assert isinstance(src, tuple)\n      assert len(src) == len(self._trains)\n      for (s, t) in zip(src, self._trains):\n         t.link(s)\n\n   def enable(self) -&gt; None:\n      self.apply(lambda x: x.enable())\n\n   def disable(self) -&gt; None:\n      self.apply(lambda x: x.disable())\n\n   def finished(self, *args: Any, **kwargs: Any) -&gt; None:\n      self.apply(lambda x: x.finished(*args, **kwargs))\n\n   def extract(self, *args: Any, **kwargs: Any) -&gt; None:\n      self.apply(lambda x: x.extract(*args, **kwargs))\n\n   def save(self, *args: Any, **kwargs: Any) -&gt; None:\n      self.apply(lambda x: x.save(*args, **kwargs))\n\n   def stats(self, *args: Any, **kwargs: Any) -&gt; None:\n      self.apply(lambda x: x.stats(*args, **kwargs))\n\n   def compress(self, *args: Any, **kwargs: Any) -&gt; None:\n      self.apply(lambda x: x.compress(*args, **kwargs))\n\n   def train_data_snapshot(self) -&gt; None:\n      self.apply(lambda x: x.train_data_snapshot())\n\n   def train_data_stats(\n      self,\n      dataset: str,\n      paths: tuple[str, ...] | None = None,\n   ) -&gt; list[dict[str, Any]]:\n      paths = paths or self.path()\n      stats = [\n         train.train_data_stats(dataset, path)\n         for (train, path) in zip(self._trains, paths)\n      ]\n      return [stat for stat in stats if stat is not None]\n\n   def merge(\n      self,\n      previous: str | tuple[str, ...],\n      outfilename: str,\n   ) -&gt; None:\n      assert len(previous) == len(self._trains)\n      assert type(previous) is tuple\n      for (t0, p0) in zip(self._trains, previous):\n         t0.merge(p0, outfilename)\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/multi/#solverpy_learn.builder.plugins.multi.MultiTrains.connect","title":"<code>connect(manager: SyncManager) -&gt; None</code>","text":"<p>Connect every underlying training-data collector.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/multi.py</code> <pre><code>def connect(self, manager: \"SyncManager\") -&gt; None:\n   \"\"\"Connect every underlying training-data collector.\"\"\"\n   self.apply(lambda x: x.connect(manager))\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/multi/#solverpy_learn.builder.plugins.multi.MultiTrains.disconnect","title":"<code>disconnect() -&gt; None</code>","text":"<p>Disconnect every underlying training-data collector.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/multi.py</code> <pre><code>def disconnect(self) -&gt; None:\n   \"\"\"Disconnect every underlying training-data collector.\"\"\"\n   self.apply(lambda x: x.disconnect())\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/svm/","title":"module svm","text":""},{"location":"api/solverpy_learn/builder/plugins/svm/#solverpy_learn.builder.plugins.svm","title":"<code>solverpy_learn.builder.plugins.svm</code>","text":""},{"location":"api/solverpy_learn/builder/plugins/svm/#solverpy_learn.builder.plugins.svm.SvmTrains","title":"<code>SvmTrains</code>","text":"<p>               Bases: <code>Trains</code></p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/svm.py</code> <pre><code>class SvmTrains(Trains):\n\n   def __init__(\n      self,\n      dataname: str,\n      filename: str = \"train.in\",\n      chunk_size: int = 1_000_000,\n      **kwargs: Any,\n   ):\n      Trains.__init__(self,\n                      dataname,\n                      filename=filename,\n                      chunk_size=chunk_size,\n                      **kwargs)\n      self.info = SimpleNamespace(\n         total=0,\n         pos=0,\n         neg=0,\n         line_count=0,\n         raw_chunk_n=0,\n         chunk_size=chunk_size,\n      )\n\n   def connect(self, manager: \"SyncManager\") -&gt; None:\n      \"\"\"Move local statistics into a namespace owned by the session Manager.\"\"\"\n      if self._lock is not None:\n         return\n      local = self.info\n      super().connect(manager)\n      self.info = manager.Namespace()\n      self.info.total = local.total\n      self.info.pos = local.pos\n      self.info.neg = local.neg\n      self.info.line_count = local.line_count\n      self.info.raw_chunk_n = local.raw_chunk_n\n      self.info.chunk_size = local.chunk_size\n\n   def disconnect(self) -&gt; None:\n      \"\"\"Copy shared statistics locally before discarding Manager proxies.\"\"\"\n      if self._lock is None:\n         return\n      shared = self.info\n      self.info = SimpleNamespace(\n         total=shared.total,\n         pos=shared.pos,\n         neg=shared.neg,\n         line_count=shared.line_count,\n         raw_chunk_n=shared.raw_chunk_n,\n         chunk_size=shared.chunk_size,\n      )\n      super().disconnect()\n\n   def represent(self) -&gt; dict[str, Any]:\n      return dict(\n         cls=f\"{self.__class__.__module__}.{self.__class__.__name__}\",\n         dataname=self._dataname,\n         filename=self._filename,\n         chunk_size=self.info.chunk_size,\n      )\n\n   def reset(\n      self,\n      dataname: (str | None) = None,\n      filename: str = \"train.in\",\n   ) -&gt; None:\n      super().reset(dataname=dataname, filename=filename)\n      if hasattr(self, \"info\"):\n         self.info.total = 0\n         self.info.pos = 0\n         self.info.neg = 0\n         self.info.line_count = 0\n         self.info.raw_chunk_n = 0\n\n   def exists(self) -&gt; bool:\n      return svm.exists(self.path())\n\n   def stats(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n      samples: str,\n   ) -&gt; None:\n      count = samples.count(\"\\n\")\n      s0 = samples[0]\n      pos = samples.count(\"\\n1 \") + (1 if s0 == \"1\" else 0)\n      neg = samples.count(\"\\n0 \") + (1 if s0 == \"0\" else 0)\n      self.info.total += count\n      self.info.pos += pos\n      self.info.neg += neg\n      with open(self.path() + \"-stats.txt\", \"a\") as infa:\n         infa.write(f\"{instance} {strategy}: {count} ({pos} / {neg})\\n\")\n\n   def save(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n      samples: str,\n   ) -&gt; None:\n      if (not samples) or (not self._enabled):\n         return\n      new_lines = samples.count(\"\\n\")\n      if self._lock is None:\n         raise RuntimeError(\"SvmTrains must be connected before evaluation\")\n      self._lock.acquire()\n      try:\n         if self.info.line_count + new_lines &gt; self.info.chunk_size and \\\n            self.info.line_count &gt; 0:\n            self.info.raw_chunk_n += 1\n            self.info.line_count = 0\n         raw_path = svm.raw_path(self.path(), self.info.raw_chunk_n)\n         os.makedirs(os.path.dirname(self.path()), exist_ok=True)\n         with open(raw_path, \"a\") as fa:\n            fa.write(samples)\n         self.info.line_count += new_lines\n         self.stats(instance, strategy, samples)\n      finally:\n         self._lock.release()\n\n   def compress(self,\n                chunk_size: int | None = None,\n                cores: int | None = None) -&gt; None:\n      logger.info(\n         f\"Training vectors count: {self.info.total} ({self.info.pos} / {self.info.neg}) \"\n      )\n      svm.compress(self.path(),\n                   chunk_size=chunk_size or self.info.chunk_size,\n                   cores=cores)\n\n   def train_data_snapshot(self) -&gt; None:\n      \"\"\"Persist counts and uncompressed bytes for the current logical file.\"\"\"\n      path = self.path()\n      if not svm.exists(path):\n         return\n      metadata = svm.metadata_load(path)\n      if svm.format(path).startswith(\"text/\"):\n         raw_bytes = svm.size(path)\n      else:\n         raw_bytes = metadata.get(\"raw_bytes\")\n      if self.info.total or \"vectors\" not in metadata:\n         metadata.update({\n            \"vectors\": self.info.total,\n            \"positive\": self.info.pos,\n            \"negative\": self.info.neg,\n         })\n      if raw_bytes is not None:\n         metadata[\"raw_bytes\"] = raw_bytes\n      svm.metadata_save(path, metadata)\n\n   def train_data_stats(\n      self,\n      dataset: str,\n      path: str | None = None,\n   ) -&gt; dict[str, Any] | None:\n      \"\"\"Return report-ready statistics without loading vector data.\"\"\"\n      path = path or self.path()\n      if not svm.exists(path):\n         return None\n      storage = svm.storage(path)\n      metadata = svm.metadata_load(path)\n      if \"raw_bytes\" not in metadata and storage[\"format\"].startswith(\"text/\"):\n         metadata[\"raw_bytes\"] = storage[\"stored_bytes\"]\n      return {\n         \"dataset\": dataset,\n         \"path\": path,\n         **metadata,\n         **storage,\n      }\n\n   def merge(\n      self,\n      previous: str | tuple[str, ...],\n      outfilename: str,\n   ) -&gt; None:\n      assert self._filename != outfilename\n      assert type(previous) is str\n      if not svm.exists(self.path()):\n         logger.warning(f\"Trains not found: {self.path()}.\")\n         return\n      f_out = self.path(filename=outfilename)\n      svm.merge(previous, self.path(), f_out)\n      #self.reset(filename=outfilename)\n\n   def link(self, src: str | tuple[str]):\n      assert isinstance(src, str)\n      if not svm.exists(src):\n         logger.warning(f\"Link source not found: {src}.\")\n         return\n      dst = self.path()\n      if svm.exists(dst):\n         logger.warning(f\"Link targed exists: {dst}.\")\n         return\n      svm.link(src, dst)\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/svm/#solverpy_learn.builder.plugins.svm.SvmTrains.connect","title":"<code>connect(manager: SyncManager) -&gt; None</code>","text":"<p>Move local statistics into a namespace owned by the session Manager.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/svm.py</code> <pre><code>def connect(self, manager: \"SyncManager\") -&gt; None:\n   \"\"\"Move local statistics into a namespace owned by the session Manager.\"\"\"\n   if self._lock is not None:\n      return\n   local = self.info\n   super().connect(manager)\n   self.info = manager.Namespace()\n   self.info.total = local.total\n   self.info.pos = local.pos\n   self.info.neg = local.neg\n   self.info.line_count = local.line_count\n   self.info.raw_chunk_n = local.raw_chunk_n\n   self.info.chunk_size = local.chunk_size\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/svm/#solverpy_learn.builder.plugins.svm.SvmTrains.disconnect","title":"<code>disconnect() -&gt; None</code>","text":"<p>Copy shared statistics locally before discarding Manager proxies.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/svm.py</code> <pre><code>def disconnect(self) -&gt; None:\n   \"\"\"Copy shared statistics locally before discarding Manager proxies.\"\"\"\n   if self._lock is None:\n      return\n   shared = self.info\n   self.info = SimpleNamespace(\n      total=shared.total,\n      pos=shared.pos,\n      neg=shared.neg,\n      line_count=shared.line_count,\n      raw_chunk_n=shared.raw_chunk_n,\n      chunk_size=shared.chunk_size,\n   )\n   super().disconnect()\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/svm/#solverpy_learn.builder.plugins.svm.SvmTrains.train_data_snapshot","title":"<code>train_data_snapshot() -&gt; None</code>","text":"<p>Persist counts and uncompressed bytes for the current logical file.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/svm.py</code> <pre><code>def train_data_snapshot(self) -&gt; None:\n   \"\"\"Persist counts and uncompressed bytes for the current logical file.\"\"\"\n   path = self.path()\n   if not svm.exists(path):\n      return\n   metadata = svm.metadata_load(path)\n   if svm.format(path).startswith(\"text/\"):\n      raw_bytes = svm.size(path)\n   else:\n      raw_bytes = metadata.get(\"raw_bytes\")\n   if self.info.total or \"vectors\" not in metadata:\n      metadata.update({\n         \"vectors\": self.info.total,\n         \"positive\": self.info.pos,\n         \"negative\": self.info.neg,\n      })\n   if raw_bytes is not None:\n      metadata[\"raw_bytes\"] = raw_bytes\n   svm.metadata_save(path, metadata)\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/svm/#solverpy_learn.builder.plugins.svm.SvmTrains.train_data_stats","title":"<code>train_data_stats(dataset: str, path: str | None = None) -&gt; dict[str, Any] | None</code>","text":"<p>Return report-ready statistics without loading vector data.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/svm.py</code> <pre><code>def train_data_stats(\n   self,\n   dataset: str,\n   path: str | None = None,\n) -&gt; dict[str, Any] | None:\n   \"\"\"Return report-ready statistics without loading vector data.\"\"\"\n   path = path or self.path()\n   if not svm.exists(path):\n      return None\n   storage = svm.storage(path)\n   metadata = svm.metadata_load(path)\n   if \"raw_bytes\" not in metadata and storage[\"format\"].startswith(\"text/\"):\n      metadata[\"raw_bytes\"] = storage[\"stored_bytes\"]\n   return {\n      \"dataset\": dataset,\n      \"path\": path,\n      **metadata,\n      **storage,\n   }\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/trains/","title":"module trains","text":""},{"location":"api/solverpy_learn/builder/plugins/trains/#solverpy_learn.builder.plugins.trains","title":"<code>solverpy_learn.builder.plugins.trains</code>","text":""},{"location":"api/solverpy_learn/builder/plugins/trains/#solverpy_learn.builder.plugins.trains.Trains","title":"<code>Trains</code>","text":"<p>               Bases: <code>Managed</code></p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/trains.py</code> <pre><code>class Trains(Managed):\n\n   def __init__(self, dataname: str, filename: str = \"train.in\", **kwargs: Any):\n      Managed.__init__(\n         self,\n         pid=\"trains\",\n         dataname=dataname,\n         filename=filename,\n         **kwargs,\n      )\n      self._lock = None\n      self._enabled = True\n      self.reset(dataname, filename)\n\n   def connect(self, manager: \"SyncManager\") -&gt; None:\n      \"\"\"Create process-shared state from the session-owned Manager.\"\"\"\n      if self._lock is None:\n         self._lock = manager.Lock()\n\n   def disconnect(self) -&gt; None:\n      \"\"\"Discard process-shared proxies before their Manager is shut down.\"\"\"\n      self._lock = None\n\n   def represent(self) -&gt; dict[str, Any]:\n      return dict(\n         cls=f\"{self.__class__.__module__}.{self.__class__.__name__}\",\n         dataname=self._dataname,\n         filename=self._filename,\n      )\n\n   def reset(\n      self,\n      dataname: (str | None) = None,\n      filename: str = \"train.in\",\n   ) -&gt; None:\n      if dataname:\n         self._dataname = dataname\n      self._filename = filename\n\n   def path(\n      self,\n      dataname: (str | None) = None,\n      filename: (str | None) = None,\n   ) -&gt; Any:\n      dataname = dataname or self._dataname\n      filename = filename or self._filename\n      return os.path.join(bids.dbpath(NAME), dataname, filename)\n\n   def exists(self) -&gt; bool:\n      return os.path.isfile(self.path())\n\n   def link(self, src: str):\n      if not os.path.isfile(src):\n         logger.warning(f\"Link source not found: {src}.\")\n         return\n      rellink(src, self.path())\n\n   def register(self, solver: \"SolverPy\") -&gt; None:\n      super().register(solver)\n      self._solver = solver\n\n   def finished(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n      output: str,\n      result: dict[str, Any],\n   ):\n      if not (output and self._solver.solved(result)):\n         return\n      samples = self.extract(instance, strategy, output, result)\n      self.save(instance, strategy, samples)\n\n   def extract(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n      output: str,\n      result: dict[str, Any],\n   ) -&gt; Any:\n      del instance, strategy, output, result  # unused arguments\n      \"Extract training samples from `output`.\"\n      raise NotImplementedError()\n\n   def save(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n      samples: str,\n   ) -&gt; None:\n      if (not samples) or (not self._enabled):\n         return\n      if self._lock is None:\n         raise RuntimeError(\"Trains must be connected before evaluation\")\n      self._lock.acquire()\n      try:\n         os.makedirs(os.path.dirname(self.path()), exist_ok=True)\n         with open(self.path(), \"a\") as fa:\n            fa.write(samples)\n            self.stats(instance, strategy, samples)\n      finally:\n         self._lock.release()\n\n   def stats(\n      self,\n      instance: tuple[str, str],\n      strategy: str,\n      samples: str,\n   ):\n      \"Save optional statistics.\"\n      del instance, strategy, samples  # unused arguments\n      pass\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/trains/#solverpy_learn.builder.plugins.trains.Trains.connect","title":"<code>connect(manager: SyncManager) -&gt; None</code>","text":"<p>Create process-shared state from the session-owned Manager.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/trains.py</code> <pre><code>def connect(self, manager: \"SyncManager\") -&gt; None:\n   \"\"\"Create process-shared state from the session-owned Manager.\"\"\"\n   if self._lock is None:\n      self._lock = manager.Lock()\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/trains/#solverpy_learn.builder.plugins.trains.Trains.disconnect","title":"<code>disconnect() -&gt; None</code>","text":"<p>Discard process-shared proxies before their Manager is shut down.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/trains.py</code> <pre><code>def disconnect(self) -&gt; None:\n   \"\"\"Discard process-shared proxies before their Manager is shut down.\"\"\"\n   self._lock = None\n</code></pre>"},{"location":"api/solverpy_learn/builder/plugins/trains/#solverpy_learn.builder.plugins.trains.Trains.stats","title":"<code>stats(instance: tuple[str, str], strategy: str, samples: str)</code>","text":"<p>Save optional statistics.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/builder/plugins/trains.py</code> <pre><code>def stats(\n   self,\n   instance: tuple[str, str],\n   strategy: str,\n   samples: str,\n):\n   \"Save optional statistics.\"\n   del instance, strategy, samples  # unused arguments\n   pass\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/","title":"module looptalker","text":""},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker","title":"<code>solverpy_learn.report.talker.looptalker</code>","text":""},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker--looptalker-progress-reporter-for-hyperparameter-tuning","title":"LoopTalker \u2014 progress reporter for hyperparameter tuning","text":"<p>Combines ATP evaluation progress (tqdm bars or log lines) and LightGBM tuning progress into a single talker for <code>AutoTuner</code>.</p> <p>Runs entirely in the parent process; cross-process forwarding is handled by <code>RemoteTalker</code> wrapping it.</p>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker","title":"<code>LoopTalker</code>","text":"<p>               Bases: <code>EvalTalker</code></p> <p>Progress talker for the hyperparameter tuning pipeline.</p> <p> </p> <p>Outside a tuning phase, behaves identically to <code>EvalTalker</code> (per-job <code>SolvingBar</code> + cumulative <code>RunningBar</code>).</p> <p>Inside a tuning phase (between <code>tune_begin</code> / <code>tune_end</code>), switches to two-bar tuning mode: an outer <code>tune</code> bar counting completed trials, and an inner <code>[N/M] build</code> or <code>[N/M] eval</code> bar for the current LightGBM training or ATP evaluation.  All desc strings are padded to the same width for alignment.</p> <p>Set <code>headless=True</code> for non-interactive use: all handlers use <code>logger.info</code> and no tqdm bars are created.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>class LoopTalker(EvalTalker):\n   \"\"\"\n   Progress talker for the hyperparameter tuning pipeline.\n\n   ```plantuml name=\"autotune-looptalker\"\n   abstract class solverpy.report.talker.talker.Talker\n   class solverpy.report.talker.logtalker.LogTalker extends solverpy.report.talker.talker.Talker\n   class solverpy.report.talker.evaltalker.EvalTalker extends solverpy.report.talker.logtalker.LogTalker\n   class solverpy_learn.report.talker.looptalker.LoopTalker extends solverpy.report.talker.evaltalker.EvalTalker {\n      - _tune_bar: DefaultBar | None\n      - _builder_bar: BuilderBar | None\n      - _total_trials: int\n      - _current_trial: int\n      - _in_tuning: bool\n      - _in_optuna_trial: bool\n      --\n      + eval_begin(jobs, *, refjob, sidnames, miniters, **kwargs)\n      + eval_end(results, refjob, **kwargs)\n      + eval_launch(tasks)\n      + eval_done()\n      + tune_begin(t_start, total)\n      + tune_end(t_end)\n      + tune_trial_begin(nick, it, values)\n      + tune_trial_done(stats)\n      + build_begin(f_mod, total)\n      + build_step(n, total, metrics)\n      + build_selected(iteration, metrics)\n      + build_done(score)\n   }\n   ```\n\n   Outside a tuning phase, behaves identically to\n   [`EvalTalker`][solverpy.report.talker.evaltalker.EvalTalker] (per-job\n   ``SolvingBar`` + cumulative ``RunningBar``).\n\n   Inside a tuning phase (between ``tune_begin`` / ``tune_end``), switches to\n   two-bar tuning mode: an outer ``tune`` bar counting completed trials, and\n   an inner ``[N/M] build`` or ``[N/M] eval`` bar for the current LightGBM\n   training or ATP evaluation.  All desc strings are padded to the same width\n   for alignment.\n\n   Set ``headless=True`` for non-interactive use: all handlers use\n   ``logger.info`` and no tqdm bars are created.\n   \"\"\"\n\n   def __init__(self, headless: bool = False) -&gt; None:\n      \"\"\"\n      Args:\n          headless: if ``True``, use log lines instead of tqdm bars.\n      \"\"\"\n      super().__init__(headless=headless)\n      self._builder_bar: BuilderBar | None = None\n      self._tune_bar: DefaultBar | None = None\n      self._total_trials: int = 0\n      self._current_trial: int = 0\n      self._in_tuning: bool = False\n      self._in_optuna_trial: bool = False\n      self._in_tune_eval: bool = False\n\n   # --- Helpers ---\n\n   def _trial_desc(self, name: str) -&gt; str:\n      \"\"\"Return '[N/M] name' aligned with the tune bar, or just 'name' if total unknown.\"\"\"\n      if not self._total_trials:\n         return name\n      dw = len(str(self._total_trials))\n      n = self._current_trial + 1\n      return f\"[{n:&gt;{dw}}/{self._total_trials}] {name}\"\n\n   def _inner_width(self) -&gt; int:\n      \"\"\"Width of '[N/M] build' / '[N/M] eval ' for tune bar desc alignment.\"\"\"\n      if not self._total_trials:\n         return 0\n      dw = len(str(self._total_trials))\n      return 2 * dw + 9  # len(\"[N/M] build\") with dw-wide fields\n\n   # --- ATP evaluation overrides ---\n\n   def eval_begin(self,\n                  jobs,\n                  *,\n                  refjob=None,\n                  sidnames=True,\n                  miniters=1,\n                  **kwargs) -&gt; None:\n      \"\"\"Outside tuning: full EvalTalker bars. Inside tuning: single RunningBar, skips report.\"\"\"\n      if not self._in_tuning:\n         EvalTalker.eval_begin(self,\n                               jobs,\n                               refjob=refjob,\n                               sidnames=sidnames,\n                               miniters=miniters,\n                               **kwargs)\n         return\n      LogTalker.eval_begin(self,\n                           jobs,\n                           refjob=refjob,\n                           sidnames=sidnames,\n                           report=False,\n                           **kwargs)\n      if not self._headless:\n         max_job = max(len(_bids.problems(bid)) for (_, bid, _) in jobs)\n         self._total_bar = RunningBar(\n            total=self._total_count,\n            desc=self._trial_desc(\"eval \"),\n            miniters=miniters,\n            colour=\"blue\",\n            leave=True,\n            postfix_width=_postfix_width(max_job),\n         )\n\n   def eval_end(self, results, refjob=None, **kwargs) -&gt; None:\n      \"\"\"Outside tuning: full EvalTalker end. Inside tuning: close eval bar, update tune bar.\"\"\"\n      if not self._in_tuning:\n         EvalTalker.eval_end(self, results, refjob=refjob, **kwargs)\n         return\n      if self._total_bar:\n         self._total_errors = self._total_bar._errors\n         self._total_bar.close()\n         self._total_bar = None\n      LogTalker.eval_end(self, results, refjob=refjob, report=False)\n      if not self._in_optuna_trial:\n         self._current_trial += 1\n         if self._tune_bar:\n            self._tune_bar.update(1)\n\n   def eval_status(self, new: \"bool | None\", n: int = 1) -&gt; None:\n      \"\"\"Update eval bar and refresh tune bar to keep it visible.\"\"\"\n      super().eval_status(new, n)\n      if self._tune_bar:\n         self._tune_bar.refresh()\n\n   def eval_launch(self, tasks: Sequence[\"Task\"]) -&gt; None:\n      \"\"\"Outside tuning: per-job SolvingBar. Inside tuning: record time only.\"\"\"\n      if not self._in_tuning:\n         EvalTalker.eval_launch(self, tasks)\n      else:\n         LogTalker.eval_launch(self, tasks)\n\n   def eval_done(self) -&gt; None:\n      \"\"\"Outside tuning: close job bar. Inside tuning: log without closing any bar.\"\"\"\n      if not self._in_tuning:\n         EvalTalker.eval_done(self)\n      else:\n         LogTalker.eval_done(self)\n\n   # --- Tuning lifecycle overrides ---\n\n   def tune_begin(self, t_start: float, total: int = 0) -&gt; None:\n      \"\"\"Enter tuning mode; create outer tune bar if not headless.\"\"\"\n      self._in_tuning = True\n      self._total_trials = total\n      self._current_trial = 0\n      self._suspend_info = not self._headless\n      if self._headless or not total:\n         return\n      self._tune_bar = DefaultBar(total, \"tune\".ljust(self._inner_width()))\n\n   def tune_end(self, t_end: float) -&gt; None:\n      \"\"\"Leave tuning mode; close tune bar and restore info logs.\"\"\"\n      self._in_tuning = False\n      self._suspend_info = False\n      self._total_trials = 0\n      self._current_trial = 0\n      if self._tune_bar:\n         self._tune_bar.close()\n         self._tune_bar = None\n\n   def tune_trial_begin(self, nick: str, it: int, values: list) -&gt; None:\n      \"\"\"Mark start of Optuna trial and delegate to LogTalker.\"\"\"\n      self._in_optuna_trial = True\n      LogTalker.tune_trial_begin(self, nick, it, values)\n\n   def tune_trial_done(self, stats: dict[str, Any]) -&gt; None:\n      \"\"\"Increment tune bar at end of Optuna trial.\"\"\"\n      LogTalker.tune_trial_done(self, stats)\n      self._in_optuna_trial = False\n      self._current_trial += 1\n      if self._tune_bar:\n         self._tune_bar.update(1)\n\n   def tune_eval_begin(self) -&gt; None:\n      \"\"\"Suppress eval log messages while ATP evaluation runs inside a tuning trial.\"\"\"\n      self._in_tune_eval = True\n\n   def tune_eval_end(self, results: dict) -&gt; None:\n      \"\"\"Re-enable eval log messages after tuning ATP evaluation.\"\"\"\n      self._in_tune_eval = False\n\n   def terminate(self) -&gt; None:\n      \"\"\"Delegate to parent; _tune_bar is closed by tune_end().\"\"\"\n      super().terminate()\n\n   # --- Model build overrides ---\n\n   def build_begin(self, f_mod: str, total: int) -&gt; None:\n      \"\"\"Open a ``BuilderBar`` with [N/M] build label if not headless.\"\"\"\n      LogTalker.build_begin(self, f_mod, total)\n      if not self._headless:\n         self._builder_bar = BuilderBar(total,\n                                        self._trial_desc(\"build\"),\n                                        colour=\"blue\",\n                                        leave=True)\n\n   def build_step(\n      self,\n      n: int,\n      total: int,\n      metrics: dict[str, dict[str, float]],\n   ) -&gt; None:\n      \"\"\"Update the builder bar, refresh tune bar, and emit periodic log lines.\"\"\"\n      if self._builder_bar:\n         self._builder_bar.status(metrics)\n      if self._tune_bar:\n         self._tune_bar.refresh()\n      LogTalker.build_step(self, n, total, metrics)\n\n   def build_selected(\n      self,\n      iteration: int,\n      metrics: dict[str, dict[str, float]],\n   ) -&gt; None:\n      \"\"\"Report selected-model metrics through the log/reporting layer.\"\"\"\n      LogTalker.build_selected(self, iteration, metrics)\n\n   def build_done(self, score: float) -&gt; None:\n      \"\"\"Close the builder bar.\"\"\"\n      if self._builder_bar:\n         self._builder_bar.close()\n         self._builder_bar = None\n      LogTalker.build_done(self, score)\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.__init__","title":"<code>__init__(headless: bool = False) -&gt; None</code>","text":"<p>Parameters:</p> Name Type Description Default <code>headless</code> <code>bool</code> <p>if <code>True</code>, use log lines instead of tqdm bars.</p> <code>False</code> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def __init__(self, headless: bool = False) -&gt; None:\n   \"\"\"\n   Args:\n       headless: if ``True``, use log lines instead of tqdm bars.\n   \"\"\"\n   super().__init__(headless=headless)\n   self._builder_bar: BuilderBar | None = None\n   self._tune_bar: DefaultBar | None = None\n   self._total_trials: int = 0\n   self._current_trial: int = 0\n   self._in_tuning: bool = False\n   self._in_optuna_trial: bool = False\n   self._in_tune_eval: bool = False\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.build_begin","title":"<code>build_begin(f_mod: str, total: int) -&gt; None</code>","text":"<p>Open a <code>BuilderBar</code> with [N/M] build label if not headless.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def build_begin(self, f_mod: str, total: int) -&gt; None:\n   \"\"\"Open a ``BuilderBar`` with [N/M] build label if not headless.\"\"\"\n   LogTalker.build_begin(self, f_mod, total)\n   if not self._headless:\n      self._builder_bar = BuilderBar(total,\n                                     self._trial_desc(\"build\"),\n                                     colour=\"blue\",\n                                     leave=True)\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.build_done","title":"<code>build_done(score: float) -&gt; None</code>","text":"<p>Close the builder bar.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def build_done(self, score: float) -&gt; None:\n   \"\"\"Close the builder bar.\"\"\"\n   if self._builder_bar:\n      self._builder_bar.close()\n      self._builder_bar = None\n   LogTalker.build_done(self, score)\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.build_selected","title":"<code>build_selected(iteration: int, metrics: dict[str, dict[str, float]]) -&gt; None</code>","text":"<p>Report selected-model metrics through the log/reporting layer.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def build_selected(\n   self,\n   iteration: int,\n   metrics: dict[str, dict[str, float]],\n) -&gt; None:\n   \"\"\"Report selected-model metrics through the log/reporting layer.\"\"\"\n   LogTalker.build_selected(self, iteration, metrics)\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.build_step","title":"<code>build_step(n: int, total: int, metrics: dict[str, dict[str, float]]) -&gt; None</code>","text":"<p>Update the builder bar, refresh tune bar, and emit periodic log lines.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def build_step(\n   self,\n   n: int,\n   total: int,\n   metrics: dict[str, dict[str, float]],\n) -&gt; None:\n   \"\"\"Update the builder bar, refresh tune bar, and emit periodic log lines.\"\"\"\n   if self._builder_bar:\n      self._builder_bar.status(metrics)\n   if self._tune_bar:\n      self._tune_bar.refresh()\n   LogTalker.build_step(self, n, total, metrics)\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.eval_begin","title":"<code>eval_begin(jobs, *, refjob=None, sidnames=True, miniters=1, **kwargs) -&gt; None</code>","text":"<p>Outside tuning: full EvalTalker bars. Inside tuning: single RunningBar, skips report.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def eval_begin(self,\n               jobs,\n               *,\n               refjob=None,\n               sidnames=True,\n               miniters=1,\n               **kwargs) -&gt; None:\n   \"\"\"Outside tuning: full EvalTalker bars. Inside tuning: single RunningBar, skips report.\"\"\"\n   if not self._in_tuning:\n      EvalTalker.eval_begin(self,\n                            jobs,\n                            refjob=refjob,\n                            sidnames=sidnames,\n                            miniters=miniters,\n                            **kwargs)\n      return\n   LogTalker.eval_begin(self,\n                        jobs,\n                        refjob=refjob,\n                        sidnames=sidnames,\n                        report=False,\n                        **kwargs)\n   if not self._headless:\n      max_job = max(len(_bids.problems(bid)) for (_, bid, _) in jobs)\n      self._total_bar = RunningBar(\n         total=self._total_count,\n         desc=self._trial_desc(\"eval \"),\n         miniters=miniters,\n         colour=\"blue\",\n         leave=True,\n         postfix_width=_postfix_width(max_job),\n      )\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.eval_done","title":"<code>eval_done() -&gt; None</code>","text":"<p>Outside tuning: close job bar. Inside tuning: log without closing any bar.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def eval_done(self) -&gt; None:\n   \"\"\"Outside tuning: close job bar. Inside tuning: log without closing any bar.\"\"\"\n   if not self._in_tuning:\n      EvalTalker.eval_done(self)\n   else:\n      LogTalker.eval_done(self)\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.eval_end","title":"<code>eval_end(results, refjob=None, **kwargs) -&gt; None</code>","text":"<p>Outside tuning: full EvalTalker end. Inside tuning: close eval bar, update tune bar.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def eval_end(self, results, refjob=None, **kwargs) -&gt; None:\n   \"\"\"Outside tuning: full EvalTalker end. Inside tuning: close eval bar, update tune bar.\"\"\"\n   if not self._in_tuning:\n      EvalTalker.eval_end(self, results, refjob=refjob, **kwargs)\n      return\n   if self._total_bar:\n      self._total_errors = self._total_bar._errors\n      self._total_bar.close()\n      self._total_bar = None\n   LogTalker.eval_end(self, results, refjob=refjob, report=False)\n   if not self._in_optuna_trial:\n      self._current_trial += 1\n      if self._tune_bar:\n         self._tune_bar.update(1)\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.eval_launch","title":"<code>eval_launch(tasks: Sequence[Task]) -&gt; None</code>","text":"<p>Outside tuning: per-job SolvingBar. Inside tuning: record time only.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def eval_launch(self, tasks: Sequence[\"Task\"]) -&gt; None:\n   \"\"\"Outside tuning: per-job SolvingBar. Inside tuning: record time only.\"\"\"\n   if not self._in_tuning:\n      EvalTalker.eval_launch(self, tasks)\n   else:\n      LogTalker.eval_launch(self, tasks)\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.eval_status","title":"<code>eval_status(new: bool | None, n: int = 1) -&gt; None</code>","text":"<p>Update eval bar and refresh tune bar to keep it visible.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def eval_status(self, new: \"bool | None\", n: int = 1) -&gt; None:\n   \"\"\"Update eval bar and refresh tune bar to keep it visible.\"\"\"\n   super().eval_status(new, n)\n   if self._tune_bar:\n      self._tune_bar.refresh()\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.terminate","title":"<code>terminate() -&gt; None</code>","text":"<p>Delegate to parent; _tune_bar is closed by tune_end().</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def terminate(self) -&gt; None:\n   \"\"\"Delegate to parent; _tune_bar is closed by tune_end().\"\"\"\n   super().terminate()\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.tune_begin","title":"<code>tune_begin(t_start: float, total: int = 0) -&gt; None</code>","text":"<p>Enter tuning mode; create outer tune bar if not headless.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def tune_begin(self, t_start: float, total: int = 0) -&gt; None:\n   \"\"\"Enter tuning mode; create outer tune bar if not headless.\"\"\"\n   self._in_tuning = True\n   self._total_trials = total\n   self._current_trial = 0\n   self._suspend_info = not self._headless\n   if self._headless or not total:\n      return\n   self._tune_bar = DefaultBar(total, \"tune\".ljust(self._inner_width()))\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.tune_end","title":"<code>tune_end(t_end: float) -&gt; None</code>","text":"<p>Leave tuning mode; close tune bar and restore info logs.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def tune_end(self, t_end: float) -&gt; None:\n   \"\"\"Leave tuning mode; close tune bar and restore info logs.\"\"\"\n   self._in_tuning = False\n   self._suspend_info = False\n   self._total_trials = 0\n   self._current_trial = 0\n   if self._tune_bar:\n      self._tune_bar.close()\n      self._tune_bar = None\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.tune_eval_begin","title":"<code>tune_eval_begin() -&gt; None</code>","text":"<p>Suppress eval log messages while ATP evaluation runs inside a tuning trial.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def tune_eval_begin(self) -&gt; None:\n   \"\"\"Suppress eval log messages while ATP evaluation runs inside a tuning trial.\"\"\"\n   self._in_tune_eval = True\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.tune_eval_end","title":"<code>tune_eval_end(results: dict) -&gt; None</code>","text":"<p>Re-enable eval log messages after tuning ATP evaluation.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def tune_eval_end(self, results: dict) -&gt; None:\n   \"\"\"Re-enable eval log messages after tuning ATP evaluation.\"\"\"\n   self._in_tune_eval = False\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.tune_trial_begin","title":"<code>tune_trial_begin(nick: str, it: int, values: list) -&gt; None</code>","text":"<p>Mark start of Optuna trial and delegate to LogTalker.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def tune_trial_begin(self, nick: str, it: int, values: list) -&gt; None:\n   \"\"\"Mark start of Optuna trial and delegate to LogTalker.\"\"\"\n   self._in_optuna_trial = True\n   LogTalker.tune_trial_begin(self, nick, it, values)\n</code></pre>"},{"location":"api/solverpy_learn/report/talker/looptalker/#solverpy_learn.report.talker.looptalker.LoopTalker.tune_trial_done","title":"<code>tune_trial_done(stats: dict[str, Any]) -&gt; None</code>","text":"<p>Increment tune bar at end of Optuna trial.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/report/talker/looptalker.py</code> <pre><code>def tune_trial_done(self, stats: dict[str, Any]) -&gt; None:\n   \"\"\"Increment tune bar at end of Optuna trial.\"\"\"\n   LogTalker.tune_trial_done(self, stats)\n   self._in_optuna_trial = False\n   self._current_trial += 1\n   if self._tune_bar:\n      self._tune_bar.update(1)\n</code></pre>"},{"location":"api/solverpy_learn/scripts/autotune/","title":"module autotune","text":""},{"location":"api/solverpy_learn/scripts/autotune/#solverpy_learn.scripts.autotune","title":"<code>solverpy_learn.scripts.autotune</code>","text":""},{"location":"api/solverpy_learn/scripts/compress/","title":"module compress","text":""},{"location":"api/solverpy_learn/scripts/compress/#solverpy_learn.scripts.compress","title":"<code>solverpy_learn.scripts.compress</code>","text":""},{"location":"api/solverpy_learn/scripts/decompress/","title":"module decompress","text":""},{"location":"api/solverpy_learn/scripts/decompress/#solverpy_learn.scripts.decompress","title":"<code>solverpy_learn.scripts.decompress</code>","text":""},{"location":"api/solverpy_learn/scripts/deconflict/","title":"module deconflict","text":""},{"location":"api/solverpy_learn/scripts/deconflict/#solverpy_learn.scripts.deconflict","title":"<code>solverpy_learn.scripts.deconflict</code>","text":""},{"location":"api/solverpy_learn/scripts/filter/","title":"module filter","text":""},{"location":"api/solverpy_learn/scripts/filter/#solverpy_learn.scripts.filter","title":"<code>solverpy_learn.scripts.filter</code>","text":""},{"location":"api/solverpy_learn/setups/","title":"Setups Overview","text":""},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups","title":"<code>solverpy_learn.setups</code>","text":""},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.Evalset","title":"<code>Evalset</code>","text":"<p>               Bases: <code>TypedDict</code></p> <p>Per-dataset configuration: problems, strategies, and ML training state.</p> Source code in <code>packages/solverpy/src/solverpy/setups/evalset.py</code> <pre><code>class Evalset(TypedDict, total=False):\n   \"\"\"Per-dataset configuration: problems, strategies, and ML training state.\"\"\"\n   label: str\n   benchmarks: list[str]\n   strategies: list[str]\n   bidfile: str\n   sidfile: str\n   refs: list[str]\n   ref: bool | int | str | None\n   limit: str\n   cores: int\n   binary: str\n   static: list[str]\n   complete: bool\n   force: bool\n   shuffle: bool\n   solvedby: str\n   max_proofs: int\n   pool_context: str\n   plugin: Any  # SvmTrains\n   plugins: list[\"Plugin\"]\n   solver: \"SolverPy\"\n   dataname: str\n   basedataname: str\n   previous_trains: str | tuple[str]\n   proofs: dict[str, int] | None\n   start_dataname: str\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.Setup","title":"<code>Setup</code>","text":"<p>               Bases: <code>TypedDict</code></p> <p>A (typed) dictionary summarizing the configuration of the experiment. An ordinary dictionary can be used in its place but type annotations provide additional safety.</p> <p>Some parameters should be set by the user, others are automatically filled in by functions in the <code>setups</code> module.</p>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.Setup--evaluation-parameters","title":"Evaluation parameters","text":"<p>The following are the most important parameters to be set by the user for an evaluation run:</p> <p>Attributes:</p> Name Type Description <code>limit</code> <code>str</code> <p>Resource limit for solvers.</p> <code>cores</code> <code>int</code> <p>Number of CPU cores for parallel evaluation.</p> <code>benchmarks</code> <code>int</code> <p>List of benchmark ids. </p> <code>strategies</code> <code>int</code> <p>List of strategy ids.</p> <code>complete</code> <code>bool</code> <p>Complete use of the solver (sat/Satisfiable are successes).</p> <p>The following parameters additional parameters for an evaluation run:</p> <p>Attributes:</p> Name Type Description <code>ref</code> <p>Reference strategy id.</p> <code>binary</code> <code>str</code> <p>Custom solver binary.  Must be in <code>PATH</code>.</p> <code>static</code> <code>list[str]</code> <p>Fixed solver options.</p> <code>ntfy</code> <code>str</code> <p>Address for user push notifications via ntfy.</p> <code>options</code> <code>list[str]</code> <p>Experiment boolean options. An option is identified by its string name, and represents a boolean yes/no value. Use <code>no-</code>option, like <code>no-compress</code>, to set the option to <code>no</code>. Currently supported options are:</p> option description default <code>outputs</code> Keep raw solver output files from all runs. no <code>compress</code> Compress output files (outputs, trains, results). yes <code>flatten</code> Put all output files in a single directory (replace <code>/</code> with <code>_._</code>). yes <code>compress-trains</code> Compress trains. yes <code>debug-trains</code> Dump training data for each file separately. no <code>proofs</code> Store proof objects in the database. no <code>premises</code> Store premise selections in the database. no <code>headless</code> Suppress progress bars and interactive UI (for non-terminal use). no <code>plugins</code> <code>list[Plugin]</code> <p>List of additional solver plugins.</p> <code>delfix</code> <code>int | str | None</code> <p>Prefix to remove from problem names.</p> <ul> <li>If the value is of type <code>str</code>, then this prefix is removed from the beginning of every problem name.</li> <li>If the value is of type <code>int</code>, then the problem name is expected to be a <code>/</code>-separated path, and the first <code>delfix</code> components are removed.</li> <li>If the value <code>None</code>, nothing is removed.</li> </ul> <p>The typical use is to remove the prefix <code>problems/</code> from problem names when the problems are stored inside <code>problems/</code> subdirectory. In that case, set <code>delfix=1</code>.</p> <p>This applies to DB providers that support <code>delfix</code> (e.g. <code>Solved</code> and <code>Status</code>). The default value is <code>None</code>.</p>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.Setup--ml-parameters","title":"ML parameters","text":"<p>The following are user options for ML experiments.</p> <p>Attributes:</p> Name Type Description <code>loops</code> <code>int</code> <p>Number of iterations of the eval/ML loop.</p> <code>force</code> <code>bool</code> <p>Recompute everything.</p> <code>shuffle</code> <code>bool</code> <p>Shuffle problem order.</p> <code>e_training_examples</code> <code>str</code> <p>Output format of training samples for <code>eprover</code>.</p> <code>gen_features</code> <code>str</code> <p>ENIGMA features for generation filtering.</p> <code>sel_features</code> <code>str</code> <p>ENIGMA features for clause selection.</p> <code>posneg_ratio</code> <code>float</code> <p>Maximum ratio of negative to positive examples.</p> <code>templates</code> <code>list[str]</code> <p>Templates for strategy generation.</p> <code>evals</code> <code>Evalset</code> <p>Primary evaluation dataset configuration (Evalset with benchmarks, strategies, plugin, etc.).</p> <code>devels</code> <code>Evalset</code> <p>Development dataset configuration (Evalset).</p>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.Setup--internal-parameters","title":"Internal parameters","text":"<p>The following parameters are used internally and should not be set directly by the user.</p> <p>Attributes:</p> Name Type Description <code>it</code> <code>int</code> <p>Current iteration number.</p> <code>news</code> <code>list[str]</code> <p>New ML strategies.</p> <code>db</code> <code>DB</code> <p>Database object.</p> <code>builder</code> <code>Any</code> <p>Builder object.</p> Source code in <code>packages/solverpy/src/solverpy/setups/setup.py</code> <pre><code>class Setup(TypedDict, total=False):\n   \"\"\"\n   A (typed) dictionary summarizing the configuration of the experiment.\n   An ordinary dictionary can be used in its place but type annotations\n   provide additional safety.\n\n   Some parameters should be set by the user, others are automatically\n   filled in by functions in the [`setups`][solverpy.setups] module.\n\n   # Evaluation parameters\n\n   The following are the most important parameters to be set by the user for an evaluation run:\n\n   Attributes: \n       limit: Resource limit for solvers.\n       cores: Number of CPU cores for parallel evaluation.\n       benchmarks: List of benchmark ids. \n       strategies: List of strategy ids.\n       complete: Complete use of the solver (sat/Satisfiable are successes).\n\n   The following parameters additional parameters for an evaluation run:\n\n   Attributes:\n       ref: Reference strategy id.\n       binary: Custom solver binary.  Must be in `PATH`.\n       static: Fixed solver options.\n       ntfy: Address for user push notifications via [ntfy](https://ntfy.sh).\n       options: Experiment boolean options.\n           An *option* is identified by its string name, and represents a boolean yes/no value.\n           Use `no-`option, like `no-compress`, to set the option to `no`.\n           Currently supported options are:\n\n           |option|description|default|\n           |------|-----------|-------|\n           |`outputs`|Keep raw solver output files from all runs.|no|\n           |`compress`|Compress output files (outputs, trains, results).|yes|\n           |`flatten`|Put all output files in a single directory (replace `/` with `_._`).|yes|\n           |`compress-trains`|Compress trains.|yes|\n           |`debug-trains`|Dump training data for each file separately.|no|\n           |`proofs`|Store proof objects in the database.|no|\n           |`premises`|Store premise selections in the database.|no|\n           |`headless`|Suppress progress bars and interactive UI (for non-terminal use).|no|\n\n       plugins: List of additional solver plugins.\n       delfix: \n           Prefix to remove from problem names.\n\n           + If the value is of type `str`, then this prefix is removed from the\n           beginning of every problem name.\n           + If the value is of type `int`, then the problem name is expected to\n           be a `/`-separated path, and the first `delfix` components are\n           removed.\n           + If the value `None`, nothing is removed.\n\n           The typical use is to remove the prefix `problems/` from problem\n           names when the problems are stored inside `problems/` subdirectory.\n           In that case, set `delfix=1`.\n\n           This applies to DB providers that support `delfix` (e.g.\n           [`Solved`][solverpy.benchmark.db.providers.solved] and\n           [`Status`][solverpy.benchmark.db.providers.status]).\n           The default value is `None`.\n\n\n\n\n\n   # ML parameters\n\n   The following are user options for ML experiments.\n\n   Attributes:\n       loops: Number of iterations of the eval/ML loop.\n       force: Recompute everything.\n       shuffle: Shuffle problem order.\n       e_training_examples: Output format of training samples for `eprover`.\n       gen_features: ENIGMA features for generation filtering.\n       sel_features: ENIGMA features for clause selection.\n       posneg_ratio: Maximum ratio of negative to positive examples.\n       templates: Templates for strategy generation.\n       evals: Primary evaluation dataset configuration (Evalset with benchmarks, strategies, plugin, etc.).\n       devels: Development dataset configuration (Evalset).\n\n   # Internal parameters\n\n   The following parameters are used internally and should not be set\n   directly by the user.\n\n   Attributes:\n       it: Current iteration number.\n       news: New ML strategies.\n       db: Database object.\n       builder: Builder object.\n   \"\"\"\n   limit: str\n   cores: int\n   binary: str\n   static: list[str]\n   ntfy: str\n   it: int\n   loops: int\n   news: list[str]\n   options: list[str]\n   common: dict[str, Any]\n   delfix: (int | str | None)\n   complete: bool\n\n   force: bool\n   shuffle: bool\n\n   db: \"DB\"\n   builder: Any  # set by solverpy_learn\n   talker: \"Talker\"\n   reloader: bool\n   evals: \"Evalset\"  # primary evaluation dataset (set by solverpy_learn or evaluation())\n   devels: \"Evalset\"  # development dataset (set by solverpy_learn)\n   plugins: list[\"Plugin\"]\n   e_training_examples: str\n   gen_features: str\n   sel_features: str\n   posneg_ratio: float\n   posneg_weight: float\n   templates: list[str]\n   chunk_size: int  # rows per NPZ chunk when compressing SVM-Light trains (default 1_000_000)\n   pool_context: str  # multiprocessing start method for the evaluation pool (default \"forkserver\")\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.bitwuzla","title":"<code>bitwuzla(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Bitwuzla</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def bitwuzla(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Bitwuzla`][solverpy.solver.smt.bitwuzla.Bitwuzla] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Bitwuzla, BWZ_STATIC.split())\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.cvc5ml","title":"<code>cvc5ml(setup: Setup, tuneargs: dict[str, Any] | None = None) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> with a <code>Cvc5ML</code> builder.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/setups/tuner.py</code> <pre><code>def cvc5ml(\n   setup: Setup,\n   tuneargs: (dict[str, Any] | None) = None,\n) -&gt; Setup:\n   \"\"\"Configure `setup` with a [`Cvc5ML`][solverpy_learn.builder.cvc5ml.Cvc5ML] builder.\"\"\"\n   defaultweight(setup, tuneargs)\n   return autotuner(Cvc5ML, setup, tuneargs)\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.enigma","title":"<code>enigma(setup: Setup, tunesel: dict[str, Any] | None = None, tunegen: dict[str, Any] | None = None) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> with an <code>Enigma</code> builder.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/setups/tuner.py</code> <pre><code>def enigma(\n   setup: Setup,\n   tunesel: (dict[str, Any] | None) = None,\n   tunegen: (dict[str, Any] | None) = None,\n) -&gt; Setup:\n   \"\"\"Configure `setup` with an [`Enigma`][solverpy_learn.builder.enigma.Enigma] builder.\"\"\"\n   default(setup, \"templates\", None)\n   defaultweight(setup, tunesel)\n   defaultweight(setup, tunegen)\n   assert \"templates\" in setup\n   return autotuner(\n      Enigma,\n      setup,\n      tunesel,\n      tunegen,\n      templates=setup[\"templates\"],\n   )\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.evaluation","title":"<code>evaluation(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> for benchmark evaluation: DB, cores, strategies, and benchmarks.</p> Source code in <code>packages/solverpy/src/solverpy/setups/loop.py</code> <pre><code>def evaluation(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` for benchmark evaluation: DB, cores, strategies, and benchmarks.\"\"\"\n   def configure_evalset(evalset: Evalset) -&gt; None:\n      default(evalset, \"ref\", True)\n      if \"strategies\" not in evalset:\n         default(evalset, \"sidfile\", \"sids\")\n         with open(evalset[\"sidfile\"]) as f:\n            evalset[\"strategies\"] = f.read().strip().split(\"\\n\")\n      if \"benchmarks\" not in evalset:\n         default(evalset, \"bidfile\", \"bids\")\n         with open(evalset[\"bidfile\"]) as f:\n            evalset[\"benchmarks\"] = f.read().strip().split(\"\\n\")\n\n   def check_list(evalset: Evalset, key: str, fun: Callable) -&gt; None:\n      if key not in evalset:\n         return\n      for id in evalset[key]:\n         try:\n            fun(id)\n         except Exception as e:\n            logger.error(f\"Wrong id in {key}: {id}\")\n            logger.error(f\"Exception: {e}\")\n            sys.exit(0)\n\n   default(setup, \"cores\", 4)\n   default(setup, \"delfix\", None)\n   assert \"delfix\" in setup\n   default(setup, \"db\", db.default(delfix=setup[\"delfix\"]))\n   default(setup, \"ntfy\", None)\n\n   for key in (\"evals\", \"devels\"):\n      if key not in setup:\n         continue\n      evalset = setup[key]\n      configure_evalset(evalset)\n      check_list(evalset, \"strategies\", sids.load)\n      check_list(evalset, \"refs\", sids.load)\n      check_list(evalset, \"benchmarks\", bids.problems)\n   return setup\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.experiment","title":"<code>experiment(setup: Setup) -&gt; Setup</code>","text":"<p>Normalize a user-facing setup before evaluation or looping starts.</p> Source code in <code>packages/solverpy/src/solverpy/setups/loop.py</code> <pre><code>def experiment(setup: Setup) -&gt; Setup:\n   \"\"\"Normalize a user-facing setup before evaluation or looping starts.\"\"\"\n   common = setup.pop(\"common\", {})\n   if \"evals\" in setup and \"trains\" in setup:\n      if setup[\"evals\"] != setup[\"trains\"]:\n         raise ValueError(\"Use either 'evals' or 'trains', not both.\")\n      setup.pop(\"trains\")\n   elif \"trains\" in setup:\n      setup[\"evals\"] = setup.pop(\"trains\")\n\n   for key in (\"evals\", \"devels\"):\n      if key not in setup:\n         continue\n      evalset = setup[key]\n      setup[key] = Evalset(common, **evalset)\n   return setup\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.initialize","title":"<code>initialize(setup: Setup) -&gt; Runtime</code>","text":"<p>Initialize all Managed plugins in the setup with a shared Manager.</p> Source code in <code>packages/solverpy/src/solverpy/setups/runtime.py</code> <pre><code>def initialize(setup: Setup) -&gt; Runtime:\n   \"\"\"Initialize all Managed plugins in the setup with a shared Manager.\"\"\"\n   configured = list(setup.get(\"plugins\", []))\n   for key in (\"evals\", \"devels\"):\n      if key in setup:\n         configured.extend(setup[key].get(\"plugins\", []))\n   plugins = [p for p in configured if isinstance(p, Managed)]\n   return Runtime(plugins)\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.prover9","title":"<code>prover9(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Prover9</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def prover9(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Prover9`][solverpy.solver.atp.prover9.Prover9] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Prover9)\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.vampire","title":"<code>vampire(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Vampire</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def vampire(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Vampire`][solverpy.solver.atp.vampire.Vampire] solver.\"\"\"\n   init(setup)\n   return _evalset_solvers(setup, Vampire, V_STATIC.split())\n</code></pre>"},{"location":"api/solverpy_learn/setups/#solverpy_learn.setups.z3","title":"<code>z3(setup: Setup) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> to use the <code>Z3</code> solver.</p> Source code in <code>packages/solverpy/src/solverpy/setups/solver.py</code> <pre><code>def z3(setup: Setup) -&gt; Setup:\n   \"\"\"Configure `setup` to use the [`Z3`][solverpy.solver.smt.z3.Z3] solver.\"\"\"\n   init(setup)\n   # No static: `Z3` is a `StdinSolver`, where `static` is the prefix of the\n   # stdin payload and not the command line.  `Z3_STATIC` is already baked\n   # into the command by `Z3.__init__`, and passing it here as well sent\n   # `-smt2 -st` down the SMT-LIB2 stream, so every z3 run answered with a\n   # leading `(error \"line 1 column 1: invalid command, '(' expected\")`.\n   return _evalset_solvers(setup, Z3)\n</code></pre>"},{"location":"api/solverpy_learn/setups/loop/","title":"module loop","text":""},{"location":"api/solverpy_learn/setups/loop/#solverpy_learn.setups.loop","title":"<code>solverpy_learn.setups.loop</code>","text":""},{"location":"api/solverpy_learn/setups/solver/","title":"module solver","text":""},{"location":"api/solverpy_learn/setups/solver/#solverpy_learn.setups.solver","title":"<code>solverpy_learn.setups.solver</code>","text":""},{"location":"api/solverpy_learn/setups/tuner/","title":"module tuner","text":""},{"location":"api/solverpy_learn/setups/tuner/#solverpy_learn.setups.tuner","title":"<code>solverpy_learn.setups.tuner</code>","text":""},{"location":"api/solverpy_learn/setups/tuner/#solverpy_learn.setups.tuner.cvc5ml","title":"<code>cvc5ml(setup: Setup, tuneargs: dict[str, Any] | None = None) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> with a <code>Cvc5ML</code> builder.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/setups/tuner.py</code> <pre><code>def cvc5ml(\n   setup: Setup,\n   tuneargs: (dict[str, Any] | None) = None,\n) -&gt; Setup:\n   \"\"\"Configure `setup` with a [`Cvc5ML`][solverpy_learn.builder.cvc5ml.Cvc5ML] builder.\"\"\"\n   defaultweight(setup, tuneargs)\n   return autotuner(Cvc5ML, setup, tuneargs)\n</code></pre>"},{"location":"api/solverpy_learn/setups/tuner/#solverpy_learn.setups.tuner.enigma","title":"<code>enigma(setup: Setup, tunesel: dict[str, Any] | None = None, tunegen: dict[str, Any] | None = None) -&gt; Setup</code>","text":"<p>Configure <code>setup</code> with an <code>Enigma</code> builder.</p> Source code in <code>packages/solverpy-learn/src/solverpy_learn/setups/tuner.py</code> <pre><code>def enigma(\n   setup: Setup,\n   tunesel: (dict[str, Any] | None) = None,\n   tunegen: (dict[str, Any] | None) = None,\n) -&gt; Setup:\n   \"\"\"Configure `setup` with an [`Enigma`][solverpy_learn.builder.enigma.Enigma] builder.\"\"\"\n   default(setup, \"templates\", None)\n   defaultweight(setup, tunesel)\n   defaultweight(setup, tunegen)\n   assert \"templates\" in setup\n   return autotuner(\n      Enigma,\n      setup,\n      tunesel,\n      tunegen,\n      templates=setup[\"templates\"],\n   )\n</code></pre>"},{"location":"tutorials/enigma-training/","title":"\ud83e\udde0 Tutorial: Training an ENIGMA Model","text":"<p>ENIGMA trains a machine-learning model that guides E Prover's clause selection, by repeating: evaluate a strategy \u2192 collect proofs \u2192 train a model from them \u2192 generate a new, ML-enhanced strategy \u2192 re-evaluate. This requires the <code>solverpy-learn</code> package and an <code>eprover-ho</code> binary built with ENIGMA's ML features.</p>"},{"location":"tutorials/enigma-training/#1-prepare-the-database","title":"\u25c6 1. Prepare the database","text":"<pre><code>$ mkdir my-enigma-loop &amp;&amp; cd my-enigma-loop\n$ solverpy init eprover\n</code></pre> <p>As in Evaluating E, this gives you <code>solverpy_db/strats/</code> with bundled strategies. We'll use one of these as the base strategy that ENIGMA learns to improve.</p> <p>\ud83d\uddd2\ufe0f Not every strategy works as a base strategy. ENIGMA folds its model into an existing clause-evaluation-function definition (E's <code>-H</code> option), so the base strategy must define one explicitly. <code>auto</code> (and <code>--auto-schedule</code>) pick a heuristic internally and don't expose a <code>-H</code> string, so they can't be used here. <code>mzr02</code> (and the rest of the <code>mzr*</code> family, tuned for Mizar problems) does define one \u2014 use that instead.</p>"},{"location":"tutorials/enigma-training/#2-prepare-the-problems","title":"\u25c6 2. Prepare the problems","text":"<pre><code>$ mkdir -p problems\n$ cp -r /path/to/some/problems problems/mine\n</code></pre> <p>Unlike a plain evaluation, a loop needs proofs to train from, so pick a problem set your base strategy can actually solve a good fraction of.</p>"},{"location":"tutorials/enigma-training/#3-write-the-loop-yaml","title":"\u25c6 3. Write the loop YAML","text":"<pre><code>loop: enigma\nstrategy: mzr02\n\nsel_features: \"C(l,x,s,r,v[b=2048],h,c,d,t,a):M:F:S:G\"\nloops: 3\n\ncommon:\n  limit: T5\n  cores: 4\n  binary: eprover-ho       # ENIGMA needs the ML-enabled build\n\nevals:\n  dataname: enigma/train\n  benchmarks:\n    - problems/mine\n\noptions:\n  - outputs\n  - debug-trains\n\ntune:\n  phases: \"l\"              # which LightGBM parameter groups to tune (l: leaves)\n  atpeval: true\n  timeout: null\n  iters: 8                 # Optuna trials per loop iteration\n  min_leaves: 8\n  max_leaves: 256\n  init_params:\n    metric: auc\n    num_round: 250\n    num_leaves: 4\n</code></pre> <p>Save this as <code>loop-eprover-enigma.yaml</code>.</p> <ul> <li><code>strategy:</code> is the base sid both trained on and refined.</li> <li><code>sel_features:</code> picks E's clause-selection feature set (see E's own   documentation for the feature-string syntax); a <code>gen_features:</code> key is   also accepted for the clause-generation model and defaults to none.</li> <li><code>tune.phases</code> controls which LightGBM hyperparameter groups Optuna tunes   each loop iteration, one after another: <code>l</code>=leaves (<code>num_leaves</code>),   <code>b</code>=bagging, <code>r</code>=regularization, <code>m</code>=<code>min_data</code>, <code>d</code>=<code>max_depth</code>,   <code>e</code>=<code>learning_rate</code>, <code>w</code>=positive/negative sample weight ratio (see   <code>solverpy tune --help</code> in Commands). <code>iters:</code> is a   total trial budget split evenly across the listed phases (so <code>\"l\"</code> alone   gives leaves the full <code>iters:</code> trials, while the CLI default <code>\"l:b:m:r\"</code>   spreads the same budget across all four, fewer trials each, in exchange   for tuning more parameter groups) \u2014 bump <code>iters:</code> accordingly if you add   phases.</li> </ul>"},{"location":"tutorials/enigma-training/#4-launch-it","title":"\u25c6 4. Launch it","text":"<pre><code>$ solverpy run loop-eprover-enigma.yaml\n</code></pre> <p>Each of the <code>loops:</code> iterations: evaluates the current strategy on <code>evals:</code>, collects proofs from the successful runs, trains a LightGBM model from them (<code>solverpy_db/trains/</code>, <code>solverpy_db/models/</code>), derives new ml-enhanced strategies from the trained model, and evaluates those.</p>"},{"location":"tutorials/enigma-training/#5-what-ends-up-in-solved","title":"\u25c6 5. What ends up in <code>solved/</code>","text":"<p>Even a minimal, fast config \u2014 <code>loops: 1</code>, <code>tune.iters: 2</code>, <code>limit: T5</code> \u2014 exercises the same mechanism as the fuller settings above, just quicker. On the same 10-problem TPTP set as Evaluating E:</p> <pre><code>$ cd solverpy_db/solved/problems--mine--T5\n$ wc -l * | sort -n\n   8 mzr02\n   8 mzr02-coop@sel=enigma--train--loop00--sel_Clxsrvb2048hcdtaMFSG\n   ...\n   8 mzr02-solo@sel=enigma--train--loop00--sel_Clxsrvb2048hcdtaMFSG\n   ...\n  72 total\n</code></pre> <p>(With <code>loops: 3</code>/<code>iters: 8</code> as in the YAML above, this list is the same shape but longer \u2014 <code>loop01</code>/<code>loop02</code> subdirectories, and more <code>--opt--</code> entries per loop.)</p> <p>That's more than just \"the base strategy plus one new one\":</p> <ul> <li><code>mzr02</code> is the unmodified base strategy, evaluated for reference.</li> <li><code>mzr02-coop@sel=...</code> and <code>mzr02-solo@sel=...</code> are the two ways E can use a   trained model during search \u2014 cooperatively alongside the original   heuristic, or solo, driving selection on its own. These are synthesized   sid strings (embedding the model path), not files under   <code>solverpy_db/strats/</code>.</li> <li>The rest of the lines (elided above as <code>...</code>), each strategy name suffixed   <code>--opt--init</code> or <code>--opt--01-leaves--modelNNNN</code>, are Optuna trials: one   entry per trial (<code>init</code> plus one per <code>iters:</code>) \u2014 each candidate model gets   evaluated so the tuner can pick the best-performing one.</li> </ul> <p>On this tiny toy set all nine variants solve the same 8 of 10 problems \u2014 one loop and two trials aren't enough data to show a real improvement over the base strategy; that's expected here, not a sign anything is broken. With more problems, more loops, and more tuning trials, later strategies should start pulling ahead.</p> <pre><code>$ ls solverpy_db/models/enigma/train/loop00/sel_Clxsrvb2048hcdtaMFSG/\nenigma.map  model.lgb  opt/\n</code></pre> <p>Re-running <code>solverpy run loop-eprover-enigma.yaml</code> picks up cached evaluation results and models rather than recomputing them \u2014 <code>solverpy clean</code> first if you want a fresh run.</p>"},{"location":"tutorials/enigma-training/#adding-a-development-set","title":"\u25c6 Adding a development set","text":"<p>The example above trains and evaluates on the same problem set (<code>evals:</code>). For anything beyond a quick trial run, add a separate development set (<code>devels:</code>) \u2014 problems held out from training, used to check that the trained model actually generalizes rather than just memorizing its own training proofs:</p> <pre><code>evals:\n  dataname: enigma/train\n  benchmarks:\n    - problems/mine\n\ndevels:\n  dataname: enigma/devel\n  benchmarks:\n    - problems/heldout\n</code></pre> <p>With <code>devels:</code> present, each loop iteration additionally evaluates every candidate strategy on <code>problems/heldout</code>, so the reported solved-count improvements reflect problems the model never saw proofs from during training.</p>"},{"location":"tutorials/enigma-training/#next-steps","title":"\u25c6 Next steps","text":"<ul> <li>Commands \u2014 <code>solverpy tune</code>/<code>compress</code>/<code>decompress</code>/<code>deconflict</code>/<code>filter</code> reference for working with training data directly.</li> </ul>"},{"location":"tutorials/eval-cvc5/","title":"\u2697\ufe0f Tutorial: Evaluating cvc5 Strategies","text":"<p>The same workflow as Evaluating E Prover, for the SMT solver <code>cvc5</code>.</p>"},{"location":"tutorials/eval-cvc5/#1-bootstrap-the-project","title":"\u25c6 1. Bootstrap the project","text":"<pre><code>$ mkdir my-cvc5-eval &amp;&amp; cd my-cvc5-eval\n$ solverpy init cvc5\n</code></pre> <p>This creates <code>solverpy_db/strats/</code> populated with several bundled cvc5 strategies (<code>ls solverpy_db/strats</code>), each a plain-text line of <code>cvc5</code> command-line options, e.g.:</p> <pre><code>$ cat solverpy_db/strats/smtcomp01\n--simplification=none --enum-inst\n</code></pre>"},{"location":"tutorials/eval-cvc5/#2-add-benchmark-problems","title":"\u25c6 2. Add benchmark problems","text":"<p>Download a ready-made set of SMT-LIB2 problems (translated from the Mizar Mathematical Library) together with a few extra example strategies tuned for them:</p> <pre><code>$ wget https://cbboyan.github.io/solverpy/example.tar.gz\n# or: curl -LO https://cbboyan.github.io/solverpy/example.tar.gz\n$ tar xzf example.tar.gz --strip-components=1\n</code></pre> <p>This adds <code>problems/</code> (your <code>bid</code>) and a few more strategy files \u2014 <code>buzzard</code>, <code>sparrow</code>, <code>chickadee</code>, <code>enum</code> \u2014 into <code>solverpy_db/strats/</code>, alongside the bundled <code>smtcomp*</code> ones from step 1.</p>"},{"location":"tutorials/eval-cvc5/#3-pick-strategies-to-compare","title":"\u25c6 3. Pick strategies to compare","text":"<p>Any of the bundled <code>smtcomp*</code> strategies work directly, as do <code>buzzard</code>, <code>sparrow</code>, and <code>chickadee</code> from the downloaded set \u2014 or add your own by creating a new file under <code>solverpy_db/strats/</code> with the cvc5 options you want to try.</p>"},{"location":"tutorials/eval-cvc5/#4-write-the-experiment","title":"\u25c6 4. Write the experiment","text":"<pre><code>evaluate: cvc5\ncommon:\n  limit: T10\n  cores: 4\nevals:\n  dataname: eval\n  benchmarks:\n    - problems\n  strategies:\n    - buzzard\n    - sparrow\n    - chickadee\noptions:\n  - outputs\n</code></pre> <p>Save this as <code>eval-cvc5.yaml</code>.</p>"},{"location":"tutorials/eval-cvc5/#5-launch-it","title":"\u25c6 5. Launch it","text":"<pre><code>$ solverpy run eval-cvc5.yaml\n</code></pre> <p>As before, results are cached: re-running the same command is instant unless you <code>solverpy clean</code> first.</p>"},{"location":"tutorials/eval-cvc5/#6-inspect-the-results","title":"\u25c6 6. Inspect the results","text":"<pre><code>$ cat solverpy_db/status/problems--T10/buzzard\n$ cat solverpy_db/solved/problems--T10/sparrow\n</code></pre> <p>The full per-problem results (gzip JSON in <code>solverpy_db/results/</code>) include cvc5's own <code>--stats</code>/<code>--stats-internal</code> counters \u2014 see <code>Cvc5.process</code> for what's captured, and an explicit <code>timeout</code> status when cvc5 reports being interrupted.</p>"},{"location":"tutorials/eval-cvc5/#next-steps","title":"\u25c6 Next steps","text":"<ul> <li>Training an ENIGMA model \u2014 the ML training loop, for E Prover.</li> <li>Python API: benchmark evaluation \u2014 the same workflow, called from Python instead of YAML.</li> <li>Commands \u2014 full YAML/options reference.</li> </ul>"},{"location":"tutorials/eval-eprover/","title":"\ud83e\uddea Tutorial: Evaluating E Prover Strategies","text":"<p>This walks through evaluating a few different <code>eprover</code> strategies on a set of TPTP problems, end to end. See Usage first if you haven't already, for the <code>sid</code>/<code>bid</code> concepts used below.</p>"},{"location":"tutorials/eval-eprover/#1-bootstrap-the-project","title":"\u25c6 1. Bootstrap the project","text":"<pre><code>$ mkdir my-eprover-eval &amp;&amp; cd my-eprover-eval\n$ solverpy init eprover\n</code></pre> <p>This creates <code>solverpy_db/strats/</code> with several bundled example strategies (inspect them with <code>ls solverpy_db/strats</code>), and a starter <code>eval-eprover.yaml</code>.</p>"},{"location":"tutorials/eval-eprover/#2-pick-strategies-to-compare","title":"\u25c6 2. Pick strategies to compare","text":"<p>Strategy files are plain E command-line options, so you can compare any of the bundled ones directly. A few useful ones to start with:</p> <pre><code>$ cat solverpy_db/strats/default        # (empty: E's own defaults)\n$ cat solverpy_db/strats/auto           # --auto\n$ cat solverpy_db/strats/autoschedule   # --auto-schedule\n$ cat solverpy_db/strats/mzr02          # tuned for Mizar problems\n</code></pre> <p><code>ls solverpy_db/strats</code> also shows a whole <code>mzr01</code>\u2013<code>mzr22</code> family, tuned for Mizar problems (<code>mzr02</code> is a solid, well-tested default among them), alongside a <code>bls*</code>/<code>e-*</code> family from other automated tuning runs \u2014 good material to compare once you're past the basics.</p> <p>You can also add your own: create <code>solverpy_db/strats/my-strategy</code> and put any <code>eprover</code> command-line options in it as plain text.</p>"},{"location":"tutorials/eval-eprover/#3-add-your-benchmark-problems","title":"\u25c6 3. Add your benchmark problems","text":"<p>Create a directory of TPTP problem files, e.g.:</p> <pre><code>$ mkdir -p problems/mine\n$ cp /path/to/some/*.p problems/mine/\n</code></pre> <p>Every regular file directly inside <code>problems/mine/</code> is treated as one benchmark problem (no recursion, hidden files ignored) \u2014 that's your <code>bid</code>.</p> <p>A <code>bid</code> can also point to a file instead of a directory. In that case the file is a plain list of problem paths, one per line, each resolved relative to the directory containing the bid file itself \u2014 handy when your problems live in a nested/hierarchical layout you don't want to flatten. For example, a bid file <code>problems/mine/subset1</code> (a plain text file, sitting right inside the tree it lists):</p> <pre><code>category1/problemA.p\ncategory1/problemB.p\ncategory2/subcat/problemC.p\n</code></pre> <p>used as <code>benchmarks: [problems/mine/subset1]</code>, resolves each line relative to <code>problems/mine/</code> \u2014 i.e. to <code>problems/mine/category1/problemA.p</code>, etc.</p>"},{"location":"tutorials/eval-eprover/#4-write-the-experiment","title":"\u25c6 4. Write the experiment","text":"<p>Edit <code>eval-eprover.yaml</code> to list the strategies from step 2 and point at your problems:</p> <pre><code>evaluate: eprover\ncommon:\n  limit: T10\n  cores: 4\nevals:\n  dataname: eval\n  benchmarks:\n    - problems/mine\n  strategies:\n    - default\n    - auto\n    - autoschedule\n    - mzr02\noptions:\n  - outputs\n</code></pre>"},{"location":"tutorials/eval-eprover/#5-launch-it","title":"\u25c6 5. Launch it","text":"<pre><code>$ solverpy run eval-eprover.yaml\n</code></pre> <p>You'll see per-strategy progress bars (solved/unsolved/errors) while it runs. Run the same command again afterwards and notice it finishes instantly \u2014 results are cached in <code>solverpy_db/</code> and are not recomputed unless you <code>solverpy clean</code> first.</p>"},{"location":"tutorials/eval-eprover/#6-inspect-the-results","title":"\u25c6 6. Inspect the results","text":"<p><code>solverpy_db/solved/</code> and <code>solverpy_db/status/</code> each hold one subdirectory per <code>bid</code>+<code>limit</code> (here <code>problems--mine--T10</code>), with one file per <code>sid</code> inside it:</p> <ul> <li>a <code>solved/.../&lt;sid&gt;</code> file lists the problem names that <code>&lt;sid&gt;</code> solved, one   per line;</li> <li>a <code>status/.../&lt;sid&gt;</code> file has one <code>problem&lt;TAB&gt;status&lt;TAB&gt;runtime</code> line   per problem <code>&lt;sid&gt;</code> was run on (including unsolved ones).</li> </ul> <p>Two quick tricks for <code>solved/</code>, run from inside its <code>problems--mine--T10/</code> directory \u2014 count solved problems per strategy:</p> <pre><code>$ wc -l * | sort -n\n  4 default\n  7 auto\n  8 mzr02\n  9 autoschedule\n 28 total\n</code></pre> <p>and how many distinct problems got solved by at least one strategy (the union), out of 10 problems in this set:</p> <pre><code>$ sort -u * | wc -l\n9\n</code></pre> <p>And in <code>status/</code>, tally how many problems ended up in each status for one strategy, e.g. <code>auto</code>:</p> <pre><code>$ cut -f2 auto | sort | uniq -c | sort -n\n      3 ResourceOut\n      7 Theorem\n</code></pre> <p>(7 <code>Theorem</code> matches <code>auto</code>'s solved count above \u2014 <code>ResourceOut</code> is E's status for hitting the <code>T10</code> cutoff.)</p> <p><code>solverpy_db/results/</code> holds the full per-problem result dictionaries (gzip JSON), including runtime and E's parsed proof-search statistics \u2014 see <code>E.process</code> for exactly what's captured.</p>"},{"location":"tutorials/eval-eprover/#next-steps","title":"\u25c6 Next steps","text":"<ul> <li>Evaluating cvc5 \u2014 the same workflow for an SMT solver.</li> <li>Training an ENIGMA model \u2014 go from \"evaluate a   strategy\" to \"train E Prover's ML guidance from these problems\".</li> <li>Commands \u2014 full YAML/options reference.</li> </ul>"},{"location":"tutorials/python-evaluation/","title":"\ud83d\udd27 Tutorial: Python API \u2014 Benchmark Evaluation","text":"<p><code>solverpy run some.yaml</code> is really just a thin wrapper around the <code>setups</code> Python API. Calling it directly is useful for scripting many related experiments, or generating the setup dynamically.</p>"},{"location":"tutorials/python-evaluation/#the-setup-dict","title":"\u25c6 The <code>Setup</code> dict","text":"<p><code>Setup</code> is a <code>TypedDict</code> describing an experiment; functions in <code>setups</code> fill in its required keys and then launch it.</p> <pre><code>from solverpy import setups\n\nmysetup = setups.Setup(\n    cores=4,\n    benchmarks=[\"problems/bushy010\"],\n    strategies=[\"default\", \"auto\", \"autoschedule\"],\n    limit=\"T10\",\n)\n\nsetups.eprover(mysetup)     # choose the solver, fill in solver-specific defaults\nsetups.evaluation(mysetup)  # configure the evaluation pipeline (DB, cores, ...)\nsetups.launch(mysetup)      # run it\n</code></pre> <p>This is the exact Python equivalent of the <code>eval-eprover.yaml</code> file from Evaluating E Prover Strategies \u2014 the top-level YAML keys map directly onto <code>Setup</code>/<code>Evalset</code> fields, and <code>evaluate: eprover</code> becomes <code>setups.eprover(mysetup)</code>.</p> <p>\ud83e\udd1e As with the YAML form, <code>solverpy_db/strats/</code> must already contain a file for every sid in <code>strategies</code>, and the benchmark directories must exist \u2014 run <code>solverpy init eprover</code> first if you haven't.</p> <p>\u2615 After it finishes, inspect results the same way as before, under <code>solverpy_db/results</code>, <code>solverpy_db/solved</code>, and <code>solverpy_db/status</code>.</p>"},{"location":"tutorials/python-evaluation/#swapping-solvers","title":"\u25c6 Swapping solvers","text":"<p>Only the setup function changes:</p> <pre><code>setups.cvc5(mysetup)      # instead of setups.eprover(mysetup)\n</code></pre> <p><code>setups</code> provides one such function per solver \u2014 see Commands for the full list, or <code>setups.solver</code> in the API reference.</p>"},{"location":"tutorials/python-evaluation/#scripting-multiple-experiments","title":"\u25c6 Scripting multiple experiments","text":"<p>Because <code>mysetup</code> is a plain dict under the hood, it's easy to generate many variants in a loop, e.g. sweeping over resource limits:</p> <pre><code>from solverpy import setups\n\nfor limit in [\"T10\", \"T60\", \"T300\"]:\n    setup = setups.Setup(\n        cores=4,\n        dataname=f\"eval-{limit}\",\n        benchmarks=[\"problems/bushy010\"],\n        strategies=[\"default\"],\n        limit=limit,\n    )\n    setups.eprover(setup)\n    setups.evaluation(setup)\n    setups.launch(setup)\n</code></pre>"},{"location":"tutorials/python-evaluation/#next-steps","title":"\u25c6 Next steps","text":"<ul> <li>Training an ENIGMA model \u2014 the ML training loop   built on top of this same evaluation pipeline.</li> </ul>"},{"location":"tutorials/python-solving/","title":"\ud83d\udca1 Tutorial: Python API \u2014 Solving a Single Problem","text":"<p>For one-off use, or when embedding SolverPy in a larger Python program, you can call a solver directly without going through a YAML experiment.</p>"},{"location":"tutorials/python-solving/#create-a-solver-object","title":"\u25c6 Create a solver object","text":"<pre><code>from solverpy.solver.smt.cvc5 import Cvc5\n\ncvc5 = Cvc5(\"T5\")  # time limit of 5 seconds\n</code></pre> <p>The constructor argument is a resource limit string: <code>T</code> for time (seconds) is supported by every solver, and some solvers accept additional limits (e.g. <code>M</code> for memory, in GB). Multiple limits can be combined with <code>-</code>, like <code>T10-M4</code>; the string must always start with <code>T</code>.</p>"},{"location":"tutorials/python-solving/#solve-a-problem","title":"\u25c6 Solve a problem","text":"<pre><code>result = cvc5.solve(\"myproblem.smt2\", \"--enum-inst\")\n</code></pre> <p>The first argument is the problem file; the second is the solver-specific strategy \u2014 typically a command-line-options string, exactly what you would otherwise put in a <code>solverpy_db/strats/</code> sid file.</p> <p>The result is a <code>dict</code>; its keys and values are solver-specific, but it is always guaranteed to contain at least <code>status</code> (<code>str</code>) and <code>runtime</code> (<code>float</code>).</p> <pre><code>print(result[\"status\"], result[\"runtime\"])\n</code></pre> <p>\ud83d\udca1 Call <code>cvc5.run(p, s)</code> instead of <code>cvc5.solve(p, s)</code> to get the raw solver output without any post-processing.</p> <p>\ud83d\udca1 Call <code>cvc5.command(p, s)</code> to see the shell command that would be executed, without running anything.</p>"},{"location":"tutorials/python-solving/#other-solvers","title":"\u25c6 Other solvers","text":"<p>The same pattern works for every solver \u2014 only the import and the strategy syntax change:</p> <pre><code>from solverpy.solver.atp.eprover import E\n\ne = E(\"T10\")\nresult = e.solve(\"myproblem.p\", \"--auto-schedule\")\n</code></pre> <p>See <code>solver</code> for the full list of available solver classes.</p>"},{"location":"tutorials/python-solving/#next-steps","title":"\u25c6 Next steps","text":"<ul> <li>Python API: benchmark evaluation \u2014 the same   solvers, run over many problems and strategies in parallel with database   caching.</li> </ul>"}]}